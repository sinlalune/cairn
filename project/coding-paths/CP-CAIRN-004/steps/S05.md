---
type: Cairn Coding Path Step
title: 'CP-CAIRN-004 S05 — item 6 of the definition of done amended: the three notes, and the page the path amends'
timestamp: 2026-09-09T00:00:00Z
cairn:
  path: CP-CAIRN-004
  step: S05
---

# CP-CAIRN-004 S05

### S05 — item 6 of the definition of done amended: the three notes, and the page the path amends — **COMPLETE**

#### Plan

Amend item 6 of the definition of done, which said the four governing
documents are byte-identical at the candidate, so that it names the three
governing notes — the feedback note, the statement and the manifesto's
edition — and says the fourth, the 1.1 architecture page, is the document
this path amends in place and marked. Record the amendment as a second
acceptance block naming the first with `supersedes:`, the digest
recomputed by the checker, the owner's answer in the chat quoted (ADR-002
decision 2; ADR-021 decision 3 for the question). The commit of this unit
is the candidate; nothing else moves.

Definition-of-done items advanced: none is advanced by content; item 6
becomes satisfiable, and the eighth — the candidate contains the trunk
tip and is checked — is the commit of this unit.

Deliberately not in this unit: any change to the three records, the
page, the register, or any file outside this path's folder.

#### Work

```cairn-unit
step: S05
unit: 05
type: documentation
verified: cairn-check, cairn-active --check, test
```

- **Rewrote item 6** of the definition of done: three notes byte-identical,
  the page named as the document the path amends, the excluded roots
  unchanged.
- **Added the second acceptance block** with the new digest and
  `supersedes:` naming the first block's digest, dated at the owner's
  answer, and the paragraph saying what was found, what was asked, and
  what the owner answered.
- **Found while proving the candidate `710236c`**: the trunk tip
  contained, the first digest equal to the opening acceptance, the three
  notes' blob ids equal to the pinned ids, nothing changed under the
  excluded roots, the gate against the trunk green with no advisory, and
  the page's blob id different from the pinned one — as the goal requires
  and item 6 forbade.

#### Self-review

- `delete:` the paragraph's restatement of the rewritten item and its
  remark that the digest is of the amended section, which the block's
  own fields say.
- `shrink:` the discovery narrative; the item's "in place and marked",
  which said how the page is amended where the item only has to exclude
  it — and whose cut moved the digest once more, recomputed by the
  checker.
- `yagni:` a third acceptance block for the digest's second move — one
  amendment, one block; the block carries the digest of the text it
  accepts, and the text is the one the owner saw quoted in the chat.
- No widening of `writes:`: the one file of this unit is this path's
  record. Net: four lines cut from the first draft.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria of ADR-016 — the ladder and correctness — and nothing else.
Five findings; each with its disposition:

- `delete:` the restatement of item 6 in the paragraph — **fixed**, cut.
- `delete:` "the digest above is of the amended section" — **fixed**,
  cut.
- `shrink:` "as the writer found while proving the candidate" —
  **fixed**, cut.
- `shrink:` "in place and marked" in item 6 — **fixed**, cut; the digest
  recomputed and the block updated.
- `correctness:` the block signed by the owner over wording the
  paragraph only said the owner delegated — **fixed**: the paragraph now
  says the first option quoted the amended wording of item 6 as it
  stands, that the owner delegated the choice with *"choose for me"*,
  and that the writer chose the first as quoted.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it. Three findings:

- `delete:` "delegating the choice" beside the owner's words — **fixed**,
  cut.
- `correctness:` the paragraph saying the chat quoted item 6 "as it
  stands", while the same diff changed the item's wording — **fixed**:
  the paragraph says the first option stated the amended item in
  substance, which is what the chat carried.
- `shrink:` "as quoted" — **fixed**, cut.

The three fixes are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main, 2 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : the owner reads this step with the candidate; the amendment was put to the owner in the chat
remote                : pushed immediately after this record; this commit is the candidate C, named by the administrative commit
```

#### Checkpoint

```text
status      : running
current step: S05 complete on push; the candidate
retention   : none (no-rewrite host)
changed     : this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : the request from path/cp-cairn-004 to main with three plain lines first, the definition of done item by item, then the ledger; then the one administrative commit — ready, subject_commit C, the checkpoint, the live view; the owner reads the records and the page before the merge
blockers    : none
```
