---
type: Cairn Coding Path Step
title: 'CP-CAIRN-004 S01 — ADR-021, what a session writes for its reader'
timestamp: 2026-09-09T00:00:00Z
cairn:
  path: CP-CAIRN-004
  step: S01
---

# CP-CAIRN-004 S01

### S01 — ADR-021, what a session writes for its reader — **COMPLETE**

#### Plan

Write the first record of the path, ADR-021, from P1, P5 and P6 of the
pedagogy feedback at blob `e9bd3e92` and the statement at blob `57d2e87a`:
one line in the bootloader saying an explanation is written for the
reader who is learning it (P1); the request's description and the path
record's goal opening with three plain lines before the ledger (P5); a
question to the owner put in the chat and well signalled as what it is, in
the owner's own words (P6). Three numbered decisions in one record, as
ADR-016 did, each naming its question, the tag the note gave the option
kept, and the surface it changes by the name the skills, the templates and
the kit use today; every option not taken named as rejected with its tag.
List the record in `docs/adr/index.md` after ADR-020.

Definition-of-done items advanced: the first (one of the three records
exists in the layout's shape, with a *promoted from* link to the note and
the statement at the pinned blob ids, and the index lists it with no gap),
the second for P1, P5 and P6 (each answer named by exactly one record, the
owner's words under P6 quoted and implemented as written, each refused
option named with its tag), and the fourth (the record names `AGENTS.md`
as `tools/cairn.mjs` writes it, the request template, `cairn-audit`, the
path template's goal, and the open, close and unit skills, so paths 1, 3
and 4 of the register can be scoped from it).

Deliberately not in this unit: ADR-022 and ADR-023 (S02, S03), the 1.1
architecture page and the roadmap register (S04), and any change under
`spec/`, `skills/`, `tools/`, `site/`, `.github/`, `AGENTS.md` or
`cairn.config.json` — the record names those surfaces and stops. The four
governing documents are read, not written.

#### Work

```cairn-unit
step: S01
unit: 01
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Wrote `docs/adr/ADR-021-what-a-session-writes-for-its-reader.md`**:
  three decisions, in the order of the reader's day — the chat, the
  request, the question. Status `accepted` on landing, dated 2026-09-09,
  the day of the owner's answers; the request that lands the path is its
  review.
- **Decision 1 places the tone where ADR-011 placed the concept note**,
  the bootloader, and says the line is the concept template's order
  applied to the chat that precedes the note; it names both the kit's
  bootloader text and this repository's own `AGENTS.md`, because the
  latter is not written by the kit.
- **Decision 2 fixes the order of the request's description** — three
  lines, the item-by-item answer of ADR-018, then the ledger — and
  extends the same opening to the record's goal, the page the owner reads
  at the plan review; for this repository the link goes to a README
  section, because ADR-012 made the README this repository's surface page.
- **Decision 3 keeps the content of the offered shape and moves it to the
  chat**, as the owner's line says; it lists the five moments where an
  agent stops for the owner, names the opening line that signals a
  decision, and says the post-mortem tool prints facts and not the
  question.
- **Added the ADR-021 line to `docs/adr/index.md`**, the folder's
  timestamp refreshed to 2026-09-09.

#### Self-review

- `delete:` the consequences bullet repeating decision 2's README link;
  the bullet repeating decision 3's "nothing further on that path"; the
  sentence *Nothing enters the checker* stated twice; the sentence
  announcing the shape of the decisions that the three headings show.
- `shrink:` P6's "content kept, place moved" said three times across
  decision 3 and its alternative, now once; the manifesto's-test section
  restating decision 1 and decision 3, now two sentences.
- `yagni:` kept — the abstractions of the trade named in decision 1 are
  the statement's own emphasis, the line the record promotes.
- `yagni:` naming the exact opening words of the signal of decision 3 —
  the record says one recognisable opening line and leaves the words to
  the skill.
- No widening of `writes:`: every file of this unit is under `docs/adr/`
  or this path's folder. Net: nine lines cut from the first draft.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria of ADR-016 — the ladder and correctness — and nothing else.
Twelve findings; each with its disposition:

- `delete:` *Nothing enters the checker* twice — **fixed**, the
  consequences copy cut.
- `delete:` the README-section consequence repeating decision 2 —
  **fixed**, cut.
- `delete:` "what the agent does while it waits" repeating decision 3 —
  **fixed**, cut.
- `delete:` the sentence announcing the decisions' shape — **fixed**, cut.
- `shrink:` the three *what this changes* paragraphs and the table carry
  the same surfaces twice — **kept**: the shape of every record of 1.1,
  ADR-016 to ADR-020 included; the table is the index a coding path is
  scoped from, the paragraphs carry the specifics.
- `shrink:` P6's point stated three times — **fixed**, once in decision
  3 and once in the alternative.
- `shrink:` the manifesto's-test section restating decisions 1 and 3 —
  **fixed**, the two restatements cut.
- `yagni:` the abstractions of the trade in decision 1 — **kept**: the
  owner's own emphasis in the statement the record promotes.
- `correctness:` five rules "all about paths" against "beside ADR-011's
  line" and "seven" — **fixed**: the context now says five today and a
  sixth with ADR-011; decision 1 says "beside the line ADR-011 adds"; the
  consequence says five to seven, two not about paths.
- `correctness:` "the Crumbz merges of twenty seconds" against one read
  of twenty and one merge of twenty-nine — **fixed**, the alternative
  names both as they were.
- `correctness:` "the checker reads none of the description" against "a
  ledger the checker's twin reads" — **fixed**: the twin is named as the
  owner, and the sentence says the checker reads no line of the
  description.
- `correctness:` decision 3's paragraph naming no file for the
  post-mortem tool while its row does; decision 2's row omitting the goal
  section — **fixed**, the paragraph names `tools/cairn-postmortem.mjs`
  and the row names the goal section.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it. Two findings:

- `delete:` "in the register's row 3" beside the post-mortem tool's file —
  **fixed**, the pointer cut; the file name stays, which is what the first
  read asked for.
- `shrink:` "and by nobody else" widening the claim about who reads the
  three lines — **fixed**, the sentence now says the owner reads them and
  the checker reads no line of the description.

The two fixes are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 4 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the record with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-021-what-a-session-writes-for-its-reader.md, docs/adr/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S02 — ADR-022, the learning note, the learning session and its offer (P2, P3, P4); the manifesto read for P3, the one answer that adds a file; the bootloader's entry route and the pointer page named as the surfaces that list the sixth skill; the kit's budget of ADR-013 answered
blockers    : none
```
