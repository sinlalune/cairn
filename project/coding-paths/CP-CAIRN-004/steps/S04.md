---
type: Cairn Coding Path Step
title: 'CP-CAIRN-004 S04 — the 1.1 page amended with the pedagogy and the register widened'
timestamp: 2026-09-09T00:00:00Z
cairn:
  path: CP-CAIRN-004
  step: S04
---

# CP-CAIRN-004 S04

### S04 — the 1.1 page amended with the pedagogy and the register widened — **COMPLETE**

#### Plan

Amend `docs/architecture/01-cairn-1-1.md` in place with ADR-021 to
ADR-023, as CP-CAIRN-003 S05 did for ADR-016 to ADR-020: an *Amended on
2026-09-09* paragraph after the 2026-09-07 one, carrying the *promoted
from* links to the pedagogy feedback at blob `e9bd3e92` and the statement
at blob `57d2e87a` and naming the manifesto's edition; each added sentence
naming its record and its decision, marked *since 2026-09-09* where it
changes what an earlier sentence said, the earlier sentence kept visible;
rows of the documentation-plane and tools tables amended and two rows
added; a closing section *What the pedagogy adds*; no record of ADR-001
to ADR-020 superseded, and the page says so. Refresh
`docs/architecture/index.md`. Widen the register's rows 1, 3, 4 and 5 with
the records and surfaces of this path, and the 1.1 milestone's row to
twenty-three records and three promotions; no new path, no path opened.

Definition-of-done items advanced: the third (the page states, naming the
record behind each sentence, how a session speaks and puts a question,
what a request and a goal open with, what a learning note is and which
skill writes it, which pages a reader meets and who keeps the API
documentation, and links the note and the statement as *promoted from*
at the pinned blob ids; ADR-011, ADR-012, ADR-013 and ADR-019 extended,
none superseded), and the fifth (rows 1, 4 and 5 carry the records and
surfaces named in the definition of done, row 3 the two tools ADR-021
names, no path opened).

Deliberately not in this unit: the candidate (S05), and any change under
`spec/`, `skills/`, `tools/`, `site/`, `.github/`, `AGENTS.md` or
`cairn.config.json`. The three records are not edited; the page states
them. The four governing documents are read, not written.

#### Work

```cairn-unit
step: S04
unit: 04
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Amended the page in place, marked**: the frontmatter's description
  and tags; an *Amended on 2026-09-09* paragraph with the two links at
  their blob ids, the edition named, and the sentence that ADR-011,
  ADR-012, ADR-013 and ADR-019 are extended and nothing superseded; the
  opening gains the goal's three lines and the question in the chat; the
  running gains the API refresh beside the module note, and a paragraph on
  how a session explains, the offer of a learning session, and what
  `cairn-learn` reads and writes; the closing's description sentence is
  rewritten in the new order — three lines, the item-by-item answer, the
  ledger — with each part dated, and the try is asked for in the chat.
- **Amended the tables**: the documentation plane gains a row for the
  adopter's README and one for the API documentation, and the rows of the
  surface page, the architecture folder, the concept folders and the
  pointer page carry the pedagogy's clauses; the tools table's audit and
  `cairn` rows carry theirs; the kit's file count is restated as thirty at
  a target of *at most thirty*, the earlier sentence kept in the past
  tense.
- **Added the closing section *What the pedagogy adds***: the two
  readers, the three records in three sentences, the statement as the
  manifesto's own section and not a record, nothing in the checker, one
  kit file.
- **Refreshed `docs/architecture/index.md`**: the page's line says what
  the amendment added and through which records; the folder's timestamp
  moved to 2026-09-09.
- **Widened the register**: rows 1, 3, 4 and 5 name the pedagogy's
  records and surfaces in their outcome and records columns — the writes
  columns already cover every surface named, so none changes; the 1.1
  milestone's row counts twenty-three records and three promotions. Every
  state stays *no path yet*.

#### Self-review

- `delete:` the closing section's summary of the three records, every
  fact of which the page already carried; the areas paragraph's clause
  on the API tools, which the table row carries; the description of the
  feedback note in the amendment paragraph.
- `shrink:` the closing section to what is new on the page — the two
  readers, the statement as the owner's and not a record, nothing in the
  checker; "like every question to the owner"; "the sixth skill being the
  thirtieth"; a second *since 2026-09-09* inside one table cell.
- `yagni:` kept — the *Amended on 2026-09-09* paragraph beside the
  2026-09-07 one rather than merged into it: two amendments, two dates,
  two sources, as the page's rule of not rewriting the past asks.
- No widening of `writes:`: every file of this unit is under
  `docs/architecture/`, the register or this path's folder. Net:
  twenty-two lines cut from the first draft.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria of ADR-016 — the ladder and correctness — and nothing else.
Eleven findings; each with its disposition:

- `delete:` the whole closing section, every fact already on the page —
  **fixed** by shrinking: the section keeps the three facts that were
  nowhere else — the two readers, the statement as the owner's and not a
  record, nothing in the checker — under a title that says so.
- `delete:` the API clause in the areas paragraph, a copy of the table
  row — **fixed**, cut.
- `shrink:` the description of the feedback note — **fixed**, cut.
- `shrink:` "like every question to the owner" — **fixed**, cut.
- `shrink:` "the sixth skill being the thirtieth" — **fixed**, cut.
- `shrink:` a second *since 2026-09-09* in the architecture cell —
  **fixed**, one date for the cell.
- `correctness:` row 4 saying the kit ships *under thirty* against the
  page's *at most thirty* — **fixed**, the row says *at most thirty* and
  names the sixth skill.
- `correctness:` the page's timestamp unmoved while the index's moved —
  **fixed**, the page's timestamp is 2026-09-09.
- `correctness:` row 4 citing ADR-023 d1 to d3 for the API link, which
  is d4 — **fixed**, d4 added.
- `correctness:` row 3 giving the post-mortem tool a claim under ADR-021
  d3 that the page's tool row lacked — **fixed**, the tool's row says it
  prints the facts a question is built from, and not the question.
- `correctness:` the extended-records sentence naming four records while
  the page also pairs ADR-001 and ADR-018 with the new ones — **fixed**,
  six records named.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it. Two findings:

- `delete:` "the edited manifesto follows it" in the closing section,
  said by the amendment paragraph — **fixed**, cut.
- `shrink:` "and not the question" in the post-mortem tool's row —
  **fixed**, cut.

The two fixes are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 4 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the page with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by the administrative commit
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : docs/architecture/01-cairn-1-1.md, docs/architecture/index.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S05 — the candidate: the trunk tip is contained, the gates run bare on this commit, the request opened from path/cp-cairn-004 with three plain lines first, the definition of done item by item, then the ledger; the owner reads the three records and the page before the merge
blockers    : none
```
