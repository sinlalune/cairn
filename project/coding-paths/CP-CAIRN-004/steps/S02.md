---
type: Cairn Coding Path Step
title: 'CP-CAIRN-004 S02 — ADR-022, the learning note and the learning session'
timestamp: 2026-09-09T00:00:00Z
cairn:
  path: CP-CAIRN-004
  step: S02
---

# CP-CAIRN-004 S02

### S02 — ADR-022, the learning note and the learning session — **COMPLETE**

#### Plan

Write the second record of the path, ADR-022, from P2, P3 and P4 of the
pedagogy feedback at blob `e9bd3e92` and the statement at blob `57d2e87a`:
a learning note is a concept note with an order, in `docs/concepts/learning`
and under no root of its own (P2); a sixth skill, `cairn-learn`, is the
procedure of a learning session, listed by the bootloader's entry route
and the pointer page, and the kit's file budget is answered (P3); a unit
that explains an abstraction writes the concept note, offers the session
in its step and parks it (P4). Three numbered decisions in the shape of
ADR-021; the manifesto read for P3, the one answer that adds a file; every
option not taken named as rejected with its tag. List the record in
`docs/adr/index.md` after ADR-021.

Definition-of-done items advanced: the first (the second of the three
records exists in the layout's shape, with a *promoted from* link to the
note and the statement at the pinned blob ids, and the index lists it with
no gap), the second for P2, P3 and P4, and the fourth: the record names
chapter 6, the concept template, the new skill, the bootloader's *start
here* item, the pointer page, `planInstall` and the lock, the conformance
page's kit line and `cairn-unit` step 2, and says how the budget pays for
the file — the target moves to *at most thirty* and the release path
re-measures it, no file removed for the count.

Deliberately not in this unit: ADR-023 (S03), the 1.1 architecture page
and the roadmap register (S04), and any change under `spec/`, `skills/`,
`tools/`, `site/`, `.github/`, `AGENTS.md` or `cairn.config.json` — the
record names those surfaces and stops. The four governing documents are
read, not written.

#### Work

```cairn-unit
step: S02
unit: 02
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Wrote `docs/adr/ADR-022-the-learning-note-and-the-learning-session.md`**:
  three decisions in the order of the reader's need — the note, the
  session that writes it, the offer that leads to it. Status `accepted`
  on landing, dated 2026-09-09.
- **Decision 1 settles the collision the note flagged the owner's way**:
  one folder, two bodies — a definition and a sequence — read by the two
  concept rules alike; chapter 6 stops calling the note optional under a
  root of its own, and the concept template gains one sentence.
- **Decision 2 counts the kit**: twenty-six files in the lock today, the
  five additions and two removals of 1.1, and this skill as the
  thirtieth; the target of *under thirty* becomes *at most thirty*,
  re-measured by the release path, and no file is removed for the number —
  the choice the definition of done left to this record.
- **Decision 3 separates an offer from a question**: the offer of a
  learning session is one line of the chat and one sentence of the step,
  and nothing waits on it, unlike a decision of ADR-021 decision 3.
- **Added the ADR-022 line to `docs/adr/index.md`**.

#### Self-review

- `delete:` three consequences restating decisions 2 and 3 and the
  manifesto's-test sentence restating the tags of decisions 1 and 3.
- `shrink:` the manifesto's-test paragraph, which argued the file count a
  second time; the argument stays in decision 2 and the section points at
  it.
- `yagni:` naming a candidate file to remove for the count — the record
  decides no file is removed, so none is named.
- `yagni:` kept — the arithmetic of the thirtieth file in decision 2; the
  definition of done asks the record to say how the budget pays for the
  file, and the count is the answer.
- No widening of `writes:`: every file of this unit is under `docs/adr/`
  or this path's folder. Net: thirteen lines cut from the first draft.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria of ADR-016 — the ladder and correctness — and nothing else. Nine
findings; each with its disposition:

- `delete:` the consequence repeating decision 3's offer and "nothing
  waits" — **fixed**, cut.
- `delete:` the consequence restating decision 2's budget, restated a
  third time under the manifesto's test — **fixed**: the consequence
  shrunk to the fact that the kit ships thirty files and the count binds;
  the manifesto's-test restatement cut.
- `delete:` "Decisions 1 and 3 are the simplest option and one sentence"
  — **fixed**, cut.
- `shrink:` the manifesto's-test paragraph arguing the count again —
  **fixed**, it now points at decision 2.
- `shrink:` "the same three parts a reader needs separately" with no
  referent — **fixed**, the clause cut.
- `shrink:` the consequence summarising decision 1 — **fixed**, reduced
  to "one folder, two bodies".
- `correctness:` decision 1 editing ADR-011's table while the header says
  no record is superseded and the table row names only chapter 6 —
  **fixed**: the record no longer edits ADR-011's table; chapter 6's
  scopes table is the live one and is the surface named.
- `correctness:` "the one answer of the twelve" with no referent in the
  diff — **fixed**, "of the note's twelve questions".
- `correctness:` the register paragraph assigning no path to the lock and
  the conformance page — **fixed**, the lock in path 4 and the
  conformance page's budget in path 5.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it. One finding:

- `correctness:` "the number lost" beside "the count binds", with the
  target's move cut from between them — **fixed**: the consequence names
  the target of *at most thirty* that binds, and the manifesto's-test
  sentence says what was weighed and that the target moved instead.

The fix is not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 4 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the record with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S03
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-022-the-learning-note-and-the-learning-session.md, docs/adr/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S03 — ADR-023, the pages a reader meets (P7 to P11): the README above the surface pages, a flow page as an architecture page of kind flow, one diagram and one example, the API documentation kept by the agent, nothing more on search; the owner's words under P10 quoted
blockers    : none
```
