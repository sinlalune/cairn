---
type: Cairn Coding Path Step
title: 'CP-CAIRN-004 S06 — the request reviewer''s three findings answered: no fixed count, the note linked from outside the wiki, the page''s diagram'
timestamp: 2026-09-09T00:00:00Z
cairn:
  path: CP-CAIRN-004
  step: S06
---

# CP-CAIRN-004 S06

### S06 — the request reviewer's three findings answered: no fixed count, the note linked from outside the wiki, the page's diagram — **COMPLETE**

#### Plan

Answer the three findings the request's reviewer made on the first
candidate `72f62d9`, which is void; this unit's commit is the second
candidate, and the path returns to `running` until its administrative
commit. One: ADR-022 decision 2 moved the kit's file count to thirty while
ADR-013 sets a rule of at most twenty-nine and the record claimed to
supersede nothing — put to the owner in the chat, who ruled *"stop
stupid fixed counters, just do what make sense and provide added
value"*; ADR-022 now supersedes ADR-013's count clause on that ruling,
the count is measured and reported and bounds nothing, and items 3 and 4
of the definition of done are amended by a third acceptance block to say
so. Two: `concept-orphan` counts no link from inside the concept root, so
ADR-022 decision 2's completion — the note linked from the concept note
of the term — was not one; the note is linked from the step that offered
the session or the surface page whose glossary it joins. Three: the 1.1
page requires a Mermaid diagram of pages that name components and had
none, and no later path was scoped to add one; the page gains a section
with the diagram and the direction sentence ADR-019 asks for.

Definition-of-done items advanced: the second and fourth (ADR-022 says
which count rule it supersedes and why, and that the count is no bound),
the third (the page says which clause is superseded, and carries the
diagram it requires), the fifth (rows 4 and 5 of the register no longer
name a target), and the eighth (this commit is the candidate).

Deliberately not in this unit: anything under the excluded roots; the
feedback note, the statement and the manifesto, still byte-identical.

#### Work

```cairn-unit
step: S06
unit: 06
type: decision
verified: cairn-check, cairn-active --check, test
```

- **ADR-022** — the header names the one clause superseded and the
  records extended; decision 2 quotes the owner's ruling, supersedes
  ADR-013's rule and ADR-014's consequence that repeats it, leaves the
  two removals ADR-013 named to path 4 on their own reasons, and keeps
  the conformance page's kit line as a measurement with no target; the
  orphan completion corrected; two more options refused — *at most
  thirty* (this record's own first draft) and a third removal (the
  reviewer's); the consequences, the manifesto's-test paragraph, the
  table and the closing paragraph follow.
- **`docs/adr/index.md`** — ADR-022's line follows.
- **The 1.1 page** — the amendment paragraph names the superseded clause;
  a section *The shape in one diagram* after *What 1.1 is for*, with the
  Mermaid diagram of the components and the one-way sentence; the kit
  paragraph says the count is no rule since 2026-09-09.
- **The register** — rows 4 and 5 name no target.
- **The record** — items 3 and 4 amended; the third acceptance block,
  superseding the second, dated at the owner's ruling; `status: running`
  and `subject_commit: null` because `72f62d9` is void.

#### Self-review

- `delete:` a consequences bullet of ADR-022 restating its header and
  decision 2; the second half of the orphan sentence, which the first
  half carries.
- `shrink:` the record's amendment paragraph, which restated the two
  items it amends; the direction paragraph on the page to the one
  sentence ADR-019 asks for; two clauses of ADR-022 that said what does
  not change or restated a quote.
- `delete:` the count "thirty at this record", which presupposed the two
  removals the same paragraph leaves to path 4; the record now states the
  only counts it can: twenty-six today, thirty-two before any removal.
- `yagni:` kept — the owner's words quoted in the record's acceptance
  paragraph as well as in ADR-022: the block is the acceptance, and
  ADR-021 decision 3 has the agent write the owner's words into the
  record that holds the answer.
- No widening of `writes:`: every file of this unit is under `docs/adr/`,
  `docs/architecture/`, the register or this path's folder. Net: twelve
  lines cut from the first draft.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria of ADR-016 — the ladder and correctness — and nothing else. Ten
findings, the count of refused options confirmed at seven; each with its
disposition:

- `delete:` the consequences bullet restating the header and decision 2
  — **fixed**, cut.
- `shrink:` the orphan sentence's second half — **fixed**, cut.
- `shrink:` "Chapter 6's weight budget keeps its measurements…" —
  **fixed**, the sentence says only what the kit's line carries.
- `shrink:` "a number is not a rule" after the pointer to the quote —
  **fixed**, cut.
- `shrink:` the amendment paragraph restating items 3 and 4 — **fixed**,
  "amended accordingly".
- `shrink:` the owner's words quoted a second time, in the record —
  **kept**: the acceptance paragraph is where the record holds the
  owner's answer.
- `shrink:` the direction paragraph, three sentences where ADR-019 asks
  one — **fixed**, one sentence.
- `correctness:` "one clause of one record" while ADR-014's consequence
  goes with it — **fixed**: one rule stated in two records, in the
  header, on the page and in item 3 of the definition of done, whose
  digest moved and whose block follows it.
- `correctness:` "thirty at this record" presupposing the removals —
  **fixed**: twenty-six today, thirty-two before any removal.
- `correctness:` "nothing lower reads anything above" against the
  left-to-right diagram and the sentences before it — **fixed**: each
  node depends only on the nodes to its left.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it. Two findings:

- `shrink:` the two dated counts written into decision 2 after the
  record unfroze the count — **fixed**, cut; the record states no count.
- `correctness:` item 3 saying no record is superseded while excepting a
  rule that lives in two — **fixed**: one rule of 1.1 is superseded,
  stated in ADR-013 and repeated in ADR-014, and no other record; the
  digest moved and the block follows it.

The two fixes are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main (exit 0)
cairn-active --check  : pass — view current after regeneration for status running (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : the owner ruled on the count in the chat; the owner reads this step with the candidate
remote                : pushed immediately after this record; this commit is the second candidate, named by the administrative commit
```

#### Checkpoint

```text
status      : running — the first candidate 72f62d9 and its administrative commit c5abbad are void
current step: S06 complete on push; the second candidate
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-022-the-learning-note-and-the-learning-session.md, docs/adr/index.md, docs/architecture/01-cairn-1-1.md, project/coding-paths/index.md, ACTIVE.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : the request's description rewritten for the second candidate; the one administrative commit — ready, subject_commit, the checkpoint, the live view; the owner reads the records, the page and the steps S05 and S06 before the merge
blockers    : none
```
