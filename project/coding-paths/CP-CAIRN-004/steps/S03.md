---
type: Cairn Coding Path Step
title: 'CP-CAIRN-004 S03 — ADR-023, the pages a reader meets'
timestamp: 2026-09-09T00:00:00Z
cairn:
  path: CP-CAIRN-004
  step: S03
---

# CP-CAIRN-004 S03

### S03 — ADR-023, the pages a reader meets — **COMPLETE**

#### Plan

Write the third record of the path, ADR-023, from P7 to P11 of the
pedagogy feedback at blob `e9bd3e92` and the statement at blob `57d2e87a`:
the adopter's README says what the project is and lists the surface pages
(P7); a workflow page is an architecture page of kind *flow*, one per flow
that crosses components, with a diagram, under `docs/architecture/` and
no folder of its own (P8); the architecture page carries one diagram and
the surface page opens with one worked example (P9); Cairn owns no API
page and the agent creates and maintains the API documentation, in the
owner's own words (P10); nothing more on where the senior searches, kept
as today (P11). Five numbered decisions in the shape of ADR-021; every
option not taken named as rejected with its tag. List the record in
`docs/adr/index.md` after ADR-022.

Definition-of-done items advanced: the first (the third and last record
exists in the layout's shape, with a *promoted from* link to the note and
the statement at the pinned blob ids, and the index lists it with no gap),
the second for P7 to P11 — with which every answer P1 to P11 is now named
by exactly one record, and P12 by the statement — and the fourth: the
record names chapter 3's promotion list and kinds, the architecture
concept where ADR-019 put its sentence, the architecture and documentation
indexes the kit writes, and the close and unit skills, so paths 1, 4 and
5 of the register can be scoped from it.

Deliberately not in this unit: the 1.1 architecture page and the roadmap
register (S04), and any change under `spec/`, `skills/`, `tools/`,
`site/`, `.github/`, `AGENTS.md` or `cairn.config.json` — the record
names those surfaces and stops. The four governing documents are read,
not written.

#### Work

```cairn-unit
step: S03
unit: 03
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Wrote `docs/adr/ADR-023-the-pages-a-reader-meets.md`**: five
  decisions in the order a newcomer meets the pages — the README, the
  flow, the diagram and the example, the API documentation, the search.
  Status `accepted` on landing, dated 2026-09-09.
- **Decision 2 avoids a new folder**: chapter 3 already says an
  architecture page is about one kind of thing, so a flow is a fourth
  kind under `docs/architecture/`, and the concept page where ADR-019 put
  the dependency sentence carries the flow page's shape.
- **Decision 3 puts the diagram and the example where ADR-019 and ADR-012
  put their sentences** — the architecture concept and chapter 3's
  sentence on the surface page — and names Mermaid because the forge
  renders it with no library.
- **Decision 4 reads the owner's line as a refresh rule**: the agent
  keeps the API documentation as ADR-010 has it keep the module note, in
  the same unit, and the surface page links it; the tool and the format
  stay the ecosystem's.
- **Decision 5 is a record of what was kept**, as ADR-020 was.
- **Added the ADR-023 line to `docs/adr/index.md`**.

#### Self-review

- `delete:` the closing sentence of decision 5 that repeated "nothing is
  added"; the render-without-a-library sentence the context already
  carried; one of two *Nothing enters the checker*.
- `shrink:` the tool-and-format sentence of decision 4, said again under
  the manifesto's test where it is weighed; the no-new-folder point, said
  in decision 2 and weighed under the manifesto's test, cut from the
  consequences; decision 1's restatement of its own first sentence.
- `yagni:` kept — "then its glossary" in the newcomer's path, which is
  ADR-012's consequence referenced, not a step this record adds.
- No widening of `writes:`: every file of this unit is under `docs/adr/`
  or this path's folder. Net: ten lines cut from the first draft.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria of ADR-016 — the ladder and correctness — and nothing else.
Eleven findings, the count of refused options confirmed at eight; each
with its disposition:

- `delete:` *Nothing enters the checker* twice — **fixed**, the
  consequences copy cut.
- `delete:` "Both render on the forge without a library" — **fixed**,
  cut.
- `delete:` "The record keeps what is" — **fixed**, cut.
- `shrink:` decision 1 restating its first sentence — **fixed**, the
  clause cut.
- `shrink:` the tool-and-format point said three times — **fixed**, kept
  where it is weighed, under the manifesto's test.
- `shrink:` the no-new-folder point said three times — **fixed**, cut
  from the consequences.
- `shrink:` "The owner wrote this line under the two options offered; it
  wins" — **kept**: the sentence ADR-021 decision 3 uses for the owner's
  other line, and the note's rule made explicit.
- `yagni:` "then its glossary" — **kept**: ADR-012's own consequence,
  referenced.
- `correctness:` this repository's README called a surface page against
  decision 1's "page above" — **fixed**: the closing paragraph and
  decision 1 now say ADR-012 made it the page of this repository's
  surfaces, and decision 1 names it as a surface changed.
- `correctness:` path 5 carrying a README no decision names — **fixed**
  by the same edit.
- `correctness:` `docs/architecture/index.md` called an index the kit
  writes — **fixed**: the closing paragraph names this repository's
  architecture index and the kit's indexes apart, both in path 4.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it: clean. Nothing is
read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 4 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the record with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-023-the-pages-a-reader-meets.md, docs/adr/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S04 — the 1.1 architecture page amended with ADR-021 to ADR-023, each added sentence naming its record and marked since 2026-09-09, promoted-from links to the note and the statement at their blob ids, the manifesto's edition cited; the architecture index refreshed; the register's rows 1, 3, 4 and 5 widened and the milestone row counting twenty-three records and three promotions; no path opened
blockers    : none
```
