---
type: Cairn Coding Path Step
title: 'CP-CAIRN-006 S01 — the two debts, two records'
timestamp: 2026-09-11T00:00:00Z
cairn:
  path: CP-CAIRN-006
  step: S01
---

# CP-CAIRN-006 S01

### S01 — the two debts, two records — **COMPLETE**

#### Plan

Settle, by two decision records, the two debts
[path 1's journal entry](../../../log/2026-09-10-cp-cairn-005.md) named
for this path, before any rule of the checker is written.

ADR-024 supersedes one clause of ADR-001 decision 1 — *the kit's default
stays `pull-request`* — because path 1 deleted the only registration
sequence that default described, so `cairn init` now installs a
configuration that contradicts the skill installed beside it. A
repository installed at 1.1 declares `transport.registration:
manual-git`; `--transport` names the integration transport alone;
`pull-request` stays a value the field accepts, and an installed
repository keeps the declaration it made. The surfaces are named as path
4's: `tools/cairn.mjs` (`defaultOptions`, `buildConfig`, the
`--transport` usage line, the generated binding row) and
`spec/reference/configuration.md`.

ADR-025 decides the clause path 1 answered its item *less*, because no
record carried it: the post-mortem a red run produces — in the run's
log, and on the request where ADR-014 decision 1 posts it — is read by
the writer before the next unit starts. One sentence in movement 0 of
`cairn-unit`, named as path 3's, with the tool it sends the writer to.
Nothing enters the checker.

Then every document the two records amend: the 1.1 page in place, marked
*since 2026-09-11*, with the journal entry linked as *promoted from* —
the amendment paragraph, *How a path opens*, *How a path runs*, the
`cairn` row of the tools table, the first bullet of *What 1.1 removes*;
the architecture index; the ADR index; and rows 3 and 4 of the roadmap
register, each naming its record and the surfaces it changes.

Definition-of-done item advanced: the first.

Type `decision`: the records are the change, and every document they
amend moves with them. No test: nothing under `tools/` changes here, and
no rule is added — ADR-024 changes a declaration the checker does not
read, ADR-025 changes a skill.

Deliberately not in this unit: every rule of the checker (S02 to S06);
the kit and the configuration reference ADR-024 names, which are path
4's; the unit skill's sentence ADR-025 names, which is path 3's; the
`#### Review` rule that would require this very section, which is S06.
Observed and left: the 1.1 page's opening sentence *Nothing here is
implemented yet* is false since path 1 landed, and neither record of this
unit decides it — it is not corrected here.

#### Work

```cairn-unit
step: S01
unit: 01
type: decision
verified: cairn-check, test
```

- **`docs/adr/ADR-024-the-kit-installs-what-the-skills-perform.md`**, new
  and `accepted`, promoted from the journal entry at blob
  `b69c03fc`, superseding the kit-default clause of ADR-001 decision 1
  and naming the four `tools/cairn.mjs` surfaces and the configuration
  reference as path 4's. Five alternatives refused, a rule and an
  in-place edit of ADR-001 among them.
- **`docs/adr/ADR-025-a-red-run-is-read-before-the-next-unit.md`**, new
  and `accepted`, promoted from the same entry, extending ADR-014
  decision 1 by naming the reader of what it writes, and placing the
  sentence in movement 0 rather than in verify. Five alternatives
  refused, a rule reading the forge among them.
- **`docs/adr/index.md`** lists both, with no gap after ADR-023.
- **`docs/architecture/01-cairn-1-1.md`** amended in place: the
  amendment paragraph naming the superseded clause and the journal entry
  it is promoted from; *How a path opens* saying the kit now installs the
  declaration; *How a path runs* saying a red run's post-mortem is read
  before the next unit; the `cairn` row of *Which tools exist*; the first
  bullet of *What 1.1 removes*. Every addition marked *since
  2026-09-11*; the frontmatter's description and timestamp follow.
- **`docs/architecture/index.md`** records the 2026-09-11 amendment on
  the page's line.
- **`project/coding-paths/index.md`**: row 3 gains ADR-025, the
  sentence's outcome and `skills/cairn-unit/SKILL.md`; row 4 gains
  ADR-024, the transport the kit installs and
  `spec/reference/configuration.md`; the 1.1 milestone row counts
  twenty-five records.

#### Self-review

- `shrink:` ADR-024's decision closing *the value is a declaration about
  how commits reach the trunk, and the two commands that write it are the
  installer's* — vague about which two, and the three preceding clauses
  already bound the blast radius; cut to the three clauses.
- `shrink:` ADR-024's consequence *one clause of ADR-001 is superseded
  and nothing else of it moves; the 1.1 page is amended where it names
  the kit* — the first half repeats the header, the second repeats the
  diff; one clause left.
- `shrink:` ADR-025's *in which case the act is the unit's own work* —
  true and unnecessary once the obligation is bounded to reading.
- `yagni:` a `cairn-postmortem` row edit on the tools table of the 1.1
  page for ADR-025 — the row says what the tool does and the reader
  obligation is *How a path runs*, where the writer's gates are already
  described; one placement, not two.
- No widening of `writes:`: `docs/adr/**`,
  `docs/architecture/01-cairn-1-1.md`,
  `docs/architecture/index.md`, `project/coding-paths/index.md` and
  `project/coding-paths/CP-CAIRN-006/**` are all declared.
- `delete:` (added by the review's fixes) ADR-024's *the configuration is
  the half the binding table quotes*, and its bullet *`tools/cairn-config.schema.json`
  does not change*, which restates *the schema keeps both values for both
  fields* two paragraphs above.
- `shrink:` (added by the review's fixes) ADR-024's *ADR-001 decision 1
  decided two halves in one sentence* to *has two halves* — the false
  detail first, then the empty one; and the same record's
  *`registration` and `registration-base` read the same shape on both
  transports* in the paragraph that proves the checker reads the field
  not at all.
- Net: 23 lines across the four amended documents, plus the two records,
  247 lines, and this step.

#### Review

First read, a fresh context given this unit's diff — the two records and
the five documents they amend — and the two criteria, the ladder and
correctness, and nothing else. Thirteen findings; each with its
disposition:

- `correctness:` ADR-024 claiming, twice, that the profile line of
  ADR-001 decision 6 names the declared transports — **fixed**, both
  claims cut. The reader proved it: decision 6 defines the line as what
  the forge does not enforce, the 1.1 page's transition table says the
  same, and `tools/cairn-check.mjs` prints profile, branch, base and a
  file count. Item 2 of this path's definition of done does ask S02 for
  the transports on that line; that is this path's extension of decision
  6, and this record is not where it is claimed.
- `correctness:` ADR-024 contradicting itself on the generated binding —
  the Context naming it a reader of the declaration, the surfaces list
  saying it prints the integration transport only — **fixed**: the
  Context now separates this repository's hand-written binding, which
  names both, from the kit's generated one, which names one.
- `correctness:` ADR-024's *decided two halves in one sentence*, when
  ADR-001 decision 1 carries them as two bullets and the journal says *in
  one decision* — **fixed**.
- `correctness:` ADR-024 attributing the survival of a 1.0 declaration to
  ADR-015's edited-file rule, when `cairn.config.json` is a host entry
  the update never rewrites, pristine or not — **fixed**, and the second
  read tightened it further.
- `correctness:` the ADR index's *the same command's configuration*, with
  no correct antecedent — **fixed** to the 1.1 page's own wording, *the
  configuration written beside it*.
- `correctness:` ADR-025's *the verdict a red run gives is the exit code
  the writer read before the unit was called complete*, which contradicts
  the record's own context and confuses a push's exit code with a run's
  verdict — **fixed**, cut; the paragraph's two remaining reasons carry
  it.
- `correctness:` ADR-025 mixing two movement numberings — *movement 0*
  and *movement 6* from the skill's headings against *not a sixth
  movement* from ADR-017's five — **fixed**, the clause cut; the two
  surviving references are both the skill's.
- `delete:` ADR-024's consequence *Nothing else of ADR-001 decision 1
  moves*, restating the record's own opening — **fixed**, cut.
- `delete:` ADR-024's schema bullet, restating the paragraph above it —
  **fixed**, cut.
- `delete:` ADR-025's consequence *Nothing enters the checker, and no rule
  of the checker path reads it* — **fixed**, cut.
- `delete:` ADR-025's consequence *The unit skill gains one sentence and
  no movement* — **fixed**, cut.
- `delete:` ADR-025 absorbing ADR-014's learning-note clause twice, in the
  decision and in a rejected alternative — **fixed**, the decision's
  sentence cut; the second read then showed the cut left *no note is
  owed* overstating, and the bound came back as a clause of that sentence
  rather than a sentence of its own.
- `shrink:` ADR-025 arguing the sentence's placement three times, in the
  context, the decision and an alternative — **fixed**: the context keeps
  the argument, the decision states the placement, the alternative refuses
  verify on its own ground.

Second read, a fresh context given only the diff of those fixes and the
same two criteria, as ADR-017 decision 3 bounds it. Six findings:

- `correctness:` the replacement *`update` writes the configuration back
  from the host's own answers* — **fixed**. `applyUpdate` writes only the
  files whose action is `write`, and an installed host file is `keep` or
  `review`, so the update does not write it at all; read in
  `tools/cairn.mjs` before the fix. The record now says the update never
  rewrites the configuration, because it is a host file.
- `correctness:` narrowing the declaration's readers to the binding table
  left the sentence resting on the one table that, as the kit generates
  it, does not carry the field — **fixed**, the two bindings separated.
- `correctness:` *by passing `--transport`*, when the option's default is
  `pull-request` — **fixed** to `--transport manual-git`.
- `correctness:` cutting ADR-025's learning-note sentence left *no note is
  owed* unqualified — **fixed**, the bound is a clause of that sentence
  now.
- `shrink:` *decided two halves in one decision*, an empty detail in place
  of a false one — **fixed**, *has two halves*.
- `shrink:` the paragraph stating one fact three times once the checker's
  single transport read was added — **fixed**, the two clauses about
  `registration` and `registration-base` cut.

The fixes above are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 8 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
dependencies          : none to install — the package declares none; `npm install` in this fresh worktree only left a stray `package-lock.json`, which `scope-drift` refused and which was deleted before the unit. The gates ran bare.
user review           : not required for the unit; the owner reads the two records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-024-the-kit-installs-what-the-skills-perform.md, docs/adr/ADR-025-a-red-run-is-read-before-the-next-unit.md, docs/adr/index.md, docs/architecture/01-cairn-1-1.md, docs/architecture/index.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S02 — the profile line and the transition: what the forge does not enforce and the declared transports printed on every run as no finding, `transitionErrors` losing `trunkIntegration` so a trunk commit taking a path from `running` to `done` with no `ready` commit is refused with its remedy, the fixture being the shape path 1's closing took
blockers    : none
```
