---
type: Cairn Coding Path Step
title: 'CP-CAIRN-009 S06 — this repository''s own'
timestamp: 2026-09-15T00:00:00Z
cairn:
  path: CP-CAIRN-009
  step: S06
---

# CP-CAIRN-009 S06

### S06 — this repository's own — **COMPLETE**

#### Plan

This repository says about itself what the specification now says about
every repository: flow pages live in the architecture folder, the module
note describes the tools as they are at the candidate, and the register's
row 4 names what this path actually wrote. It is the least because the
three surfaces are already written and only one read-through stands
between them and the candidate. It changes no tool and adds no rule.

Definition-of-done item advanced: the tenth.

Type `documentation`: the documents and the register move with this step,
and no test is carried — nothing under `tools/` changes.

**A defect found and NOT fixed here.** Four of the register's five
milestone rows read `running` for paths that are done and integrated —
rows 1, 2, 3 and 5, for CP-CAIRN-005 to CP-CAIRN-008. Only row 4, this
path's, is true. The status is hand-kept in the register while each
path's own record already carries `status:`, so the register restates a
fact nothing reconciles it with, and four of five copies have drifted.
This path's acceptance scopes the register to *this path's id in row 4
and the widened files in its writes column*; correcting four other rows
is outside it and is not slipped in. Row 6 does not own the register
either, so today nothing does — which is the finding, and it is raised to
the owner rather than quietly patched.

Deliberately not in this unit: `docs/architecture/01-cairn-1-1.md`,
`README.md`, `site/**` and anything under `skills/`, all excluded by the
path's acceptance; the candidate itself and the administrative commit,
which are S07's.

#### Work

```cairn-unit
step: S06
unit: 06
type: documentation
verified: cairn-check, cairn-test
```

- **`docs/architecture/index.md`**: a page that names components states
  the direction of its dependencies in one sentence a reader can check
  against an import line and carries one Mermaid diagram saying the same
  thing, and where the two disagree the page is wrong, not the reader
  (ADR-019 d2, ADR-023 d3). Flow pages live in this folder with the rest
  and in no folder of their own; a flow inside a single folder is that
  folder's module note's business (ADR-023 d2). The paragraph links the
  architecture concept, which S05 gave the shape.
- **`docs/modules/application.md`**, the read-through. The *kit* section
  had been appended to by S01, S02, S03 and S04 in that order and read
  as the pile that makes: the file count, then the plane, then a sentence
  about copying no specification glued to the paragraph above it, then
  Ponytail, then the removals, then the lock, then the pointer page, then
  `update`, then the workflow, then `adopt`, and last — after `adopt` —
  the paragraph about what `init` declares. It is now six named passages
  in the order a reader needs them: what `init` writes, what it does not
  copy, what the installation knows about itself, what `update` does and
  refuses, what `adopt` does, and the two host files an adopter's forge
  reads. No fact was added and none was meant to be lost; see below.
  The note's timestamp moves to today, because a note that describes now
  should say when now was.
- **`project/coding-paths/index.md`**: row 4's writes column records the
  widening this path made in flight — `tools/cairn-rules.mjs`, in S02,
  because the conformance page's two rule rows are generated from it and
  the page cannot state the recursive read unless its generator does.
  The row already carried this path's id and the registration widenings.

**The advisory this unit raises.** `decision-drift` fires: the
architecture folder changed with no decision record in the same change.
It is correct that the folder changed and correct that no record was
added — the two it states, ADR-019 d2 and ADR-023 d2 and d3, were
accepted on 2026-09-07 and 2026-09-09 and are pinned in this path's
`governs:`. The index is a folder index catching up with them, not a new
decision. Dispositioned as **accepted, with the records named**, and
carried into the closing review at S07.

#### Self-review

A unit of documents, read in the tags that apply to prose.

- `correctness:` the rewrite of the kit section was checked term by term
  against the version it replaces, and against the set of every code span
  in both. One thing was lost and is restored: `npx cairn-protocol init`,
  the command itself, which the rewrite had shortened to `init`. Nothing
  else dropped, and nothing was invented — the added set is empty.
- `delete:` *this note* appeared in the list of host files the kit
  installs, meaning "a note like this one". The kit installs a module
  note for the adopter's own area; this note is this repository's. Now
  *a module note*.
- `shrink:` the kit section's first sentence carried both *installs* and
  *what that comes to is*, and the paragraph break after *request
  template* had been lost when S02 spliced into it. Both fixed by the
  reordering rather than by patching around them.
- `shrink:` the architecture index's dependency paragraph first repeated
  the reason ADR-019 gives — that a direction right and unwritten is a
  direction nobody chose. That sentence is in `spec/concepts/architecture.md`
  where S05 put it, and the record keeps the argument; the index states
  the rule and links the concept.
- `delete:` the register row's new clause nearly said *widened in the
  path by `tools/cairn-rules.mjs` (S02)* and stopped there. A reader of
  the register cannot see why a generator is in a documentation path's
  writes, so the clause gives the reason in the same breath.
- Considered and not done: four of the register's five rows read
  `running` for paths that are done. Named in the plan above and raised
  to the owner. It is outside this path's acceptance, nothing else owns
  it, and patching it here would be the scope creep the register exists
  to prevent.
- No widening of `writes:`; all three files were already declared.

#### Review

**How this read was made.** Two fresh contexts were given this unit's
diff and the two criteria. Both hung; the second reached *"Let me check
the concrete claims against the source"* and produced nothing after it.
That is fourteen of eighteen review contexts across S01 to S06. The read
below was made by this same context and is recorded as such, as S03, S04
and S05 were.

For a unit whose whole risk is a rewrite losing a fact, the read was
made mechanically rather than by eye, which is stronger here than a
reader would have been: the *kit* section before and after was compared
term by term on every claim it makes — the file counts, the folders, the
lock, the pointer page, `--take`, the Git diff, the two records, both
transports, the absent suite — and then on the **set of every code span
in both texts**, which catches a name dropped anywhere in seventy lines
without relying on anyone noticing it was gone.

Findings, each with its disposition:

- `correctness:` the rewrite dropped `npx cairn-protocol init`, the
  command itself, shortened to `init` in a heading. The set comparison
  caught it; no reading of the prose would have, because the paragraph
  still made sense without it. **Fixed** — the heading names the command
  again. The added set was empty, so nothing was invented.
- `correctness:` *this note* listed among the host files the kit
  installs, meaning *a note like this one*. The kit writes a module note
  for the adopter's own area; this note is this repository's. **Fixed**
  in the rewrite itself.
- `correctness:` the `decision-drift` advisory — checked, and it is the
  rule doing its job on a folder index catching up with two records
  accepted a week earlier. Dispositioned as accepted with the records
  named, in the Work section above and again in the closing review.
- `correctness:` every concrete claim in the rewritten section was
  re-checked against the code it describes — 33 files and the lock on
  `ci` and 32 on `local` from a real `init`, the three concept folders
  and the absent root index, `takeRelease` moving one lock entry,
  `applyAdopt` digesting the migrated configuration, and the generated
  workflow having no test step. All true at this commit.
- `shrink:` four of the register's five rows still read `running` for
  paths that are done. Named, raised to the owner, and **not fixed**:
  outside this path's acceptance, as the plan says.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, 1 advisory (exit 0)
advisory              : decision-drift — the architecture folder changed with no record in the same change. Accepted with its records named: ADR-019 d2 and ADR-023 d2, d3, both pinned in this path's governs:. Carried into the closing review
npm test              : pass — 344 tests, 0 failed (exit 0); unchanged since S04, as two documentation units should leave it
cairn-active --check  : pass — the view is already current; no path state changed
no new test           : nothing under `tools/` changed and no rule was added, so this unit carries none and its plan says so (ADR-018 d1)
the rewrite           : compared with the text it replaces term by term and on the set of every code span in both — one loss found and restored, nothing invented
claims re-checked     : the figures and behaviours the kit section states, read off the code and a real `init` at this commit
review                : one read, by this context; two fresh contexts hung. Recorded in the Review section, not implied
user review           : not required for the unit; the owner runs `cairn init` and reads what it wrote before the merge
remote                : pushed immediately after this record; the candidate is S07's
```

#### Checkpoint

```text
status      : running
current step: S06 complete on push
retention   : none (no-rewrite host)
changed     : docs/architecture/index.md, docs/modules/application.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S07 — the candidate: trunk merged in, gates bare, the request opened with the description cairn-audit prints, the administrative commit and its check green, and the owner running `cairn init` into an empty folder before the merge
blockers    : none
```
