---
type: Cairn Coding Path Step
title: 'CP-CAIRN-013 S01 — the open skill and the configuration reference'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-013
  step: S01
---

# CP-CAIRN-013 S01

### S01 — the open skill and the configuration reference — **COMPLETE**

#### Plan

Write into `cairn-open` what four records of 1.2 decided, as the plan's
first item says: the two reads before the go-ahead (ADR-030); the
precondition of the direct-push sequence and the `pull-request` sequence
beside it, each under the declaration it serves (ADR-032 decisions 1 and
2); step 1 reading `project/backlog/` (ADR-041); and the definition of
done written as a plain list with no box (ADR-042). The configuration
reference follows in the same commit: its `init` paragraph and its
`transport` row, which are where a reader looks for what a declaration
means.

Definition-of-done item advanced: the first, in full — the skill, its
reference and `spec/reference/configuration.md`.

Type `documentation`: the change is the skill, its command reference and
the specification page that says the same thing; no rule, tool or fixture
moves, so this unit carries no new test, and the gate that proves it is
`cairn-check` on the tree plus the tools' own suite unchanged.

Deliberately not in this unit: the close skill, the unit skill, the
seventh skill, the template, the chapters and the layout reference (S02
to S05); the `registration` rule's second reading, the installer's
refusals and the pilot, which ADR-032 decisions 3 and 4 name and the
register gives to rows 2 and 3 — this unit's sentences describe the
sequence a writer performs, which needs no tool change to be performed.

Found while reading, and left: `spec/concepts/trunk-registration.md`
says *a protected transport needs a special registration route*, which
ADR-032 decision 2 now supplies. The sentence is not contradicted by
this unit — it names a need, and ADR-032 meets it — but the sentence now
reads as an open problem when it is a solved one. The concept file is
outside the `writes:` the acceptance pinned and outside every item of the
definition of done, so this unit leaves it and the boundary report names
it for the owner. Chapter 5 of
`spec/index.md` already lands the registration commit *through the
repository's declared transport* and says nothing this unit changes.

#### Work

```cairn-unit
step: S01
unit: 01
type: documentation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`skills/cairn-open/SKILL.md`**, step 1: one sentence reading
  `project/backlog/` before the folder is written, with what taking an
  item costs — the goal names it, `writes:` declares its file, the last
  unit deletes it; and the definition-of-done paragraph now asks for a
  plain list and says why nothing inside the pinned text is a control to
  tick.
- **`skills/cairn-open/SKILL.md`**, step 2: one paragraph before the
  owner's reading — an angle-bracketed name in a surface the records
  name is put to the owner with the plan, and each item of the
  definition of done is read once against the decision it cites and
  corrected while the correction is still free.
- **`skills/cairn-open/SKILL.md`**, step 3: the commit is the same under
  both transports and the direct push has a precondition — an
  unprotected trunk, or a bypass for the writer; the landing is two
  bullets, one per declaration, the request sequence carrying the commit
  alone on its own branch, one request, its run green, the merge the
  trunk's rules allow.
- **`skills/cairn-open/reference.md`**: the shared sequence gains the
  one line that branches from `origin/main` under `pull-request`
  registration, and the single `git push origin HEAD:main` becomes two
  sections under the declaration each serves, the second ending with the
  ancestry check that says whether the merge kept the commit.
- **`spec/reference/path-template.md`**: the definition of done is five
  plain items and the sentence beside it says why no box stands in the
  pinned text. The plan gives the template to S05; it moved here because
  step 1 of the skill links it three lines from the sentence that now
  refuses a box, and the review found the two pages disagreeing.
- **`spec/reference/configuration.md`**: the `init` paragraph says *the
  sequence its declaration names* where it said *the one registration
  sequence the open skill ships*, and names what declares
  `pull-request`; the `transport` row states both registration
  sequences beside the two integration ones it already stated.

#### Self-review

- `shrink:` step 3's first draft said the precondition in the paragraph
  and again in the `manual-git` bullet — the bullet keeps only the
  action, the paragraph keeps the precondition.
- `shrink:` the reference's two new sections first repeated the
  precondition sentence of the skill in full; the `manual-git` section
  states it in one clause because the reference is where the writer is
  standing when the push is refused.
- `delete:` a third bullet in step 3 saying *neither sequence codes
  before the trunk carries the declaration* — the sentence is already in
  the `pull-request` bullet, where it is the one that is new.
- `yagni:` no sentence names a record; no sentence names the checker's
  second reading, the installer's refusals or the pilot, which are rows 2
  and 3 and would be a claim about a tool that does not do it yet.
- `native:` the `pull-request` sequence adds no Cairn machinery — a
  branch, a request, a required check and a merge, which is what the
  host already does for a candidate.
- `correctness:` the reference branches with `git switch -c` after the
  clean-status check and before the record is edited, so the commit is
  born alone on its branch and the local trunk keeps nothing of its own;
  an earlier draft committed on `main` and moved the branch afterwards,
  which left the writer resetting a trunk.
- `delete:` `package-lock.json`, written by `npm install` in this fresh
  worktree and untracked; removed, not committed, as CP-CAIRN-012 S01 did.
- No widening of `writes:`: `skills/**`, `spec/reference/configuration.md`
  and this path's folder are all declared.
- `shrink:` after the review's fixes the unit lost the clause restating the
  scope-amendment sentence four paragraphs below it, the precondition's
  fourth copy in the `transport` cell, and the paragraph after the
  request block that said the squash trap a second time.
- Net: 100 lines added and 25 removed across the four files, and this step
  and the record's resume.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's diff
in one file and the two criteria and nothing else; it returned in 185 seconds
with sixteen findings, on the first attempt. Each with its disposition:

- `correctness:` step 4 branched the worktree from local `main`, which under
  `pull-request` registration never carries the registration commit —
  **fixed**: `git fetch origin main` and `origin/main`, as the reference
  already had it.
- `correctness:` *merge as the trunk's own rules allow* permits a squash,
  which replaces the commit whose parent `registration-base` reads —
  **fixed**, and confirmed against `pathRegistrationBaseState` before
  writing: the skill and the reference both refuse a squash and a
  rebase-merge, and the reference ends with the ancestry check.
- `correctness:` the register branch was cut from local `main`, not from the
  `origin/main` just printed as `base_commit` — **fixed**, the branch is
  created at `origin/main`.
- `correctness:` the request block's commands do not run in order — the merge
  is a manual step and nothing marked it — **fixed**, one comment line.
- `correctness:` *read its run bare and green* — the file's own preamble
  defines *bare* for a gate, and a run on the host is not run bare —
  **fixed**, *read its run green*.
- `correctness:` *never as checkboxes* contradicts the template it links to,
  `definitionItems` in `tools/cairn-audit.mjs` and the close skill's clause —
  **fixed in part**: the template's list moved into this unit, which is what
  the link points at. The close skill's clause is S02. `definitionItems` is
  row 4 of the register and `tools/**` is excluded from this path by the
  acceptance, so it is **refused here** and named in the boundary report: a
  path opened from the new template prints no item-by-item section at its
  closing until row 4 lands, which this record's *Tried and rejected*
  already states.
- `correctness:` `project/backlog/` does not exist in this repository —
  **refused**: ADR-041 gives the folder and its index to `planInstall`, row 3
  of the register, and `tools/**` is excluded here. Hedging the sentence
  would write a condition no record decided.
- `correctness:` *this path's last unit deletes the file* is a close-time
  obligation lodged in step 1 — **refused**: ADR-041 names step 1, and the
  writer who declares the file in `writes:` is the one who must know what
  taking it costs.
- `correctness:` *read the plan* when the second read is of the record, and
  `plan.md` is optional — **fixed**, the sentence reads the record.
- `correctness:` *the decision it cites* assumes every item cites one —
  **fixed**, *the record it rests on*.
- `correctness:` *before the folder is written* attached to `project/backlog/`
  — **fixed**, *the path folder*.
- `shrink:` the new clause on the free correction restated the
  scope-amendment sentence four paragraphs below — **fixed**, cut.
- `shrink:` the direct push's precondition written four times — **fixed in
  part**: cut from the `transport` cell; kept in step 3, which ADR-032
  decision 1 names, and as one clause in the reference, where the writer is
  standing when the push is refused.
- `shrink:` *nothing is coded before the merge* restates step 4 — **fixed**,
  cut from the reference here and from the skill by the second read.
- `correctness:` the `init` paragraph named no mechanism by which a
  repository comes to declare `pull-request` registration — **fixed**, with
  the second read's finding 7.
- `shrink:` the `transport` cell was the table's only three-sentence cell —
  **fixed**, one sentence.

Second read: a fresh context, a subagent of this harness, given the fixes and
the same two criteria; it returned in 247 seconds, on the first attempt. The
snapshot handed to it had dropped the two skill files, which it said in its
first line and then read their working-tree diff itself, so it judged the
whole unit and not only the fixed lines. Eleven findings, each with its
disposition:

- `correctness:` the plain template breaks `cairn-audit` — the same finding
  as the first read's, with the two versions run through `definitionItems` to
  prove it — **refused here** for the same reason: the regex is row 4's and
  `tools/**` is excluded.
- `correctness:` *turns `registration-base` red on the path's first run*
  overstates — a squash on a trunk that did not move yields a commit whose
  parent is still `base_commit` — **fixed**: the rule goes red for every
  trunk that moved in between, and the ancestry check is the proof.
- `correctness:` the comment said *delete the branch* before the command that
  reads the local branch — **fixed**, the deletion moved after the check.
- `correctness:` `git fetch origin main` does not advance local `main`, so
  the `manual-git` sequence can commit on a stale trunk — **fixed**,
  `git merge --ff-only origin/main` in both blocks. The push would have been
  rejected, but only after the record was written.
- `correctness:` `project/backlog/` does not exist — **refused**, as above.
- `correctness:` *and it names two* has no antecedent — **fixed**, *performs
  the sequence the declaration names, and carries both*.
- `correctness:` the surviving *keeps it* clause had no antecedent once the
  added clause went — **fixed**, one clause names the case and says the
  repository declares it itself.
- `correctness:` step 2 mixed *the record* with an undefined plural, and the
  angle brackets were abstract — **fixed in part**: `<placeholder>` and *the
  records this path is scoped from*; ADR-030's *a folder, a file, a field*
  stays, because it is what the record enumerates.
- `shrink:` step 3's bullets restated the reference's rationale — **fixed in
  part**: the `registration-base` reason and the branch deletion cut from the
  skill, which keeps *never a squash, never a rebase-merge* — a writer who
  reads only the skill must not fall in it.
- `shrink:` *nothing is coded before it*, unclear referent and already step
  4's first line — **fixed**, cut.
- `shrink:` the `transport` row rewrite buys nothing — **refused**: the old
  cell described only what each value means for a candidate and said nothing
  about a registration, which is the half this unit's item requires it to
  name.

The fixes of the second read are not read a third time; the self-review above
carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare after the template's list changed; nothing under tools/ moved in this unit
cairn-active --check  : pass — the view is already current; no path state changed
the squash claim      : read in tools/cairn-check.mjs, pathRegistrationBaseState — base_commit is compared with the parent of the commit where status became running, and recordHistory walks the trunk without --first-parent; both the refusal and its limit are written from that code, not from memory
the audit claim       : read in tools/cairn-audit.mjs, definitionItems — the only match is a boxed line, which is why the gap is named and left to row 4
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's snapshot dropped the skill files and it read them itself, which its report states and this section repeats
user review           : not required for the unit; the owner reads the skills with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : skills/cairn-open/SKILL.md and reference.md, spec/reference/configuration.md, spec/reference/path-template.md, this path's index and this step
session     : safe boundary after a successful push
next action : S02 — the close skill's order and its sentences
blockers    : none
```
