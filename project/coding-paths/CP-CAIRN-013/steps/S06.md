---
type: Cairn Coding Path Step
title: 'CP-CAIRN-013 S06 — the candidate'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-013
  step: S06
---

# CP-CAIRN-013 S06

### S06 — the candidate — **COMPLETE**

#### Plan

Make the branch a candidate, as the plan's sixth item says: the trunk
merged in, the register's row 1 of 1.2 naming this path, the gates run
bare, and the exclusions of the definition of done verified rather than
assumed. Closing follows this unit and writes no step of its own.

Definition-of-done items advanced: the seventh, whose last clause is the
register's row; and the eighth, whose first clause — the candidate
containing the trunk tip and checked — is this unit's merge and gate.

Type `documentation`: the register is a document of the project plane,
and the merge carries no change of its own. No rule, tool or fixture
changes, so this unit carries no new test.

The trunk moved while this path ran: `217826a` landed two feedback notes
from Atomik's first `update`, through pull request 29. `feedbacks/**` is
in neither this path's `writes:` nor its `governs:`, so the acceptance
drift predicate is not met and the merge is an ordinary one. The notes
were read, because one of them is about the movement S04 wrote a skill
for; what they ask for is below, unimplemented and named.

Where the path has departed from the seventh item of its definition of
done, stated here so the closing review states it too: the item says the
register *gains this path's id in row 1 of 1.2 and nothing else*, and
four cells of it have moved. S02 and S03 added to row 1's Writes and
Outcome cells the concept notes those units widened `writes:` by, so the
record and the register would not disagree about which files this row
touches. This unit adds the path's id, and with it the two surfaces the
row never listed although the record declared them at registration —
`AGENTS.md` and the register itself — and corrects the 1.2 milestone
row, whose state cell still said *the coding paths: no path yet* after
row 1 named one — that cell now says what the 1.1 milestone row said,
and the coding-paths table carries which path runs. Each change keeps
the register true about this path;
none of them is the id alone. The owner arbitrates it at the merge.

Deliberately not in this unit, and named for the owner rather than
silently carried: the new note's fourth observation is a live defect in
`.github/pull_request_template.md`, a file this path declares — line 45
carries the one blank the file's own header says must be backticked, and
the forge strips it. It is one line. It is not implemented here, because
a provisional note is not authority: chapter 3 is where an idea becomes a
reason, and ADR-038 gives a note the channel that ends in a record. The
same holds for its fifth observation, the missing `steps/` directory that
three consecutive paths in this repository have hit, this one included.

#### Work

```cairn-unit
step: S06
unit: 06
type: documentation
verified: cairn-check, cairn-test, cairn-active --check
```

- **the trunk merged in**: `origin/main` at `217826a` merged into the
  path branch, never rebased, as this repository's `forbidden` history
  policy requires. The merge carries only what the trunk brought — two
  feedback notes and the folder's index — and no change of this path's.
- **`project/coding-paths/index.md`**: row 1 of the 1.2 table names
  `CP-CAIRN-013` and `running`, in the shape every row of the 1.1 table
  uses. Its Writes cell keeps the list the promotion wrote and gains the
  two clauses that table already has a vocabulary for — *widened at
  registration* for `AGENTS.md` and the register, which the record
  declared at registration and the row never listed, and *widened in the
  path* for the four concept notes S02 and S03 added, one clause per
  step. The 1.2 milestone row's state cell drops the clause row 1 now
  falsifies.
- **the exclusions, measured rather than asserted**: the twelve
  governing documents read by `git rev-parse HEAD:<path>` against the
  ids the record pins — all twelve identical; the hunks of
  `spec/index.md` mapped to their chapters — 1, 4 and 5 only, so
  chapters 2, 3 and 6 are untouched; the file list of the whole path
  read against `writes:` — every file declared, the two feedback notes
  and the live view arriving with the trunk and the registration commit
  rather than from this branch.

#### Self-review

- `yagni:` the register's State column is not touched. ADR-031 decision
  2 makes those cells `cairn-active`'s, which is row 2 of the register;
  until then they are the owner's, and the definition of done asks this
  path for the id and nothing else.
- `delete:` a paragraph in this step retelling what the two feedback
  notes say — they say it themselves, on the trunk, and this step needs
  only what it does about them.
- `native:` nothing was built for the candidate: a merge, a table cell,
  and three readings by command.
- No widening of `writes:`: `project/coding-paths/index.md` and this
  path's folder are declared.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's diff
in one file and the two criteria and nothing else; it returned in 80 seconds
with three findings, on the first attempt. Each with its disposition:

- `correctness:` the changed cell falsified the 1.2 milestone row three lines
  above it, whose state still read *the coding paths: no path yet* —
  **fixed**, and by the second read reduced to what the 1.1 milestone row
  said, the coding-paths table carrying which path runs.
- `correctness:` row 1's Writes cell contradicted the record it now links to,
  listing neither `AGENTS.md` nor the register although the record declared
  both at registration — **fixed**, in the table's own vocabulary.
- `correctness:` with those two named, row 1 and row 4 share a file, which
  the table's preamble already governs: row 4's path declares `depends_on`
  this one at its registration — **accepted**, not a change to make here;
  the preamble does the work once the file is named twice, which the fix
  above does.

Second read: a fresh context, a subagent of this harness, given the fixes and
the same two criteria; it returned in 118 seconds with three findings, on the
first attempt. Each with its disposition:

- `correctness:` the two registration surfaces were put in the base list,
  which contradicts the register as the promotion wrote it — **fixed**,
  *widened at registration*, the clause 1.1's rows 3, 4 and 5 use.
- `shrink:` *coding path 1 running* in the milestone row restates the Path
  cell below it and is a second cell to edit at every opening and closing —
  **fixed**, the milestone row says what it said for 1.1.
- `correctness:` one clause lumped two widenings the record attributes to
  different steps — **fixed**, one clause per step, as every 1.1 precedent
  writes it.

The fixes of the second read are not read a third time; the self-review above
carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare; one advisory, remote-checkpoint, which this commit's push clears
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare; nothing under tools/ moved in this unit or in this path
cairn-active --check  : pass — the view is already current; the path's status has not changed in this unit
governing documents   : all twelve read by `git rev-parse HEAD:<path>` against the ids the record pins — identical, so the candidate governs what it was accepted under
chapters untouched    : every hunk of spec/index.md mapped to its chapter — 1, 4 and 5 only; chapters 2, 3 and 6 are byte-identical
excluded surfaces     : the path's whole file list read against writes: — tools/, the workflow, cairn.config.json, cairn.lock.json, docs/, README.md, CHANGELOG.md and site/ are untouched; feedbacks/ and the live view moved on the trunk, not on this branch
trunk merged in       : origin/main at 217826a, by merge, never rebase; the candidate contains the trunk tip
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's fixes not read again
user review           : the owner reads and tries the skills before the merge, after the administrative commit, as ADR-040 and this path's own close skill now order it
remote                : pushed immediately after this record; this commit is the candidate unless closing finds a reason for another
```

#### Checkpoint

```text
status      : running
current step: S06 complete on push
retention   : none (no-rewrite host)
changed     : project/coding-paths/index.md (row 1 of 1.2 and the 1.2 milestone row), the trunk merged in, this path's index and this step
session     : safe boundary after a successful push
next action : close the path with cairn-close, as this path leaves it — the gate on C, the administrative commit, the fresh-context coherence read in the request's description, the owner's try, the merge
blockers    : none
```
