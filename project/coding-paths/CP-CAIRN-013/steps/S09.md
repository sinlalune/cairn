---
type: Cairn Coding Path Step
title: 'CP-CAIRN-013 S09 — what the third closing read found'
timestamp: 2026-09-24T00:00:00Z
cairn:
  path: CP-CAIRN-013
  step: S09
---

# CP-CAIRN-013 S09

### S09 — what the third closing read found — **COMPLETE**

#### Plan

The fresh coherence read of candidate `aa0e38a` answered *Yes* to its
first and fourth questions. This unit carries the two findings that are
defects of this path's own writing, records the owner's two rulings on
the rest, and voids that candidate: a fourth is produced from this
commit.

Carried here:

- `spec/reference/repository-layout.md`'s new `feedbacks/*.md` row said
  a note is *never edited after a release answers it*. ADR-038 edits one
  twice: decision 4 gives a note whose claim was withdrawn a correction
  line at its head, written by whoever corrects it, and decision 3
  rewrites its relative links when the release moves it into
  `feedbacks/<release>/`. The cell now names both.
- `spec/concepts/index.md` walks the concepts of a path's close in the
  order this path supersedes — acceptance, drift, then administrative
  closure — beside chapter 5, the execution protocol and the portable
  convention, which all give the new one. It is the shape the owner
  ruled on for `spec/reference/paths.md` in S07, fix in the candidate,
  so `writes:` is widened by the page without asking a second time; the
  register's row names the widening in its own clause, and moves S07's
  widening into one too, where it had been written into the base list.

Ruled by the owner in the chat of 2026-09-24, and carried by no change:

- **The squash ban stays, and its limit is named.** The open skill and
  its reference say a `pull-request` registration merges keeping the
  commit, never by squash or rebase-merge, because `registration-base`
  reads the parent of the commit that declared the path `running`.
  ADR-032 decision 2 says *the merge the trunk's own rules allow*, and
  no record says that a trunk allowing only squash merges cannot
  register. The closing review names that gap for the record that
  follows.
- **The fifth input stays.** The fresh reader is given the records of
  the running sibling paths beside ADR-043's closed list, because the
  live view cannot answer the second question. The superseding record
  that follows this path writes it into ADR-043, with what `ready`
  states and the squash limit.

Definition-of-done item advanced: the ninth — closed by the skill as
this path leaves it, whose third coherence read found these.

Type `repair`: a statement of this path's own that contradicts a
governing record, and a page of the specification left in the order the
path supersedes. No rule, tool or fixture changes, so this unit carries
no new test.

Not carried, and named in the closing review: the ordering exception is
now written in nine places with no cross-links, four of which ADR-040
did not name; and `requestTemplate` in `tools/cairn.mjs`, the pilot's
boxed definition of done and `definitionItems` in `tools/cairn-audit.mjs`
disagree with the specification this path changed, each assigned to a
later row of the register.

#### Work

```cairn-unit
step: S09
unit: 09
type: repair
verified: cairn-check, cairn-test, cairn-active --check
```

- **`spec/reference/repository-layout.md`**: the `feedbacks/*.md` row's
  last cell names its owner and nothing else — the *never edited* this
  path had written there is gone.
- **`spec/concepts/administrative-closure.md`**: the frontmatter
  description, which still said the commit *records audit and
  acceptance after the accepted candidate*, false on `pull-request`
  since S02, now says it records `C` ready.
- **`spec/concepts/index.md`**: the closing chain reads audit,
  administrative closure, acceptance, drift, integration.
- **`project/coding-paths/CP-CAIRN-013/index.md`**: `writes:` widened by
  `spec/concepts/index.md`; `current_step` reads `S09`.
- **`project/coding-paths/index.md`**: row 1's Writes cell gives each
  widening its own clause — by S07, `spec/reference/paths.md`; by S09,
  `spec/concepts/index.md`.

#### Self-review

- `delete:` the feedbacks cell's edit rule, entirely, on the reads: the
  history shows notes growing whole passages after they were written and
  no note carrying a correction line, so a rule that the repository does
  not follow was stated as the layout. ADR-038 never asked the row for
  one; the cell names who writes the note, as its neighbours name their
  owners.
- `yagni:` no text changes for the two rulings. Both are about what a
  record must still say, and the closing review is where a gap between a
  record and the text is stated.
- `correctness:` the chain's old order put acceptance drift before
  administrative closure, which matched neither the old skill nor the
  new one; it now matches the new one.
- Widening of `writes:`, named here as movement 3 asks:
  `spec/concepts/index.md`, under the owner's S07 ruling on the same
  shape. No item of the definition of done moves, so the scope digest is
  untouched.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's diff
in one file and the two criteria and nothing else, and told to check claims by
running commands where they can be — the lesson of S08; it returned in 118
seconds with three findings, on the first attempt. It confirmed by command
that the chain's new order matches chapter 5, the execution protocol and the
portable convention, and that the register's row now equals `writes:`. Each
finding with its disposition:

- `correctness:` the feedbacks rule — *only a correction line, and its links
  when moved* — contradicts the files it governs: notes grew whole passages
  after they were written, none carries a correction line, and the glob also
  matches the folder's index — **fixed**, the rule is deleted.
- `correctness:` the link clause named the wrong notes; the release commit
  moved its notes as pure renames and re-pointed the links of notes that
  stayed — **fixed** by the same deletion.
- `correctness:` the administrative-closure concept's description said the
  commit records audit and acceptance, which the reorder made false on
  `pull-request` — **fixed**; the file is in `writes:`.

Second read: a fresh context, a subagent of this harness, given the two fixed
lines and the same two criteria; it returned with one finding, on the first
attempt:

- `correctness:` the deletion also removed a true clause — a release that
  moves notes fixes the links in the notes that stay — **refused**: the
  column names who owns a file, not each mechanical edit a move makes, and
  the next row already gives the move to the release path. The reader
  offered this reading itself.

The fixes of the second read are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare
cairn-active --check  : pass — the view is already current
the chain's order     : confirmed by the reader against spec/index.md §5, spec/reference/execution-protocol.md and spec/reference/paths.md
the register row      : its Writes cell equals the record's writes:, each widening in its own clause
review                : two reads by fresh contexts, both obtained on the first attempt, the first told to check by command
user review           : the owner ruled on the squash ban and the fifth input in the chat of 2026-09-24, before this unit was written
remote                : pushed immediately after this record; this commit is the fourth candidate
```

#### Checkpoint

```text
status      : running
current step: S09 complete on push
retention   : none (no-rewrite host)
changed     : spec/reference/repository-layout.md, spec/concepts/index.md, spec/concepts/administrative-closure.md, the register's row 1, this path's index and this step
session     : safe boundary after a successful push
next action : close on this commit — the gate bare, a fresh coherence read of it, request 30's description rewritten, the administrative commit, the owner's try, the merge
blockers    : none
```
