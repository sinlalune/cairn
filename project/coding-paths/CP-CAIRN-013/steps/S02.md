---
type: Cairn Coding Path Step
title: "CP-CAIRN-013 S02 — the close skill's order and its sentences"
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-013
  step: S02
---

# CP-CAIRN-013 S02

### S02 — the close skill's order and its sentences — **COMPLETE**

#### Plan

Reorder the close skill and write its five changed sentences, as the
plan's second item says: the administrative commit before the reading,
with the execution protocol's closure list, chapter 5 and the
administrative-closure concept giving the same order and `manual-git`
unchanged (ADR-040); the measured figure written once in the conformance
page's budget table and linked (ADR-031 decision 1); the pointer at
`tools/soundness.md` gone from step 5 and from the reference (ADR-034
decision 8); the `deferred` disposition naming a file under
`project/backlog/` (ADR-041); the clause keeping the record's checkboxes
gone (ADR-042); and the two release-path sentences — the changelog's
section and the notes moved into `feedbacks/<release>/` (ADR-039,
ADR-038 decision 3).

Definition-of-done item advanced: the second, in full.

Type `documentation`: the skill, its command reference and the three
specification pages that give the same order move together, so no page
says the old order between units. No rule, tool or fixture changes, so
this unit carries no new test.

A decision put to the owner in the chat, and answered before this unit
was committed: ADR-040 makes `A` — the commit that sets `status: ready` —
land before the acceptance, and five pages say `ready` means the
candidate is accepted. The record named none of them. Two of the five are
outside the `writes:` the acceptance pinned. The owner was given four
ways to go on — amend all five here and widen `writes:`, amend only the
three already in scope, change nothing and carry the gap into the closing
review, or reopen ADR-040 — and chose the first: the wording follows the
order already accepted. Reading them, `spec/reference/human-records.md`
needed nothing: its example is the closing record, which exists only on
`manual-git`, where the acceptance does precede the closure. Four pages,
not five.

Where this unit departs from the plan: two files it does not name move
one phrase each. Chapter 5 and this repository's
`.github/pull_request_template.md` both say a deferral is *deferred to a
named owner and follow-up*, which is the same act the close skill's
sentence now sends to a file; leaving either would have the chapter and
the skill asking for two different things in the same breath. Both are
declared in `writes:`. The template's *Coherence* list is still S03's.

Deliberately not in this unit: the coherence questions as a fresh-context
read, which is S03 and touches the same two steps of this skill — this
unit leaves step 4's reader sentence exactly as it was; the unit skill and
the seventh skill (S04); the template, the chapters' folder lists and the
layout reference (S05). The changelog, `feedbacks/<release>/` and
`project/backlog/` are folders and files the kit and the release build —
rows 3 and 5 of the register — and this unit only says what a closing
does with them.

#### Work

```cairn-unit
step: S02
unit: 02
type: documentation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`skills/cairn-close/SKILL.md`**: steps 3 and 4 exchange places, so the
  administrative commit stands between the review and the acceptance, with
  one paragraph saying it lands there on `pull-request` — the gate green on
  `C`, nothing in `A` that the reader reads, and a merge click that then
  integrates a branch whose `ready` is already on it — and is made after
  step 4 on `manual-git`, where `A` carries the closing record. The head
  paragraph and the skill's own description follow the new order.
- **`skills/cairn-close/SKILL.md`**, step 1: one paragraph for a release
  path — the changelog's section naming the adopter repairs absorbed and
  not absorbed by their path ids, then what the release changes for an
  adopter; and every answered note moved into `feedbacks/<release>/` with
  an index naming what answered each line.
- **`skills/cairn-close/SKILL.md`**, step 2: the advisory disposition names
  a file under `project/backlog/`; the clause keeping the record's
  checkboxes is gone, *nothing is ticked* standing alone; and the
  README paragraph gains the sentence that puts a measured figure of the
  kit in the conformance page's budget table, one row per figure, linked
  everywhere else.
- **`skills/cairn-close/SKILL.md`**, step 5, and
  **`skills/cairn-close/reference.md`**: the pointer at
  `tools/soundness.md` is gone from both; the sentences keep the rule and
  drop the finding.
- **`skills/cairn-close/reference.md`**: *Review and accept exactly C*
  becomes *Review exactly C*, the acceptance leaving it for a section of
  its own after *Create administrative commit A*, which now opens with the
  one line saying when it is made on each transport.
- **`spec/reference/execution-protocol.md`**: closure steps 4 and 5
  exchange places, and one paragraph after the list says why, and that the
  transport whose closure carries the acceptance keeps the other order.
- **`spec/index.md`**, chapter 5: the same exchange in the numbered
  closure list, the reason on the closure item, and the `manual-git`
  paragraph saying the two steps there keep the other order. Its advisory
  disposition names a backlog file.
- **`spec/concepts/administrative-closure.md`**: one sentence saying when
  `A` is made on each transport, above the paragraph that already
  separates the two.
- **`.github/pull_request_template.md`**: the advisory line names the
  owner and the follow-up file.
- **`spec/index.md`**, chapter 5, and **`spec/concepts/lifecycle.md`**,
  **`spec/reference/path-template.md`**, **`spec/concepts/implementation-candidate.md`**:
  what `ready` states. It is the candidate checked, administratively
  closed and proposed; the acceptance comes with the closure where the
  closing record carries it, and after it where an approval on the
  request is the acceptance. The state table's cell follows.
- **`project/coding-paths/CP-CAIRN-013/index.md`**: `writes:` widened by
  `spec/concepts/lifecycle.md` and `spec/concepts/implementation-candidate.md`,
  the two pages of the five outside it, on the owner's answer.

#### Self-review

- `delete:` the reference's *The approval is the acceptance* stood in the
  review section describing a step two sections below; cut there, stated
  once in the new acceptance section.
- `shrink:` the skill's new `pull-request` paragraph first repeated the
  reason twice — that `A` changes nothing the reader reads, and that the
  candidate is fixed before either; the second is the head paragraph's
  already, and went.
- `shrink:` the execution protocol's new paragraph first named both
  transports by value; the page is portable and already names
  `manual-git` once, so the paragraph says *where the acceptance is
  written into the closing record the closure carries* and names neither.
- `yagni:` no sentence names a record, and none claims the changelog,
  `feedbacks/<release>/` or `project/backlog/` exists in a repository
  today: each says what a closing does with the folder its release or its
  kit makes.
- `native:` the reorder adds nothing — two sections change places, and
  the one act that was already conditional on the transport says so in
  one more line.
- `correctness:` the first draft of the deferral sentence sent the
  disposition to a backlog file and dropped the owner; the `acceptance`
  rule refuses a `deferred` entry without both `owner` and `follow_up`,
  so a `manual-git` closing written from it would have been refused by
  the gate. The file is the follow-up and the owner stays.
- Widening of `writes:`, named here as movement 3 asks: two concept
  files, `spec/concepts/lifecycle.md` and
  `spec/concepts/implementation-candidate.md`, on the owner's answer to
  the decision above. No item of the definition of done moves, so the
  scope digest is untouched.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's diff
in one file and the two criteria and nothing else; it returned in 206 seconds
with sixteen findings, on the first attempt. Each with its disposition:

- `correctness:` five pages say `ready` means the candidate is accepted,
  while `A` — which sets `ready` — now lands before the acceptance;
  `spec/concepts/lifecycle.md` says the closure commit comes *after* the
  acceptance in so many words — **put to the owner** as the decision above,
  and **fixed** on their answer in four of the five pages, with `writes:`
  widened by the two outside it. The fifth,
  `spec/reference/human-records.md`, needed nothing: its example is the
  closing record, which exists only on `manual-git`.
- `correctness:` the `acceptance` rule refuses a `deferred` entry without
  both `owner` and `follow_up`, so the new disposition wording would have
  made a `manual-git` closing refused by the gate — **fixed**, read in
  `tools/cairn-check.mjs` before rewriting: the owner stays and the backlog
  file is the follow-up. `spec/concepts/finding.md`, which says a deferral
  names an owner and a follow-up, is then true untouched.
- `correctness:` the release paragraph sat under *Produce the candidate*
  two lines after *closing adds no implementation commit of its own* —
  **fixed**, and by the second read moved above the produce instructions,
  where it can still be acted on.
- `correctness:` the budget table has no skill-count row, so the new
  sentence named a row that does not exist — **fixed**, the sentence names
  no figure; the four restatements and the table's rows are ADR-031
  decision 1's other surfaces, outside this path.
- `correctness:` *before anyone is asked to read* contradicts step 2, which
  opens the request — **fixed**, *before the owner is asked to read and
  try*, which is the record's own phrase and is not the opening of a
  request.
- `correctness:` *the two are one act in the other order* — one act has no
  order, and they are two — **fixed**, and cut to one sentence by the
  ladder finding below.
- `correctness:` `project/backlog/` does not exist in this repository —
  **refused**, as in S01: the folder is `planInstall`'s, row 3.
- `correctness:` dropping *the record's own checkboxes stay as they are*
  removes a safeguard while records still carry boxes — **refused**:
  ADR-042 names that clause, and the safeguard is the `scope-digest` rule,
  which refuses a tick in a pinned definition of done whatever a skill
  says.
- `shrink:` the order's rationale written six times — **fixed in part**:
  cut from the execution protocol and from chapter 5's step 4, kept in the
  concept, which is its home, and in the skill, where the writer acts.
- `shrink:` the reference's new acceptance section carries no command —
  **fixed in part**: kept, because a sequence file that omits a step of the
  sequence is worse, and cut to three lines that say why it has none.
- `correctness:` one line spliced to 104 characters — **fixed**, the budget
  sentence is its own paragraph.
- The reader also confirmed what it found clean: the renumbering is
  consistent, no anchor points at the renamed heading, the two
  `tools/soundness.md` pointers are gone with no third, and the checker's
  `acceptance` rule reads the same facts under either order, so the reorder
  breaks no fixture.

Second read: a fresh context, a subagent of this harness, given the fixes and
the same two criteria; it returned in 279 seconds with ten findings, on the
first attempt. Each with its disposition:

- `correctness:` the lifecycle rewrite dropped closing acceptance from the
  `ready` invariants, but `closingAcceptanceErrors` refuses a `ready` record
  whose closing record lacks `accepted_by`, `accepted_at` or
  `decision: accepted` — **fixed**, read in the checker first: `ready`
  requires the acceptance on `manual-git`, where the closure carries the
  record, and the approval follows the closure on `pull-request`.
- `correctness:` the execution protocol's surviving sentence lost both its
  antecedents when the two above it were cut — **fixed**, it names step 5
  and step 4.
- `correctness:` *read the pages and try the result* names one act as two,
  and step 4 says the reader may be a bot or a fresh context — **fixed**,
  *read and try*.
- `correctness:` the candidate concept's new clause was ungrammatical and
  carried a transport rule the concept it links states in full — **fixed**,
  one clause naming `manual-git`.
- `shrink:` the path template made two edits where one does — **fixed**,
  the acceptance returns to the lead sentence and the added paragraph goes.
- `shrink:` the disposition was three clauses in three files — **fixed**,
  *deferred, with its owner and a follow-up file under `project/backlog/`*;
  dropping the owner, which the reader proposed, is what the first read
  caught as a gate refusal.
- `correctness:` the reason the order exists survived only in a skill file
  — **fixed**, one clause added to the concept that holds it.
- `correctness:` a paragraph left unwrapped in chapter 5 — **fixed**.
- `correctness:` the register's row 1 lists the concept notes this row
  touches and did not carry the two just added, so the record and the
  register disagreed — **fixed**, the row's file cell names them. The
  row's path-id cell is still S06's.
- The reader's note outside the diff: `tools/cairn-check.mjs` still
  comments that `ready` is an acceptance. `tools/**` is excluded from this
  path and the comment is row 2's; it is named in the boundary report.

The fixes of the second read are not read a third time; the self-review above
carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare; nothing under tools/ moved in this unit
cairn-active --check  : pass — the view is already current; no path state changed
the deferral claim    : read in tools/cairn-check.mjs — a deferred advisory_disposition entry without both owner and follow_up is an error of the blocking acceptance rule, which is why the owner stays in all three sentences
the ready claim       : read in closingAcceptanceErrors and where it is called — a path declaring ready on manual-git is refused without accepted_by, accepted_at and decision: accepted, which is why the lifecycle concept states the acceptance as required there
the budget table      : read in spec/reference/conformance.md — four rows, one for the files the kit installs and none for a skill count, which is why the skill's sentence names no figure
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's fixes not read again
user review           : the owner ruled on what `ready` states, in the chat, before this unit was committed; the skills themselves are read with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S03
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : skills/cairn-close/SKILL.md and reference.md, spec/index.md, spec/reference/execution-protocol.md, spec/reference/path-template.md, spec/concepts/administrative-closure.md, lifecycle.md and implementation-candidate.md, .github/pull_request_template.md, the register's row 1 file cell, this path's index and this step
session     : safe boundary after a successful push
next action : S03 — the coherence questions as a fresh-context read
blockers    : none
```
