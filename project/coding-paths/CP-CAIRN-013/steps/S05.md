---
type: Cairn Coding Path Step
title: 'CP-CAIRN-013 S05 — the chapters and the layout'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-013
  step: S05
---

# CP-CAIRN-013 S05

### S05 — the chapters and the layout — **COMPLETE**

#### Plan

Name in the specification the folders and the file that the skills of
S02 to S04 now send work to, as the plan's fifth item says: chapters 1
and 4 of `spec/index.md` name `project/backlog/` among the project
plane's spaces, chapter 4 saying what a file there holds and who reads
the folder (ADR-041); and `spec/reference/repository-layout.md` gains
its rows and tree entries for that folder, for `feedbacks/` with its
`<release>/` subfolders and the type `Cairn Feedback` (ADR-038 decisions
3 and 5), and for `CHANGELOG.md` (ADR-039).

Definition-of-done item advanced: the sixth. Its first clause — the path
template written as plain items with the sentence saying why no box
stands in the pinned text — was carried by S01, where the open skill's
step 1 links the template three lines from the sentence that refuses a
box; this unit carries the rest, and the item is complete at this
commit.

Type `documentation`: the two chapters and the layout reference move
together. No rule, tool or fixture changes, so this unit carries no new
test.

One correction the read found in a line this unit rewrote, and it is
carried here: the layout tree's `skills/` node still enumerated five
procedures where the table beside it now names six and the stance.
`spec/reference/path-template.md`'s review block still wrote a deferral
as *to <unit or path>*, which S04 changed in the skill the block is
filled from; the template is in `writes:` and follows here.

A second correction was made and then withdrawn on the reads: the tree
asserts a `project/brainstorm/log.md` that neither this repository nor
`planInstall` has, and this unit removed it while putting `backlog/`
beside it. No `log.md` exists anywhere in this repository, and the tree
shows five more; the table's own exception — *Path folders and the
specification keep none* — does not name the brainstorm folder either
way. Removing one of six is half a change to a convention this unit did
not come to decide, so the line is restored. `backlog/` carries an index
and no log, which is what ADR-041 names.

Deliberately not in this unit: the folders themselves. `project/backlog/`
and `feedbacks/` are installed by `planInstall` in `tools/cairn.mjs` with
their indexes, and `CHANGELOG.md` is written by the release path before
the tag — rows 3 and 5 of the register. This unit describes the layout a
conforming repository has; it does not build one. `feedbacks/` exists
here already and its index says what ADR-038 asks, which is why the
path's coverage excludes it from change.

#### Work

```cairn-unit
step: S05
unit: 05
type: documentation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`spec/index.md`**, chapter 1: three lines after the brainstorm space,
  separating the two folders a reader would otherwise confuse — work a
  path decided not to do now is not a brainstorm note, and lives in
  `project/backlog/`, one file per item, chapter 4 saying who reads it.
- **`spec/index.md`**, chapter 4: one paragraph after the rule that every
  milestone is accounted for — a deferred item is not a milestone and is
  not a row of the register; what its file says; the open skill reading
  the folder when a path is proposed, the item named in the goal and its
  file in `writes:`, and the taking path's last unit deleting it; and
  that no rule reads the folder.
- **`spec/reference/repository-layout.md`**, the tree: `CHANGELOG.md` and
  `feedbacks/` with its `<release>/` subfolder at the root, and
  `backlog/` beside `brainstorm/` under the project plane.
- **`spec/reference/repository-layout.md`**, the table: four rows —
  `project/backlog/` with what a file holds and who deletes it;
  `feedbacks/*.md` with the type `Cairn Feedback`;
  `feedbacks/<release>/` with the index that names what answered each
  line; and `CHANGELOG.md` with the adopter repairs it names first.
- **`spec/reference/repository-layout.md`**, the skills row and the
  tree's `skills/` node: the procedures they enumerate now include
  `update`, the seventh skill S04 added.
- **`spec/reference/repository-layout.md`**, the absence paragraph: the
  two surfaces their first event creates, index and all — the backlog
  until something is deferred, the changelog until the first release
  that writes one.
- **`spec/reference/path-template.md`**, the review block: a deferral is
  written to its file under `project/backlog/`, as the unit skill asks
  since S04, with a slot to name the file.

#### Self-review

- `yagni:` no `log.md` under `project/backlog/`, where `brainstorm/` has
  one: the backlog's files are deleted by the paths that take them, and
  a folder history of items that left is a second record of what the
  journal already says.
- `correctness:` chapter 4's last sentence first said the tools check a
  backlog file's links *and its frontmatter*, which the brainstorm
  sentence says of a note that has a conformance row behind it; nothing
  parses a backlog file's frontmatter, so the sentence says only what is
  true — no rule reads the folder as a backlog, and a file there is
  checked for its links.
- `correctness:` chapter 1's lines first landed between the brainstorm
  paragraph and *Three rules, all of them about honesty rather than
  form* — which are the brainstorm note's rules, and read as the
  backlog's with a paragraph wedged in front of them. Moved to the end
  of that section, after the sentence on what the tools check.
- `shrink:` chapter 1's first draft listed every space of the plane and
  which chapter names it; the chapters name each where it is used, and
  the only confusion worth preventing is between the backlog and the
  brainstorm folder beside it.
- `delete:` a fifth layout row for `feedbacks/index.md` — the last row
  of that table already covers each meaningful folder's index.
- `yagni:` the type `Cairn Feedback` is stated in the row and nowhere
  else; no rule reads it, and the record says so.
- `native:` nothing is added to the protocol — two folders and a file
  the ecosystem already has names for, written down where a reader looks
  for the layout.
- No widening of `writes:`: `spec/index.md` and
  `spec/reference/repository-layout.md` are both declared.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's diff
in one file and the two criteria and nothing else; it returned in 281 seconds
with nine findings, on the first attempt. Each with its disposition:

- `correctness:` *No rule reads the folder* is false — `markdownCorpus` roots
  on the project plane, so the blocking `links` rule reads every markdown
  file under it — **fixed**, read in `tools/cairn-check.mjs` first: no rule
  reads the folder *as a backlog*, and a file there is checked for its links.
- `correctness:` the tree's `skills/` node enumerated five procedures where
  the table beside it now names six and the stance — **fixed**.
- `correctness:` `CHANGELOG.md` does not exist and the tree's preamble
  excuses only an absent repeatable record — **fixed**, the paragraph names
  the two surfaces their first event creates.
- `shrink:` the backlog row restated chapter 4's four fields word for word
  where its neighbours are one clause — **fixed**, the row defers to the
  chapter, and by the second read the reference is a link like every other
  on the page.
- `shrink:` chapter 1 repeated chapter 4's path and its *one file per item* —
  **fixed**, chapter 1 keeps only what it must prevent: a deferral misfiled
  as a brainstorm note.
- `correctness:` the tree re-asserted `project/brainstorm/log.md`, which does
  not exist — **fixed**, then **withdrawn on the second read**: no `log.md`
  exists anywhere in this repository and the tree shows five more, so
  removing one is half a change to a convention this unit did not come to
  decide. The line is restored and the step says why.
- `correctness:` the `feedbacks/*.md` row said *dated like a journal entry*,
  which is a different shape, and stated the type as though it covered the
  fifteen notes that predate it — **fixed**, the clause is cut and the type
  is named from 1.2 on.
- `correctness:` *reads that folder first, so an item is taken by a path
  that names it* — reading does not cause the naming — **fixed**, one
  sentence for the read and one for what taking obliges.
- `correctness:` `spec/reference/path-template.md` still wrote a deferral as
  *to <unit or path>*, which S04 changed in the skill the block is filled
  from — **fixed**, the template follows; it is in `writes:`.

Second read: a fresh context, a subagent of this harness, given the fixes and
the same two criteria; it returned in 288 seconds with eight findings, on the
first attempt. Each with its disposition:

- `correctness:` *beyond its links and its frontmatter* — the frontmatter
  half is true of a brainstorm note, which has a conformance row behind it,
  and of nothing under the backlog — **fixed**, the sentence claims only the
  links, and needs no row on a page this path may not write.
- `correctness:` the new absence sentence contradicted the one it extended,
  which says an absent repeatable record still keeps its directory and index
  — **fixed**, it is an exception now and names the two surfaces made by
  their first event, index and all.
- `correctness:` removing the brainstorm log alone is half a change in both
  directions, against the repository and against the table's own exception —
  **fixed**, the line is restored.
- `correctness:` the deferral in the path template had no slot to name its
  file, unlike every other field on that line — **fixed**.
- `correctness:` *as chapter 4 defines it* was the page's only unlinked
  cross-reference — **fixed**.
- `correctness:` *a changelog before the first release* does not excuse this
  repository, where 1.1.0 shipped and no changelog exists — **fixed**, the
  first release *that writes one*.
- `correctness:` a line left at 146 characters — **fixed** before the read
  returned; the paragraph is rewrapped.
- `correctness:` the tree is now the only place saying seven skills, while
  `tools/cairn.mjs` still generates five in the bootloader, six in
  `SKILL_LINES` and *six skills* on the pointer page — **refused**: those
  are the installer's, row 3 of the register, and `tools/**` is excluded by
  this path's acceptance. Named in the boundary report, as S04 named the
  README.

The fixes of the second read are not read a third time; the self-review above
carries the lines the cuts and the withdrawal add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare; nothing under tools/ moved in this unit
cairn-active --check  : pass — the view is already current; no path state changed
what reads the folder : read in tools/cairn-check.mjs — markdownCorpus roots on the project plane and the links rule walks it, while schema targets path records and ADRs, which is why the sentence claims the links and not the frontmatter
the log convention    : `find` for log.md returns nothing in this repository, and the tree shows six; that is why the brainstorm line is restored rather than corrected here
the feedbacks folder  : read in feedbacks/ and feedbacks/1.1/ — the release subfolder and its index exist and match the tree this unit writes
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's fixes not read again
user review           : not required for the unit; the owner reads the chapters with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S06
```

#### Checkpoint

```text
status      : running
current step: S05 complete on push
retention   : none (no-rewrite host)
changed     : spec/index.md (chapters 1 and 4), spec/reference/repository-layout.md, spec/reference/path-template.md, this path's index and this step
session     : safe boundary after a successful push
next action : S06 — the candidate
blockers    : none
```
