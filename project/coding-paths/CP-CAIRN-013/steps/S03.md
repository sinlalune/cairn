---
type: Cairn Coding Path Step
title: 'CP-CAIRN-013 S03 — the coherence questions as a fresh-context read'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-013
  step: S03
---

# CP-CAIRN-013 S03

### S03 — the coherence questions as a fresh-context read — **COMPLETE**

#### Plan

Give the four closing questions the reader every unit's diff has had
since 2026-09-07, as the plan's third item says: the close skill's review
step hands the candidate to a second context of the writer's own agent —
the diff against the base, the `governs:` documents at their pinned ids,
the live view and the four questions, and nothing else — and its answers
go under *Coherence* beneath one line naming the reader, the owner
arbitrating; the acceptance step says the answers are there already; and
the reference, the human-records reference's *Coherence* section, chapter
5's `full` route sentence, the coherence-audit concept and this
repository's request template say the same (ADR-043).

Definition-of-done item advanced: the third, in full.

Type `documentation`: the skill, its reference and the four pages that
state the same act move together. No rule, tool or fixture changes, so
this unit carries no new test.

Where this unit departs from ADR-043's names: the record names *steps 2
and 3* of the close skill, which were the review and the acceptance when
it was written; S02 put the administrative commit between them, so the
same two are steps 2 and 4 today. The act is unchanged and both are in
this unit.

Two pages outside the acceptance's `writes:` move with it, and `writes:`
is widened for them in this unit: `spec/concepts/route.md`, whose table
makes the questions *the review's ... answered explicitly*, and
`spec/concepts/roles.md`, whose reviewer row says in so many words that
the closing review reads the candidate against `governs:` and the
sibling paths — the read this record hands to a fresh context. Leaving
them would have two concept notes stating the reader this unit replaces.
The owner ruled on the same shape in S02, for the pages ADR-040 left
behind: the wording follows the order already accepted, here. That
ruling is applied and not re-asked; it is named in the boundary report
so it can be refused.

`spec/reference/conformance.md` carries the same stale sentence — row 93,
*`full`'s two additions ... are the reviewer's and the forge's* — and is
NOT touched: this path's Required coverage names it *read, never
written*, which is the acceptance saying so in words. It is named in the
boundary report.

Deliberately not in this unit: `QUESTIONS` and the *Coherence* scaffold
of `tools/cairn-audit.mjs` with its test, and `requestTemplate` in
`tools/cairn.mjs`, which generates an adopter's request template —
ADR-043 names them and the register gives them to rows 2 and 4; the
skill's sentence says the scaffold is where the reader starts, which is
true of the four lines the tool prints today. The unit skill and the
seventh skill are S04; the template, the chapters and the layout
reference are S05.

#### Work

```cairn-unit
step: S03
unit: 03
type: documentation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`skills/cairn-close/SKILL.md`**, step 2: one paragraph saying the four
  questions are a fresh context's read and what that context is given —
  the diff of `C` against its base, the `governs:` documents at those ids,
  the live view and the four questions, and nothing else — with the
  answers under *Coherence* beneath one line naming the reader in the
  shape ADR-017 decision 4 already gives the unit's read, and the audit
  command's scaffold named as where the reader starts, never its answer.
  The README ask goes to that same reader.
- **`skills/cairn-close/SKILL.md`**, step 4: the `full` route's sentence
  no longer has the reviewer answering the questions; the answers are in
  the description already and the owner arbitrates what the reader found.
- **`skills/cairn-close/reference.md`**: the two commands that produce
  what the fresh context is given, under the review section where the
  request and the closing record are filled.
- **`spec/reference/human-records.md`**: the closing review's *Coherence*
  section opens with its reader line.
- **`spec/index.md`**, chapter 5: the `full` route's first addition is the
  questions answered by a fresh context, with the owner arbitrating.
- **`spec/concepts/coherence-audit.md`**: one paragraph naming the reader,
  what it is given, who arbitrates, and the case where no fresh context
  can be obtained.
- **`.github/pull_request_template.md`**: the *Coherence* list opens with
  the blank for the reader, in the wording
  `spec/reference/path-template.md` already uses for the unit's read.
- **`spec/concepts/route.md`** and **`spec/concepts/roles.md`**: what
  `full` adds is the answers written out, not who reads them; the
  reviewer role arbitrates what the fresh context found instead of
  making the read itself.
- **`project/coding-paths/CP-CAIRN-013/index.md`** and the register's row
  1 file cell: `writes:` widened by those two concept notes, both cells
  naming the same list.

#### Self-review

- `shrink:` the skill's new paragraph first repeated the list of
  materials that the reference gives as commands; the skill names them
  once, and the reference is where they are produced.
- `correctness:` the reference's first draft printed
  `git rev-parse <C>:<each path pinned in governs:>`, which resolves a
  path at a commit and not the blob the record pins; the block now reads
  the pinned ids with `git cat-file -p`, which is what `governs:` holds.
- `delete:` a sentence in the concept repeating that no checker rule
  reads the answers — the page's *It does not prove* section already
  says the reasoning is never proved.
- `yagni:` no sentence names a record, and none claims the audit tool
  prints more than the four lines it prints today.
- `native:` the read is the movement the unit skill already performs, in
  the words it already uses; nothing new is built for it.
- Widening of `writes:`, named here as movement 3 asks:
  `spec/concepts/route.md` and `spec/concepts/roles.md`, for the reason
  in the plan above. No item of the definition of done moves, so the
  scope digest is untouched.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's diff
in one file and the two criteria and nothing else; it returned in 222 seconds
with fourteen findings, on the first attempt. Each with its disposition:

- `correctness:` the brief could not answer the second question — the live
  view carries id, title, status, branch and base, nothing of what a sibling
  is building, and the sentence the unit deleted was the one that read the
  candidate *against every path running beside it* — **fixed**: the records
  of the paths running beside it are in the brief. This keeps a requirement
  the skill already carried rather than widening the record's; ADR-043's own
  scaffold paragraph gives that reader the sibling `writes:`.
- `correctness:` the README ask went to the reader that had just been given
  the four questions *and nothing else* — **fixed**: it is the writer's, and
  says so.
- `correctness:` *Read the candidate against every document pinned in
  `governs:` ... and against every path running beside it* still addressed
  the writer, eighteen lines after the read became the reader's — **fixed**,
  the sentence is gone and the brief above carries it; the digest sentence
  stays, which is the writer's.
- `correctness:` *the scaffold the audit command prints under each question*
  is false today — the tool prints `TO BE FILLED BY THE REVIEWER` and no
  facts — **fixed**, the sentence is cut. The scaffold is
  `tools/cairn-audit.mjs`, which ADR-043 names and the register gives to a
  later row.
- `correctness:` the `full` route's first addition was now what every route
  does — **fixed**: what `full` adds is the answers written out explicitly,
  and who answers does not change with the route.
- `correctness:` `git cat-file -p` takes one object, not a list — **fixed**,
  `git show <blob>`, once per document, which is the idiom the path template
  already uses.
- `correctness:` the reader line reached only the `pull-request` example of
  `spec/reference/human-records.md` — **fixed**, the closing record's
  *Findings* example opens with it too.
- `correctness:` three surrounding pages still make the four the reviewer's
  read — **fixed in part**: `spec/concepts/route.md` and
  `spec/concepts/roles.md` move here, with `writes:` widened for them;
  `spec/reference/conformance.md` is **refused**, because this path's
  Required coverage names it *read, never written*.
- `correctness:` `tools/cairn-audit.mjs`'s header and placeholder still say
  the questions are the reviewer's, and its closing-record preamble still
  reads *against every path running beside it* — **refused**: `tools/**` is
  excluded by the acceptance and the tool is ADR-043's other surface, on a
  later row. Named in the boundary report.
- `correctness:` `spec/reference/human-records.md` says `cairn-audit` prints
  the example filled in, and `requestDescription` emits no reader line —
  **refused**, same reason: the sentence this path writes names the surface
  a later row makes true, which is how the whole path is scoped.
- `shrink:` the concept repeated the `governs:` pinning the paragraph below
  it states in full — **fixed**, cut.
- `shrink:` the reference restated the rule the skill states — **fixed**,
  the reference keeps the commands and drops the prose.
- `shrink:` the skill re-derived movement 4's mechanism in eight lines,
  including its reader line verbatim — **fixed**, it points at the movement,
  which also brings the condition the skill omitted: the writer answers only
  where no fresh context can be obtained.
- `shrink:` the request template's blank shortened the established wording —
  **fixed**, it matches `spec/reference/path-template.md`'s.

Second read: a fresh context, a subagent of this harness, given the fixes and
the same two criteria; it returned in 217 seconds with eleven findings, on the
first attempt. Each with its disposition:

- `correctness:` naming movement 4 imported its closed list — *the diff and
  two criteria and nothing else* — beside this one, two incompatible lists
  for one movement — **fixed**: the close skill borrows the movement's
  shape, and only the list below it binds.
- `correctness:` *the movement gives every unit's diff* was garbled, the
  movement being what is run on the diff — **fixed** with the sentence above.
- `correctness:` *under Coherence* is the request's heading; the closing
  record's is *Findings*, in the file this unit also edits — **fixed**, the
  sentence names both.
- `correctness:` the restored README sentence had lost its actor — **fixed**,
  *that one is yours*.
- `correctness:` the reference's heading claimed to list what the reader is
  given and listed two of four inputs — **fixed**: four commands, the live
  view and the sibling records among them, and the heading says *give the
  fresh context these*.
- `correctness:` *obtained before either is filled* attached to the inputs
  and belongs to the answers — **fixed** with the heading.
- `correctness:` the roles table said *this role arbitrates* where every
  other page says the owner, and silently dropped *records the answer* —
  **fixed**, the reviewer arbitrates and records.
- `shrink:` chapter 5, `route.md` and the concept stated the same fact three
  times — **fixed**, `route.md`'s aside is cut and its cell links the
  concept as it already did.
- `correctness:` a 103-character line left in chapter 5 — **fixed**.
- `correctness:` the register's row 1 wrote the roles page without naming it
  as an outcome — **fixed**, the outcome cell names the three concepts.
- `shrink:` the request template's blank is longer and more passive than
  movement 4's prose line — **refused**: it is a template blank, and it
  matches the blank `spec/reference/path-template.md` already gives the same
  slot. The two readers disagreed on this one; the sibling template decided
  it.
- The reader's note beyond the diff: the reader line is printed by neither
  `tools/cairn-audit.mjs` nor the kit's generated template, so pasting the
  audit output over this repository's template drops it. Both are later
  rows; named in the boundary report.

The fixes of the second read are not read a third time; the self-review above
carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare; nothing under tools/ moved in this unit
cairn-active --check  : pass — the view is already current; no path state changed
the live view's shape : read in project/coding-paths/ACTIVE.md — id, title, status, branch, base and the blocked line, and nothing of a sibling's surfaces, which is why the brief carries the sibling records
the scaffold's output : read in tools/cairn-audit.mjs — PLACEHOLDER is the literal `TO BE FILLED BY THE REVIEWER`, which is why the sentence claiming facts under each question was cut
the three stale pages : read in spec/concepts/route.md, spec/concepts/roles.md and spec/reference/conformance.md; the first two moved, the third is named read-only by this path's coverage
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's fixes not read again
user review           : the owner's S02 ruling on the same shape is applied to route.md and roles.md and named in the report, not re-asked; the skills are read with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : skills/cairn-close/SKILL.md and reference.md, spec/index.md, spec/reference/human-records.md, spec/concepts/coherence-audit.md, route.md and roles.md, .github/pull_request_template.md, the register's row 1, this path's index and this step
session     : safe boundary after a successful push
next action : S04 — the unit skill and the seventh skill
blockers    : none
```
