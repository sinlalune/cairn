---
type: Cairn Coding Path Step
title: 'CP-CAIRN-013 S04 — the unit skill and the seventh skill'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-013
  step: S04
---

# CP-CAIRN-013 S04

### S04 — the unit skill and the seventh skill — **COMPLETE**

#### Plan

Write the unit skill's two changed sentences and the seventh skill, as
the plan's fourth item says: section 7 says how a note about Cairn
reaches the protocol's repository — a pull request against it, or the
owner carrying it — an adopter's note about its own use staying where it
was written (ADR-038 decision 2); movement 4's `deferred` disposition
names a file under `project/backlog/` (ADR-041); and
`skills/cairn-update/SKILL.md` gives an adopter's update as a path on the
adopter's register, in the seven movements ADR-035 states, carrying no
command the pointer page does not list. This repository's `AGENTS.md`
names the seventh beside the six.

Definition-of-done items advanced: the fourth and the fifth, both in
full.

Type `documentation`: the two skills and the bootloader move together.
No rule, tool or fixture changes, so this unit carries no new test.

Deliberately not in this unit: `planInstall` and `SKILL_LINES` in
`tools/cairn.mjs`, which copy the skills folder and write the pointer
page's list and the generated bootloader's line, and `cairn.lock.json` —
ADR-035 names them and the register gives them to row 3, so the kit does
not install the seventh skill until that row lands, and this repository
carries it either way. The kit's count rows on the conformance page and
the README's link are the same row's, and that page is named *read,
never written* by this path's coverage.

Found by the read and left, because the acceptance excludes it:
`README.md` says *six skills* in three places and the kit's pointer page
installs six. The README and the release are row 5, and this path's
coverage names `README.md` among the pages it does not write; the
pointer page's list is `SKILL_LINES` in `tools/cairn.mjs`, row 3. Both
are named in the boundary report. This repository's bootloader, which
this path does declare, names seven.

Where the record leaves a hole and this unit does not fill it: ADR-033
decision 2 ends *what the option is called and how a declination is
withdrawn is the coding path's*, and that path has not run. The skill's
third movement therefore states the decision — decline a host file so
this update and every later one leave it alone — and names no flag,
because a flag named here would be invented. The same rule the skill
opens with covers it: no command the pointer page does not already list.

#### Work

```cairn-unit
step: S04
unit: 04
type: documentation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`skills/cairn-unit/SKILL.md`**, section 7: one paragraph after the
  feedback sentence — a note about this repository's own use of the
  protocol stays in its `feedbacks/`; a note about Cairn travels, as a
  pull request against the protocol's repository or carried by the
  owner, and in that repository the file is written where it lands.
- **`skills/cairn-unit/SKILL.md`**, movement 4: a review finding is
  disposed of as fixed, refused, or deferred to a file under
  `project/backlog/` that the disposition names, where it said *deferred
  to a named unit or path*.
- **`skills/cairn-update/SKILL.md`**, new, the seventh skill: an update
  is a path on the adopter's register, its writes the kit's files and
  the host files the report names; then the seven movements — the trunk
  settled; `status` read before anything is written, with the release's
  notes read for the repairs it absorbed; the owner's two decisions in
  the chat before the go-ahead, the edited file kept or taken and the
  unwanted host file declined; registration as `cairn-open` says; the
  run, with every kept file's diff read and decided; the reconcile by
  hand of only what the report and the pointer page named; and the
  close, with what the update cost said back to Cairn by the route
  section 7 now gives. Four commands, all of them the pointer page's.
- **`AGENTS.md`**: the skills line names `cairn-update` between
  `cairn-close` and `cairn-learn`, the order the movements run in.

#### Self-review

- `yagni:` the skill has no `reference.md`. The four commands fit in the
  movements that use them, and a second file would be a sequence page
  for a sequence of four lines.
- `delete:` a movement between the run and the reconcile, for reading
  the lock — `status` already reads it, and the pointer page's list is
  the authority the reconcile movement names.
- `shrink:` the first draft restated `cairn-open`'s registration and
  `cairn-close`'s closing in the fourth and seventh movements; both now
  point at the skill that owns them and say only what an update adds.
- `yagni:` no flag is named for declining a host file, and none for the
  kept-file reading beyond `update --take`, which the pointer page
  lists: the option's name is a later path's, and inventing one here
  would be the placeholder `cairn-open` step 2 now tells a writer to put
  to the owner.
- `native:` the skill adds no artefact — an update is a path, and every
  movement it names is `cairn-open`, `cairn-unit` or `cairn-close`
  performing what it already performs.
- No widening of `writes:`: `skills/**` and `AGENTS.md` are declared.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's diff
in one file and the two criteria and nothing else; it returned in 213 seconds
with twelve findings, on the first attempt. Each with its disposition:

- `correctness:` the run movement never ran `update --take`, so the first of
  the two decisions was executed by no command the skill names — **fixed**,
  the take is the first line of that block.
- `correctness:` *the four commands the pointer page at the repository root
  already lists* was wrong three ways — the page is `cairn/README.md`, it
  names two command forms, and this skill uses two — **fixed**, read in
  `tools/cairn.mjs` before rewriting: the kit's part is `status` and
  `update`, and no claim is made about the page.
- `correctness:` *the link `status` prints* — `describeStatus` prints no URL
  — **fixed**: the changelog section is read where it ships, with the
  package. The link is ADR-039's, on a later row.
- `correctness:` *reads every kit file as pristine, edited or missing* left
  out the unmanaged state and the files that have left the kit — **fixed**,
  from the command's real output.
- `correctness:` *does nothing without the name* reads as a no-op without
  `--take`, when `update` alone performs the whole update — **fixed**,
  *takes nothing you have not named*.
- `correctness:` declining a host file names a capability the kit does not
  have — no flag in `parseArgs`, no field in the lock — **refused**: ADR-033
  decision 2 leaves the option's name to the installer's path, and that path
  and this skill's installation are the same row, so an adopter meets the
  sentence and the flag together. The skill names no flag, which is what the
  hole requires.
- `correctness:` the README says *six skills* while the bootloader now says
  seven — **refused**: `README.md` is the release's, row 5, and this path's
  coverage excludes it; `SKILL_LINES` in `tools/cairn.mjs` is row 3. Both
  named in the boundary report.
- `correctness:` the note-routing paragraph was undecidable against the
  paragraph above it, which already asks for a note naming the change to
  Cairn — **fixed**, the route folds into that sentence: the note it
  produces is the one that travels, and a note about this repository's own
  conventions is the one that stays.
- `correctness:` the unit skill's `deferred` disposition and the close
  skill's read as two shapes — **fixed**, the same shape; the closing
  record's also carries the owner, because the `acceptance` rule requires
  that field and a step's review section has no schema.
- `correctness:` *the newer release's notes* and the close skill's *the
  changelog's section* named one artefact twice — **fixed**, one name.
- `shrink:` movements 5 and 6 acted on the same list with nothing
  distinguishing them — **fixed**: 5 decides each kept file, 6 carries the
  decision into the file.
- `shrink:` four lines carrying no instruction — *Nothing is written in this
  movement*, the closing aside, and the third prohibition — **fixed**, cut.

Second read: a fresh context, a subagent of this harness, given the fixes and
the same two criteria; it returned in 228 seconds with ten findings, on the
first attempt. Each with its disposition:

- `correctness:` `update --take <path>` writes the file and prints *about to
  be discarded* afterwards, so following the block literally broke this
  skill's own prohibition against taking a file unread — **fixed**, read in
  `takeRelease` and in `main`: the dry run of the take is the first command,
  and the skill says why.
- `correctness:` *two commands* was false for a file whose movement 1 is Git
  and whose 4 and 7 are other skills — **fixed**, the kit's part is two.
- `correctness:` the changelog still named no location — **fixed**, it ships
  with the release's package.
- `correctness:` *so no later update writes it* dropped the current update,
  which is the one the decision is taken before — **fixed**, this one and
  every later one.
- `correctness:` *reports every kit file's state* overstates what `status`
  prints — **fixed**, from the real output: counts by state, then the files
  it would write, the edited ones, and the ones that left.
- `correctness:` the pointer page's reconcile list goes stale when the page
  itself is a kept file — **fixed**, the run's report is the authority and
  the page is that list written down.
- `correctness:` *where this one writes it directly* had no referent in a
  portable skill — **fixed**, the protocol's own repository writes it
  straight into `feedbacks/`.
- `correctness:` *stays in the folder it was written in* named no folder —
  **fixed**, this repository's `feedbacks/`.
- `shrink:` the handoff between movements 5 and 6 stated from both sides —
  **fixed**, once, in 5.
- `correctness:` a line left unwrapped in movement 6 — **fixed**.

The fixes of the second read are not read a third time; the self-review above
carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare; nothing under tools/ moved in this unit
cairn-active --check  : pass — the view is already current; no path state changed
every command named   : read in tools/cairn.mjs — status, update, --take, --dry-run and the npx cairn-protocol form are parseArgs's and main's; no flag is named for declining a host file, because parseArgs carries none and ADR-033 leaves its name to the installer's path
the take's order      : read in takeRelease and in the update branch of main — the write happens, then the report prints, and dryRun is honoured, which is why the dry run of the take is the first command of movement 5
the status output     : read in describeStatus — counts by state, the files it would write, the edited ones, the ones that left, and no URL
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's fixes not read again
user review           : not required for the unit; the owner reads the seventh skill with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S05
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : skills/cairn-update/SKILL.md (new), skills/cairn-unit/SKILL.md, AGENTS.md, this path's index and this step
session     : safe boundary after a successful push
next action : S05 — the template, the chapters and the layout
blockers    : none
```
