---
type: Cairn Coding Path Step
title: 'CP-CAIRN-013 S10 — what the fourth closing read found'
timestamp: 2026-09-24T00:00:00Z
cairn:
  path: CP-CAIRN-013
  step: S10
---

# CP-CAIRN-013 S10

### S10 — what the fourth closing read found — **COMPLETE**

#### Plan

The fresh coherence read of candidate `8d55673` answered *No* to three of
its four questions and *Yes* to the fourth with three drifts, each in a
page this path wrote or changed. The owner chose, in the chat of
2026-09-24, to fix them and cut a fifth candidate, on the understanding
that if that candidate's read finds only wording of the same weight the
path closes on it and lists what was found.

- `spec/concepts/administrative-closure.md`, *Build the idea*: the
  self-reference was stated as *acceptance necessarily creates a commit
  after the thing accepted*, and the field-level argument as a rewrite
  *after the acceptance*. On `pull-request` the reorder puts `A` before
  the acceptance. Both sentences now hold on either transport: declaring
  `C` ready is what necessarily follows `C`, and the standard at risk is
  the one the acceptance is measured against.
- *The reader* meant two people. Chapter 5's closure item and the
  concept said `A` lands *before the reader is asked*, meaning the owner;
  the close skill's reader is the fresh context, which reads before `A`.
  Both say the owner now.
- `spec/reference/human-records.md`'s two deferral examples, and the same
  example in `spec/concepts/finding.md`, still followed up on a path id
  where this path's skills, chapter 5 and the request template name a
  file under `project/backlog/`. The examples name one. The finding
  concept is outside the acceptance's `writes:` and is widened for it
  under the owner's standing ruling on that shape.

Also recorded here, about the read itself: its materials were incomplete.
The scratchpad holding the pinned `governs/` documents was cleared across
the session boundary of 2026-09-24, and the reader read the governing
records from the worktree instead. All twelve are byte-identical at the
candidate to their pinned ids, measured by `git rev-parse`, so it read
exactly the documents it should have; the folder is restored for the
next read. It also cited a stale gate transcript left by an earlier
subagent as showing `8d55673` red; the gate there is green and S09
carries its review.

Definition-of-done item advanced: the ninth.

Type `repair`: statements of this path's own that contradict each other
or the rule they illustrate. No rule, tool or fixture changes, so this
unit carries no new test.

#### Work

```cairn-unit
step: S10
unit: 10
type: repair
verified: cairn-check, cairn-test, cairn-active --check
```

- **`spec/concepts/administrative-closure.md`**: the self-reference and
  the field-level argument restated to hold on both transports; the
  ordering paragraph names the owner twice where it said the reader.
- **`spec/index.md`**, chapter 5's closure item: *before the owner is
  asked to read*.
- **`spec/reference/human-records.md`** and **`spec/concepts/finding.md`**:
  the deferral examples' follow-up is a file under `project/backlog/`.
- **`spec/concepts/path-record.md`**, found by this unit's read: *After
  acceptance, the only fields administrative closure may touch* tied the
  closure to a moment it no longer follows on `pull-request`, and named two
  fields where the concept also lets `A` move the resume checkpoint. It says
  *At closure, the only frontmatter fields*.
- **`spec/index.md`** and **`spec/concepts/administrative-closure.md`**, on
  the same read: the approver is *the reviewer*, the role the portable spec
  defines, not *the owner*, which it does not; and the merge-click reason is
  stated for the case it holds in, one participant holding every role. The
  close skill keeps *owner*, the word it uses throughout for the person it
  addresses.
- **`project/coding-paths/CP-CAIRN-013/index.md`**: `writes:` widened by
  `spec/concepts/finding.md` and `spec/concepts/path-record.md`;
  `current_step` reads `S10`.
- **`project/coding-paths/index.md`**: row 1's Writes cell names the
  widening by S10.

#### Self-review

- `correctness:` the first edit of the ordering paragraph left *a reader
  whose approval is the merge click* two lines below the owner it had
  just named, and one line at eighty-three columns; the paragraph is
  rewritten whole.
- `yagni:` `tools/cairn-audit.mjs` and `tools/cairn.mjs` still say *a
  named owner and follow-up*; the reader named them, and they are rows 4
  and 3. Not touched.
- `shrink:` the self-reference sentence is shorter than the one it
  replaces and says only what holds.
- Widening of `writes:`, named here as movement 3 asks:
  `spec/concepts/finding.md` and `spec/concepts/path-record.md`, under the
  owner's standing ruling. No item of the definition of done moves.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's diff
and the two criteria and nothing else, told to check claims by command; it
returned in 70 seconds with four findings, on the first attempt. It confirmed
by `git grep` that no `CP-EXAMPLE-002` follow-up remains in `spec/` or
`skills/`, and that `dispositionErrors` does not check the shape of a
follow-up, so nothing in the checker breaks. Each finding with its
disposition:

- `correctness:` `spec/concepts/path-record.md` still said *after
  acceptance* of the closure's fields — **fixed**, with `writes:` widened.
- `correctness:` *the owner* is no role of the portable spec; the reviewer
  is — **fixed** in chapter 5 and the concept.
- `correctness:` the merge-click reason holds only where one participant
  holds every role, and the rule is stated for every `pull-request`
  repository — **fixed**, the reason names its case.
- `correctness:` chapter 5's closure item stated the order with no
  condition, the `manual-git` reversal coming twelve lines later —
  **fixed**, *On `pull-request`*.

Second read: a fresh context, given the three fixed lines and the same two
criteria; it returned with two findings, on the first attempt:

- `correctness:` the path-record sentence named `status` and
  `subject_commit` as the only fields, where the concept also lets `A` move
  the resume checkpoint — **fixed**, *the only frontmatter fields*.
- `shrink:` *On `pull-request`* repeats the section's context — **refused**:
  the first read asked for it so the step does not read as universal, and
  the reader called keeping it harmless.

The fixes of the second read are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare
cairn-active --check  : pass — the view is already current
the examples          : git grep finds no CP-EXAMPLE-002 follow-up left in spec/ or skills/
the role name         : git grep finds no "owner is asked" left in spec/; roles.md defines the reviewer
review                : two reads by fresh contexts, both obtained on the first attempt
user review           : the owner chose to fix and cut a fifth candidate, in the chat of 2026-09-24
remote                : pushed immediately after this record; this commit is the fifth candidate
```

#### Checkpoint

```text
status      : running
current step: S10 complete on push
retention   : none (no-rewrite host)
changed     : spec/concepts/administrative-closure.md, finding.md and path-record.md, spec/index.md, spec/reference/human-records.md, the register's row 1, this path's index and this step
session     : safe boundary after a successful push
next action : close on this commit — its own coherence read, request 30's description rewritten, the administrative commit, the owner's try, the merge
blockers    : none
```
