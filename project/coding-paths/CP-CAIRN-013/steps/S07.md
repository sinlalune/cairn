---
type: Cairn Coding Path Step
title: 'CP-CAIRN-013 S07 — what the closing read found'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-013
  step: S07
---

# CP-CAIRN-013 S07

### S07 — what the closing read found — **COMPLETE**

#### Plan

The coherence read of candidate `1b24173` — the fresh context ADR-043
asks for, performed for the first time under the sentence this path
wrote — answered two of its four questions *Yes*. This unit carries what
the owner arbitrated, and voids that candidate: implementation changes,
so a new one is produced from this commit.

The first *Yes*, and a defect of this path's own writing:
`spec/reference/repository-layout.md` said `project/backlog/` is absent
until something is deferred. ADR-041 decision 1 installs the folder and
its index with the kit, at `init`, and names `planInstall` for it. The
paragraph now excepts `CHANGELOG.md` alone, which is the only one of the
two its first event creates.

The third *Yes*, put to the owner and ruled in the chat of 2026-09-22:
`spec/reference/paths.md` still printed the closure order this path
supersedes — the acceptance before the administrative closure — in the
PORTABLE convention the bootloader names as its first read. ADR-040's
implementation table does not name that page, and `writes:` did not
carry it. The owner chose to fix it in the candidate rather than leave
it live or open a path for it, so `writes:` is widened by
`spec/reference/paths.md` and the register's row follows.

Definition-of-done items advanced: the ninth, whose clause *closed by
the `cairn-close` skill as this path leaves it* is what produced these
findings — the read happened because the skill this path wrote now asks
for it, and its answers are dispositioned here rather than merged
unanswered.

Type `repair`: the unit corrects a statement of this path's own that
contradicts a governing record, and a page of the specification that the
path left saying what it no longer means. No rule, tool or fixture
changes, so this unit carries no new test.

What the read found and this unit does NOT carry, by the owner's
ruling of 2026-09-22: the second *Yes* — that redefining what `ready`
requires is carried by no accepted record, ADR-040 scoping itself to an
ordering and saying no rule changes. The owner ruled the candidate
merges as it is and a superseding record follows on the next path. The
closing review states it, so the merge is not silent about it.

#### Work

```cairn-unit
step: S07
unit: 07
type: repair
verified: cairn-check, cairn-test, cairn-active --check
```

- **`spec/reference/repository-layout.md`**: the absence paragraph
  excepts `CHANGELOG.md` alone. `project/backlog/` is installed with the
  kit, index and all, as ADR-041 decision 1 has it, so it is absent from
  no conforming repository and the tree entry stands unqualified.
- **`spec/reference/paths.md`**: the closure sequence puts
  administrative closure before closing acceptance, and one sentence
  below it names the case that keeps the other order, linking the two
  concepts it turns on. The reasoning stays in the concept note, where
  the bootloader's own rule puts an explanation; the portable page
  carries the order and the exception, as it already does for the two
  history policies. No product name, no command, no host path.
- **`project/coding-paths/CP-CAIRN-013/index.md`**: `writes:` widened by
  `spec/reference/paths.md`, on the owner's ruling.
- **`project/coding-paths/index.md`**: row 1 of 1.2 names that page
  among the surfaces the row writes, and its outcome cell names the
  portable sequence beside the execution protocol and chapter 5.
- **`spec/concepts/administrative-closure.md`** and
  **`skills/cairn-close/SKILL.md`**: what `A` does not change, said
  truly. Both said it *changes nothing the reader reads*, which is false
  — `A` sets `status: ready` and `subject_commit`, regenerates the live
  view and moves the checkpoint, and on the transport where it lands
  first the reader reads all four. What is true, and what the order
  rests on, is that it adds no implementation and changes nothing of the
  result under review.

#### Self-review

- `delete:` a second sentence in the portable page restating that the
  closure changes nothing the reader reads — the concept note carries
  the reasoning, and the convention needs the order and the exception.
- `correctness:` the portable page's paragraph was first drafted with
  the two transport values in it, which is a repository's declaration
  and not a portable role; it names the case by what is written where
  instead, as the execution protocol does.
- `yagni:` no other page of the portable convention is touched. The read
  named one contradiction in it, and the rest of the file says nothing
  this path changes.
- `native:` the unit is one code block, one paragraph, one clause and
  two declarations following the record.
- Widening of `writes:`, named here as movement 3 asks:
  `spec/reference/paths.md`, on the owner's answer to the decision the
  closing read raised. No item of the definition of done moves, so the
  scope digest is untouched.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's diff
in one file and the two criteria and nothing else; it returned in 222 seconds
with seven findings, on the first attempt. Each with its disposition:

- `correctness:` *it changes nothing the reader reads* is false — `A` sets
  `status: ready` and `subject_commit`, regenerates the live view and moves
  the checkpoint, and on the transport where it lands first the reader reads
  all four — **fixed**, here and in the two other places this path had
  written it, the concept and the close skill.
- `correctness:` the sentence joined the permission and the motive with
  *and*, making the permission the reason — **fixed** by the cut below: the
  portable page keeps the exception and the concept keeps the why.
- `correctness:` *the closing record* appeared on the portable page
  undefined and unlinked where both its neighbours link — **fixed**, and by
  the second read the adjective is gone: the record that administrative
  closure carries, which needs no transport value.
- `correctness:` the layout's new sentence said one fact twice and *one*
  had lost its antecedent — **fixed**, and reworded again on the second read
  to state the contrast the *instead* was promising.
- `correctness:` dropping `project/backlog/` from the exception makes the
  page assert a folder this repository does not have — **refused**: the page
  says in its own words that it describes *what Cairn defines, not what any
  one adoption happens to contain*, and ADR-041 has the kit install the
  folder and its index. This repository receives it at the register's row 3.
- `shrink:` the new paragraph was the third copy of a rationale the concept
  note already carries in full — **fixed**, cut to the exception sentence
  alone, which is what the portable page needs: the sequence genuinely
  branches, as it already does for the two history policies.
- `correctness:` the register's row gained the page in Writes while its
  outcome cell still named two pages — **fixed**.

Second read: a fresh context, a subagent of this harness, given the fixes and
the same two criteria; it returned in 202 seconds with six findings, on the
first attempt. Each with its disposition:

- `correctness:` the replacement — *adds no implementation and changes
  nothing of `C`* — is false too, and contradicts the skill seven lines
  above, which lists the four things `A` does change — **fixed**, in the
  phrase this repository already owns on its conformance page: it moves no
  field the acceptance was measured against.
- `correctness:` the concept carried the same defect against its own
  MAY-list — **fixed**, the same phrase.
- `shrink:` one rule in two wordings across the skill and the concept —
  **fixed**, one phrase in both; the skill states it of the acceptance, the
  concept keeps the fuller why.
- `shrink:` the layout sentence still said its fact twice and never stated
  the contrast — **fixed**, *`CHANGELOG.md` has no such placeholder*.
- `correctness:` *the closing record* was still an undefined term on a
  portable page — **fixed**, the adjective is gone.
- `correctness:` the register's outcome cell misparsed as a list of things
  the steps were swapped with — **fixed**, one comma.

The fixes of the second read are not read a third time; the self-review above
carries the lines the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare; nothing under tools/ moved in this unit or in this path
cairn-active --check  : pass — the view is already current
the false claim       : read in skills/cairn-close/SKILL.md step 3 and spec/concepts/administrative-closure.md's MAY-list — A changes status, subject_commit, the live view and the checkpoint, which is why "changes nothing the reader reads" and "changes nothing of C" are both wrong and the conformance page's phrase is right
the links             : spec/concepts/closing-acceptance.md and spec/concepts/administrative-closure.md both resolve from spec/reference/; the links rule reads them green
the portable rule     : the new sentence carries no product name, host path, command or transport value — the two linked roles and the record one of them carries
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's fixes not read again
user review           : the owner ruled on this unit's existence — paths.md fixed in the candidate rather than deferred — in the chat of 2026-09-22, before it was written
remote                : pushed immediately after this record; this commit is the new candidate, the one the coherence read voided being 1b24173
```

#### Checkpoint

```text
status      : running
current step: S07 complete on push
retention   : none (no-rewrite host)
changed     : spec/reference/paths.md, spec/reference/repository-layout.md, spec/concepts/administrative-closure.md, skills/cairn-close/SKILL.md, the register's row 1, this path's index and this step
session     : safe boundary after a successful push
next action : close the path on the new candidate — the gate bare, the review as the request's description with a fresh coherence read of THIS candidate, the administrative commit, the owner's try, the merge
blockers    : none
```
