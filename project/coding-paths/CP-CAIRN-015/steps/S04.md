---
type: Cairn Coding Path Step
title: 'CP-CAIRN-015 S04 — what the kit installs'
timestamp: 2026-09-25T00:00:00Z
cairn:
  path: CP-CAIRN-015
  step: S04
---

# CP-CAIRN-015 S04

### S04 — what the kit installs — **COMPLETE**

#### Plan

Name `cairn-update` where the six skills are named — the pointer page's
skill lines and the generated bootloader's skills line (ADR-035); install
`feedbacks/` with an index that says what the folder holds and names the
type `Cairn Feedback` (ADR-038 decisions 2 and 5); install the project
root's `backlog/` with its index (ADR-041); ship `CHANGELOG.md` in the
package's file list, `status` printing its link at the release's commit
under the line that says a newer release exists, and the pointer page
linking it (ADR-039).

Definition-of-done item advanced: the fourth, in full.

Type `implementation`: three tests failed first — the two folders and
the package's file list, the notes line in `status`, and the pointer
page's new skill, bootloader line and link.

Read before writing: `skills/cairn-update/SKILL.md` already exists here
and `planInstall` copies every folder under `skills/`, so the kit already
installed the skill and the lock already owned it; what was missing is
the skill's name on the two generated pages. `CHANGELOG.md` does not
exist yet — the release path writes it, row 5 — so until 1.2.0 is
tagged the link points at a file the commit does not carry, and `npm
pack` skips a listed file that is absent.

The owner's word of 2026-09-25 on S03's two open points — classic branch
protection unread, and the installer's own copy of the two GitHub
functions — was *choose what's closest to the manifesto*: both stand as
S03 left them, the one reading the record decided and the consolidation
on the backlog for the row that owns the post-mortem.

Deliberately not in this unit: Ponytail and the harness (S05); the
kit's counts, which row 5 writes once — the module note stops carrying
them rather than carry a stale one; this repository's `AGENTS.md`,
which already names `cairn-update`.

#### Work

```cairn-unit
step: S04
unit: 04
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn.mjs`**: `SKILL_LINES` gains `cairn-update`; the
  bootloader names it and says *the skills* for the pointer page's list;
  `feedbacksIndex` and `backlogIndex`, planned as host files at
  `feedbacks/index.md` and `<project>/backlog/index.md`, and
  `feedbacks` among the kit's own places; `atRelease` beneath
  `specUrl` and `releaseNotes`; the pointer page links the notes;
  `describeStatus` prints them when a newer release exists.
- **`package.json`**: `CHANGELOG.md` in `files`.
- **`tools/cairn.test.mjs`**: two tests, and the pointer-page test's new
  lines.
- **`docs/modules/application.md`**: what `init` writes, without the
  counts; the pointer page's notes and `status`'s line.

#### Self-review

- `shrink:` `atRelease` is the one place a release's URL is built;
  `specUrl` and `releaseNotes` are a line each over it.
- `shrink:` the two indexes are built by `folderIndex`, as the others
  are, and none of the three new functions is exported — on the
  review's finding.
- `delete:` the module note's figures — 33 files, nine skill files —
  and *six skills* in the bootloader and the docblock: counts row 5
  writes once, and a word that turns false when a skill is added.
- Widening of `writes:`: none.
- Net: 121 lines added and 25 removed across the four files, 41 of the
  added the tests, beside this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria; it ran the suite and `status` on this
repository and returned in about a minute and a half, on the first
attempt, with six findings:

- `correctness:` `CHANGELOG.md` is absent, so every notes link is a 404
  at the commit it pins and `npm pack` skips the entry — **refused**:
  ADR-039 decides the file and the release path writes it before the
  tag, which is the only commit a package is published from; the plan
  above says so, and until then only a checkout of the trunk links it.
- `correctness:` the notes line printed whenever the two releases
  differed, so an older package run on a newer installation called
  itself an update and linked the older notes — **fixed**, the releases
  are compared numerically: an update, current, or a package older than
  the installation, which prints no notes; a test case each.
- `correctness:` a repository that already has `feedbacks/index.md` —
  this one — meets it as newly managed and to reconcile, and the module
  note did not say — **fixed** in the note.
- `shrink:` the two indexes rebuilt what `folderIndex` does — **fixed**.
- `shrink:` three exports nothing imports — **fixed**.
- `shrink:` a module-note line left unwrapped — **fixed**.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it returned
in about a minute, on the first attempt, with two findings:

- `correctness:` a numeric comparison orders `1.2.0-rc.1` after `1.2.0`
  — **fixed** as a named limit: Cairn cuts no prerelease tags, and a
  `ponytail:` comment says what to do if it ever does.
- `shrink:` `String()` around a release the lock always carries as a
  string hid a missing field rather than guarding it — **fixed**, gone.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 353 tests, 0 failed, exit 0; the two new tests among them
cairn-active --check  : pass — the view is current; no path state changed
the red first         : the folders, the notes line and the pointer page's new lines failed before the change
remote                : pushed immediately after this record; the commit id is named in the resume section by S05
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn.mjs, tools/cairn.test.mjs, package.json, docs/modules/application.md, this path's index and this step
session     : safe boundary after a successful push
next action : S05 — Ponytail and the harness
blockers    : none
```
