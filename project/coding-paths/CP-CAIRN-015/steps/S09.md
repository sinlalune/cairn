---
type: Cairn Coding Path Step
title: 'CP-CAIRN-015 S09 — what the coherence read found'
timestamp: 2026-09-25T00:00:00Z
cairn:
  path: CP-CAIRN-015
  step: S09
---

# CP-CAIRN-015 S09

### S09 — what the coherence read found — **COMPLETE**

#### Plan

The fresh coherence reader of candidate `1e7a32c` found three things
the writer can settle without the owner, and this unit settles them, so
`1e7a32c` is void and this unit's commit is the candidate:

- ADR-032 decision 4 has the installer say in one line when it did not
  read; a reading that finds no ruleset requiring a request said
  nothing, though other push rules and classic branch protection are
  never read — it now says so in one line.
- The module note said `cairn-audit` does not yet read the pilot's plain
  list and that teaching it is row 4's; row 4's `writes:` does not hold
  the note, so the sentence would turn false where no path can correct
  it — it now names the register's row 4, which stays true.
- `adopt`'s link reading copies the checker's pattern with nothing
  tracking the pair — a backlog item, for the next path that writes the
  checker.

The reader's other answers are the owner's to arbitrate, in the
request's description.

Definition-of-done item advanced: the third — the installer's one line
when it did not read; the ninth.

Type `implementation`. No failing test first: the line is printed only
after a live GitHub reading answers, which the suite never makes; the
reading itself is `readTrunk`'s, already tested.

#### Work

```cairn-unit
step: S09
unit: 09
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn.mjs`**, `refuseUnpushableTrunk`: the one line on a
  reading that found no ruleset in the way; the refusal no longer
  wrapped in a condition the return above it already settles.
- **`docs/modules/application.md`**: the line said; the audit sentence
  points at the register's row 4 in place of a claim that would turn
  false.
- `staleShapes` reads a declared exemption through `slash()` from
  `tools/cairn-config.mjs`, as the checker does.
- **`project/backlog/2026-09-25-adopt-reads-links-as-the-checker-does-by-copy.md`**.

#### Self-review

- `delete:` the dead `if (!reading.direct)` around the refusal.
- `stdlib:` the hand-written trailing-slash test, `slash()` from the
  module the installer already imports — on the review's finding.
- Widening of `writes:`: none.
- Net: 46 lines added and 11 removed across the three files, the
  backlog item 30 of them, beside this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria; it returned in about a minute, on the
first attempt, with the code change confirmed and four findings:

- `correctness:` the backlog item said the exemption match is a copy
  too, but `slash()` lives in `tools/cairn-config.mjs`, which loads
  nothing and the installer already imports — **fixed**, the installer
  reads `slash()` and the item covers the link pattern alone.
- `stdlib:` the hand-written trailing-slash test — **fixed** with it.
- `correctness:` dropping the module note's audit sentence hid a defect
  still true today — **fixed**: the sentence points at the register's
  row 4, which stays true whether that row is open or done, the claim
  about the future gone.
- `correctness:` the new line named only classic protection as unread,
  while required checks and restricted updates are not read either, so
  *none requires a pull request* could read as *you can push* —
  **fixed**, the line names both.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it returned
in about a minute, on the first attempt, with two findings:

- `shrink:` keeping the old `if (!reading.direct) throw` and adding the
  line after it would be a one-line diff — **refused**: the same length
  at the candidate, and one path instead of a guard the branch above
  already settles.
- `correctness:` the new line has no test — **refused**: it is printed
  only after a live GitHub reading, which the suite never makes by
  design; the reading it reports is `readTrunk`'s, tested on every
  answer.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, 1 advisory: record-integrity, S07 superseded by S08 — accepted, as at S08 (exit 0)
cairn-test            : pass — 357 tests, 0 failed, exit 0
cairn-active --check  : pass — the view is current; no path state changed
remote                : pushed immediately after this record, the commit waiting on the gate; its commit is the candidate C
```

#### Checkpoint

```text
status      : running
current step: S09 complete on push; its commit is the candidate
retention   : none (no-rewrite host)
changed     : tools/cairn.mjs, docs/modules/application.md, project/backlog/2026-09-25-adopt-reads-links-as-the-checker-does-by-copy.md, this step, this path's index
session     : safe boundary after a successful push
next action : close — the review in the request, the administrative commit, the owner's try, the merge
blockers    : none
```
