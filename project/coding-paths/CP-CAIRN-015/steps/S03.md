---
type: Cairn Coding Path Step
title: 'CP-CAIRN-015 S03 — what adopt and init say and refuse'
timestamp: 2026-09-25T00:00:00Z
cairn:
  path: CP-CAIRN-015
  step: S03
---

# CP-CAIRN-015 S03

### S03 — what `adopt` and `init` say and refuse — **COMPLETE**

#### Plan

Say under `adopt`'s stale list which kept host file still calls what it
made stale and that the gate is red until they go (ADR-033 decision 4);
name a file whose relative links resolve nowhere as a shape that wants a
`linkExemptions` declaration (ADR-037 decision 2); refuse the two
pairings of ADR-032 decision 4 — `protected` with `manual-git` at
`adopt` from the declarations alone, and `manual-git` registration on a
trunk that takes no direct push from the owner, read once on GitHub by
the installer — with the configuration and conformance rows; and make
the pilot register through a request on `pull-request` (ADR-032
decision 2).

Definition-of-done item advanced: the third, in full.

Type `implementation`: four tests failed first — Atomik's shape of a
kept workflow running a stale suite through a script's glob, a portrayal
with two dead links, `protected` beside `manual-git`, and the GitHub
reading against a fake API with the offline line at `init`. The pilot's
own test, outside this path's `writes:`, passes unchanged: its stage
names and counts are the same on both transports.

Read before writing: Atomik's pre-adoption `package.json` and
`.github/workflows/cairn.yml`, at the parent of its adoption commit
`37f56a5` — the workflow called `npm run cairn-check:test`, whose
script was `node --test 'tools/**/*.test.mjs'`. A match on exact paths
would have missed it; the line follows one `npm run` and reads a glob as
a glob.

The two GitHub functions ADR-032 decision 4 leaves to the path: the
post-mortem's cannot be imported by the package command — it imports the
checker, which loads a host configuration the command runs without — and
the post-mortem is outside this path's writes. The installer carries two
shorter ones; the backlog item names the consolidation for row 4.

The reading is of rulesets: `GET /repos/{o}/{r}/rules/branches/{trunk}`
for the rules that apply, and for each `pull_request` rule its ruleset's
`current_user_can_bypass`, which answers for the owner whose token asks.
Classic branch protection is not read, and the module note says so.

Deliberately not in this unit: what the kit installs (S04, S05); the
templates (S06).

#### Work

```cairn-unit
step: S03
unit: 03
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn.mjs`**: `staleCallers` — the manifest's scripts and the
  workflows that still call a stale path, a glob or a script that does;
  `staleShapes` names a Markdown file of either plane with dead relative
  links and the field, reading `linkExemptions` and the plan's files;
  `relativeLinks`, shared with `outwardLinks`; `adoptable` — no lock,
  and not `protected` beside `manual-git`; `readTrunk`, pure over a
  request function; `refuseUnpushableTrunk` with the installer's own
  `githubSlug` and `githubRequest`; `--registration` at `init`; `main`
  is async.
- **`tools/cairn-pilot.mjs`**: on `pull-request` the registration rides
  `register/cp-pilot-001`, its run is read with the trunk as base, it
  lands by a `--no-ff` merge, the branch is deleted and the trunk's run
  on the merge is read.
- **`tools/cairn.test.mjs`**: four tests.
- **`spec/reference/configuration.md`**, the `enforcementProfile` and
  `transport` rows; **`spec/reference/conformance.md`**, the profile
  row; **`docs/modules/application.md`**: the refusals, the reading, the
  two new adopt lines, the pilot.
- **`project/backlog/2026-09-25-two-copies-of-the-github-reading.md`**.

#### Self-review

- `shrink:` `outwardLinks`'s link walk became `relativeLinks`, shared
  with the new dead-link shape, rather than written twice.
- `native:` `AbortSignal.timeout` in the installer's GitHub request, not
  a controller and a timer.
- `yagni:` classic branch protection not read — named in the code and
  the module note, for the adopter whose trunk uses it.
- Widening of `writes:`: none.
- Net: 347 lines added and 35 removed across the seven files, 95 of the
  added the tests and 30 the backlog item, beside this step and the
  resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria and asked to check the GitHub endpoints
and fields; it ran the suite and returned in about a minute and a half,
on the first attempt, with nine findings, the API checked against its
knowledge of GitHub's reference rather than fetched:

- `correctness:` `current_user_can_bypass` can also read `exempt`, a
  ruleset that does not apply to the owner, and the reading refused it —
  **fixed**, `exempt` reads as a direct push, with a test case.
- `correctness:` a ruleset's required checks or restricted updates can
  also turn the push away, and the prose promised *whether the trunk
  takes a direct push* — **fixed in the prose**: the reading is whether
  the trunk requires a pull request, which is the pairing ADR-032
  decision 4 names, and the module note says the other rules are not
  read.
- `correctness:` the adopt scan read links the checker's `links` rule
  does not — bare names, titles — and a folder declared with a trailing
  slash was not exempted — **fixed**, `relativeLinks` uses the rule's
  own pattern and drops backslashes as it does, shared with
  `outwardLinks`, and the slash is honoured; the test carries a title, a
  bare link and a folder.
- `correctness:` a workflow that only mentions a flat record or a page
  with dead links was named as turning the gate red — **fixed**, a stale
  page is never counted as called; the test's other workflow mentions
  one.
- `correctness:` the rules list is paginated at thirty — **fixed**,
  `per_page=100`.
- `correctness:` `init` into a folder with no remote said *not on
  GitHub* — **fixed**, it says there is no remote to read.
- `shrink:` `adoptable` runs twice on the command's path — **refused**:
  `applyAdopt` stays safe for a direct caller, and the second run costs
  one small file read.
- `yagni:` the `npm test` case in the script matcher answered nothing
  reported — **fixed**, deleted.
- `shrink:` a test's second temporary folder was never removed —
  **fixed**.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it ran the
suite and returned in about a minute and a half, on the first attempt,
with seven findings:

- `correctness:` the installer's `githubSlug` had no anchor, so
  `https://evil.example/github.com/a/b.git` read as GitHub's `a/b` and
  the owner's token would have asked about it — **fixed**, the
  post-mortem's anchored pattern, and the look-alike is a test case; the
  drift the backlog item warns of, met once already.
- `correctness:` a ruleset answer with no `current_user_can_bypass`
  refused — **fixed**, it is a reading not obtained, with a test case.
- `correctness:` a workflow's `npm test` reaching a stale script was not
  followed — **fixed**, `npm test` and `npm t` reach the `test` script;
  this reverses the first read's `yagni:` deletion, which was wrong: it
  is the commonest step a workflow has. Fixing it named the kit's own
  workflow, whose comment says `npm test`, so a workflow's comments are
  dropped before it is read.
- `correctness:` a link inside an inline code span counted as broken
  for `adopt` and not for the checker — **fixed**, spans are stripped as
  the checker strips them.
- `correctness:` a glob's `?` was matched literally — **fixed**, one
  character; the test's `test` script carries one.
- `shrink:` the `'none'` sentinel beside a slug — **fixed**, `readTrunk`
  takes the remote's URL and decides.
- `shrink:` two sentences on what is not read — **fixed**, one list.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 351 tests, 0 failed, exit 0; the four new tests among them, the pilot's two unchanged
cairn-active --check  : pass — the view is current; no path state changed
the red first         : the suite failed to load on the missing readTrunk; the adopt tests failed on the absent lines
by hand               : a kept pilot on pull-request — its trunk carries "Merge branch 'register/cp-pilot-001'", the branch gone, registration pull-request declared
network               : no test reaches GitHub; the reading is exercised against a fake request function, and init into a folder with no remote prints the one line
remote                : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn.mjs, tools/cairn.test.mjs, tools/cairn-pilot.mjs, spec/reference/configuration.md, spec/reference/conformance.md, docs/modules/application.md, project/backlog/2026-09-25-two-copies-of-the-github-reading.md, this path's index and this step
session     : safe boundary after a successful push
next action : S04 — what the kit installs
blockers    : none
```
