---
type: Cairn Coding Path Step
title: 'CP-CAIRN-015 S10 — what the request reviewer found'
timestamp: 2026-09-25T00:00:00Z
cairn:
  path: CP-CAIRN-015
  step: S10
---

# CP-CAIRN-015 S10

### S10 — what the request reviewer found — **COMPLETE**

#### Plan

The reviewer on request #32, Codex's GitHub bot, left two findings on
candidate `67f1cb0`, and the owner brought them into the chat. Both are
defects; this unit fixes them, so `67f1cb0` is void, the record returns
from `ready` to `running`, and this unit's commit is the next candidate.

- A declined host file a release does not carry lost its name from the
  next lock, so a later release that brought the file back wrote it —
  against ADR-033 decision 2's *not now or at any later update*. The
  lock now keeps every declined name, carried by the release or not.
- When Ponytail was not read, a copy of the stance the lock owns but the
  tree no longer holds was planned nowhere, reported nowhere, and dropped
  from the lock — the repository left with part of the stance and no
  word of it. Such a copy now keeps its lock entry, and `status` and the
  offline `update` name it missing; the next run that reads Ponytail
  restores it.

Definition-of-done item advanced: the first, the declination; the
fifth, the offline stance.

Type `implementation`: both failed first — a declined name for a file
the release does not carry, dropped at the next `update`; a deleted
stance copy absent from `status`.

#### Work

```cairn-unit
step: S10
unit: 10
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn.mjs`**: `updateStatus` passes every declined name;
  `keptPonytail` records every copy the lock owns and lists the ones
  gone as `missing`; `lockFor` keeps a missing copy's recorded digest,
  the manifest still sorted as before; `describeStatus` names each.
- **`tools/cairn.mjs`**, on the review: `stanceFor`'s line reads the
  kept version; `takeRelease` takes back a declined name the release
  does not carry, and `update --take` says so.
- **`tools/cairn.test.mjs`**: the two cases, in the declination and the
  stance tests, with the restoring `update`.
- **`docs/modules/application.md`**: both.
- **`project/coding-paths/CP-CAIRN-015/index.md`**: `running`, no subject,
  and the live view with it.

#### Self-review

- `shrink:` the manifest is built from one map of what the lock owns,
  the missing copies added to it, and sorted once.
- Widening of `writes:`: none.
- Net: 55 lines added and 18 removed across the three files, 17 of the
  added the tests, beside this step, the view and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria; it ran the suite and returned in under
a minute, on the first attempt, with four findings:

- `correctness:` an offline run whose every stance copy was gone printed
  *nothing installed* while the lock and `status` still named the
  version and the missing copies — **fixed**, the line reads the kept
  version.
- `correctness:` the pointer page lists the stance only where its copy
  is on disk, so an offline run drops a missing copy's line while the
  lock owns it — **refused**: the page links every skill it names, the
  plan refuses a link to a file the tree lacks, and `status` names the
  missing copy.
- `correctness:` a declined name the release no longer carries could
  never be taken back, `--take` refusing any path outside the plan —
  **fixed**, `--take` moves such a name out of `declined` and writes
  nothing, and says so; the test takes one back.
- `shrink:` the manifest built through one map — **refused**: one sorted
  pass keeps the lock's key order, which the byte-equal generations of
  S02 rely on.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it returned
in under a minute, on the first attempt, with three findings, each
**fixed**:

- `correctness:` the message for a taken-back name the release does not
  carry left out the pointer page it rewrites, and the test called that
  *nothing written* — the message names the page, the test says *no
  file of it*.
- `correctness:` with every stance copy gone the line said *kept* the
  version and nothing more — it counts the missing copies.
- `shrink:` two tests of the plan in a row — one, with the declined list
  read once above them.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, 1 advisory: record-integrity, S07 superseded by S08 — accepted, as at S08 (exit 0)
cairn-test            : pass — 357 tests, 0 failed, exit 0
cairn-active --check  : pass — the view regenerated with the record back at running
the red first         : the dropped decline and the unreported missing copy, each failing before the change
remote                : pushed immediately after this record, the commit waiting on the gate; its commit is the candidate C
```

#### Checkpoint

```text
status      : running
current step: S10 complete on push; its commit is the candidate
retention   : none (no-rewrite host)
changed     : tools/cairn.mjs, tools/cairn.test.mjs, docs/modules/application.md, project/coding-paths/ACTIVE.md, this step, this path's index
session     : safe boundary after a successful push
next action : close — the request's description updated, the administrative commit, the owner's merge
blockers    : none
```
