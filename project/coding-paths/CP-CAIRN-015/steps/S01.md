---
type: Cairn Coding Path Step
title: 'CP-CAIRN-015 S01 — the installer reads what the repository declared'
timestamp: 2026-09-25T00:00:00Z
cairn:
  path: CP-CAIRN-015
  step: S01
---

# CP-CAIRN-015 S01

### S01 — the installer reads what the repository declared — **COMPLETE**

#### Plan

Make the installer plan under every root the configuration declares
(ADR-033 decision 1), let `update` decline a host file with the lock
remembering it (decision 2), write the documentation index's two
sentences in the shape they describe (decision 3), and put the empty
`steps/` folder on the backlog.

Definition-of-done item advanced: the first, in full; the seventh's
backlog item and the lock row's declination.

Type `implementation`: three failing tests first — a repository whose
roots sit elsewhere, a declined file across two updates and a `--take`,
the template's two sentences — each red before the change.

The option and its withdrawal, which ADR-033 decision 2 leaves to the
path: `update --decline <path>`, repeatable, and withdrawal by the
`--take <path>` the command already has — taking the release's version
of a declined file is exactly what taking it back means, so no second
flag. Only host files can be declined, the configuration excepted: the
record's title says *a host file*, a portable file an adopter does not
want is already kept when edited, and the configuration is what every
tool reads.

Deliberately not in this unit: the one plan function and the reconcile
predicate (S02) — `status` still plans its pointer page without the
reconcile and declined lists, which S02 settles; `adopt`'s report (S03);
what the kit installs (S04, S05); this repository's own lock, which no
unit rewrites by running `update` here.

#### Work

```cairn-unit
step: S01
unit: 01
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn.mjs`**: `optionsFromConfig` carries the architecture,
  decisions and modules roots beside the concept root; `rootsOf`
  replaces `conceptsRootOf` and derives each under the documentation
  root only where none is declared, and `buildConfig`, `hostBinding`,
  `documentationIndex` and `planInstall` read it — so the area's note
  follows the modules root too. `planInstall` throws when a path it
  writes falls under no declared root and outside the kit's own folders
  (`tools`, `skills`, `cairn`, `.github`) and the root. The documentation
  index says *one page at this root per surface, as they are written*
  and *where the repository has one*.
- **`tools/cairn.mjs`**, the declination: `--decline` in `parseArgs`;
  `main`'s update refuses a path that is not a host file and adds the
  rest to `lock.declined`; `installationStatus` reads a declined file as
  `declined`, action none; `describeStatus` prints it; `lockFor` and
  `writeLock` record `declined` and no digest for it; `applyUpdate`
  leaves it off the owned list of the pointer page, which gains a
  *Declined* section; `takeRelease` accepts a declined path, writes it
  (making its folder if the repository removed that too) and moves it
  out of `declined`.
- **`tools/cairn.test.mjs`**: the roots test, the declination test, two
  assertions on the documentation index.
- **`docs/modules/application.md`**: the roots, the index's sentences,
  the pointer page's declined list, and `--decline` beside `--take`.
- **`spec/reference/repository-layout.md`**: the lock's row names the
  declination.
- **`project/backlog/2026-09-25-an-empty-steps-folder-is-not-carried.md`**:
  Atomik's fifth observation's second half, for the next path that
  writes the skills.

#### Self-review

- `shrink:` withdrawal is the existing `--take`, not a second flag.
- `yagni:` no `--decline` on `adopt` or `init`: the record names
  `update`, and `init` writes into an empty target.
- `delete:` `conceptsRootOf`, folded into `rootsOf`.
- `shrink:` the pointer page's three path lists share one `bullets`
  helper, on the review's finding.
- Widening of `writes:`: none — every file is declared.
- Net: 187 lines added and 58 removed across the four files, 65 of the
  added the tests, beside the backlog item, this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria; it ran the suite and returned in about
a minute and a half, on the first attempt, with seven findings:

- `correctness:` `update --take` on a declined file leaves the pointer
  page listing it as declined until the next `update` — **deferred to
  S02**, carried in the resume's next action: the same page is already
  stale after a `--take` of an edited file (its reconcile list), and S02
  makes the page and `status` read one predicate; the command's own next
  line says to run `update`.
- `correctness:` `--take` of a declined path the release no longer ships
  crashes on an undefined template — **refused**: `takeRelease` opens by
  refusing any path the plan does not carry, before the declined branch.
- `correctness:` the pointer page listed every declined name while the
  lock keeps only those the plan still carries — **fixed**, `applyUpdate`
  filters them the way `lockFor` does.
- `correctness:` `update --take X --decline Y` ignored `--decline`
  without a word — **fixed**, the two together are refused.
- `correctness:` the module note claimed a stricter assertion than the
  code — **fixed**, it says the root's own files and names the kit's four
  folders.
- `yagni:` the declared-root assertion cannot fire today — **refused**:
  ADR-033 decision 1 asks for it, and it exists for the next `put` that
  hard-codes a path.
- `shrink:` two lists formatted alike — **fixed** (self-review).

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it ran the
suite and returned in about a minute, on the first attempt, with five
findings:

- `shrink:` `lockFor` filtered the declined names a second time —
  **fixed**, `applyUpdate` filters, `lockFor` only sorts, so the lock's
  bytes do not follow the order the flags came in.
- `yagni:` the declared-root assertion, again — **refused**, as above.
- `correctness:` the pointer page after `--take` of a declined file,
  again — **deferred to S02**, as above.
- `correctness:` `--decline ./docs/x.md` was refused as *not a host
  file* — **fixed**, `--decline` and `--take` normalise the path.
- `shrink:` the `--take`/`--decline` refusal ran after the plan was
  built — **fixed**, it runs straight after the arguments are read.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 344 tests, 0 failed, exit 0; the two new tests among them
cairn-active --check  : pass — the view is current; no path state changed
the red first         : the roots test, the declination test and the index's two sentences failed before the change
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn.mjs, tools/cairn.test.mjs, docs/modules/application.md, spec/reference/repository-layout.md, project/backlog/2026-09-25-an-empty-steps-folder-is-not-carried.md, this path's index and this step
session     : safe boundary after a successful push
next action : S02 — what update and status report
blockers    : none
```
