---
type: Cairn Coding Path Step
title: 'CP-CAIRN-015 S08 — repair: S07 committed on a red gate'
timestamp: 2026-09-25T00:00:00Z
cairn:
  path: CP-CAIRN-015
  step: S08
---

# CP-CAIRN-015 S08

### S08 — repair: S07 committed on a red gate — **COMPLETE**

#### Plan

The violation: S07, `40e1175`, was committed and pushed with
`cairn-check` red. Its step record quoted the register's new cell as a
live Markdown link, `./CP-CAIRN-015/index.md`, which resolves from the
`steps/` folder to nothing, and `links` refused it. The writer ran the
gate, then committed in the same command without making the commit wait
on the gate's exit code — the sequence the bootloader's mechanical
contract exists to prevent — and read the red verdict only after the
push. The commit is published and is not rewritten.

The repair: the one line in S07 that carries the link quotes the cell in
backticks, as code the rule does not read; S07 is superseded from here,
bound by its two blob ids, both read and never chosen. Nothing else in
S07 changes, and nothing outside this path's folder.

Definition-of-done item advanced: the ninth — every completed step with
its record, and a candidate checked bare; this unit's commit is the
candidate `C` in place of S07's.

Type `repair`.

#### Work

```cairn-unit
step: S08
unit: 08
type: repair
verified: cairn-check, cairn-test, cairn-active --check
supersedes: project/coding-paths/CP-CAIRN-015/steps/S07.md@4e77b3ae6bef5a7de4f737ecb9cc888116726139..c7a1bb7a60d0bbf640d6b5a350bb99b43a735fa4
```

- **`project/coding-paths/CP-CAIRN-015/steps/S07.md`**: the register
  cell in backticks.
- **`project/coding-paths/CP-CAIRN-015/index.md`**: the step list and
  the resume.

From here to the merge, every commit of this path runs as
`npm run cairn-check && git commit …`, so a red gate stops the commit.

#### Self-review

- Lean already.

#### Review

A fresh context, a subagent of this harness, given the diff and the two
criteria and asked to recompute both blob ids; it returned in under a
minute, on the first attempt: both ids as `git` reads them, S07 changed
in the one line, the gate green with the one expected advisory. Two
findings:

- `shrink:` the S07 entry on the path's step list repeats what the S08
  entry below it says — **refused**: the list is where a reader meets
  S07, and learns there that a repair follows it.
- `shrink:` the plan's sentence on how the commit came to skip the gate
  repeats the closing line — **refused**: the mechanism of the violation
  is what a repair step records; the closing line is the practice that
  follows from it.

Nothing was changed, so there is no second read.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, 1 advisory: record-integrity, S07 superseded by this step with its two blob ids — accepted, the correction stated (exit 0)
cairn-test            : pass — 357 tests, 0 failed, exit 0
cairn-active --check  : pass — the view is current; no path state changed
remote                : pushed immediately after this record, the commit waiting on the gate; its commit is the candidate C
```

#### Checkpoint

```text
status      : running
current step: S08 complete on push; its commit is the candidate
retention   : none (no-rewrite host)
changed     : project/coding-paths/CP-CAIRN-015/steps/S07.md, this step, this path's index
session     : safe boundary after a successful push
next action : close — the review in the request, the administrative commit, the owner's try, the merge
blockers    : none
```
