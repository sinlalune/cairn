---
type: Cairn Coding Path Step
title: 'CP-CAIRN-015 S06 — the two templates and the module note'
timestamp: 2026-09-25T00:00:00Z
cairn:
  path: CP-CAIRN-015
  step: S06
---

# CP-CAIRN-015 S06

### S06 — the two templates and the module note — **COMPLETE**

#### Plan

Write the pilot's definition of done as a plain list (ADR-042); give the
generated request template's *Coherence* section the first line naming
the reader, in this repository's words (ADR-043), backtick its one bare
blank and assert that none survives (Atomik's fourth observation); read
`docs/modules/application.md` for what the installer is at this unit and
leave no sentence of history; confirm the layout reference's lock row is
final.

Definition-of-done item advanced: the sixth, in full; the seventh, in
full — the lock row already names the declination (S01), the host
digests (S02), Ponytail's version and the harness copies (S05), and the
backlog item was written by S01.

Type `implementation`: two tests failed first — the generated template's
bare blanks, `<who approves>` and the roles, and the missing reader
line; and the pilot's boxed items.

Deliberately not in this unit: the candidate (S07); `tools/cairn-audit.mjs`,
which still reads only boxed items and is row 4's — the pilot's audit
prints its description without items, and the pilot asserts only that
the description names the candidate.

#### Work

```cairn-unit
step: S06
unit: 06
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn.mjs`**, `requestTemplate`: *Read by* under *Coherence*,
  the roles line's two blanks backticked.
- **`tools/cairn-pilot.mjs`**: the record's two items unboxed.
- **`tools/cairn.test.mjs`**: the no-bare-blank test with the reader
  line compared to this repository's template, and the pilot's list.
- **`docs/modules/application.md`**: the request template's reader line
  and blanks, the pilot's plain list; three paragraphs earlier units left
  unwrapped, rewrapped. Read whole for history: none found.

#### Self-review

- `shrink:` the reader line is compared with this repository's own
  rather than written into the test a second time.
- Widening of `writes:`: none.
- `delete:` the older test's partial backtick check, on the review's
  finding.
- Net: 45 lines added and 18 removed across the four files, 21 of the
  added the tests, beside this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria; it ran the suite and `definitionItems`
on the pilot's new list and returned in under a minute, on the first
attempt, with five findings:

- `correctness:` `cairn-audit`'s `definitionItems` reads only boxed
  items, so the audit reads no item from the pilot's plain list and says
  so, and the pilot does not notice — **refused**: `tools/cairn-audit.mjs`
  is outside this path's writes and excluded by its definition of done,
  and row 4 of 1.2 on the register names exactly this — *`cairn-audit`
  reading a plain-list definition of done*, ADR-042's `definitionItems`.
  The pilot asserting the items would fail until then.
- `correctness:` the module note said the pilot writes a plain list and
  not what the audit then reads — **fixed**, it says the audit prints
  its *no item* line there and whose work the fix is.
- `delete:` the older template test's backtick check on the part before
  the ledger, now covered by the whole-template one — **fixed**, gone.
- `shrink:` the pilot test read its file from the working directory —
  **fixed**, from the repository root as the line above it does.
- `shrink:` the pilot test pinned the items' literal text — **fixed**,
  it asserts only that no box survives.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it ran the
changed tests and the audit's suite and returned in about a minute, on
the first attempt, with four findings:

- `correctness:` on the pilot's record the audit's *no item* line tells
  the reader to check `scope_ref`, the wrong fault, and the module note
  did not warn — **fixed** in the note.
- `correctness:` the audit tool's docblock still says the kit's template
  does not carry its sections — **refused**: Atomik's observation 5a,
  placed on row 4 at registration; `tools/cairn-audit.mjs` is outside
  this path's writes.
- `correctness:` the bare-blank pattern missed a blank wrapped across a
  line, which the deleted check caught — **fixed**, a tag-like `<` read
  across lines.
- `shrink:` the plain-list check belongs in the pilot's own test, on the
  record it generates — **refused**: `tools/cairn-pilot.test.mjs` is
  outside this path's writes.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 357 tests, 0 failed, exit 0; the two new tests among them, the pilot's two green on the plain list
cairn-active --check  : pass — the view is current; no path state changed
the red first         : the template test named the two bare blanks; the pilot test found the boxes
remote                : pushed immediately after this record; the commit id is named in the resume section by S07
```

#### Checkpoint

```text
status      : running
current step: S06 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn.mjs, tools/cairn-pilot.mjs, tools/cairn.test.mjs, docs/modules/application.md, this path's index and this step
session     : safe boundary after a successful push
next action : S07 — the candidate
blockers    : none
```
