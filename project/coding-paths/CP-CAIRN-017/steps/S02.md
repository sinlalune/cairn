---
type: Cairn Coding Path Step
title: 'CP-CAIRN-017 S02 — what the backlog and the reports owe the adopters'
timestamp: 2026-09-26T00:00:00Z
cairn:
  path: CP-CAIRN-017
  step: S02
---

# CP-CAIRN-017 S02

### S02 — what the backlog and the reports owe the adopters — **COMPLETE**

#### Plan

Four backlog items answered and deleted, and the two sentences Atomik's
closing report asks of the unit skill. Not in this unit: any other line
of the skills, the checker or the installer; no record, since no
decision changes.

Definition-of-done item advanced: the second, in full.

Type `implementation`: the failing fixture first — a note of a root
named `wiki`, one folder down, linked from `docs/index.md` as
`../wiki/learning/linked-idea.md`, was refused as an orphan before the
change (`concept-orphan refused a linked note`, 0 pass, 1 fail).

#### Work

```cairn-unit
step: S02
unit: 02
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn-check.mjs`**: `conceptLinkTargets(text, from, root)`
  reads every link target, inline or by reference, strips angle
  brackets, skips a scheme, resolves it as GitHub does — against the
  linking file's folder, or the repository's root after a leading `/` —
  and keeps it when it lands under the declared root; the caller passes
  the document and `CONCEPTS_DIR`. The word `concepts/` is read no more.
- **`tools/cairn-fixture.test.mjs`**: the fixture above, green after.
- **`tools/cairn-check.test.mjs`**: the function's unit test rewritten
  for the new signature — a root named `wiki`, a reference definition, a
  leading `/`, angle brackets, a same-named file beside the linker not
  counted. `writes:` widened by this file: its test called the old
  one-argument form.
- **`docs/modules/application.md`**: the function's sentence.
- **`skills/cairn-open/SKILL.md`**: `steps/` is made by the first unit,
  Git carrying no empty folder. This repository registers with the same
  skill, so its sequence does the same.
- **`skills/cairn-unit/SKILL.md`**: movement 1, the first unit makes
  `steps/`; beside ADR-009's sentence on ids, *every figure, id or
  outcome written into a record is pasted from a command's output, never
  recalled*; movement 4, the review is written *before the unit's commit
  and never after*; movement 5, set `current_step` to this unit's step.
- **`tools/cairn.mjs`**: the bootloader template's sixth entry says the
  chapters are linked at the release's commit, then the skills and every
  file the kit owns — this repository's `AGENTS.md` line, byte for byte.
- **`project/backlog/`**: the four items deleted.

Measured before choosing to read only link targets: this repository's
gate stays green, so no concept note here was cleared by a bare mention
alone.

#### Self-review

- `shrink:` the function is nine lines where it was eleven, with no
  special case for `./`.
- `stdlib:` `join`, `dirname`, `relative` from `node:path`, already
  imported.
- Widening of `writes:`: `tools/cairn-check.test.mjs`, named above.
- Net: 67 lines added and 175 removed across twelve files, 131 of the
  removals the four backlog items, beside this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria; it ran the suite, the checker and a
node probe of link forms, and returned in about two and a half minutes,
on the first attempt, with eight findings:

- `correctness:` a link starting with `/` was skipped, where GitHub
  resolves it from the repository root and the old code counted it — a
  regression — **fixed**, resolved from the root, the test asserting it.
- `correctness:` angle-bracket targets were dropped — **fixed**,
  stripped, two assertions.
- `correctness:` `%20`, `?plain=1` and `.MD` targets are dropped —
  **refused**: note names are lower-case words, and nothing here writes
  them otherwise.
- `correctness:` a link written without `./` from a subfolder that does
  not resolve no longer clears a note, and `links` does not flag it —
  **refused**: GitHub does not follow that link either, and the orphan's
  message says to link the note where it is used.
- `correctness:` `startsWith('..')` rejected a note named `..x.md` —
  **fixed**, the test is `..` or `../`.
- `shrink:` the module note said the function resolves *as `links`
  does*, which it does not — **fixed**, it says *as GitHub does*; sharing
  one resolver is the backlog's shared-link-pattern item, left to the
  row after 1.2.
- `shrink:` a source line in the unit skill ran long — **fixed**,
  rewrapped.
- The other sentences read correctly in place: no finding.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it ran the
suite and a node probe and returned in about two minutes, on the first
attempt, with five findings:

- `shrink:` the docblock told the old bug's story — **fixed**, cut.
- `correctness:` a stray `>` at a target's end is stripped too —
  **refused**, harmless, and a stricter match costs more than it saves.
- `correctness:` the scheme test misses `svn+ssh:` and the like —
  **refused**: only a contrived link reaches it.
- `shrink:` the module note left a stub line — **fixed**, reflowed.
- `shrink:` two lines of the unit skill ran long or short — **fixed**,
  both paragraphs reflowed.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare (exit 0)
cairn-test            : pass — 365 tests, 365 pass, 0 fail (exit 0)
cairn-active --check  : pass — view and register cells already current (exit 0)
the red first         : the new fixture failed before the change, 0 pass, 1 fail
remote                : pushed immediately after this record; the commit id is named in the resume section by S03
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : the files above, this path's index and this step
session     : safe boundary after a successful push
next action : S03 — the changelog
blockers    : none
```
