---
type: Cairn Coding Path Step
title: 'CP-CAIRN-017 S05 — the README and the site'
timestamp: 2026-09-26T00:00:00Z
cairn:
  path: CP-CAIRN-017
  step: S05
---

# CP-CAIRN-017 S05

### S05 — the README and the site — **COMPLETE**

#### Plan

The README lists 1.2's surfaces in the order a newcomer meets them — the
adopter's update, the backlog and the channel among them — and every
sequence in it is 1.2's; the quick starts run against the candidate's
package; the site builds from the candidate and says what the README
says (ADR-023 decision 1, ADR-012). Not in this unit: the version, which
S07 sets.

Definition-of-done item advanced: the fifth, in full.

Type `documentation`. Widened in the unit, on the owner's ruling in the
chat: `skills/cairn-code/**`, by the amendment of 2026-09-26 in the
record.

#### Work

```cairn-unit
step: S05
unit: 05
type: documentation
verified: cairn-check, cairn-test, cairn-active --check, the quick starts and the build below
```

- **`README.md`**: the surfaces — adopting, opening and running with
  Ponytail's two skills fetched by the kit, deferring work to
  `project/backlog/`, closing with a fresh reader of the coherence
  questions, updating the kit with `cairn-update` and the release notes,
  telling Cairn what it cost under `feedbacks/`, learning. Quick start 1:
  Ponytail fetched, offline said. Quick start 2: the backlog read first,
  the plain-list definition of done, the registration by direct push or
  by request, `current_step`, a deferral's backlog file, the
  administrative commit before the owner reads. Quick start 3: the
  update as a path, `status` then the notes then the owner's decisions,
  `--decline`. *Where to go next*: the 1.2 page and the release notes.
  No count of the kit.
- **`site/index.html`**: its meta description's *six skills* replaced by
  *a handful of skills*, the README's words. The *Start* group's two
  entries and the *Skills* group read from the bundle still say what the
  README says; `docs/modules/site.md` describes the site as it is, so the
  checker's advisory on it is answered by reading, not by an edit.
- **`skills/cairn-code/SKILL.md`**: Ponytail's two skills fetched by the
  kit from the latest release and written beside these, the version in
  the lock — no tag, nothing for the adopter to install (ADR-036).
- **`CHANGELOG.md`**: the template line for `cairn-code`.
- **This path's record**: the amendment, its superseding acceptance, item
  9 and `writes:`.

The quick starts, run against `npm pack` of the candidate (version still
`1.1.0` until S07), with `npx --yes --package=<tarball> cairn` for
`npx cairn-protocol`:

```text
# quick start 1, in an empty repository
cairn — installed 50 file(s) and the lock into …/qs/my-project
release 1.1.0 from 2111757; trunk main via origin; profile ci; registration manual-git, integration pull-request; path history forbidden
OK — protocol satisfied
skills/ and .claude/skills/: cairn-brainstorm cairn-close cairn-code cairn-learn cairn-open cairn-unit cairn-update ponytail ponytail-review
# quick start 2, driven by the pilot on each transport
cairn-pilot — install to done on pull-request: green at every gate
cairn-pilot — install to done on manual-git: green at every gate
# quick start 3, status and update on a 1.1.0 installation
kit files: 32 pristine, 13 missing, 1 host
cairn — updated to release 1.1.0: 39 written, 0 deleted, 0 newly managed
OK — protocol satisfied
# quick start 3, adopt on a copy with no lock
next — run npm run cairn-check, read its findings, and commit the adoption as one unit
OK — protocol satisfied
```

The site, `npm run build` in `site/`:

```text
site — bundled 76 documents at 1.1.0 (2111757)
✓ built in 7.67s                 # exit 0; the chunk-size warning is Mermaid's, as at 1.1.0
skills bundled: cairn-brainstorm cairn-close cairn-code cairn-learn cairn-open cairn-unit cairn-update
```

#### Self-review

- `shrink:` no count restated; the site's meta takes the README's words.
- Widening of `writes:`: `skills/cairn-code/**`, by the amendment.
- Net: 58 lines added and 30 removed across the README, the site's
  shell, the skill and the changelog, beside the record's amendment, this
  step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria, told to check behaviour by running it;
it installed the kit, ran `init` offline and `update --decline`, and
returned in about two minutes, on the first attempt, with five findings:

- `correctness:` *reading the release notes first* — `cairn-update`
  reads `status` first — **fixed**.
- `correctness:` quick start 3's order — **fixed**, `status`, the notes,
  the owner's decisions, the registration, `update`.
- `correctness:` the backlog item is named in the path's goal and its
  file in `writes:`, and read before the record is written — **fixed**.
- `correctness:` `skills/cairn-code/SKILL.md` still pins Ponytail at
  `v4.9.0` and asks the adopter to install it, against ADR-036 — put to
  the owner in the chat as a decision, fix here or backlog; **fixed** on
  the owner's ruling, the scope amended.
- `delete:` a rewrap with no change of words — **fixed**, reverted.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it recomputed
the scope digest and returned in about a minute, on the first attempt,
and found nothing.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, one advisory (work-unit: site source changed, docs/modules/site.md did not — read, still accurate) (exit 0)
cairn-test            : pass — 365 tests, 365 pass, 0 fail (exit 0)
cairn-active --check  : pass — view and register cells already current (exit 0)
remote                : pushed immediately after this record; the commit id is named in the resume section by S06
```

#### Checkpoint

```text
status      : running
current step: S05 complete on push
retention   : none (no-rewrite host)
changed     : README.md, site/index.html, skills/cairn-code/SKILL.md, CHANGELOG.md, this path's index and this step
session     : safe boundary after a successful push
next action : S06 — this repository updated with its own kit
blockers    : none
```
