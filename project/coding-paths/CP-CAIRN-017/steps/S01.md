---
type: Cairn Coding Path Step
title: 'CP-CAIRN-017 S01 — the milestone rule''s record'
timestamp: 2026-09-26T00:00:00Z
cairn:
  path: CP-CAIRN-017
  step: S01
---

# CP-CAIRN-017 S01

### S01 — the milestone rule's record — **COMPLETE**

#### Plan

ADR-045 states the milestone rule `cairn-active` has implemented since
CP-CAIRN-016, promoted from that path's journal entry at blob
`fd04ab18b99823efbdffeb34297c5a092ccce973` and the owner's ruling of
2026-09-25, superseding ADR-031 decision 2's *that path's* for a
milestone. ADR-031 marked, the 1.2 page amended in place, the records'
index, the backlog item deleted. Not in this unit: the tool (unchanged),
chapter 4 of the specification (the definition of done excludes it).

Definition-of-done item advanced: the first, in full.

Type `decision`: the record and every document it amends.

#### Work

```cairn-unit
step: S01
unit: 01
type: decision
verified: cairn-check, cairn-test, cairn-active --check
```

- **`docs/adr/ADR-045-a-milestone-reads-the-paths-it-counts.md`**: the
  rule as `fillRegister` implements it — counted paths, *done* dated from
  the last done path's entry, *running* otherwise, the author's cell kept
  first; dated 2026-09-25, the ruling's day; three alternatives refused.
- **`docs/adr/ADR-031-one-place-for-a-fact.md`**: the status line and a
  mark under decision 2 naming the clause.
- **`docs/architecture/02-cairn-1-2.md`**: the description's range to
  ADR-045, an amendment paragraph under ADR-044's, one sentence marked
  *since 2026-09-25*, ADR-045 beside ADR-031 d2 in the tools table.
- **`docs/adr/index.md`**: ADR-045's entry, no gap; ADR-031's entry
  marked.
- **`project/backlog/`**: the milestone item deleted; one item added,
  chapter 4 saying nothing of the heading a milestone is counted by —
  ADR-045's consequences link it.

#### Self-review

- `shrink:` the 1.2 page's sentence points at ADR-045 instead of
  restating the rule; ADR-031's mark links it without a copy.
- Widening of `writes:`: none.
- Net: 134 lines added and 42 removed across six files, beside this step
  and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria; it ran the checker and returned in
under two minutes, on the first attempt, with eight findings:

- `correctness:` the context said the 1.1 and 1.2 rows name their release
  path; they name their promotion paths — **fixed**, and the first
  refused alternative rewritten on the true premise.
- `correctness:` the context said the 1.2 page says *that path's*; it
  says *from each path's `status:`* — **fixed**.
- `correctness:` ADR-045 and the backlog item said chapter 4 shows
  neither way to count; its example has a Paths column — **fixed**, only
  the heading is missing.
- `correctness:` the amendment paragraph said the changed sentence is
  marked; the added one is — **fixed**.
- `correctness:` the description and index entry dropped the *counts no
  path* case — **fixed**.
- `shrink:` the 1.2 page restated the whole rule — **fixed**, it points
  at ADR-045; the overlong line rewrapped.
- `shrink:` ADR-031's mark read as the whole decision superseded —
  **fixed**, it names the clause as ADR-028's does.
- `correctness:` the backlog item deferred to *the next path that may
  change the specification*, which does not exist — **fixed**, it names
  the register's row after 1.2.0.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it returned
in about a minute, on the first attempt, with four findings:

- `correctness:` the backlog said no path of 1.2 may change the
  chapters; CP-CAIRN-013 did — **fixed**, it names this path.
- `correctness:` the description stated *running* and the kept cell
  without saying which wins — **fixed** in the description, the index
  entry and the decision: the kept cell comes first, as line 134 of the
  tool reads.
- `shrink:` ADR-031's mark copied the rule — **fixed**, the link alone.
- `shrink:` the backlog item restated the three items this path leaves —
  **fixed**, cut.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step and the resume written (exit 0); a first run failed `work-unit` on the resume's unwritten checkpoint, written then
cairn-test            : pass — 364 tests, 364 pass, 0 fail (exit 0)
cairn-active --check  : pass — view and register cells already current (exit 0)
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : the six files above, this path's index and this step
session     : safe boundary after a successful push
next action : S02 — what the backlog and the reports owe the adopters
blockers    : none
```
