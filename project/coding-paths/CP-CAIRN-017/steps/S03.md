---
type: Cairn Coding Path Step
title: 'CP-CAIRN-017 S03 — the changelog'
timestamp: 2026-09-26T00:00:00Z
cairn:
  path: CP-CAIRN-017
  step: S03
---

# CP-CAIRN-017 S03

### S03 — the changelog — **COMPLETE**

#### Plan

`CHANGELOG.md` with 1.2.0's section: the adopter repairs absorbed and
not, by Crumbz's, Atomik's and ECOS's path ids, read from their
repositories; what the release changes for an adopter, in the pointer
page's names; one line per kit template changed since 1.1.0 (ADR-039,
ADR-015 decision 2). ADR-015's table and the register's row 5 cleared of
*pending* (ADR-031 decision 3). Not in this unit: the counts, the README,
the notes, the version.

Definition-of-done item advanced: the third, in full.

Type `documentation`: the changelog, the record's table, the register
row.

#### Work

```cairn-unit
step: S03
unit: 03
type: documentation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`CHANGELOG.md`**: new, at the root, three parts. The repairs: Atomik's
  forked checker (adoption commit `37f56a5`, recorded by CP-OPS-003 S05 at
  `731fe4c`) absorbed by `linkExemptions`, with the two declarations it
  needs first; CP-OPS-003 S07's workflow workaround (`8010b1c`) absorbed by
  `CAIRN_RUN_RED`; the roots `adopt` derived, absorbed; ECOS's
  CP-BACKLOG-001 (`7f929f7`) absorbed in substance; ECOS's `feedbacks/`
  (`645725d` outside a path, then CP-R0-001 S05 at `82c2d11`) absorbed;
  ECOS's CP-LOOK-002 S05 (`97f994c`, `3f79700`) **not absorbed** — the
  checker still cannot read a record a merge added
  (`--diff-filter=A` without `-m`, `tools/cairn-check.mjs:2989`) nor a blob
  over the default buffer; Crumbz's CP-CAIRN-SUPERSESSION-005,
  CP-CAIRN-PROVISIONAL-006 and CP-CAIRN-DETACHED-CHECKOUT-007 absorbed by
  1.1.0 and dropped by CP-CAIRN-UPDATE-027. Every id and commit was
  printed by `git log --oneline -1 <id>` or `ls` in the adopter's
  repository during this unit.
- The template list: 1.1.0 (from `git archive 1.1.0`) and the candidate
  each installed with `init --profile ci` into an empty repository, then
  `diff -rq`: fifteen files differ beside the pointer page and the lock,
  each given one line; new files under *What the kit owns*.
- **`docs/adr/ADR-015-a-release-reaches-an-edited-file.md`**: row 2 says
  the line is written in the changelog, `update` prints the diff and the
  pointer page links the notes — *pending* cleared.
- **`project/coding-paths/index.md`**: row 5's *pending* clause replaced
  by the line in the changelog's section.

The definition of done, item 3, says `update` links the section *beside
the diff*; `update` prints no link — the pointer page it writes and
`status` do. The row says what the code does. The same item and item 9
disagree on ADR-015, which item 9 lists among the documents left
byte-identical: item 3 names the change, and the owner's ruling of the
opening is item 3's; the closing read is told.

Found and not carried here: three ECOS notes of 2026-09-22 —
`a-record-added-by-a-merge-cannot-be-read`,
`a-record-larger-than-a-megabyte-reads-as-rewritten`,
`the-remedy-is-not-in-the-skill` — sit in ECOS's `feedbacks/` and have not
reached this repository; carrying them is the owner's, told in the chat.

#### Self-review

- `shrink:` the second part names what changes by the pointer page's
  headings and points at the 1.2 page for the records, rather than
  restating its tables.
- `delete:` the pointer page's and the lock's lines — generated files
  nobody reconciles by hand.
- Widening of `writes:`: none.
- Net: 138 lines added, 2 changed, beside this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria, told to check claims by running
commands; it installed both releases, read the three adopters'
repositories and returned in under five minutes, on the first attempt,
with thirteen findings:

- `correctness:` ADR-015 decision 2 has `update` print the line; the row
  clears *pending* with that half unimplemented and no superseding record
  — **refused**: the owner ruled it in this path's definition of done,
  item 3, and the row now says so and what the code does; offered to the
  owner in the chat as a record if wanted.
- `correctness:` six more files differ, by the release commit their
  links pin — **fixed**, said once as not listed.
- `correctness:` *this line says why* — **fixed**, *what changed*.
- `correctness:` *the four readings* miscounted five — **fixed**, *the
  changes above*.
- `correctness:` *To reconcile by hand* presented as new — **fixed**, only
  the files an update starts managing.
- `correctness:` the host files' list missed Atomik's binding — **fixed**,
  the list cut.
- `correctness:` ECOS's `feedbacks/` named no path id — **fixed**,
  CP-R0-001 S05 and the commit outside a path.
- `correctness:` *every pointer page links it* — **fixed**, *the pointer
  page*.
- `shrink:` the open line left out the plan's read before the go-ahead —
  **fixed**.
- `shrink:` the close and audit lines left out the deferral's backlog
  file and the figure written once — **fixed**.
- `delete:` the pointer page's and the lock's lines — **fixed**, cut.
- `shrink:` the ADR-015 row restated ADR-039 — **fixed**.
- `yagni:` a rewrap of one unit-skill line — **fixed**, dropped.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it ran both
installations with equal commit ids and returned in about four minutes,
on the first attempt, with four findings:

- `correctness:` the pointer page links the whole file, not the section
  — **fixed**, *links the release notes*.
- `correctness:` the ruling is in the definition of done, not the
  acceptance's prose — **fixed**, the row cites the definition of done.
- `correctness:` a note is cleared only by a link from outside the wiki
  — **fixed**.
- `shrink:` *rather than printing the line* — **fixed**, cut.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare (exit 0)
cairn-test            : pass — 365 tests, 365 pass, 0 fail (exit 0)
cairn-active --check  : pass — view and register cells already current (exit 0)
remote                : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : CHANGELOG.md, ADR-015, the register, this path's index and this step
session     : safe boundary after a successful push
next action : S04 — the counts written once
blockers    : none
```
