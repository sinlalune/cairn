---
type: Cairn Coding Path Step
title: 'CP-CAIRN-017 S04 — the counts written once'
timestamp: 2026-09-26T00:00:00Z
cairn:
  path: CP-CAIRN-017
  step: S04
---

# CP-CAIRN-017 S04

### S04 — the counts written once — **COMPLETE**

#### Plan

The conformance page's budget table measured at 1.2.0 by the tools,
beside the 1.0.0 and 1.1.0 columns, with the skills and the rules as rows
of their own (ADR-022 decision 2); the kit row, the README, the tools'
module note and the installer's header comment link the table and
restate no figure (ADR-031 decision 1). Not in this unit: the README's
surfaces and sequences (S05), the site (S05).

Definition-of-done item advanced: the fourth, in full.

Type `documentation`. Carried with it: the changelog's ECOS sentence,
made false by the owner's merge of request 34 — the three notes of
2026-09-22 now on the trunk, merged into this branch as `acf4dda`, and
the sentence now links the two that name the defects.

#### Work

```cairn-unit
step: S04
unit: 04
type: documentation
verified: cairn-check, cairn-test, cairn-active --check, the measurements below
```

The measurements, each read by a command during this unit:

```text
$ wc -w spec/index.md                                    # HEAD
6314 spec/index.md
$ git show 1.0.0:spec/index.md | wc -w ; git show 1.1.0:spec/index.md | wc -w
5630
6112
$ wc -w AGENTS.md spec/reference/paths.md project/coding-paths/binding.md spec/reference/execution-protocol.md
  350 AGENTS.md
 1389 spec/reference/paths.md
  302 project/coding-paths/binding.md
  996 spec/reference/execution-protocol.md
 3037 total
# at 1.1.0 the same four read 326, 1362, 302, 974 — 2964; at 1.0.0 272, 1362, 287, 974 — 2895
$ node tools/cairn.mjs init --target <empty> --profile ci      # from git archive HEAD
cairn — installed 50 file(s) and the lock
$ node tools/cairn.mjs init --target <empty> --profile local
cairn — installed 49 file(s) and the lock
# lock manifest keys: 50 (ci), 49 (local); at 1.1.0 33 and 32
# skills/ and .claude/skills/ each: cairn-brainstorm cairn-close cairn-code cairn-learn cairn-open cairn-unit cairn-update ponytail ponytail-review
$ node tools/cairn-rules.mjs    # distinct names, blocking where any row blocks
names 28 blocking 21 advisory 7      # at 1.1.0: 27, 21, 6; at 1.0.0: 24, 19, 5
$ node tools/cairn-pilot.mjs --transport pull-request
  one lightweight unit writes 2 protocol file(s); the whole lifecycle touches 4
$ node tools/cairn-pilot.mjs --transport manual-git
  one lightweight unit writes 2 protocol file(s); the whole lifecycle touches 5
```

- **`spec/reference/conformance.md`**: the table's 1.2.0 column and two
  rows, skills and rules; the paragraph saying how each is read and that
  this table is the one place; **the required entry chain past its
  target, 37 words over, for the first time**, its 73-word gain split
  file by file, the decision left to a later release or the owner; the
  kit row links the table; the pilots row loses its restated count; the
  1.0 claim labelled as 1.0.0's and said to hold with the table's
  figures.
- **`README.md`**: the skills, the rules and the kit's files link the
  table or the catalogue; no count remains.
- **`docs/modules/application.md`**: the rule count links the table.
- **`tools/cairn.mjs`**: the header comment names the table as the one
  place.
- **`CHANGELOG.md`**: the ECOS sentence above.

#### Self-review

- `delete:` the pilots row's restated count, and every count in the four
  places.
- Widening of `writes:`: none.
- Net: 55 lines added and 39 removed across five files, beside this
  step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria, told to re-measure every figure by
running the commands; it installed each release on both profiles, ran
the rules and both pilots at the three releases, and returned in two
minutes, on the first attempt: every figure right, five findings:

- `correctness:` the README's skills row still said *the five above* —
  **fixed**.
- `correctness:` the 1.0 claim was labelled *the current supported
  claim* — **fixed**, 1.0.0's.
- `delete:` the pilots row restated the unit's count — **fixed**, cut.
- `shrink:` a README paragraph left unwrapped — **fixed**.
- `site/index.html` says *six skills* — **deferred to S05**, which
  writes the site.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it returned
in under a minute, on the first attempt, with five findings:

- `correctness:` *those above* named no skills in the rows above —
  **fixed**, the enumeration cut; the README naming `cairn-update` is
  S05's.
- `correctness:` the kit's skills are written under two folders; the
  README named one — **fixed**.
- `correctness:` the section was left with no current claim — **fixed**,
  the 1.0.0 claim said to hold at 1.2.0 with the table's figures.
- `shrink:` the rules link pointed at a count, not the rules — **fixed**,
  it points at the catalogue.
- `shrink:` one rewrapped line ran long — **fixed**.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare (exit 0)
cairn-test            : pass — 365 tests, 365 pass, 0 fail (exit 0)
cairn-active --check  : pass — view and register cells already current (exit 0)
remote                : pushed immediately after this record; the commit id is named in the resume section by S05
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : the five files above, this path's index and this step
session     : safe boundary after a successful push
next action : S05 — the README and the site
blockers    : none
```
