---
type: Cairn Coding Path Step
title: 'CP-CAIRN-002 S04 — records and skills, five records'
timestamp: 2026-09-06T00:00:00Z
cairn:
  path: CP-CAIRN-002
  step: S04
---

# CP-CAIRN-002 S04

### S04 — records and skills, five records — **COMPLETE**

#### Plan

Themes 3 and 4 of the rulings note: R18 to R30, R38 and R39. Grouped by
the owner's questions as S03 did: Q10 and Q11 together as one record on
what closing leaves behind (R23, R24); Q12 (R21); Q13, the housekeeping
list (R18, R19, R20, R22, R28, R29, R38; R37 deferred to the tooling
record); Q14 (R25, R26, R27); Q15 with the fourth removal of Q19 (R30,
R39). Five records, ADR-006 to ADR-010, each with *promoted from* links at
the pinned blob ids and the surfaces it changes by today's names.

Definition-of-done items advanced: the second (every ticked line of themes
3 and 4 named by exactly one record; the fourth Q19 answer covered by the
record that makes the removal) and the fourth (each record names the rule,
skill, file or command it changes).

Deliberately not in this unit: themes 5 and 6 (S05), the architecture page
and the roadmap (S06), any file under `spec/`, `skills/`, `tools/`,
`site/`, `.github/` or the configuration.

#### Work

```cairn-unit
step: S04
unit: 04
type: decision
verified: cairn-check, cairn-active --check, test
```

- **ADR-006** — branches and worktrees after the merge. Says why the
  forge's *delete branch on merge* setting cannot be the mechanism: it
  would delete the path branch, which is the request's source.
- **ADR-007** — the framework block moves out of the bootloader; placed in
  the unit skill's resume step, where a writer meets the dirty file.
- **ADR-008** — seven numbered decisions. For R22 the fault was read from
  the checker: the `journal-entry` message says *declaring `path: <id>`*
  while the checker reads the namespaced key, which is how every Crumbz
  entry came to carry both. For R38 the report goes to `cairn-active`, the
  tool that reads views, not to the checker.
- **ADR-009** — three sentences; decision 3 points at ADR-008 decision 6
  for the message side.
- **ADR-010** — the module note's history removal and the split, with the
  two split signals named and the configuration's `areas` as the existing
  mechanism.
- **`docs/adr/index.md`** lists the five.
- Read for this unit: the Codex post-mortem (the `review` cascade, the
  Next.js block, the six `repair` misuses), the Gemini post-mortem (the
  five `repair` misuses, the duplicate key, the placeholder row), the
  sixteen-path audit (findings 7, 8, 9), the closure note's asks; the
  checker's `journalRecords` and its message; the kit's register and
  module note templates in `tools/cairn.mjs`; the lock's ownership of
  `AGENTS.md`.

#### Self-review

- Found and fixed: ADR-006 first said transport branches include the
  request's source; on a `pull-request` host the source is the path branch
  itself, which stays; the record now lists what is actually deleted.
- Found and fixed: ADR-008 decision 4 first blamed the template; the kit
  has no journal template, the shape was copied from the first entry, and
  the misleading text is the checker's message. The record now names the
  message and adds the shape to the close reference.
- Rejected: one record for the whole of Q13, plus one per ruling. Seven
  faults with no choice are one record with seven decisions; a reader
  holding the rulings note finds each under Q13.
- Rejected: naming R37 here because Q13 drives it. It is a tool, and the
  tooling record of S05 keeps the six themes readable.
- No widening of `writes:`.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main
cairn-active --check  : pass — view already current
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by the next unit
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-006 to ADR-010, docs/adr/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S05 — the documentation plane and the tools: themes 5 and 6, R31 to R37
blockers    : none
```
