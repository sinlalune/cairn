---
type: Cairn Coding Path Step
title: 'CP-CAIRN-002 S07 — the review of the first candidate answered'
timestamp: 2026-09-07T00:00:00Z
cairn:
  path: CP-CAIRN-002
  step: S07
---

# CP-CAIRN-002 S07

### S07 — the review of the first candidate answered — **COMPLETE**

#### Plan

The first candidate, `d11f684116159e957151ec5d304409ea71041c20`, went to
pull request #8 on 2026-09-07 and drew three findings from the request's
reviewer bot, each checked against the kit's code before this unit:

1. **Host files are never rewritten by `update`** once their template
   changes (`installationStatus` answers `review`), so ADR-005, ADR-011,
   ADR-013 and ADR-014 could not reach a 1.0 adopter; and an edited kit
   file is kept, so ADR-004's *version bump* could not reach the adopter
   whose checker is edited.
2. **The kit installs no fixture suite**: `REFERENCE_TOOLS` lists five
   production files and says the tests are deliberately not installed, and
   the installed workflow runs only the checker; ADR-014 decision 2
   described a step an adopter cannot run.
3. **The conformance target is *under* thirty files**, and the records
   counted thirty as meeting it; the count was also wrong by one between
   ADR-011 and its step.

Each is a finding about a record, not about the request, so the candidate
is void and this unit amends the records: a new record for the first,
amendments for the second and third, the page and the roadmap to match.
Definition-of-done items advanced: the first and third (the records and
the page, corrected), the fifth (the roadmap), and the eighth (the
candidate reviewed against the coherence questions, now on its second
try).

Deliberately not in this unit: anything under the excluded roots.

#### Work

```cairn-unit
step: S07
unit: 07
type: decision
verified: cairn-check, cairn-active --check, test
```

- **ADR-015** — a release reaches an edited file. Three decisions: a
  pristine file is rewritten whoever owns it; an edited one is kept, its
  diff printed, the release's one-line note beside it, and the pointer
  page lists it; the owner can take the release's version of a named
  file. It promotes no ruling and says so; it makes six records reach the
  adopter.
- **ADR-014** — decision 2 rewritten: `cairn-test` is this repository's
  script and workflow step; the kit installs no suite and names no test
  script; `npm test` stays the adopter's. The context now states what the
  kit installs. The rejected alternative of installing the suite is
  recorded.
- **ADR-013** — the count corrected: twenty-six plus three plus one is
  thirty, and thirty is over *under thirty*. The rule set: the kit of 1.1
  ships with at most twenty-nine files, the post-mortem tool included,
  two removals, candidates named for the path that owns the kit.
- **ADR-011** — the concept folders count as two files, not four: three
  folder indexes replace one root index, and the documentation index
  lists the folders. Consequences point at ADR-013's rule.
- **The architecture page** — fifteen records; the `cairn-test` and
  `cairn` rows of the tools table; the budget sentence; the pointer page's
  row in the documentation table.
- **The roadmap register** — path 3 names this repository's `cairn-test`;
  path 4 gains ADR-015 and the two removals.
- **`docs/adr/index.md`** lists ADR-015.

#### Self-review

- Found and fixed: ADR-015's first draft made `update` rewrite an edited
  host file "with a warning"; that would have overwritten the first
  adopter's three checker repairs before they are upstream. The decision
  is now never to rewrite an edited file without the owner naming it.
- Found and fixed: the arithmetic. S05's step said "three indexes and one
  more" and ADR-011 said "four more files"; the correct count is three
  for ADR-011 and one for ADR-013. S05 is append-only; this step names
  the error.
- Rejected: moving the conformance target to *thirty or fewer*. A cap
  that binds is worth keeping; the kit pays with two removals.
- Rejected: answering the findings in the request's description and
  keeping `C`. The findings change what the records decide.
- No widening of `writes:`.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main
cairn-active --check  : pass — view already current
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
coverage              : R01 to R39 each still named by exactly one record; ADR-015 promotes none
exclusions            : git diff --stat base..HEAD over the excluded roots is empty
user review           : the three findings came from the request's reviewer; the owner reads the answers with the second candidate
remote                : pushed immediately after this record; its commit is the second candidate
```

#### Checkpoint

```text
status      : running
current step: S07 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-011, ADR-013, ADR-014, ADR-015, docs/adr/index.md, docs/architecture/01-cairn-1-1.md, docs/architecture/index.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : close on the second candidate — rewrite the request's description, the owner's tick, the administrative commit
blockers    : none
```
