---
type: Cairn Coding Path Step
title: 'CP-CAIRN-002 S06 — the architecture page and the roadmap'
timestamp: 2026-09-07T00:00:00Z
cairn:
  path: CP-CAIRN-002
  step: S06
---

# CP-CAIRN-002 S06

### S06 — the architecture page and the roadmap — **COMPLETE**

#### Plan

The last forward step. One architecture page for Cairn 1.1 under
`docs/architecture/`, promoted from the two governing notes at their pinned
blob ids through the fourteen records, stating for a repository run by a
sole owner with agents how a path opens, runs and closes, what the checker
reads at each transition, what the documentation plane holds and where,
which tools exist, and what 1.1 removes from 1.0, naming the record behind
every statement. Then the roadmap register: the 1.0 and 1.1 milestones,
the five coding paths that build 1.1 with *no path yet*, the installer's
row gone.

Definition-of-done items advanced: the third (the architecture page), the
fifth (the roadmap register; `docs/index.md` was S01), and the sixth,
checked mechanically below. The second was re-checked line by line before
writing.

Deliberately not in this unit: the closure, which is not a step (ADR-008,
decision 1); any change outside `writes:`.

#### Work

```cairn-unit
step: S06
unit: 06
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Checked coverage first**: for each of R01 to R39, the count of records
  carrying *Promotes* or *Refuses* for that line, read with a script over
  `docs/adr/`; every line counts exactly one. The four Q19 answers: the
  three removals are made by ADR-001 d1, ADR-005 and ADR-010 d1, the kept
  mechanism by ADR-002 d2.
- **Checked the exclusions**: `git diff --stat` from `base_commit` to
  `HEAD` over `feedbacks/`, `spec/`, `skills/`, `tools/`, `site/`,
  `.github/`, `cairn.config.json` and `README.md` is empty; the seven
  notes are byte-identical to the base.
- **Wrote `docs/architecture/01-cairn-1-1.md`** in six sections plus what
  the page does not say. Two tables carry the checker's reading per
  transition and the documentation plane per folder, each row naming its
  record with the decision number, so a coding path can find its scope
  from the page as well as from a record. The removals section also names
  the two things the notes asked to remove and 1.1 keeps.
- **Rewrote `project/coding-paths/index.md`**: the placeholder row is
  gone; 1.0 is accounted for by CP-CAIRN-001, done; 1.1 by this path and
  five coding paths, ordered so their `writes:` do not overlap, each
  naming its records and its surface. The replacement of the stale
  citations of an earlier repository's records is assigned to path 2 for
  `tools/` and path 4 for `spec/`; the kit-file removal the post-mortem
  tool needs is assigned to path 4, which owns the kit.
- **Listed the page** in `docs/architecture/index.md`.

#### Self-review

- Found and fixed: the first cut of path 1 and path 4 both wrote
  `spec/index.md`; the register now names the chapters each writes (§5
  for path 1, §3 and §6 for path 4), which is still one file — the
  register says path 4 declares `depends_on` path 1, as ADR-003 asks, by
  its ordering sentence.
- Found and fixed: the page's first draft said the workflow "no longer
  runs on path branches"; a draft request's run is on the path branch's
  head, so the sentence now says what ADR-005 says, one run per commit
  that can land.
- Rejected: numbering the page `cairn-1-1.md` without the `01`; the
  layout names architecture pages numbered, and this is the first.
- Rejected: writing the five coding paths as path records now; a path is
  opened by the open skill with the owner's go-ahead, and the register's
  job is to say *no path yet* until then.
- No widening of `writes:`.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main
cairn-active --check  : pass — view already current
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
coverage              : R01 to R39 each named by exactly one record (script over docs/adr, 2026-09-07)
exclusions            : git diff --stat base..HEAD over the excluded roots is empty
user review           : the owner reads the page and the records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by the administrative commit
```

#### Checkpoint

```text
status      : running
current step: S06 complete on push
retention   : none (no-rewrite host)
changed     : docs/architecture/01-cairn-1-1.md, docs/architecture/index.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : close — merge the trunk in, candidate C, cairn-audit, the request with the owner's box
blockers    : none
```
