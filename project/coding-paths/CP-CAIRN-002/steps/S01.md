---
type: Cairn Coding Path Step
title: 'CP-CAIRN-002 S01 — the two folders and the first decision record'
timestamp: 2026-09-06T00:00:00Z
cairn:
  path: CP-CAIRN-002
  step: S01
---

# CP-CAIRN-002 S01

### S01 — the two folders and the first decision record — **COMPLETE**

#### Plan

Create `docs/adr/` and `docs/architecture/` with their indexes, list them in
`docs/index.md`, settle the numbering of this repository's own decision
records, and write the first record: theme 1 of the rulings note, how a sole
owner's repository opens and closes a path — R01, R02, R04, R05, R06 and R08
promoted, R03 refused with the owner's reason quoted. The record also settles
what this path's own registration met: a trunk ruleset that requires a
request, a `transport.registration` declaration that says `pull-request`, and
an admin bypass added to land the commit.

Definition-of-done items advanced: the first (`docs/adr/` exists, in shape,
with *promoted from* links), part of the second (theme 1's lines each named
by one record), the fourth (the record names the rule, skill, file or command
it changes by today's name), and the `docs/index.md` half of the fifth. The
architecture folder gets its index now so the documentation index can list
both folders in one unit; its page is S05.

Deliberately not in this unit: every other theme (S02 to S04), the
architecture page and the roadmap register (S05), and any change under
`spec/`, `skills/`, `tools/`, `site/`, `.github/` or `cairn.config.json` —
the record names those surfaces and stops.

#### Work

```cairn-unit
step: S01
unit: 01
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Wrote `docs/adr/ADR-001-sole-owner-opens-and-closes-a-path.md`**, one
  record with seven numbered decisions, in the shape the checker's `schema`
  rule reads: an `adr:` block with id, status and date, and a matching
  `Status:` line. Numbered decisions inside one record follow the precedent
  of the cited Atomik records (the checker names *ADR-020 decision 4*), so
  a later record or fixture can cite *ADR-001 decision 7*. Each decision
  names its ruling, the owner's question, and the surface it changes by the
  name the conformance page, the skills and the kit use today. The record
  states the forge facts read on 2026-09-06 from the ruleset `trunk`: pull
  request required, `protocol` check required strict, merge commits only,
  no force-push, no deletion, admin role bypass always; repository settings
  allow squash and rebase, the ruleset overriding them for the trunk.
- **Settled the numbering** in `docs/adr/index.md`: this repository counts
  from ADR-001; the eight numbers the specification and tools cite from the
  earlier repository (011, 012, 013, 017, 018, 020, 021, 022) are reserved
  and never reissued. The plan said "ADR-020 to ADR-022"; the grep found
  eight cited numbers, and all eight are reserved.
- **Wrote `docs/architecture/index.md`** with no page yet and the statement
  of what the first page will be; **rewrote `docs/index.md`** as a list of
  the four folders it holds plus the pointer to the concept wiki. Neither
  folder gets a `log.md`: `docs/modules/` set the precedent that Git keeps
  the folder's history, and the layout reference lists the log as optional
  for a documentation folder.
- **Status `accepted` on landing**: the owner took the decision on
  2026-09-06 before the record was written; the request that lands the path
  is the record's review, as chapter 3 says. A `proposed` status would have
  needed a flip to `accepted` at merge, which the administrative commit
  forbids and a further unit would have to carry.

#### Self-review

- Found and fixed: the first draft let decision 7 say the shortcut "was for
  `manual-git`" and stop; the record now says why neither transport needs
  it (the administrative commit already puts the path at `ready`), which is
  what makes the edge removable rather than merely disliked.
- Found and fixed: the ADR's *promoted from* links first pointed at the
  notes by name only; they now carry the two blob ids the path record pins,
  so the definition of done's first item is met literally.
- Rejected: writing `transport.registration: manual-git` into
  `cairn.config.json` in this unit to match decision 1. The configuration is
  deliberately excluded from this path; the record names the change and the
  coding path makes it.
- Rejected: one record per ruling for theme 1. Seven files would say seven
  times what one context explains once; the definition of done asks that
  each line be named by exactly one record, which seven numbered decisions
  in one record satisfy.
- No widening of `writes:`; every file of this unit is under `docs/adr/`,
  `docs/architecture/`, `docs/index.md` or this folder.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main, 6 changed files
cairn-active --check  : pass — view already current
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the record with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by the next unit
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/index.md, docs/adr/ADR-001-sole-owner-opens-and-closes-a-path.md, docs/architecture/index.md, docs/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S02 — what the checker reads: theme 2, R09, R10, R12 to R17, with R11 refused and R07's single run per candidate; one record per changed predicate or one for the theme, each naming the rule and the fixture the ruling asks for
blockers    : none
```
