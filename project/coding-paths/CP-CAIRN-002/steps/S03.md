---
type: Cairn Coding Path Step
title: 'CP-CAIRN-002 S03 — what the checker reads, four records'
timestamp: 2026-09-06T00:00:00Z
cairn:
  path: CP-CAIRN-002
  step: S03
---

# CP-CAIRN-002 S03

### S03 — what the checker reads, four records — **COMPLETE**

#### Plan

Theme 2 of the rulings note: R07, R09, R10, R12 to R17, with R11 refused.
The plan left open whether the theme is one record or one per changed
predicate. The lines group into four choices, each one owner question, so
four records: Q7 (R10, R11 refused), Q8 (R12), Q9 (R09, R13 to R17) and the
second removal of Q19 (R07). Each record names the rule as the conformance
page names it and the fixture the ruling asks for, links *promoted from* at
the two pinned blob ids, and stops before design.

Definition-of-done items advanced: the second (every ticked line of theme 2
named by exactly one record, R11 named as refused with the owner's reason,
the second and third items of Q19 covered), and the fourth (each record
names the rule, skill, file or command it changes by today's name).

Deliberately not in this unit: themes 3 to 6 (S04, S05), the architecture
page and the roadmap (S06), and any change under `spec/`, `skills/`,
`tools/`, `site/`, `.github/` or the configuration.

#### Work

```cairn-unit
step: S03
unit: 03
type: decision
verified: cairn-check, cairn-active --check, test
```

- **ADR-002** — the seal judged at every transition on every ref, and the
  checkboxes kept with one sentence in the template. The fault it fixes is
  read from the checker: `scope-digest` is guarded by a closed status on a
  path branch, so the trunk's integrating unit is never judged for it.
- **ADR-003** — the advisory `writes-overlap`, and the two answers the
  later path may give. Decided that `depends_on` silences it and an accepted
  race keeps it visible until disposal in the closing review; that is what
  makes it a rule the owner answers rather than a report.
- **ADR-004** — six corrections as six numbered decisions. R13's refusal
  is placed under `work-unit`, where the unit's other claims are judged,
  rather than under a rule of its own; the record says why. R17 names the
  three Crumbz repairs by their path ids and links the closure note that
  records their branches.
- **ADR-005** — the push run on `path/**` goes; the trunk's run judges what
  lands directly under ADR-001. Names the draft request as the native way
  to keep one run per unit for a writer who wants it.
- **`docs/adr/index.md`** lists the four.
- Read for this unit: the sixteen-path audit (findings 2, 3, 4, 6 and its
  asks), the closure note of 09-04 (what did not hold, and its asks), the
  Gemini post-mortem (the unpinned checkpoint), and the checker's
  `registration-base`, `scope-digest`, `provisional` and `resolveBranch`
  code, so each record states the current reading it corrects.

#### Self-review

- Found and fixed: ADR-005's first draft said the push run on the trunk
  goes too; it stays, because under ADR-001 a registration and an
  integration land directly and the trunk's run is their one run.
- Found and fixed: ADR-004 decision 2 first said "a new blocking rule";
  R13 is a claim of the unit and belongs under `work-unit`, and the
  alternatives section now records the choice.
- Rejected: one record for the whole theme. Four owner questions are four
  choices, and a reader holding the rulings note finds each line under the
  question that drove it.
- Rejected: naming R28 and R36 as implemented here. ADR-004 and ADR-005
  mention them as what a message or a red run leads to; the records that
  implement them come in S04 and S05.
- No widening of `writes:`.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main, 13 changed files
cairn-active --check  : pass — view already current
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by the next unit
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-002 to ADR-005, docs/adr/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S04 — records and skills: themes 3 and 4, R18 to R30, R38, R39; the module note's history removal
blockers    : none
```
