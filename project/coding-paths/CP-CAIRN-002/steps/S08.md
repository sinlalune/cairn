---
type: Cairn Coding Path Step
title: 'CP-CAIRN-002 S08 — the owner dropped the box'
timestamp: 2026-09-07T00:00:00Z
cairn:
  path: CP-CAIRN-002
  step: S08
---

# CP-CAIRN-002 S08

### S08 — the owner dropped the box — **COMPLETE**

#### Plan

The second candidate, `8227063e39b1af117fa325125b9a32bfc545b838`, carried
in its request an acceptance checkbox for the owner, as R04 and ADR-001
decision 3 described it. Asked why the tick was needed, the owner ruled on
2026-09-07: *"I chose that because of the testing, not because of the
tick, drop the box."* Question 3's answer stands — the owner tries the
result before every merge — and its recorded shape changes: a named step
in the close skill, nothing in the request, the merge click recording
both the try and the acceptance (decision 4).

This is a change to a record under review, so the candidate is void and
this unit amends the record and every text that repeated the box:
ADR-001 decision 3, its consequences, its manifesto weighing and its
implementation table; the architecture page's closing section and tools
table; ADR-015's list of what could not reach the adopter; the roadmap's
path 1. Definition-of-done items advanced: the first, third and fifth
(corrected) and the eighth.

Deliberately not in this unit: anything under the excluded roots.

#### Work

```cairn-unit
step: S08
unit: 08
type: decision
verified: cairn-check, cairn-active --check, test
```

- **ADR-001 decision 3** now states the step and quotes the owner's
  ruling of the seventh beside the note's checkbox variant, so a reader
  holding the rulings note sees where the two differ and why. The request
  template and `cairn-audit` leave its implementation table.
- **The architecture page**: "nothing is ticked for it"; `cairn-audit`'s
  row loses the box.
- **ADR-015**: five records could not reach the adopter, not six; the
  request template leaves the list.
- **The roadmap**: path 1 no longer writes the request template.
- **The request's description** is rewritten for the third candidate
  without the owner's section; the administrative commit follows at once,
  since the acceptance is the merge.

#### Self-review

- Found and fixed: the first draft of decision 3 deleted the checkbox
  sentence as if the note had never proposed it; the record now keeps the
  note's variant and the owner's words dropping it, because the note is
  pinned and unchanged and a reader must be able to reconcile the two.
- Rejected: a superseding record instead of an amendment. ADR-001 is on
  this branch and not yet accepted; S07 amended records under review the
  same way. Once landed, a change is a superseding record.
- No widening of `writes:`.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main
cairn-active --check  : pass — view already current
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
coverage              : R01 to R39 each still named by exactly one record
exclusions            : git diff --stat base..HEAD over the excluded roots is empty
user review           : the owner's ruling of 2026-09-07 in the chat, quoted in ADR-001
remote                : pushed immediately after this record; its commit is the third candidate
```

#### Checkpoint

```text
status      : running
current step: S08 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-001, ADR-015, docs/architecture/01-cairn-1-1.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : the administrative commit on the third candidate, then the owner reads and merges
blockers    : none
```
