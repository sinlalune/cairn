---
type: Cairn Coding Path Step
title: 'CP-CAIRN-002 S05 — the documentation plane and the tools, four records'
timestamp: 2026-09-06T00:00:00Z
cairn:
  path: CP-CAIRN-002
  step: S05
---

# CP-CAIRN-002 S05

### S05 — the documentation plane and the tools, four records — **COMPLETE**

#### Plan

Themes 5 and 6 of the rulings note, R31 to R37. Grouped as before: the
three items the owner decided before the decisions page (R31, R34, R35) as
one record on the adopter's documentation plane; Q16 (R32); Q17 (R33); and
theme 6 as the one tooling record ADR-008 promised (R36 with Q18, R37 with
Q13). Four records, ADR-011 to ADR-014. Two design choices were left to
this path and are settled here: where the proactive concept-note
instruction lives (the bootloader, one line, no new skill) and what a red
run does with the post-mortem's output (the run's log, and a comment on the
request; nothing committed).

Definition-of-done items advanced: the second, now complete for every
ticked line, and the fourth.

Deliberately not in this unit: the architecture page and the roadmap
(S06); any file under `spec/`, `skills/`, `tools/`, `site/`, `.github/` or
the configuration.

#### Work

```cairn-unit
step: S05
unit: 05
type: decision
verified: cairn-check, cairn-active --check, test
```

- **ADR-011** — `docs/inputs`, the wiki in three folders, the proactive
  note. Read chapter 6's three-scope table first: the owner's three folders
  replace the *coding beside the module notes* scope and add an
  adopter-side `cairn` folder; the record says so rather than presenting
  the split as new.
- **ADR-012** — the page per surface at `docs/<surface>.md`; for this
  repository the README and the site are those pages, written by the paths
  that build 1.1, as the path record's exclusions already say.
- **ADR-013** — `cairn/README.md`, four sections in order, generated by
  `init` and `update`. Counted the kit: the lock's manifest holds
  twenty-six files; ADR-011 adds three indexes and one more, ADR-013 adds
  one; thirty exactly, the budget on the conformance page.
- **ADR-014** — `cairn-postmortem` and `cairn-test`. The post-mortem tool
  is a thirty-first kit file, so the record says a kit file must be removed
  or merged first and leaves which to the coding path.
- **`docs/adr/index.md`** lists the four.
- Read for this unit: the decisions page's *already decided* section and
  Q16 to Q18; the rulings note's owner quotes under R34 and R35; chapter 6
  of the specification; the brainstorm skill; the lock's manifest and the
  kit's `put` list in `tools/cairn.mjs`; the package scripts and the
  workflow.

#### Self-review

- Found and fixed: ADR-011's first draft put the proactive-note
  instruction in the `cairn-code` stance; that skill is read during a
  unit's change, not in a chat, and the owner said *even in the chat
  session*. The bootloader is the one file every session reads.
- Found and fixed: the kit's file count. The first draft of ADR-013 said
  "one file, well within the budget"; the count is thirty exactly once
  ADR-011's indexes are in, and ADR-014's tool goes over. Both records now
  say so, and ADR-014 names the consequence.
- Rejected: one record for all of theme 5. The three *already decided*
  items are one ruling of one day; Q16 and Q17 are two choices with two
  tags, and a reader looks each up by its question.
- No widening of `writes:`.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main
cairn-active --check  : pass — view already current
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by the next unit
```

#### Checkpoint

```text
status      : running
current step: S05 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-011 to ADR-014, docs/adr/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S06 — the architecture page for Cairn 1.1 and the roadmap register; then merge the trunk in, produce the candidate, open the request
blockers    : none
```
