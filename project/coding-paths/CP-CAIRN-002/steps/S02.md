---
type: Cairn Coding Path Step
title: 'CP-CAIRN-002 S02 — no reserved numbers'
timestamp: 2026-09-06T00:00:00Z
cairn:
  path: CP-CAIRN-002
  step: S02
---

# CP-CAIRN-002 S02

### S02 — no reserved numbers — **COMPLETE**

#### Plan

Correct one decision S01 took in `docs/adr/index.md`. S01 reserved the
eight decision numbers the specification, the conformance page and the
checker's comments cite from the repository the protocol was built in
before this one, so that no record here could reuse them. The owner asked
what that earlier repository had to do with anything, and on 2026-09-06
agreed to the simpler option: records count from ADR-001 with no gaps, no
number is reserved, and the stale citations are replaced by the reason
itself or by a record of this folder when the coding paths of 1.1 edit the
files that carry them. The plan's remaining items shift by one, S03 to S06,
because this correction takes the S02 slot; a pushed step is append-only, so
S01 is not edited and this step names it.

Definition-of-done item advanced: the first, the shape and rule of
`docs/adr/`. Deliberately not in this unit: the citations themselves, which
sit under `spec/` and `tools/`, excluded from this path; the roadmap names
their replacement in S06.

#### Work

```cairn-unit
step: S02
unit: 02
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Rewrote the numbering section** of `docs/adr/index.md`: no gaps, no
  reservation, the citations named as pointing at nothing here, their
  replacement assigned to the paths of 1.1 through the roadmap.
- **Left ADR-001 unchanged.** The reply to the owner said the change would
  add a line to ADR-001's implementation table; on reading the record again
  that line does not belong there, because replacing citations is not a
  consequence of how a sole owner opens a path. It belongs to the roadmap
  register, which S06 writes.
- **Renumbered the plan**: S02 to S05 become S03 to S06, and item 1 now
  states the numbering as settled.
- **Refreshed the resume section**: checkpoint at S01's commit
  `5f5babd8093f964bb9be59709d6b3996742d902d`, next action S03, the
  reservation added to *tried and rejected*.

#### Self-review

- Found and fixed: the first draft of the numbering section still listed all
  eight numbers, which is the reservation under another name; it now names
  three as examples and says why none is reserved.
- Rejected: folding this into the theme-2 unit. A correction the owner asked
  for is clearer as its own two-file step than as a paragraph inside a unit
  about the checker.
- No widening of `writes:`.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main
cairn-active --check  : pass — view already current
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : the owner asked for this change in the chat on 2026-09-06
remote                : pushed immediately after this record; the commit id is named in the resume section by the next unit
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/index.md, this path's index, plan and this step
session     : safe boundary after successful push and a green CI run
next action : S03 — what the checker reads: theme 2, R09, R10, R12 to R17, with R11 refused and R07's single run per candidate
blockers    : none
```
