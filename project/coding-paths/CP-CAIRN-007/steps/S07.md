---
type: Cairn Coding Path Step
title: 'CP-CAIRN-007 S07 — the tools as they are'
timestamp: 2026-09-14T00:00:00Z
cairn:
  path: CP-CAIRN-007
  step: S07
---

# CP-CAIRN-007 S07

### S07 — the tools as they are — **COMPLETE**

#### Plan

Make `docs/modules/application.md` describe the tools as they stand at
the candidate — the post-mortem included — with no history in it, and
carry into row 3 of the register the two files this path widened its
writes by after registration.

Definition-of-done items advanced: the sixth, and the seventh's register
clause.

Type `documentation`: the note, the register, this step. No test:
nothing under `tools/` changes.

**A defect of this path, repaired here.** S03's edit of *The rules* left
a dangling fragment — *None of it is a finding: the remedy for a gap is
a setting, and no failure of* — with no ending, followed straight by
*The configuration is schema 2.* It was pushed in `dcd8849` and survived
three green runs, because no gate reads prose and no reader was given
that file. It is repaired in this unit rather than in a `repair` of its
own: this is the unit whose whole job is that document, and a broken
sentence in it is the opposite of *as they are*.

Deliberately not in this unit: the kit's file count and the eight skill
files in *The kit*, which are path 4's measurements; the twenty-six
rules in *The rules*, which is path 2's count and unchanged by this
path; the conformance page, which item 7 holds unchanged.

#### Work

```cairn-unit
step: S07
unit: 07
type: documentation
verified: cairn-check, test
```

- **`docs/modules/application.md`**: the dangling fragment repaired and
  the forge paragraph rewritten in the present tense, without the date
  of the incident that taught it — what CI reports, and why the token is
  absent, are facts about the tools; when it happened is not. The tools
  table says what `cairn-audit` prints and in what order, what
  `cairn-active` reports beside the live view, and that the suite runs
  as `cairn-test`. The CI paragraph says the post-mortem step runs only
  on the checker's own failure and posts only where there is a request.
- **`project/coding-paths/index.md`**: row 3's writes column carries
  `tools/cairn-workflow.test.mjs` and `.github/pull_request_template.md`,
  each with the reason the unit that widened for it gave.

#### Self-review

- `delete:` a *What changed in 1.1* paragraph drafted for the note,
  which is what *with no history* forbids and what the journal entries
  and the 1.1 page already hold.
- `shrink:` the forge paragraph said *which on 2026-09-13 was false of
  this very repository* — the incident is in S02, S03 and the journal;
  the note keeps the mechanism, which is what a reader of the tools
  needs.
- `shrink:` the `cairn-audit` row first reproduced the whole section
  order twice, once per transport; the `manual-git` half is one clause.
- No widening of `writes:`; both files are declared.
- Net: 26 lines of note, 1 of register.

#### Review

First read, a fresh context given this unit's diff and the two criteria,
the ladder and correctness, and nothing else. Eleven findings. The item
this unit advances asks that the note describe the tools AS THEY ARE, so
a sentence the diff did not touch and left false is this unit's to
answer; four of them were:

- `correctness:` *`tools/` is this repository's one source root and its
  one implemented area* — the configuration declares two source roots
  and two areas, and `docs/modules/site.md` sits beside this note.
  **Fixed**, measured from `cairn.config.json`.
- `correctness:` *installs 26 files* — `planInstall` yields 27 on the
  `ci` profile and 26 on `local`. It was 26 before the sixth skill
  landed in coding path 1. **Fixed**, both numbers measured.
- `correctness:` *the eight skill files* — nine, across six skills.
  **Fixed**, measured.
- `correctness:` *installing a real repository with `cairn-init`* — no
  such command exists; the kit's own `adopt` lists `tools/cairn-init.mjs`
  among the 0.2 shapes it tells an adopter to delete, and the fixtures
  install through `planInstall`. **Fixed** to `cairn init`. This is the
  stale-citation class coding path 2 cleared under `tools/`, still
  standing in a document.
- `correctness:` *One invocation form remains* — `--scope-digest` is a
  second, handled before anything else and exiting early, and it is the
  form the skills and the request's own description tell a writer to
  run. **Fixed**: one form judges a tree, one answers a question.
- `correctness:` *Three parity tests* — four are named `parity:`.
  **Fixed** by dropping the count rather than correcting it: a count in
  prose is measured when it is read, and this one had been wrong since
  the fourth was added.
- `correctness:` the post-mortem row's *no exit code of its own* — true
  of every reading, but a malformed invocation exits 2. **Fixed** to the
  tool's own words.
- `correctness:` the frontmatter description named seven tools for a
  table of eight. **Fixed**.
- `stdlib:` the forge paragraph was a second copy of the workflow's own
  comment, which carries the same argument at the step where the token
  is not set. **Fixed**: the note keeps the fact and points at the
  workflow and at the register row that will undo it.
- `shrink:` the `cairn-audit` row said *in the order the template gives*
  and then gave the order. **Fixed**.
- `delete:` the `npm test` → `npm run cairn-test` swap in the suite's
  table row, since `test` is still an alias and the section twelve lines
  below resolves it — **refused**: the row is the one-line summary, and
  naming the canonical script there is what stops a reader assuming the
  kit installs a `test` script, which is the whole of the decision
  behind the rename.

Second read, a fresh context given only the diff of those fixes and the
same two criteria, as ADR-017 decision 3 bounds it. Seven findings:

- `correctness:` the corrected file count introduced a list that reads
  as exhaustive and accounted for 24 of the 27 — `package.json`, the
  live view and this note were installed and named by nothing.
  **Fixed**: the categories are measured and they add up.
- `correctness:` the shortened forge sentence, *cannot see everything
  the line reports*, is a half-reason: it gives no ground for refusing a
  token that sees part of it, which is the decision the paragraph
  exists to justify. **Fixed** by taking the ladder's other end — the
  note states the fact and defers the argument entirely to the workflow,
  which is where it is enforced.
- `shrink:` *which is what the skills and the request's own description
  tell a writer to run* — true, and said by each of those. **Fixed**,
  cut.
- `shrink:` *The other is `site/`, whose note is beside this one* —
  `docs/modules/index.md` already lists both with their roots.
  **Fixed**, cut; the correction that earns its place is *one of two
  source roots*.
- `shrink:` naming a second of four parity tests moved the sentence from
  wrong-by-count to incomplete-by-list. **Fixed**, cut.
- `correctness:` the register row — I replaced `tools/cairn-workflow.test.mjs`
  with prose, in a column that is a list of paths, so the surface a
  predicate matches on was gone and the workflow read as listed twice.
  **Fixed**: the path is back, the parenthetical reasons stay cut.
- `correctness:` the hunks were not re-wrapped, leaving three lines long
  and one orphan. **Fixed**.

The fixes above are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory (exit 0)
cairn-active --check  : pass — view already current (exit 0)
cairn-test            : pass — 320 tests, 0 failed (exit 0)
every count in the note: measured with planInstall on both profiles and against cairn.config.json — 27 files on ci, 26 on local, five reference tools, nine skill files, six skills, five folder indexes, eight host files; the rule counts are coding path 2's and unchanged
links                 : both added links resolve; the checker's `links` rule passes
remote                : pushed immediately after this record; the request's run judges it
```

#### Checkpoint

```text
status      : running
current step: S07 complete on push
retention   : none (no-rewrite host)
changed     : docs/modules/application.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S08 — the candidate (the plan's sixth unit): merge the trunk in, run every gate bare, replace the draft request's description with what `npm run cairn-audit` now prints and fill it, then the administrative commit declaring `ready` and `subject_commit` with its check green — and only then ask the owner to merge
blockers    : none. Item 2 of the definition of done stays knowingly unmet; the candidate names it as unmet, with S03 as the reason.
```
