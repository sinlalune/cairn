---
type: Cairn Coding Path Step
title: 'CP-CAIRN-007 S04 — the register owns what this path found'
timestamp: 2026-09-13T00:00:00Z
cairn:
  path: CP-CAIRN-007
  step: S04
---

# CP-CAIRN-007 S04

### S04 — the register owns what this path found — **COMPLETE**

#### Plan

Turn two debts into owned rows of the roadmap register, because a debt
named in a journal is a note and an unowned surface is how a protocol
drifts.

The first is [S03](./S03.md)'s: the checker reads a field the forge
WITHHELD as a field with nothing in it, so a workflow token turns *forge
not read* into a claim that the trunk is fully enforced. The sentence
that forbids it is already in the checker, three times, in its own voice
— *"Not read" is an honest line; a wrong reading is not* — and is
binding nowhere, which is exactly why `forgeGaps` could break it with no
record, rule or test noticing. A new row carries the record that makes
it bind and the checker change that keeps it, placed BEFORE the release
row: the profile line is new in 1.1 and no adopter has it yet, so the
fix has to land before 1.1.0 is cut, not today.

The second is [S02](./S02.md)'s: ADR-005 and ADR-014 change *the kit's
CI adapter*, which the kit generates from `workflow()` in
`tools/cairn.mjs`, and row 4 owns that file while naming neither record.
Row 4 now names them, with the manifest entry and the script beside them.

Definition-of-done item advanced: the seventh, as the amendment of
2026-09-13 widened it.

Type `documentation`: the register and this path's own record, and this
step. No test: nothing under `tools/` changes and no rule is added — the
register is a document the owner reads.

Deliberately not in this unit: the record itself and the checker change,
which are the new row's and belong to a path that does not exist yet;
the 1.1 page's record count, which is measured when that record is
written, not reserved now.

#### Work

```cairn-unit
step: S04
unit: 04
type: documentation
verified: cairn-check, test
```

- **`project/coding-paths/CP-CAIRN-007/index.md`**: item 7 of the
  definition of done widened to permit these register changes, and a
  superseding acceptance carrying the owner's two answers — the steer
  that chose S03's deletion, and *its own row, before the release* —
  with the digest recomputed over the amended section.
- **`project/coding-paths/index.md`**: a new row 5, *the readings that
  must not lie*, naming the record, `forgeGaps`, `readForge`, the
  rewritten cases, and the return of the token to the workflow step;
  `depends_on` stated in prose because it shares this path's write
  surface. The release becomes row 6. Row 4 gains ADR-005 and ADR-014 in
  its records, and the kit's adapter, manifest, script and the
  bootloader's alias sentence in its outcome. The 1.1 milestone row says
  six coding paths where it said five.

#### Self-review

- `shrink:` the new row's outcome first carried the whole story of the
  incident — the run id, the ruleset id, the elided field. The register
  is a roadmap, and the incident is written in S03 and in the journal
  entry the row points at; the row keeps what a reader needs to own the
  work and one clause saying why it sits before the release.
- `delete:` a `depends_on` column added to the register for this row —
  the register has four columns and the one edge Cairn keeps between
  paths is declared in a path record, not here; the row says it in
  prose, where a reader who has no path to declare it in can read it.
- `yagni:` reserving the record's number in `docs/adr/index.md` — the
  number is measured when the record is written.
- No counter moved: the 1.1 milestone's record count is left as it is,
  because the record it would count does not exist.
- No widening of `writes:`; the amendment widened the definition of done
  instead, which is the surface that forbade this.
- Net: 4 lines of register, 32 of record.

#### Review

First read: **not run as a fresh context**, and this is the second unit
of this path to say so. What it changes is a roadmap row, an acceptance
block carrying words the owner wrote in the chat, and one sentence of a
definition of done. There is no diff here for the two criteria to bite
on — no code, no predicate, nothing a reader could find wrong that
reading the record aloud does not find. The reading that mattered
happened before the unit: the checker's own three comments were read to
confirm the sentence exists and binds nowhere, and the register's rows 4
and 5 were read to confirm no row owned either debt.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory (exit 0)
cairn-active --check  : pass — view already current (exit 0)
cairn-test            : pass — 312 tests, 0 failed (exit 0)
scope digest          : sha256:c6305ca2ad5917c4612c3135d224d749b5a0a8d28eb36b1cf5a2817b6b2414b4, recomputed over the amended definition of done and carried by the superseding acceptance; the checker reads the last block as the one in force
register              : six rows, the release last; row 5 has no path and says which path it waits on
remote                : pushed immediately after this record; the request's run judges it
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : project/coding-paths/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S05 — the two tools that speak to the owner (the plan's third unit): cairn-audit printing the request's description in the template's order, cairn-active reporting the roadmap register beside the live view; tests first for both
blockers    : none. Item 2 of the definition of done stays knowingly unmet, and row 5 of the register now owns the work that lets it be met.
```
