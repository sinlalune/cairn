---
type: Cairn Coding Path Step
title: 'CP-CAIRN-010 S01 — the records: ADR-017 decision 4, ADR-028, ADR-029'
timestamp: 2026-09-15T00:00:00Z
cairn:
  path: CP-CAIRN-010
  step: S01
---

# CP-CAIRN-010 S01

### S01 — the records: ADR-017 decision 4, ADR-028, ADR-029 — **COMPLETE**

#### Plan

Write the three rulings down as records before any skill, template,
folder or line of the checker moves, so that S02 and S03 implement text
the owner can read rather than a chat.

ADR-017 gains a fourth decision in place, as its third was added on
2026-09-08: when no fresh context can be obtained the writer reads the
diff against the same two criteria and the movement is complete with
that read; the review section's first line names the reader. ADR-028 is
new: a file under `feedbacks/` is the channel an agent writes when the
gate stayed green and the protocol still cost more than it should, never
as a ritual, never for a harness defect, with no tool, skill or rule.
ADR-029 is new: the checker asks the host nothing, superseding ADR-001
decision 6 and ADR-026 decision 1, with the reading's cost measured at
this path's base. The 1.1 page is amended in place and marked *since
2026-09-15*, the two superseded decisions are marked in their records'
status lines and in the index, the two indexes follow, and the register's
1.1 row and row 6 name the records.

Definition-of-done items advanced: the first (ADR-017 decision 4), the
third (ADR-028), the fifth's record half (ADR-029), the sixth (the page)
and the seventh's register clause.

Type `decision`: the records are the change, and every document they
amend moves with them. No test: nothing under `tools/` changes here, and
no rule is added or removed.

Deliberately not in this unit: the unit skill's sentence and the
template's line, the feedbacks index and the first agent file (S02);
every line of the checker, its tests, the two workflows, the conformance
page, the soundness note, the catalogue and the module note (S03). This
unit's own review section is the first written under decision 4, so its
first line names the reader.

Found while writing, and settled in the record rather than left for S03:
`githubSlug` is not the checker's alone — `tools/cairn-postmortem.mjs`
imports it, with `githubRequest`, for its red-run reading. Deleting it as
the definition of done names would break the post-mortem; ADR-029 moves
both to the tool that calls them, with their tests, and names the
widening of `writes:` that S03 will declare for the post-mortem and its
test. Found the same way: request #18 carries no comment of the owner's
— the words the register attributes to it were said in the chat over
that request — so ADR-028 and the register say where the words are
persisted, this path's record at its registration blob.

#### Work

```cairn-unit
step: S01
unit: 01
type: decision
verified: cairn-check, cairn-test
```

- **`docs/adr/ADR-017-the-review-movement.md`**, decision 4 added in
  place, dated 2026-09-15 in the status line and the description: the
  fallback, the reader line for both reads, the rule reading presence and
  nothing more, the count from paths 5 and 4 that made it necessary;
  five alternatives, one consequence, one row of the implementation
  table and the register clause. Promoted from this path's record, path
  4's journal entry and the brainstorm note of 2026-09-15 at their blobs.
- **`docs/adr/ADR-028-a-feedback-file-when-nothing-broke.md`**, new and
  `accepted`, dated 2026-09-15: two decisions — the file under
  `feedbacks/`, what it names, when it is written and what it is not —
  six alternatives refused, the owner's refusals of a skill and a tool
  quoted as refusals. Extends ADR-014 decision 1.
- **`docs/adr/ADR-029-the-checker-asks-the-host-nothing.md`**, new and
  `accepted`, dated 2026-09-14, the day of the ruling: the cost of the
  reading measured in a table — the block of 133 lines at the checker's
  head, 68 of them code, 8 lines of `main`, 13 tests and 48 assertions,
  13 lines of workflow, one line of output on one host — one decision,
  six alternatives, and the two post-mortem functions that move rather
  than die.
- **`docs/adr/ADR-001-sole-owner-opens-and-closes-a-path.md`** and
  **`docs/adr/ADR-026-the-readings-that-must-not-lie.md`**: the status
  line names the superseded decision and ADR-029, and one line under the
  decision's heading says the same; nothing else in either moves. The
  `schema` rule reads the first token after `Status:`, so both stay
  `accepted`.
- **`docs/adr/index.md`**: ADR-028 and ADR-029 listed after ADR-027 with
  no gap; ADR-017's line gains its second amendment and decision 4;
  ADR-001's and ADR-026's lines name the decision each lost.
- **`docs/architecture/01-cairn-1-1.md`**, amended in place: the
  amendment paragraph with the three blobs; the diagram's host node and
  the sentences on GitHub's runs, where *the forge* meant GitHub; the
  bypass sentence in *How a path opens*; the review's fallback and the
  feedback file in *How a path runs*; two rows of the checker table; a
  `feedbacks/` row in the documentation plane's table; the `cairn-check`
  row and a feedback-file row in *Which tools exist*. Every sentence
  that changes what an earlier one said is marked *since 2026-09-15*;
  the word replacements carry no mark, and the paragraph says so.
- **`docs/architecture/index.md`**: the page's line records the
  amendment.
- **`project/coding-paths/index.md`**: the 1.1 milestone row counts
  twenty-nine records; row 6 names ADR-017 decision 4, ADR-028 and
  ADR-029 in place of *a new record*.

#### Self-review

- `correctness:` ADR-028 said `feedbacks/` held *seven notes*; the index
  lists eight. Counted, corrected.
- `correctness:` ADR-028 named *fourteen* hung contexts on path 4, the
  brainstorm note's count at S06; the journal's final count is sixteen of
  twenty. Corrected to the journal's.
- `correctness:` ADR-029 said the workflow's token came back with a
  *fourteen-line* comment and counted *15 lines* for token and comment;
  measured, the comment is 12 lines and the two together 13. Corrected.
- `correctness:` ADR-029's first draft left `githubRequest` in the
  checker's file as *the one host-specific function left* and did not
  know the post-mortem also imports `githubSlug`; read the post-mortem's
  import list, and the decision now moves both. The table's test counts
  follow: 11 tests and 31 assertions deleted, not 12 and 42.
- `yagni:` ADR-028's implementation table named the first agent file by
  a filename S02 has not chosen; the record now says what the file is
  and leaves the name to the unit that writes it.
- `shrink:` ADR-017 decision 4, *and it says so where it is read* — a
  flourish on a sentence that had already said it; cut to *and it says
  so*.
- `delete:` `package-lock.json`, written by `npm install` in this fresh
  worktree and untracked; removed, not committed.
- Considered and kept: the 1.1 page states the checker's silence twice,
  in the transition table and in the tools table, as it stated the
  reading twice before; leaving one row at the old claim is the worse
  repetition, as path 5's S01 found for the same two rows.
- No widening of `writes:`: every file of this unit is under `docs/adr/**`,
  the two architecture surfaces, `project/coding-paths/index.md` or this
  path's folder. The widening ADR-029 names — the post-mortem and its
  test — is S03's to declare when it moves the two functions.
- `shrink:` after the two reads, the three records lost the duplicate
  of decision 4's no-rule sentence, a consequence restating the decision,
  two *What this changes* lines saying nothing twice, the manifesto
  sections' restatements of their own tables, and one blob citation
  cited again by ADR-028; the 1.1 page lost a history the record tells.
- `correctness:` ADR-029's first draft counted `githubSlug` as deleted
  in one sentence and moved in another, and its manifesto line summed the
  block without `main`; the reads caught both, and the counts now name
  what is deleted, moved and rewritten.
- Net: 140 lines added and 29 removed across the seven amended
  documents, plus the two records, 336 lines, and this step.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's
diff in one file and the two criteria and nothing else; it returned in
141 seconds with twenty-seven findings, on the first attempt.
Each with its disposition:

- `correctness:` ADR-029 said `githubSlug` was deleted in its description
  and its first sentence and moved in its second paragraph — **fixed**,
  the description and the sentence say it moves.
- `correctness:` the 133 lines, 68 code lines, 48 assertions, 63 lines
  and 31 assertions did not reconcile without saying what is deleted,
  moved and rewritten — **fixed**, the table's rows say which.
- `correctness:` ADR-029 dated 2026-09-14 while the page marks *since
  2026-09-15* — **refused**: a record is dated the day of the ruling, as
  the ADR index's shape says; the page marks the day of its amendment,
  as every earlier amendment did.
- `correctness:` the page said the checker *printed that bypass on every
  run until 2026-09-15*, which it never did — **fixed**, the page states
  the present rule and the pointer; the second read cut the history that
  first replaced it.
- `correctness:` the index counted six alternatives for decision 4 where
  the record lists five — **fixed**, five.
- `correctness:` *added in place as decision 3 was, by the path that
  found the gap* — this path did not find it — **fixed**, *by the path
  the owner opened for it*.
- `correctness:` *`cairn-unit` movement 7* against a unit of five
  movements — **fixed**, the skill's section *Report the boundary*, in the
  record, the table and this path's resume section.
- `correctness:` *nothing is added* while a test is added — **fixed**,
  *nothing is added but the test that proves the absence*.
- `correctness:` *nothing in them is rewritten* omitted the line under
  each superseded decision's heading — **fixed**, the consequence names
  the three marks.
- `correctness:` row 6's writes column lacks the post-mortem and its test
  that ADR-029 names — **deferred** to S03: the register's column
  follows the record's `writes:`, which the unit that moves the two
  functions widens and the register follows in the same unit.
- `correctness:` the `feedbacks/` row dated the whole folder to
  2026-09-15 — **fixed**, only the agents' files are new.
- `delete:` decision 4's *no rule is added, and none could be* paragraph
  repeated the first alternative — **fixed**, cut.
- `shrink:` *the two reads are not substitutes … it is less read, and it
  says so* — **refused**: it is the reason the line exists and why a
  writer's read is a completed movement and not a debt; nothing else in
  the decision says it.
- `delete:` the consequence *the section's first line lets the owner
  count* — **fixed**, cut to the sentence on path 4's step records.
- `shrink:` ADR-017's description restating decision 4 — **fixed**.
- `shrink:` the index's ADR-017 line restating the decision, the count
  and the refusals — **fixed**, one clause.
- `delete:` ADR-028 decision 2's *What this changes* and a consequence
  saying the same — **fixed**, the consequence cut, the line kept at one
  word.
- `shrink:` *written when there is something to say and never as a
  ritual* in six places — **fixed in part**: cut from decision 1's title;
  kept in the description, the decision, the index and the page's unit
  paragraph, where it is the ruling itself.
- `shrink:` the `feedbacks/index.md` change stated three times in ADR-028
  — **fixed**, the consequence cut.
- `shrink:` ADR-028's manifesto section restating the context — **fixed**,
  the last sentence cut.
- `shrink:` ADR-028 decision 2's second paragraph, refused already by the
  alternatives — **refused**: the alternatives say why each was refused,
  and the decision must state what is decided; it is the owner's ruling
  in one sentence.
- `yagni:` the 1.1 page's tools-table row *a feedback file, not a tool* —
  **refused**: the definition of done names the tools table as a surface
  the file appears on, beside the post-mortem, and the row says where the
  channel is for a reader who looks for it among the tools.
- `shrink:` the page's unit paragraph carrying the rule clause the table
  row carries — **fixed**, cut from the paragraph.
- `delete:` ADR-029's consequence on the word *the forge* — **refused**:
  the record is where the reason lives — the mechanism gone — and the
  path's exclusion of a `forge` concept note rests on it; shrunk to one
  sentence.
- `shrink:` ADR-029's first consequence restating the decision — **fixed**,
  one sentence.
- `shrink:` ADR-029's manifesto section repeating the table and the first
  alternative — **fixed**, two sentences.
- `shrink:` decision 4's three blob citations — **fixed**, the path record
  and the journal entry alone; ADR-028 cites the brainstorm note.

Second read: a fresh context, a subagent of this harness, given only the
diff of those fixes and the same two criteria; it returned in 117
seconds with ten findings. Each with its disposition:

- `shrink:` the page's bypass sentence telling ADR-029's history —
  **fixed**, the present rule and the pointer.
- `shrink:` ADR-028's aside *a section of the skill, not a movement of
  the unit* — **fixed**, cut.
- `shrink:` the index's ADR-029 line naming the test twice — **fixed**.
- `correctness:` the three ADR-029 passages disagreeing on what moves —
  **fixed**, the body names both functions; the table covers the block,
  where only `githubSlug` lives.
- `correctness:` sixty-three lines leaving out `main`'s eight — **fixed**,
  seventy-one, split.
- `correctness:` `githubSlug`'s test counted among the deleted — **fixed**
  by rewording: the 31 never included it, and the row now says its test
  moves with it.
- `correctness:` *no code is added; one test* — a test is code — **fixed**,
  *nothing is added but the test*.
- `correctness:` the page's *since 2026-09-15* against the record's
  2026-09-14 — **refused**, as in the first read.
- `correctness:` the page's *never could* against ADR-029's *no longer
  printed* — **fixed** on both sides: the page's history cut, the
  consequence now *not the checker's to print*.
- `correctness:` the index's *five options refused* for decision 4 needs
  checking — **checked**, the record lists five; no change.

The fixes of the second read are not read a third time; the self-review
above carries the two lines the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 11 changed files (exit 0), run bare with this record complete
cairn-test            : pass — exit 0, run bare in this fresh worktree before the review section existed; nothing under tools/ changed since
cairn-active --check  : pass — the view is already current; no path state changed
record shape          : `adrFrontmatterErrors` satisfied for ADR-028 and ADR-029 — id matches the file name, status accepted in both halves, ISO date; ADR-001 and ADR-026 keep `accepted` as the first token after `Status:`
measurements          : the checker's block, its tests, the workflow's comment and the feedback notes counted by command at this path's base, not from memory; the post-mortem's import list read for `githubSlug`
review                : two reads by fresh contexts, both obtained on the first attempt, 141 and 117 seconds
user review           : not required for the unit; the owner reads the records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-017, ADR-028 (new), ADR-029 (new), ADR-001 and ADR-026 (status lines and marks), docs/adr/index.md, docs/architecture/01-cairn-1-1.md, docs/architecture/index.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S02 — the unit skill's two sentences, the template's reader line, the feedbacks index and the first agent file
blockers    : none
```
