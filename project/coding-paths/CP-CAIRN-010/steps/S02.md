---
type: Cairn Coding Path Step
title: 'CP-CAIRN-010 S02 — the skill, the template, the first feedback file'
timestamp: 2026-09-15T00:00:00Z
cairn:
  path: CP-CAIRN-010
  step: S02
---

# CP-CAIRN-010 S02

### S02 — the skill, the template, the first feedback file — **COMPLETE**

#### Plan

Make the two documents that a writer reads say what ADR-017 decision 4
and ADR-028 decided, and write the first file the second record asks
for.

`cairn-unit` movement 4 gains one sentence: when no fresh context can be
obtained the writer reads the diff against the same two criteria and
opens the section with one line naming the reader; its fix paragraph
puts the second read under its own reader line. The skill's last section,
*Report the boundary*, gains one sentence pointing at `feedbacks/`. The
path template's review section shows the reader line for both reads.
`feedbacks/index.md` says the folder holds the owner's pages, the adopter
notes and the agents' files, and lists the first agent file. That file
carries what path 4's writer observed and could not place — the four
observations ADR-028's context names — each with where it was met, its
cost measured from path 4's step records, and the change to Cairn that
would remove it, with the harness's failures named as not belonging
here.

Definition-of-done items advanced: the second (the skill's sentence and
the template's line), the fourth (the first feedback file), and the
index and skill clauses of the third.

Type `documentation`: documents and their index, no test — nothing
under `tools/` changes, and the `review` rule reads presence, which the
new first line does not alter.

Deliberately not in this unit: every line of the checker, its tests, the
two workflows and the pages that describe the reading (S03); any change
to `cairn-open`, `cairn-close` or the register beyond what the
observations ask for — the file names the changes, this path makes none
of them, and its acceptance excludes the other skills. The file is
written by the agent that ran path 4 in this same harness, from that
path's step records rather than from memory, and it names its own
author.

#### Work

```cairn-unit
step: S02
unit: 02
type: documentation
verified: cairn-check, cairn-test
```

- **`skills/cairn-unit/SKILL.md`**: movement 4, one sentence naming the
  fallback and the reader line, citing ADR-017 decision 4; the fix
  paragraph, one clause putting the second read under its own reader
  line; section 7, one sentence on the feedback file, citing ADR-028.
- **`spec/reference/path-template.md`**: the step shape's `#### Review`
  section opens each read with the reader — a fresh context and which
  kind, or the writer with the reason and the wait.
- **`feedbacks/index.md`**: the description and the opening paragraph
  name the three kinds of note the folder holds and what an agent file
  is; one entry for the first agent file; the timestamp follows.
- **`feedbacks/2026-09-15-cp-cairn-009-writer-feedback.md`**, new, in
  the folder's shape — a learning note, provisional: the author, the
  sources, what is deliberately not in it; four observations, each with
  *Where*, *What it cost* and *The change to Cairn*, linked to the step
  records that measured them; one closing paragraph on what the four
  share.

#### Self-review

- `shrink:` the feedback file's second observation designed the
  register's derived State column down to *between markers*; the file
  asks for the change and does not design it — cut to one derived column,
  named as a decision of its own.
- `yagni:` a fifth observation on the hung review contexts, considered
  while planning the file and not written: it is the harness's, and the
  file's second paragraph says so instead.
- `delete:` the index's description said *one note per incident*; an
  agent file is not an incident, so the description says *or per set of
  observations* and the paragraph says what the file is.
- Considered and kept: the fallback and the reader line are stated in the
  skill and shown in the template, two surfaces for one decision; the
  record names both, and a writer reads the skill while a writer copies
  the template. Kept too: the second read's reader line is a clause in
  the fix paragraph, not a sentence of its own.
- No widening of `writes:`: `skills/cairn-unit/SKILL.md`,
  `spec/reference/path-template.md`, `feedbacks/**` and this path's
  folder are all declared.
- `shrink:` after the two reads the note lost its closing section, three
  closing sentences that summarised their own paragraphs, a repeated
  description and an argument against an option nobody proposed; the
  index lost two clauses the skill already carries.
- Net: 25 lines added and 11 removed across the three amended documents,
  plus the feedback file, 128 lines, and this step.

#### Review

First read — reader: a fresh context, a subagent of this harness, given
the unit's diff in one file and the two criteria, returned in 110 seconds
on the first attempt — given this unit's diff and the two criteria. Twenty
findings, each with its disposition:

- `delete:` the index repeating *written when there is something to say
  and never as a ritual* from the skill — **fixed**, cut.
- `delete:` the index repeating the harness-defect sentence — **fixed**,
  cut.
- `shrink:` the index's description restated by its paragraph — **fixed**,
  the description is one line.
- `shrink:` the index entry's tail repeating the note's description —
  **fixed**.
- `shrink:` the skill's three examples of an unobtainable context —
  **refused**: they are the decision's own three cases and tell a writer
  what counts, a hang among them, which *no fresh context* alone does not.
- `shrink:` the note's description repeated in its first paragraph —
  **fixed**, the paragraph keeps the author, the sources and the gates.
- `delete:` *evidence for a change, never authority* in the note —
  **fixed**, the index's sentence.
- `delete:` the harness clause in the note's description — **fixed**.
- `delete:` §1's closing sentence — **fixed**.
- `delete:` §2's *four units touched a number* — **fixed**.
- `delete:` §3's closing sentence — **fixed**.
- `yagni:` §3's audit-tool clause — **fixed**, cut.
- `shrink:` §4's *a reader believes* restating *reads as implemented* —
  **fixed**, with the correctness finding below.
- `delete:` *What these four share* — **fixed** in two steps: shrunk to
  one sentence after this read, deleted after the second.
- `correctness:` *four units* undercounting five — **fixed** by the
  deletion above.
- `correctness:` *three at a time* against *each review found one* —
  **fixed**, each unit corrected what it found and each review found one
  more.
- `correctness:` one change located in two skills — **fixed**, the close
  skill alone.
- `correctness:` §4 saying the decision allows the missing note and that
  a reader is misled — **fixed**: nothing in the record or on the
  conformance page says the note is pending, which is the observation.
- `correctness:` *step records* linked to S01 alone — **fixed**, the path
  record.
- `correctness:` the index body keeping *one note per incident* against
  its description — **fixed**, three kinds of note.

Second read — reader: a fresh context, a subagent of this harness, given
only the diff of those fixes and the same two criteria, returned in 75
seconds on the first attempt — given only the diff of those fixes. Seven findings:

- `shrink:` the close-skill clause stating the one-place rule a third
  time — **fixed**, cut.
- `delete:` the one-sentence *What these four share* — **fixed**, deleted.
- `shrink:` the index's gloss of what an agent file names — **refused**:
  the index is where a reader of the folder learns what the new kind of
  note is, in one line, without opening one.
- `shrink:` the description's *provisional* clause repeating the body —
  **fixed**, cut.
- `correctness:` *step records* labelling a link to the path record —
  **fixed**, *path record*.
- `correctness:` S04 correcting copies while the list stopped at S03 —
  **fixed**, S01 to S04.
- `correctness:` one joined line left unwrapped — **fixed**, rewrapped.

The fixes of the second read are not read a third time; the self-review
above carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci (exit 0), run bare with this record complete
cairn-test            : pass — 344 tests, 0 failed (exit 0), run bare on this state; nothing under tools/ changed in this unit
cairn-active --check  : pass — the view is already current; no path state changed
sources               : the four observations read from path 4's S01, S02, S03, S04 and S06 and its journal entry, linked from the note; no count in the note is from memory
review                : two reads by fresh contexts, both obtained on the first attempt; the section opens with the reader line as ADR-017 decision 4 and this unit's own skill sentence ask
user review           : not required for the unit; the owner reads the first feedback file before the merge, as the definition of done asks
remote                : pushed immediately after this record; the commit id is named in the resume section by S03
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : skills/cairn-unit/SKILL.md, spec/reference/path-template.md, feedbacks/index.md, feedbacks/2026-09-15-cp-cairn-009-writer-feedback.md (new), this path's index and this step
session     : safe boundary after a successful push
next action : S03 — the deletion: writes widened by the post-mortem, its test and the workflow test; forgeGaps, readForge and the host half of the profile line gone with their tests; githubSlug and githubRequest moved; the no-network test; the token off the checker's step; the pages that describe the reading
blockers    : none
```
