---
type: Cairn Coding Path Step
title: 'CP-CAIRN-016 S06 — what the coherence read of eab7ddd found'
timestamp: 2026-09-26T00:00:00Z
cairn:
  path: CP-CAIRN-016
  step: S06
---

# CP-CAIRN-016 S06

### S06 — what the coherence read of eab7ddd found — **COMPLETE**

#### Plan

The coherence read of the first candidate, `eab7ddd`, a fresh context
given the close skill's inputs, answered *drift noted*: nothing
contradicts a record, no sibling runs; two of its findings are this
path's to act on before the owner reads. The reader line, written three
times — `READER` in the audit tool, the kit's request template, this
repository's — is tied by no test; and the milestone rule the owner
ruled in the chat has no record. This unit ties the first by one test
and places the second on the backlog for a promotion. Not in this unit:
row 4's Writes cell, which the definition of done keeps as the owner
wrote it, and the kit's bootloader line, already on the backlog.

Definition-of-done item advanced: the eighth — the coherence answers
the candidate carries into the request; `eab7ddd` is void, and this
unit's commit is the new candidate.

Type `implementation`: a test that pins what three files must say, and
no behaviour changes — no test is written red first, since the three
agree today; the assertion fails the moment one of them moves.

#### Work

```cairn-unit
step: S06
unit: 06
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn-audit.test.mjs`**: the reader line the tool prints is
  this repository's request template's — the installer's test already
  ties the kit's to it; the comment that said the kit's template lacks
  the shape removed.
- **`tools/cairn-active.mjs`**: the docblock states the milestone rule
  exactly, the two edge cases included.
- **`project/backlog/2026-09-26-the-milestone-state-rule-has-no-record.md`**:
  the owner's ruling of 2026-09-25 for a milestone of several paths,
  which ADR-031 decision 2 does not state, for a promotion.

#### Self-review

- `shrink:` one assertion on the existing constant, no new export and
  no shared module for one sentence.
- Widening of `writes:`: none — both files are declared.
- Net: 7 lines added and 3 removed in the test, the docblock's four
  lines rewritten, beside the backlog item, this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria; it ran the test and checked each claim
of the backlog item against the repository, and returned in under a
minute, on the first attempt, with three findings:

- `shrink:` the kit half of the new test repeats the installer's test,
  which ties the kit's reader line to this repository's — **fixed**, the
  test asserts the tool's against this repository's alone, and the
  import goes.
- `correctness:` the backlog item's *done … otherwise* is loose: a
  milestone whose paths are all archived reads *running*, and one
  counting a path with no record keeps its cell — **fixed** in the item
  and in `fillRegister`'s docblock, which had the same gap.
- `correctness:` a comment in the audit test said the kit's template
  carries none of the shape — **fixed**, removed.

Second read, of the fixes alone: a fresh context, a subagent of this
harness, given only those lines and the two criteria; it ran the test
and returned in under a minute, on the first attempt, with four
findings:

- `correctness:` the new comment said the installer's test holds the
  kit's template to this shape, which it holds only for the reader line
  — **fixed**, the clause removed.
- `correctness:` the docblock called a milestone with no path at all
  running, and dated every done milestone — **fixed**: such a milestone
  keeps its author's cell, and the date needs every done path dated.
- `shrink:` a docblock line past the wrap — **fixed**.
- `shrink:` the backlog item said twice that the tool implements the
  rule — **fixed**, once.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 364 tests, 0 failed, exit 0; the new test among them
cairn-active --check  : pass — the view and the register's cells current (exit 0)
remote                : pushed immediately after this record; its commit is the candidate C
```

#### Checkpoint

```text
status      : running
current step: S06 complete on push; this commit is C
retention   : none (no-rewrite host)
changed     : tools/cairn-audit.test.mjs, tools/cairn-active.mjs (the docblock), project/backlog/2026-09-26-the-milestone-state-rule-has-no-record.md, this path's index and this step
session     : safe boundary after a successful push
next action : the close skill on C — the coherence read again, the administrative commit, the request
blockers    : none
```
