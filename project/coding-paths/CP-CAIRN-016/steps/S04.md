---
type: Cairn Coding Path Step
title: 'CP-CAIRN-016 S04 — this repository''s bootloader names the pointer page'
timestamp: 2026-09-26T00:00:00Z
cairn:
  path: CP-CAIRN-016
  step: S04
---

# CP-CAIRN-016 S04

### S04 — this repository's bootloader names the pointer page — **COMPLETE**

#### Plan

`AGENTS.md` gains the sixth entry the kit's generated bootloader has,
`cairn/README.md` (ADR-034 decision 5), in the kit's own words; the
tools' module note is read against the three tools as they stand after
S01 to S03. Not in this unit: any other line of the bootloader, the
candidate.

Definition-of-done item advanced: the fifth, in full.

Type `documentation`: one line, no source; no test, since nothing
behaves differently.

#### Work

```cairn-unit
step: S04
unit: 04
type: documentation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`AGENTS.md`**: item 6 of *Start here*, the line the kit's
  `AGENTS.md` generates, corrected on the review's finding: the pointer
  page pins the chapters at the release's commit, not the skills.
- **`project/backlog/2026-09-26-the-bootloader-says-the-skills-are-pinned.md`**:
  the kit's own line, which this path may not change.
- **`docs/modules/application.md`**: read, not changed. Its rows for
  `cairn-active`, `cairn-audit` and the configuration module, the
  post-mortem section and the workflow paragraph were written by S01 to
  S03 in the present tense; a search for history wording (*row*,
  *until*, *no longer*, *used to*, a path id) finds none about the three
  tools.

#### Self-review

- `shrink:` the kit's own line copied rather than a new sentence, then
  corrected where it was false, the kit's copy deferred.
- Widening of `writes:`: none — `project/backlog/**` is declared.
- Net: 2 lines added to `AGENTS.md`, beside the backlog item, this step
  and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria as they apply to prose; it read the
pointer page and the installer and returned in about half a minute, on
the first attempt, with one finding:

- `correctness:` *the six chapters and the skills linked at its commit*
  — the pointer page links the skills as local files, pinned to nothing
  — **fixed** in `AGENTS.md`, and for the kit's template, which writes
  the same sentence into every adopter's bootloader, **deferred** to
  `project/backlog/2026-09-26-the-bootloader-says-the-skills-are-pinned.md`:
  this path changes `tools/cairn.mjs` only in `workflow()`, the register
  template and the two GitHub helpers.

Second read, of the fix alone: a fresh context, a subagent of this
harness, given only the corrected line and the backlog item and the two
criteria; it read the pointer page and the installer at `HEAD` and at
`1.1.0` and returned in about a minute, on the first attempt, with three
findings:

- `correctness:` adopters on 1.1.0 carry an earlier wording, so *every
  adopter carries the sentence* was loose — **fixed**, *a version of
  the sentence*, with 1.1.0's quoted.
- `shrink:` *the skills* repeats item 5 — **refused**: the line stays
  the kit's, corrected, so the fix the backlog item asks for makes the
  two equal again.
- `shrink:` the backlog item states the corrected wording and points
  back to it — **refused**: it is stated once, and the pointer is the
  shape of the work.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 363 tests, 0 failed, exit 0; nothing behaves differently
cairn-active --check  : pass — the view and the register's cells current (exit 0)
remote                : pushed immediately after this record; the commit id is named in the resume section by S05
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : AGENTS.md, this path's index and this step
session     : safe boundary after a successful push
next action : S05 — the candidate
blockers    : none
```
