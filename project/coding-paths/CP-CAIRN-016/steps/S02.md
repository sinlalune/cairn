---
type: Cairn Coding Path Step
title: 'CP-CAIRN-016 S02 — the post-mortem and the two workflows'
timestamp: 2026-09-25T00:00:00Z
cairn:
  path: CP-CAIRN-016
  step: S02
---

# CP-CAIRN-016 S02

### S02 — the post-mortem and the two workflows — **COMPLETE**

#### Plan

The post-mortem counts the red run it runs in (ADR-034 decision 9),
prints a closed, unmerged request as closed (decision 10) and on the
trunk reads the path that arrived, or the checker's finding and no path
(decision 11); both workflows pass the base and say the run is red; the
two GitHub functions live once; the actions leave Node 20. Not in this
unit: the audit tool, `AGENTS.md`.

Definition-of-done items advanced: the second and the third, in full;
the sixth's clause on `tools/cairn.mjs`.

Type `implementation`: the failing tests first — six red before the
change (the forge readings, the trunk reading, the three workflow tests).

A decision this unit could not make, put to the owner in the chat of
2026-09-25: the accepted scope had the post-mortem import the two
functions from `tools/cairn.mjs`, which the kit never ships, so the
post-mortem would fail on its first line at every adopter. The owner
chose `tools/cairn-config.mjs`, which the kit ships, the installer
already imports and which loads no configuration on import; the scope
amendment of 2026-09-25 in the record carries it, with `writes:` widened
by that file.

#### Work

```cairn-unit
step: S02
unit: 02
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn-config.mjs`**: `githubSlug` and `githubRequest`, the
  post-mortem's fuller pattern and the installer's `AbortSignal.timeout`.
- **`tools/cairn.mjs`**: its two copies gone, imported instead;
  `workflow()` sets the base on the job, passes it to the post-mortem,
  sets `CAIRN_RUN_RED` on the failure step, and runs `checkout` and
  `setup-node` at `v5`.
- **`tools/cairn-postmortem.mjs`**: its two copies gone, imported;
  `readRedRuns` adds the current run when the workflow says it is red and
  the reading is of the branch the run is for; `readRequests` keeps
  `state` and `closed_at`, `requestsReading` prints *closed … never
  merged*; `--base`, and on the trunk the records changed from the base
  to HEAD, else the checker's failure against that base first and one
  line saying no path was read, or that the base names no commit.
- **`.github/workflows/cairn.yml`**: the same four changes as the kit's.
- **Tests**: the forge readings, the trunk reading in an installed
  repository — so the post-mortem's imports are an adopter's — with a
  red checker, the workflow's job base and actions, and the installer's
  shared-lines test carrying the new lines for both workflows.
- **`docs/modules/application.md`**: the configuration module's row, the
  post-mortem's readings and trunk reading, the workflow paragraph.
- **`project/backlog/2026-09-25-two-copies-of-the-github-reading.md`**:
  deleted, landed; the link to it from the other checker item and from
  this record's goal written as a path in code.
- **This path's `index.md`**: the amendment, its acceptance block, the
  rejected direction under *Tried and rejected*.

#### Self-review

- `delete:` the installer's copy of the two functions and the
  post-mortem's, one kept.
- `shrink:` the base set once on the job instead of a second copy of
  its expression on the post-mortem step.
- `shrink:` the kit's workflow lines asserted in the installer's
  existing shared-lines test rather than a second comparison.
- `delete:` the fixture's copy of the post-mortem, which the kit installs.
- Widening of `writes:`: `tools/cairn-config.mjs`, by the amendment.
- Net: 198 lines added and 122 removed across nine files, 60 added in the
  tests, beside this step.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff of `tools/` and `.github/` and the two criteria; it ran the
three suites and returned in about a minute and a half, on the first
attempt, with seven findings:

- `correctness:` the trunk test's order assertion was vacuous: the
  fixture's checker passed, so no finding was printed — **fixed**, the
  fixture leaves a record outside the status vocabulary and the test
  asserts the finding's heading, its text and its place.
- `correctness:` a run red only on inconclusive findings prints no
  `FAIL` heading, and the reading kept the verdict line alone —
  **fixed**, it starts at `FAIL` or `INCONCLUSIVE`.
- `yagni:` running the checker again to print what its own step printed
  above — **refused**: ADR-034 decision 11 has the post-mortem print the
  checker's finding first where no record changed.
- `stdlib:` keep the installer's `AbortSignal.timeout` over the
  controller and timer — **fixed**, the test's fake rejects with the
  signal's reason.
- `shrink:` `state` is redundant beside `closed_at` — **refused**:
  decision 10 and this path's second item keep both.
- `shrink:` the job's base asserted twice in the workflow test —
  **fixed**, one assertion, the `@v4` one joined to the existing test.
- `correctness:` a first push to a trunk, whose base is forty zeros,
  read as *no record changed* — **fixed**, it says the base names no
  commit; the unreachable `origin/main` arm for a path-branch push
  predates this unit — **refused**, the comment is the one path 3 of
  1.1 wrote and the kit keeps the same expression.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it ran two
suites and returned in about a minute, on the first attempt, with four
findings:

- `correctness:` the `@v4` assertion was negative, so `@v3` or a pinned
  v4 passed it — **fixed**, every action's major version is read and
  must be at least 5.
- `correctness:` a checker that crashes prints no heading, and the
  reading kept the stack's last frame — **fixed**, all it said is
  printed.
- `shrink:` the changed files split once per record — **fixed**, one
  set.
- `yagni:` the checker run again — **refused**, as in the first read.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 360 tests, 0 failed, exit 0; the new assertions among them
cairn-active --check  : pass — the view and the register's cells current (exit 0)
the red first         : six tests failed before the change — the forge readings, the trunk reading, three workflow tests
remote                : pushed immediately after this record; the commit id is named in the resume section by S03
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn-config.mjs, tools/cairn.mjs, tools/cairn.test.mjs, tools/cairn-postmortem.mjs, tools/cairn-postmortem.test.mjs, tools/cairn-workflow.test.mjs, .github/workflows/cairn.yml, docs/modules/application.md, project/backlog/ (one item deleted, one link), this path's index and this step
session     : safe boundary after a successful push
next action : S03 — cairn-audit
blockers    : none
```
