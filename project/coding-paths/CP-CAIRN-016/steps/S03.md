---
type: Cairn Coding Path Step
title: 'CP-CAIRN-016 S03 — cairn-audit reads a plain list and scaffolds the coherence read'
timestamp: 2026-09-25T00:00:00Z
cairn:
  path: CP-CAIRN-016
  step: S03
---

# CP-CAIRN-016 S03

### S03 — cairn-audit reads a plain list and scaffolds the coherence read — **COMPLETE**

#### Plan

`cairn-audit` reads each item of a definition of done from a plain list
as from a boxed one (ADR-042); its coherence section opens on the line
naming the reader and carries, under each of the first three questions,
what the tool can see (ADR-043); its advisories placeholder asks a
deferral for its file under the backlog (ADR-041); the docblock stops
saying the kit's template lacks the sections. Not in this unit:
`AGENTS.md`, the module note beyond the tool's own row.

Definition-of-done item advanced: the fourth, in full.

Type `implementation`: the failing tests first — the file failed on the
missing `coherenceFacts` export before the change.

#### Work

```cairn-unit
step: S03
unit: 03
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn-audit.mjs`**: `definitionItems` takes the box as
  optional; `coherenceFacts` names the decision records the diff
  touches, the running paths whose `writes:` meet this one's (the
  checker's `writesOverlaps`) and the architecture pages changed with no
  record beside them, and nothing for the fourth question; the request's
  *Coherence* opens with `READER`, the template's line, and carries each
  fact under its question; the closing record on `manual-git` opens its
  findings with the reader line on the checker's placeholder and lists
  the facts above the questions; both advisories placeholders name
  `project/backlog/`; `main` reads the path records once and the diff
  from the base; the stale docblock sentence corrected.
- **`tools/cairn-audit.test.mjs`**: the plain list read as the boxed
  one, nested included; the facts, each silence, the reader line first,
  the closing record's scaffold not reading as an answer; the deferral.
- **`docs/modules/application.md`**: the tool's row, and the kit
  paragraph's sentence that left this to row 4.

#### Self-review

- `delete:` `currentPath`, the finder folded into one reading of the
  records.
- `stdlib:` / reuse — the overlap is the checker's `writesOverlaps`,
  the writes its `parseWrites`, the roots `slash`; nothing re-derived.
- `yagni:` no fact for the fourth question: whether two places will
  drift is a judgement the tool cannot see.
- Widening of `writes:`: none.
- Net: 139 lines added and 27 removed across three files, 60 of the
  added in the test, beside this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff of `tools/` and the two criteria; it ran the test and the
command and returned in about a minute, on the first attempt, with four
findings:

- `correctness:` the facts sat inside each finding's body, so a
  hollowed-out closing record passed the checker's *no findings section
  answered* — **fixed**, the facts are listed above the questions, where
  `findingsSections` does not read.
- `correctness:` the test asserting a scaffold with facts is not a
  review passed on the placeholder alone — **fixed**, it hollows the
  scaffold, gives it a verdict and asserts that very error.
- `correctness:` the reader line is a placeholder no check reads on
  `manual-git` — **fixed** without a rule: the closing record's reader
  line carries the checker's placeholder, which `fillErrors` refuses.
- `shrink:` the records read twice in `main` — **fixed**, once.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it ran the
test and the command and returned in about a minute, on the first
attempt, with four findings:

- `correctness:` the reader-line assertion passed on the questions'
  placeholders alone — **fixed**, every question is answered in the
  test and the reader line is the one placeholder left.
- `correctness:` a reader line whose placeholder was deleted by hand
  names nobody and passes — **refused**: that is the hollowing any
  deleted placeholder is, and a rule reading a name is the checker's,
  which this path does not change; the comment no longer claims it.
- `shrink:` the facts list mapped over the questions — **fixed**, over
  the facts.
- `shrink:` a stray blank line before `main` — **fixed**.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 363 tests, 0 failed, exit 0; the three new tests among them
cairn-active --check  : pass — the view and the register's cells current (exit 0)
the red first         : the test file failed on the missing coherenceFacts before the change
remote                : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn-audit.mjs, tools/cairn-audit.test.mjs, docs/modules/application.md, this path's index and this step
session     : safe boundary after a successful push
next action : S04 — AGENTS.md and the module note
blockers    : none
```
