---
type: Cairn Coding Path Step
title: 'CP-CAIRN-016 S08 — the timeout test holds the loop a real request holds'
timestamp: 2026-09-26T00:00:00Z
cairn:
  path: CP-CAIRN-016
  step: S08
---

# CP-CAIRN-016 S08

### S08 — the timeout test holds the loop a real request holds — **COMPLETE**

#### Plan

The request's run on `4ea0101` (#33, run `36203882945`) went red in the
suite, before the checker: `tools/cairn-postmortem.test.mjs` was
cancelled whole — eight tests, *Promise resolution is still pending but
the event loop has already resolved*. S02 moved `githubRequest` to
`AbortSignal.timeout` on its first review's `stdlib:` finding, and that
timer keeps nothing alive; a real `fetch` holds its socket, but the
timeout test's fake held nothing, so on the runner the loop emptied
before the timer fired. The suite ran green here by timing. This unit
makes the fake hold the loop as a real request does, and returns the
path to `running`: `4ea0101` is void, and this unit's commit is the new
candidate. The run being the suite's, no post-mortem was printed; the
run's log was read.

Definition-of-done items advanced: the third, whose test this is; the
eighth, whose candidate the run refused.

Type `implementation`: a test's fake, no source. The red first is the
run itself, and outside the runner: the fake as it was left the process
with the timeout pending, exit 13; held, it answers *no answer in 5ms*
and exits 0.

#### Work

```cairn-unit
step: S08
unit: 08
type: implementation
verified: cairn-check, cairn-test, cairn-active --check, the fake outside the runner
```

- **`tools/cairn-postmortem.test.mjs`**: the timeout fake holds the
  loop with one bounded timer that rejects after 100ms, and rejects
  with the signal's reason when it aborts first.
- **This path's `index.md`**: `status: running`, `subject_commit: null`,
  the resume pointed at the administrative commit on the void candidate;
  the live view regenerated with it.

#### Self-review

- `native:` the helper keeps `AbortSignal.timeout`, right for a real
  request; the fake is what was wrong.
- Widening of `writes:`: none.
- Net: 5 lines added and 1 removed in the test, beside the record, the
  view and this step.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff, the helper and the two criteria; it ran the file and a
scratch test with a signal that never aborts, and returned in about a
minute, on the first attempt, with five findings:

- `correctness:` the cause and the removal of the cancellation confirmed
  — nothing to do.
- `correctness:` a `githubRequest` that stopped aborting kept the
  interval alive, and the suite hung where it had reported a
  cancellation — **fixed**, one bounded timer that rejects, so a broken abort fails the assertion.
- `correctness:` a hard-coded timeout replacing `timeoutMs` passed —
  **fixed** by the same timer: 3000ms outlasts it.
- `shrink:` the interval and its clearing, one bounded timer instead —
  **fixed**; and the four-line comment, one line — **fixed**.
- `stdlib:` `timers/promises` is not a drop-in, rejecting with an
  `AbortError` — nothing to do.

Second read, of the fix alone: a fresh context, a subagent of this
harness, given only the fake, the helper and the two criteria; it ran
the file and returned in under a minute, on the first attempt, with one
finding beyond confirming the fake fails and does not hang:

- `correctness:` the uncleared one-second bound held the process a
  second past the test — **fixed**, the bound is 100ms: far above the
  5ms timeout, far below a hard-coded 3000ms, and the line stays one.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, run bare with this step complete (exit 0)
cairn-test            : pass — 364 tests, 0 failed, 0 cancelled, exit 0
cairn-active --check  : pass — the view current (exit 0)
the red first         : run 36203882945 cancelled the file; the fake outside the runner exited 13
remote                : pushed immediately after this record; its commit is the candidate C
```

#### Checkpoint

```text
status      : running
current step: S08 complete on push; this commit is C
retention   : none (no-rewrite host)
changed     : tools/cairn-postmortem.test.mjs, project/coding-paths/ACTIVE.md, this path's index and this step
session     : safe boundary after a successful push
next action : the close skill on C — the request's run, the coherence read, the administrative commit, the description
blockers    : none
```
