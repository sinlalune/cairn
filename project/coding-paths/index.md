---
type: Cairn Folder Index
title: Coding paths
description: One folder per bounded change, the generated live view, and the roadmap register.
tags: [index, cairn]
timestamp: 2026-09-22T00:00:00Z
---

# Coding paths

One folder per bounded change, the generated live view, and the roadmap register.

## Roadmap

Every milestone is accounted for: it has a path, or it says it does not yet.

| Milestone | Outcome | Paths | State |
| :-- | :-- | :-- | :-- |
| Cairn 1.0 — the protocol cut to its manifesto | the specification on the six stages, twenty-four rules, five skills, the kit and the `cairn` command, released 1.0.0 from this repository | [CP-CAIRN-001](./CP-CAIRN-001/index.md) | done 2026-09-03 |
| Cairn 1.1 — what the first adopter taught | the twenty-nine decision records of [the 1.1 architecture page](../../docs/architecture/01-cairn-1-1.md) — fifteen from the adopter's audits, five from the coding guidelines, three from the pedagogy, two from the debts coding path 1 left, one from the readings that must not lie, one from the ready behind an integrating commit and two from the three rulings of September, with ADR-017 patched in place for the third — implemented in the checker, the skills, the kit and the specification, and released 1.1.0 | [CP-CAIRN-002](./CP-CAIRN-002/index.md), [CP-CAIRN-003](./CP-CAIRN-003/index.md) and [CP-CAIRN-004](./CP-CAIRN-004/index.md) (the promotions), then the seven coding paths below | done 2026-09-15 by [CP-CAIRN-011](./CP-CAIRN-011/index.md); the tag `1.1.0` goes on its integrating commit and the package is published from it, after the merge |
| Cairn 1.2 — what 1.1 taught | the fourteen decision records ADR-030 to ADR-043 of [the 1.2 architecture page](../../docs/architecture/02-cairn-1-2.md), promoted from [the 1.2 decisions page](../../feedbacks/2026-09-21-cairn-1-2-decisions.md) and [the asks note](../../feedbacks/2026-09-21-cairn-1-2-the-asks.md) — the thirty-six asks the owner took on 2026-09-21; four 1.1 decisions superseded, one of them in two clauses (ADR-001 d1, ADR-002 d2, ADR-016 d1's first half, ADR-028 d2's clause *no tool reads or writes the folder*) and the 1.1 page marked; implemented in the skills, the specification, the checker, the kit, the tools and this repository's files by the five coding paths below, and released 1.2.0 with the treated notes moved under `feedbacks/1.2/` | [CP-CAIRN-012](./CP-CAIRN-012/index.md) (the promotion), then the five coding paths below | running — the promotion merged 2026-09-22 |

### The coding paths of 1.1

One path per surface, in the order their `writes:` allow without overlap;
a path that must wait for an earlier one declares `depends_on` (ADR-003).
Each is scoped from the records it names and nothing else.

| Order | Outcome | Records | Writes | Path |
| :-- | :-- | :-- | :-- | :-- |
| 1 | the transports and the skills: `manual-git` registration for a sole owner, the owner's plan review and the owner's try before the merge, the sentences the open, unit and close skills gain, the request template; and the coding guidelines' skills: `cairn-code` shortened to Cairn's own with its two lines, the self-review in five tags, the review movement in the unit skill, chapter 5, the concept and the step template, the reader's two criteria in the close skill, the failing test first, the definition of done item by item in the close skill and this repository's request template, an area as a folder and the whole-root reason in the open skill and the configuration reference, the helper agent as the writer; and the pedagogy's skills: the three plain lines in the request template and the path template's goal and the sentences of the open and close skills, the question to the owner in the chat in the open, close and unit skills, the sixth skill `cairn-learn`, the offer of a learning session in the unit skill, the README's line in the close skill, the API refresh in the unit skill | ADR-001 d1–d5, ADR-002 d2, ADR-006, ADR-007, ADR-008 d1, d3, d7, ADR-009, ADR-010 d2, ADR-014 d2 (the skill's sentence); ADR-016 d2, d3, d4, ADR-017 d1 and d2 (the skill, the chapter, the concept, the template), ADR-018 d1 and d2 (the skills and the template), ADR-019 d1 and d3, ADR-020 d2; ADR-021 d2 (the templates and skills) and d3 (the skills), ADR-022 d2 (the skill) and d3, ADR-023 d1 (the close skill) and d4 (the unit skill) | `skills/**`, `spec/index.md` §5, `spec/concepts/work-unit.md`, `spec/reference/path-template.md`, `spec/reference/configuration.md`, `.github/pull_request_template.md`, `cairn.config.json`, `project/coding-paths/binding.md` | [CP-CAIRN-005](./CP-CAIRN-005/index.md), done 2026-09-10 |
| 2 | the checker reads what it did not: the profile line, the transition table, the seal at every transition, `writes-overlap`, the six corrections, one commit for one path, the journal message, refusals that name the remedy; the stale citations of an earlier repository's records replaced under `tools/`; and the coding guidelines' one rule: `review`, blocking, reading the current unit's step for a non-empty review section, with its fixture holding a trunk merge, its catalogue entry and its soundness line | ADR-001 d6, d7, ADR-002 d1, ADR-003, ADR-004, ADR-008 d2, d4, d6; ADR-017 d2 (the rule, the fixture, the catalogue) | `tools/cairn-check.mjs`, `tools/cairn-fixture.test.mjs`, `tools/cairn-check.test.mjs`, `tools/cairn-rules.mjs`, `tools/soundness.md`, `spec/reference/conformance.md`, `docs/modules/application.md` | [CP-CAIRN-006](./CP-CAIRN-006/index.md), done 2026-09-12 |
| 3 | the workflow and the tools: one run per commit that can land, `cairn-postmortem` on a red run, this repository's `cairn-test`, the roadmap register reported by `cairn-active`; and `cairn-audit` opening the request's description with one line per item of the definition of done; and the pedagogy's tools: `cairn-audit` printing the three plain lines and the surface link first, `cairn-postmortem` printing the facts a question to the owner is built from; and the sentence that gives the post-mortem its reader: the unit skill's resume movement sends the writer to a red run's post-mortem before the next unit | ADR-005, ADR-008 d5, ADR-014; ADR-018 d2 (the audit tool); ADR-021 d2 (the audit tool) and d3 (the post-mortem tool); ADR-025 (the unit skill's sentence) | `.github/workflows/cairn.yml`, `tools/cairn-active.mjs`, `tools/cairn-audit.mjs` and its test, `tools/cairn-postmortem.mjs` and tests, `package.json`, `docs/modules/application.md`, `skills/cairn-unit/SKILL.md`; widened at registration for four one-line debts of path 2: `skills/cairn-unit/reference.md`, `skills/cairn-open/SKILL.md`, `skills/cairn-close/SKILL.md`, `spec/reference/path-template.md`; widened in the path by `tools/cairn-workflow.test.mjs` and `.github/pull_request_template.md` | [CP-CAIRN-007](./CP-CAIRN-007/index.md), done 2026-09-14 |
| 4 | the kit and the documentation plane: `docs/inputs`, the concept wiki in three folders, the page per surface, the local pointer, the proactive-note line in the bootloader, the module note template; chapters 3 and 6 and the layout reference; the stale citations replaced under `spec/`; the two kit files ADR-013 named removed for their own reasons or kept, the count being no rule since ADR-022; `update` rewriting pristine files, printing what a release changes in edited ones, taking the release's version of a named file; and the coding guidelines' kit: Ponytail named at a pinned tag as a dependency the kit does not copy, the bootloader's skills line, the generator of an adopter's request template, the dependency sentence on the architecture concept and the architecture index the kit writes; and the pedagogy's kit and chapters: the tone line and the six-skill item in the bootloader text and this repository's `AGENTS.md`, the pointer page listing six skills, `planInstall` and the lock carrying `cairn-learn`, chapter 6's learning-notes paragraph and scopes row and the concept template's sentence, chapter 3's README clause, flow kind, surface-page example and API link, the flow page's shape and the diagram on the architecture concept, this repository's architecture index and the documentation and architecture indexes the kit writes; and the registration transport the kit installs: `init` writing `transport.registration: manual-git`, the `--transport` option naming the integration transport alone, the generated binding row printing both, and the configuration reference's example and `transport` row following; and the kit's CI adapter and manifest, which coding path 3 found belong to no row: the generated workflow's `push` trigger cut to the trunk and its red-run step added, `cairn-postmortem` in `REFERENCE_TOOLS` and in the scripts the kit writes, and the bootloader saying `npm test` is an alias of `cairn-test` | ADR-005 (the kit's adapter), ADR-014 (the kit's adapter, its manifest and script, and the bootloader's alias sentence); ADR-010 d1, ADR-011, ADR-012, ADR-013, ADR-015; ADR-016 d1, ADR-018 d2 (the generated template), ADR-019 d2; ADR-021 d1, ADR-022 d1 and d2 (the bootloader, the pointer page, the manifest, the lock, chapter 6, the template), ADR-023 d1, d2, d3, d4 (chapter 3, the concept, the indexes); ADR-024 (`init`, the option, the generated binding row, the reference) | `tools/cairn.mjs` and tests, `cairn.lock.json`, `spec/index.md` §3 and §6, `spec/reference/repository-layout.md`, `spec/reference/configuration.md`, `spec/concepts/**`, `docs/architecture/index.md`, `AGENTS.md`; widened at registration: `tools/cairn-check.mjs` and its tests for the two concept rules' recursive read (ADR-011 d2), `spec/reference/conformance.md` for their rows, `docs/modules/application.md`; widened in the path by `tools/cairn-rules.mjs`, in S02, because the conformance page's two rule rows are GENERATED from it and a page cannot state the recursive read unless its generator does | [CP-CAIRN-009](./CP-CAIRN-009/index.md), done 2026-09-15 |
| 5 | the readings that must not lie, in the checker and in what it says about itself: a decision record promoting its own sentence — *"Not read" is an honest line; a wrong reading is not* — into a rule that binds, so a reading the forge withheld is reported as unread and never as an answer; and the checker made to keep it, `forgeGaps` telling an unread bypass list from an empty one and `readForge` naming the half it could not read, with the `forgeGaps` cases rewritten against the new reading; then `GITHUB_TOKEN` returns to the checker's workflow step and item 2 of coding path 3's definition of done is met. Beside it, the `review` rule's own mis-selection: it judges `workUnits.find(u => u.step === current_step) ?? workUnits.at(-1)` over a list sorted by ordinal, so the field can only ever point the rule at an OLDER unit than the one just written — on coding path 3 a `current_step` left at `S01` meant six units' review sections were never read by the gate. Deleting the `find(…) ??` makes the rule judge the ledger's newest, which is the unit under review, and leaves `current_step` decoration that can go with it. Placed before the release because a gate that certifies a protection the repository does not have must not be what 1.1.0 ships | [ADR-026](../../docs/adr/ADR-026-the-readings-that-must-not-lie.md), written by S01 and promoted from [CP-CAIRN-007 S03](./CP-CAIRN-007/steps/S03.md) and coding path 3's journal entry; ADR-001 d6, the profile line it protects; extended at registration with path 3's four checker and record leftovers — ADR-008 d2 (one commit for one path, the merge-object refusal by transport), ADR-017 d2 (the review rule's selection), ADR-014 d1 (the post-mortem reading from the checker's exports), the conformance page's stale sentence; and, by the two amendments of 2026-09-14, the gate reading the integration it judges, and [ADR-027](../../docs/adr/ADR-027-the-ready-behind-an-integrating-commit.md), written by S08, extending ADR-001 d7 from the debt this path's own S03 raised | `docs/adr/**`, `tools/cairn-check.mjs`, `tools/cairn-check.test.mjs`, `.github/workflows/cairn.yml`, `docs/architecture/01-cairn-1-1.md`; widened at registration: `tools/cairn-rules.mjs` and its test, `tools/cairn-fixture.test.mjs`, `tools/cairn-postmortem.mjs` and its test, `tools/soundness.md`, `spec/reference/conformance.md`, `skills/cairn-close/SKILL.md` and its reference, `docs/modules/application.md`, `docs/architecture/index.md`; widened in the path by `tools/cairn-workflow.test.mjs`, and by the amendment of 2026-09-14 by `tools/cairn.mjs`, one line of row 4's installer | [CP-CAIRN-008](./CP-CAIRN-008/index.md), done 2026-09-15 |
| 6 | the three rulings of September, before the release: ADR-017 patched in place with the review movement's fallback — what a writer does when no fresh context can be obtained, the section's first line naming the reader — one sentence in the unit skill and one line in the path template, no rule; a record making `feedbacks/` the channel an agent writes when the gate stayed green and the protocol still cost more than it should, no tool, no skill, no ritual, and the first such file written from what coding path 4's writer observed; a record deleting the GitHub reading from the checker — `forgeGaps`, `readForge`, the profile line's host half, the token on the checker's step of this repository's workflow, the kit's generated step already without one and pinned so, `githubSlug` and `githubRequest` moved to the post-mortem that calls them — so the checker asks the host nothing and makes no network call | [ADR-017](../../docs/adr/ADR-017-the-review-movement.md) decision 4, added in place by S01; [ADR-028](../../docs/adr/ADR-028-a-feedback-file-when-nothing-broke.md), the feedback file, written by S01 and promoted from the owner's words of 2026-09-15 over request #18 as coding path 4's journal and this path's record carry them; [ADR-029](../../docs/adr/ADR-029-the-checker-asks-the-host-nothing.md), written by S01, superseding ADR-001 d6 and ADR-026 d1 from the owner's ruling of 2026-09-14 | `docs/adr/**`, `docs/architecture/01-cairn-1-1.md` and its index, `docs/modules/application.md`, `skills/cairn-unit/SKILL.md`, `spec/reference/path-template.md`, `spec/reference/conformance.md`, `feedbacks/**`, `tools/cairn-check.mjs` and its tests, `tools/cairn-rules.mjs`, `tools/soundness.md`, `tools/cairn.mjs` and its test, `.github/workflows/cairn.yml`; widened in the path by S03: `tools/cairn-postmortem.mjs` and its test, which receive the two GitHub functions the post-mortem calls, and `tools/cairn-workflow.test.mjs`, which asserted the token on the checker's step | [CP-CAIRN-010](./CP-CAIRN-010/index.md), done 2026-09-15 |
| 7 | the release: the README and the site updated for the surfaces 1.1 changes, the weight budget measured, 1.1.0 cut and the first adopter updated with `cairn update`; and the pedagogy's release: this repository's README opening with what the project is and listing its surfaces, the kit's file count measured and reported on the conformance page with no target | ADR-012 (this repository's pages), the conformance page's budget; ADR-022 d2 (the budget), ADR-023 d1 (this repository's README) | `README.md`, `site/**`, `docs/modules/site.md` (the site's note, widened by S02), `spec/reference/conformance.md` (the budget), `package.json`, `cairn.lock.json` | [CP-CAIRN-011](./CP-CAIRN-011/index.md), done 2026-09-16 |


### The coding paths of 1.2

One path per surface, run in this order. Where two rows share a file — the
Writes column names it twice — the later declares `depends_on` the earlier
at its registration (ADR-003). Each is scoped from the implementation
tables of the records it names and nothing else.

| Order | Outcome | Records | Writes | Path |
| :-- | :-- | :-- | :-- | :-- |
| 1 | the skills and the specification: the open skill's two reads before the go-ahead and the precondition of the direct-push sequence, the `pull-request` registration sequence beside it in the skill, its reference and the configuration reference; the close skill's sentence on a measured figure, its dangling link dropped, its steps 3 and 4 swapped, with the execution protocol, chapter 5 and the portable sequence following, its `deferred` disposition naming a backlog file, its release-path sentences for the changelog and the treated notes, and the coherence questions handed to a fresh context in the skill, the human-records reference, the `full` route, the concepts for the audit, the route and the roles, and this repository's request template; the unit skill's route for a note about Cairn and its `deferred` disposition; the seventh skill `cairn-update`; the path template's plain list; the project plane's folders in chapters 1 and 4 and the layout reference's rows for the backlog and the feedbacks folder | ADR-030; ADR-031 d1 (the close skill's sentence); ADR-032 d1, d2 (the skill, its reference, the configuration reference; the pilot is row 3's); ADR-034 d8; ADR-035 (the skill file); ADR-038 d2 (the unit skill's sentence), d3 (the close skill's sentence, the layout row), d5 (the layout row); ADR-039 (the close skill's sentence, the layout row); ADR-040; ADR-041 (the three skills, chapters 1 and 4, the layout rows); ADR-042 (the template, the open and close skills); ADR-043 (the close skill, the reference, §5, the concept, this repository's request template) | `skills/**`, `spec/index.md`, `spec/reference/execution-protocol.md`, `spec/reference/path-template.md`, `spec/reference/human-records.md`, `spec/reference/configuration.md`, `spec/reference/repository-layout.md`, `spec/concepts/administrative-closure.md`, `spec/concepts/coherence-audit.md`, `.github/pull_request_template.md`; widened at registration: `AGENTS.md`, `project/coding-paths/index.md`; widened in the path by S02: `spec/concepts/lifecycle.md`, `spec/concepts/implementation-candidate.md`; by S03: `spec/concepts/route.md`, `spec/concepts/roles.md`; by S07: `spec/reference/paths.md`; by S09: `spec/concepts/index.md` | [CP-CAIRN-013](./CP-CAIRN-013/index.md), running |
| 2 | the checker: the `registration` rule reading a registration in the change under review, with two fixtures; the `links` rule reading the declared exemptions, with the configuration schema and loader, one fixture, the field's reference row and the rule's rows; `feedbacks/` in the corpus, with one fixture; the two `comparison` messages saying GitHub; the fixture red once on `record-integrity` read for its order | ADR-032 d3; ADR-037 d1, d2 (the conformance page's `links` row); ADR-038 d1; ADR-034 d6, d7 | `tools/cairn-check.mjs`, `tools/cairn-check.test.mjs`, `tools/cairn-fixture.test.mjs`, `tools/cairn-rules.mjs`, `tools/cairn-config.mjs`, `tools/cairn-config.schema.json`, `tools/soundness.md`, `spec/reference/conformance.md` (the rule rows and the `links` row), `spec/reference/configuration.md` (the field's row), `docs/modules/application.md` | *no path yet* |
| 3 | the kit: `init` and `adopt` refusing the pairing that cannot work, with the installer's one GitHub reading; every declared root read, a declined host file the lock remembers, the documentation index in the shape it describes, `adopt` naming the gate it leaves red and the shape that wants a declaration; the generated view off the reconcile list, byte-equal generations, the files an update starts managing, `status` planning as `update` does; the manifest, pointer page, bootloader and lock carrying `cairn-update`, `feedbacks/` with its index, `project/backlog/` with its index, Ponytail's two skills fetched from the latest version and every skill written where the harness loads it, the pin gone; the pilot's record as a plain list; the generated request template's coherence section; `status` linking the changelog, the pointer page linking it, the package's file list shipping it | ADR-032 d2 (the pilot), d4; ADR-033; ADR-034 d1, d2, d3, d4; ADR-035 (the manifest, the pointer page, the bootloader, the lock); ADR-036; ADR-037 d2 (`adopt`'s shape); ADR-038 d2 (the folder and its index), d5 (the installed index); ADR-039 (`status`, the pointer page, `package.json`); ADR-041 (the folder and its index); ADR-042 (the pilot); ADR-043 (`requestTemplate`) | `tools/cairn.mjs`, `tools/cairn.test.mjs`, `tools/cairn-pilot.mjs`, `cairn.lock.json`, `package.json` (the file list), `docs/modules/application.md`, `spec/reference/repository-layout.md` (the lock's row) | *no path yet* |
| 4 | the tools and this repository's files: `cairn-active` filling the register's state cells and reporting a stale one, with the installed register template; the post-mortem counting the run it runs in, printing a closed request as closed and reading the one path that arrived on the trunk, with the failure step of both workflows; `cairn-audit` reading a plain-list definition of done, scaffolding the three coherence facts and pointing a deferral at its backlog file; this repository's bootloader pointing at the pointer page | ADR-031 d2; ADR-034 d5, d9, d10, d11; ADR-035 (this repository's `AGENTS.md` skills line); ADR-041 (the audit tool's placeholder); ADR-042 (`definitionItems`); ADR-043 (the audit tool) | `tools/cairn-active.mjs` and its test, `tools/cairn-postmortem.mjs` and its test, `tools/cairn-audit.mjs` and its test, `.github/workflows/cairn.yml`, `tools/cairn-workflow.test.mjs`, `AGENTS.md`, `project/coding-paths/index.md` (the state cells); `tools/cairn.mjs` for `workflow()` and the register template alone | *no path yet* |
| 5 | the release: the first section of `CHANGELOG.md`, naming the adopter repairs 1.2.0 absorbed and not, by Crumbz's, Atomik's and ECOS's path ids; the kit's counts written once in the conformance page's budget table and linked from the kit row, the README, the tools' module note and the installer's header comment; the notes 1.2 answered moved into `feedbacks/1.2/` with an index naming what answered each; the README and the site updated for the surfaces 1.2 changes; 1.2.0 cut and the three adopters updated. This row owes ADR-015 decision 2's line per changed template, *pending* in that record's table, and clears the word if it writes the line, or says in its record why it does not | ADR-039 (the changelog's section); ADR-031 d1 (the count rows and the four links), d3 (the first *pending*); ADR-038 d3 (the treated notes); ADR-015 d2 (owed); ADR-012 and ADR-023 d1 (this repository's pages) | `CHANGELOG.md`, `README.md`, `site/**`, `docs/modules/site.md`, `docs/modules/application.md`, `package.json`, `cairn.lock.json`, `feedbacks/**`, `spec/reference/conformance.md` (the budget table and the kit row), the header comment of `tools/cairn.mjs` | *no path yet* |
