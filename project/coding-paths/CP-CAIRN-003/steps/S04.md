---
type: Cairn Coding Path Step
title: 'CP-CAIRN-003 S04 — ADR-020, the one edge and the one writer, confirmed'
timestamp: 2026-09-07T00:00:00Z
cairn:
  path: CP-CAIRN-003
  step: S04
---

# CP-CAIRN-003 S04

### S04 — ADR-020, the one edge and the one writer, confirmed — **COMPLETE**

#### Plan

Write ADR-020 from section D of the coding-guidelines decisions page at
blob `48112c31` and research note 4 at blob `a52ac0ec`: the edge between
two paths lives in the record only (Q13); a path has one writer and a
helper agent inside the writer's session is the writer, one sentence in
the unit skill (Q14); the roadmap register carries no edge between
milestones (Q15). A record of what was weighed and kept, as ADR-008 was,
building on ADR-003. List it in `docs/adr/index.md` after ADR-019.

Definition-of-done items advanced: the first is now whole (five records
ADR-016 to ADR-020 exist in shape, indexed with no gap after ADR-015),
the second is now whole (every ticked answer Q1 to Q15 named by exactly
one record, every refused option named with its tag), and the fourth
(the record names `cairn-unit` and `registerIndex` in `tools/cairn.mjs`).

Deliberately not in this unit: the 1.1 architecture page and the roadmap
register (S05), the candidate (S06), and any change under `spec/`,
`skills/`, `tools/`, `site/`, `.github/` or `cairn.config.json`. The
notes are read, not written.

#### Work

```cairn-unit
step: S04
unit: 04
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Wrote `docs/adr/ADR-020-the-one-edge-and-the-one-writer.md`**, three
  decisions, two of which change nothing and say so, one of which adds
  one sentence to the unit skill.
- **Tied decision 2 to ADR-017**: the fresh reader of the review movement
  is a helper agent inside the writer's session, so the one-writer
  sentence covers it and no second rule is needed; the consequences say
  so.
- **Kept the Crumbz facts to the note's counts**: two edges declared of
  sixteen, one dropped, six overlapping pairs, no path with two writers.
- **Added the ADR-020 line to `docs/adr/index.md`.**

#### Self-review

- `delete:` a paragraph restating ADR-003's decision under decision 1 —
  the record links ADR-003 and says what it closes, once.
- `yagni:` a decision on agent-to-agent messaging between two waiting
  paths, which the note lists under *what it does not settle*; no
  question asked it and no record answers it.
- `shrink:` the context, from the note's three sources to the two facts
  that decide — the forge has the edge, Beads left Git — and the Crumbz
  counts.
- No widening of `writes:`: every file of this unit is under `docs/adr/`
  or this path's folder. Net: one paragraph cut from the draft; lean.

#### Review

A fresh context of the writer's agent was given the diff of the record
and the index line, and the two criteria of ADR-016 decision 4, nothing
else. Nine findings, seven under the ladder and two on correctness; each
with what was done:

- `delete:` the four consequences, each restating a decision or the
  context — fixed, the section shrunk to one bullet on what stays.
- `shrink:` the implements table repeating *what this changes* — refused:
  the table is the shape every record of the folder ends with, and the
  definition of done's fourth item is read from it by the coding path.
- `shrink:` the manifesto section repeating the refusal reasons — fixed,
  shrunk to the two manifesto sentences that decided.
- `shrink:` decision 2 repeating the promoted answer — fixed, the
  sentence removed.
- `correct:` "a second copy, a tool and a shape refused" counting three
  against five listed — fixed: five, those three among them.
- `correct:` the Q14 *in the writer's session* option called the same
  rule as decision 2 while filed as refused — fixed: it states subagents
  as writers the writer answers for, decision 2 states one writer.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main, 11 changed files on the branch since the trunk (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the record with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S05
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-020-the-one-edge-and-the-one-writer.md, docs/adr/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S05 — the 1.1 architecture page amended with the five records and promoted-from links to the six notes at their pinned blob ids, docs/architecture/index.md refreshed, the register's rows 1, 2 and 4 widened and tools/cairn-audit.mjs placed in a row
blockers    : none
```
