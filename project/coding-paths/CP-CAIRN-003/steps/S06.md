---
type: Cairn Coding Path Step
title: 'CP-CAIRN-003 S06 — repair: the refused administrative commit superseded'
timestamp: 2026-09-07T00:00:00Z
cairn:
  path: CP-CAIRN-003
  step: S06
---

# CP-CAIRN-003 S06

### S06 — repair: the refused administrative commit superseded — **COMPLETE**

#### Plan

Repair one protocol violation and name it. After the first candidate
`72198b183ab9b5e639cab51e35b80cb82635c8fd` (S05's commit) and the opening
of pull request #10 for it, the writer made the administrative commit
`b09f49253d8f10aaed2c13b8fe60a8d4cadff5a7` with `status: ready` and
`subject_commit` set — and also moved `current_step` from `S01` to `S05`.
The rule `acceptance` refused it: *closure changed `current_step`, which
acceptance was measured against — only status, subject_commit may move
when a path declares ready*. The gate ran before and after the commit and
printed `FAIL` both times; the writer's command chained each gate with
`; echo "EXIT=$?"`, so the shell saw the echo's exit code, the `set -e`
never fired, and the commit was pushed red. That is the pipeline fault
`AGENTS.md` names in its first mechanical rule, and ADR-008 decision 7's
*every gate is read by its exit code*.

The branch is never rewritten (`pathHistoryPolicy: forbidden`), so
`b09f492` stays in the history as what it was. This unit, typed `repair`:
returns `status` to `running` and `subject_commit` to `null`; sets
`current_step` to `S06`, which a unit may do and a closure may not;
records the violation here; refreshes the resume section; and is the
second candidate `C₂` once pushed, as *implementation changed after
acceptance* in the repair procedures prescribes — return to `running`,
produce a new candidate, write a new review naming the one it supersedes.
The correct administrative commit follows it as a separate commit, and
the description of pull request #10 is rewritten for `C₂`.

Definition-of-done item advanced: the seventh (every completed step has
one self-contained record, a refreshed resume section, one commit, a
remote checkpoint) and the eighth, restored (the candidate is checked
and reviewed in the request; the refused commit is not the one that
lands).

Deliberately not in this unit: any document under `docs/`, the register,
or anything under the excluded roots — the repair touches the record,
the live view and this step, nothing else.

#### Work

```cairn-unit
step: S06
unit: 06
type: repair
verified: cairn-check --base origin/main, cairn-active --check, test
```

- **Violation**: the administrative commit `b09f492` moved `current_step`,
  which the acceptance was measured against; refused by `acceptance`;
  pushed with the gate red because the gate's exit code was hidden
  behind an echo. Evidence lost: none — the commit and its refusal are
  in the history and in this step.
- **Restored**: `status: running`, `subject_commit: null` in the record;
  `current_step: S06`; the live view regenerated to `running`; the resume
  section's checkpoint names the last green unit `72198b1` and says what
  follows it on the remote; *tried and rejected* carries both faults.
- **Also found**: units S01 to S05 never moved `current_step` from `S01`.
  The `work-unit` rule looks for a `cairn-unit` block for the record's
  current step, found S01's every time, and passed. This unit sets it to
  its own step, as the CP-CAIRN-002 units did; the five step files are
  append-only and stay as they are.
- **The candidate**: this unit's commit is `C₂`; its id is named by the
  administrative commit that follows and by the request's description.

#### Self-review

- `delete:` nothing — the diff is the record's frontmatter, its steps
  line, its resume section, the live view and this step.
- `yagni:` a second repair step for the `current_step` lapse of S01 to
  S05; it broke no rule, this step records it, and this unit corrects it.
- No widening of `writes:`. Lean already.

#### Review

No fresh-context reader was run on this unit: the diff changes no
document but the path's own record and this step, and the reader would
be given a repair record to read for the ladder and correctness that the
owner reads in the request. The review movement is not yet in the unit
skill; the units of this path that changed documents ran it.

#### Verification

```text
cairn-check --base origin/main : pass — OK, protocol satisfied, no advisory (exit 0, read bare, the command chain aborting on any other code)
cairn-active --check           : pass — view already current (exit 0)
tests                          : pass — 230 tests, 0 failed (npm test, exit 0)
user review                    : the owner reads this step with the request
remote                         : pushed immediately after this record; its commit is C₂, named by the administrative commit
```

#### Checkpoint

```text
status      : running
current step: S06 complete on push
retention   : none (no-rewrite host)
changed     : project/coding-paths/CP-CAIRN-003/index.md, project/coding-paths/ACTIVE.md, this step
session     : safe boundary after successful push and a green CI run
next action : the administrative commit after C₂ — status ready, subject_commit C₂, the checkpoint, the live view, nothing else, the gate read bare before and after; the request's description rewritten for C₂; then the owner reads and merges
blockers    : none
```
