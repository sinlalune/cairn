---
type: Cairn Coding Path Step
title: 'CP-CAIRN-003 S07 — the request reviewer''s finding on ADR-017 answered'
timestamp: 2026-09-08T00:00:00Z
cairn:
  path: CP-CAIRN-003
  step: S07
---

# CP-CAIRN-003 S07

### S07 — the request reviewer's finding on ADR-017 answered — **COMPLETE**

#### Plan

Answer one finding of the review bot on pull request #10, left on
2026-09-08 on ADR-017 decision 2: *when the fresh review finds an issue,
this sequence allows the writer to fix the diff and proceed directly to
verification, so the final pushed diff is no longer the diff that the
fresh context reviewed, and the preceding self-review is stale as well;
require self-review/review to repeat after any disposition changes the
diff, until a review pass leaves it unchanged.* The finding is true of
this path's own units: S02 to S05 pushed their fixes unread. The remedy
proposed is unbounded. The owner was given three options in the chat of
2026-09-08 — once more, bounded; once is enough; until a read finds
nothing — and chose the first. Write it as ADR-017 decision 3, with the
two others refused; say in the record that decision 3 promotes no tick of
the page and was raised by the request's reviewer; carry one clause to
the 1.1 page and the index line. The path returns to `running`; this
unit's commit is the third candidate, and the administrative commit
follows it.

Definition-of-done items advanced: the first and fourth (ADR-017 states
the choice, its alternatives and the surface it changes), the third (the
page names the record behind the added clause), the seventh and the
eighth (a new candidate reviewed in the request; the review of the
second candidate superseded).

Deliberately not in this unit: anything under the excluded roots; the
other four records; the register, which already carries ADR-017 in rows
1 and 2 and needs no new surface for one sentence.

#### Work

```cairn-unit
step: S07
unit: 07
type: decision
verified: cairn-check --base origin/main, cairn-active --check, test
```

- **ADR-017 decision 3**: after the dispositions, the fresh context reads
  the fix once more — the lines the fix changed, the same two criteria,
  nothing else — and a finding of that second read is dispositioned under
  the same section and not read a third time; the self-review is not
  repeated, a fix that cuts more adds a tagged line. The rule `review`
  reads nothing more. Surfaces: the unit skill's sentence and the step
  template.
- **Two options refused** in the alternatives: *once is enough*, the
  record as it first stood, and *until a read finds nothing*, the
  reviewer's proposal, refused as unbounded and on the vendor's own
  warning.
- **The record says where decision 3 comes from**: no tick of the page;
  raised by the bot on this request; the owner's choice of 2026-09-08. The
  description and the index line say the same; the record's date stays
  the day of its first two decisions.
- **The 1.1 page** gains one clause after the review sentence, naming
  decision 3.
- **The path record** returns to `running` with `subject_commit: null`,
  as the repair procedures prescribe when a candidate's content moves
  after its review; the administrative commit `1a05ee2` of the second
  candidate is void and stays in the history.

#### Self-review

- `delete:` a paragraph of decision 3 describing how the harness's
  review command is given a partial diff — the record says *the lines
  the fix changed* and leaves the mechanics to path 1.
- `yagni:` a fourth option, a second read only when the fix exceeds a
  line count; nobody asked, and it is a number.
- `shrink:` the finding, quoted once in this step in full and paraphrased
  in the record in one sentence.
- No widening of `writes:`. Net: one paragraph cut from the draft; lean.

#### Review

Run as decision 3 now says. **First read**: a fresh context of the
writer's agent was given the diff of the record, the page clause and the
index line, and the two criteria of ADR-016 decision 4. Eight findings:

- `delete:` a provenance paragraph before the context repeating the
  description and decision 3's head — fixed, deleted.
- `delete:` *the rule `review` reads nothing more* — refused: it says the
  checker gains nothing from decision 3, which whoever scopes path 2
  needs to read.
- `shrink:` the new consequence restating the decision — fixed, one
  clause.
- `shrink:` the manifesto sentence repeating the refusal's reason —
  fixed, shortened.
- `correct:` the *once is enough* refusal refusing the adopted rule too
  — fixed, reworded.
- `correct:` *read* used for the self-review and the fresh context's
  read, making the count wrong — fixed: two reads by the fresh context.
- `correct:` one actor under three names — fixed: the request's
  reviewer, everywhere.
- `correct:` the timestamp and the index's date unchanged while the
  record carries a 2026-09-08 decision — fixed: timestamp 2026-09-08,
  the index line *accepted 2026-09-07, amended 2026-09-08*, the status
  line naming S07 and the date; `adr.date` stays the acceptance.

**Second read**, on the lines those fixes changed only. Five findings,
dispositioned here and not read a third time:

- `delete:` the shrunk consequence now saying only what the decision says
  — fixed, deleted.
- `shrink:` the *once is enough* refusal carrying a concession as a
  second clause — fixed, one clause.
- `shrink:` the status line's *decision 3 added by S07 on 2026-09-08*
  repeating the description — refused: the status line is where a record
  says who wrote it, and S07 is the new fact.
- `correct:` timestamp 2026-09-08 against the status line's 2026-09-07 —
  refused: the status line and `adr.date` carry the acceptance, the
  timestamp the document's last change, and the index line says both.
- `correct:` *the diff the fresh context first read* against the order
  of movements, the self-review coming before any read — fixed: the
  tagged lines are the writer's cut, made before the first read.

#### Verification

```text
cairn-check --base origin/main : pass — OK, protocol satisfied, no advisory (exit 0, read bare, the command chain aborting on any other code)
cairn-active --check           : pass — view already current (exit 0)
tests                          : pass — 230 tests, 0 failed (npm test, exit 0)
user review                    : the owner's choice of 2026-09-08 in the chat, written into decision 3; the owner reads the record with the request
remote                         : pushed immediately after this record; its commit is C₃, named by the administrative commit
```

#### Checkpoint

```text
status      : running
current step: S07 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-017-the-review-movement.md, docs/adr/index.md, docs/architecture/01-cairn-1-1.md, project/coding-paths/ACTIVE.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : the administrative commit after C₃; the request's description rewritten for C₃; the reviewer's thread answered with the unit; then the owner reads and merges
blockers    : none
```
