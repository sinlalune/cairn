---
type: Cairn Coding Path Step
title: 'CP-CAIRN-003 S01 — ADR-016, the coding stance'
timestamp: 2026-09-07T00:00:00Z
cairn:
  path: CP-CAIRN-003
  step: S01
---

# CP-CAIRN-003 S01

### S01 — ADR-016, the coding stance — **COMPLETE**

#### Plan

Write the first record of the path, ADR-016, from Q1 to Q5 of the
coding-guidelines decisions page at blob `48112c31` and research note 1 at
blob `65987c7a`: the kit points at Ponytail at the tag `v4.9.0` for the
ladder and the review and `cairn-code` shrinks to what is Cairn's own (Q2);
the self-review speaks in Ponytail's five tags (Q1); two lines on secrets
and errors (Q4); the ladder and correctness as the only criteria of whoever
reads a diff (Q5); no size for a unit (Q3). Five numbered decisions in one
record, as ADR-008 and ADR-009 did, each naming its question, the tag the
decisions page gave the option kept, and the surface it changes by the name
the skills and the kit use today; every option not ticked named as
rejected with its tag. List the record in `docs/adr/index.md` after ADR-015.

Definition-of-done items advanced: the first (one of the five records
exists in the layout's shape, with a *promoted from* link to both notes at
the pinned blob ids, and the index lists it with no gap), the second for
Q1 to Q5 (each ticked answer named by exactly one record, each refused
option named with its tag), and the fourth (the record names
`tools/cairn.mjs`, `cairn.lock.json`, `skills/cairn-code/SKILL.md`,
`cairn-unit` step 3, `cairn-close` step 3 and the step template, so paths 1
and 4 of the register can be scoped from it).

Deliberately not in this unit: ADR-017 to ADR-020 (S02 to S04), the 1.1
architecture page and the roadmap register (S05), and any change under
`spec/`, `skills/`, `tools/`, `site/`, `.github/` or `cairn.config.json` —
the record names those surfaces and stops. The six governing notes are
read, not written.

#### Work

```cairn-unit
step: S01
unit: 01
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Wrote `docs/adr/ADR-016-the-coding-stance-absorbs-ponytail.md`**: five
  decisions, ordered as the plan lists them — Q2 first because the other
  four stand on it. Status `accepted` on landing, dated 2026-09-07, the day
  of the owner's ticks; the request that lands the path is its review.
- **Decision 1 says what "the kit points at Ponytail" is by today's
  names** and no more: `planInstall` in `tools/cairn.mjs` resolves the
  kit's files and generates the bootloader whose skills line names the
  stance, `cairn.lock.json` records what the kit installed, and Ponytail
  is a pinned dependency the kit names and does not copy — so the file
  budget of ADR-013 is not touched, and the record says so. How the
  dependency is declared is the coding path's work.
- **Decision 2 resolves a sentence of `cairn-code` the answer collides
  with**: the three-line cap says the plan and the self-review are those
  three lines twice; the record keeps the plan at three lines and makes
  the self-review the tagged lines, and names the cap's sentence as a
  surface.
- **Decision 4 draws the line to S02**: the criterion is this record's
  (Q5), the reader and the movement are ADR-017's (Q6, Q7), which hands
  its reader the criterion and adds none. Correctness is stated as
  Cairn's addition because `ponytail-review` excludes it by design.
- **Decision 5 is a record of what was kept**: no size, nothing changes,
  and the alternatives section says where the refused sentence rule's
  second half returns (Q8, ADR-018).
- **Added the ADR-016 line to `docs/adr/index.md`**, the folder's
  timestamp refreshed to 2026-09-07.
- **This record's self-review is written in the five tags** decision 2
  chooses, before any skill says to, because a unit is the place a stance
  is applied.

#### Self-review

- `delete:` a link from decision 4 to ADR-017 as `./index.md` — the record
  does not exist yet and the index is not it; plain words now.
- `delete:` the index line's count of refused options, *six, one a rule*:
  the record names nine, three of them tagged as rules.
- `yagni:` naming the conformance page's weight budget as a surface —
  the kit copies nothing of Ponytail, so the budget does not move; the
  consequences say so and the table stays at what changes.
- `yagni:` a decision on how the pinned dependency is declared — a lock
  field, a manifest entry, a line in the pointer page; the record names
  the files and leaves the shape to path 4.
- `shrink:` the alternatives, one bullet per refused option with its tag
  and one reason, nine bullets; a paragraph each would have repeated the
  note.
- No widening of `writes:`: every file of this unit is under `docs/adr/`
  or this path's folder. Net: two lines cut from the draft; lean.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main, 4 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the record with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-016-the-coding-stance-absorbs-ponytail.md, docs/adr/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S02 — ADR-017, the review movement (Q6, Q7), and ADR-018, two sentences of the cycle (Q8, Q9), from research note 2 at blob 65e7a2a9; the manifesto read for Q7, the owner's words under Q6 quoted, the rule and the fixture named
blockers    : none
```
