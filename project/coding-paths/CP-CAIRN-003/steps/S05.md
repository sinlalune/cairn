---
type: Cairn Coding Path Step
title: 'CP-CAIRN-003 S05 — the 1.1 page amended and the register widened'
timestamp: 2026-09-07T00:00:00Z
cairn:
  path: CP-CAIRN-003
  step: S05
---

# CP-CAIRN-003 S05

### S05 — the 1.1 page amended and the register widened — **COMPLETE**

#### Plan

Amend `docs/architecture/01-cairn-1-1.md` so that it states, naming the
record behind each sentence, what the five records ADR-016 to ADR-020
decide: the five movements of a unit and what the checker reads in a step
record (ADR-017), the two criteria of any reader and the self-review in
five tags (ADR-016), the coding stance and where it comes from (ADR-016),
the two sentences of the cycle (ADR-018), an area as a folder and what the
architecture page says of dependencies (ADR-019), the one writer and the
one edge (ADR-020); link back to the six governing notes as *promoted
from* at the blob ids pinned in this path's record; say that no 1.1
record is superseded; mark every sentence that changes an earlier one
*since 2026-09-07* so that nothing reads as if it had always been so.
Refresh `docs/architecture/index.md`. Widen the roadmap register's rows
1, 2 and 4 with the records and surfaces each carries, place the audit
tool of ADR-018 decision 2 in row 3, and count CP-CAIRN-003 among the
promotions of the 1.1 milestone. No new path; no path opened.

Definition-of-done items advanced: the third (the page states the five
movements, the stance, the areas and the dependency sentence, naming
records, with *promoted from* links to the six notes at the pinned blob
ids, and says no 1.1 record is superseded) and the fifth (the register's
rows carry the new records and surfaces with no new path and no path
opened).

Deliberately not in this unit: the candidate (S06); any change under
`spec/`, `skills/`, `tools/`, `site/`, `.github/` or `cairn.config.json`;
the notes, read and linked, not written.

#### Work

```cairn-unit
step: S05
unit: 05
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Amended the page in place, marked**: an *Amended on 2026-09-07*
  paragraph after the *promoted from* preamble carries the six links at
  their pinned blob ids and the sentence that no record is superseded;
  each added sentence names its record and its decision; where an added
  sentence changes an earlier one — the unit's four movements, the
  request's description, the checker's reading, the tools' rows — it
  says *since 2026-09-07*, and the earlier sentence stays visible in the
  past tense. A closing section, *What the coding guidelines add*, lists
  the five records in five sentences.
- **Added one row to the checker's table** for the rule `review`, and one
  clause each to the rows of `docs/architecture/`, `docs/modules/`,
  `cairn-audit` and `cairn`.
- **Refreshed `docs/architecture/index.md`**: the page's line says what
  the amendment added and through which records.
- **Widened the register**: rows 1, 2, 3 and 4 name the records and
  surfaces of this path in their outcome, records and writes columns —
  row 3 gains the audit tool, which no row's `writes:` carried; the 1.1
  milestone's row counts twenty records and two promotions. Row 5 is
  unchanged. Every state stays *no path yet*.

#### Self-review

- `yagni:` a new section of the page restating each record's decisions
  in full; the closing section is five sentences, one per record, and
  the sentences in place carry the substance.
- `yagni:` a second copy of the six *promoted from* links in the page's
  heading paragraph beside the 1.1 ones; the amendment paragraph is the
  one place for them.
- No widening of `writes:`: the page, the architecture index, the
  register and this path's folder are all declared. Lean already; the
  register's row 1 is long because it names the surfaces of five records
  by their skill, and each is a path's scope.

#### Review

A fresh context of the writer's agent was given the diff of the page,
the architecture index and the register, and the two criteria of ADR-016
decision 4, nothing else. Eleven findings, six under the ladder and five
on correctness; each with what was done:

- `delete:` the closing section *What the coding guidelines add*, every
  clause of which restated a sentence added elsewhere on the page —
  fixed, deleted.
- `delete:` the amendment paragraph's last sentence repeating the intro's
  *the record is named* — fixed, shrunk to the *since 2026-09-07* marking
  clause, which the intro does not say.
- `shrink:` Ponytail as a pinned dependency said in the stance paragraph
  and the `cairn` tool row — fixed, the kit clause left to the tool row.
- `shrink:` an area as a folder said in the prose and the `docs/modules/`
  row — fixed, the row's clause and citation removed.
- `shrink:` the definition of done item by item said in the closing
  prose and the `cairn-audit` row — fixed, the row shrunk to a clause;
  the table lists what each tool does and keeps its citation.
- `yagni:` the front-matter description saying the page is amended —
  refused: the description is the page's own summary line, and a page
  amended on a date says so where a reader sees it first.
- `correct:` the five tags ascribed to the review in the Ponytail list
  and to the self-review in the sentence above — fixed: the tags are
  Ponytail's review skill's, and Cairn's self-review speaks in them.
- `correct:` ADR-017 cited bare in prose and as d2 in the table — fixed,
  decisions 1 and 2 in the prose.
- `correct:` *no step file for the review* against the unit's review
  section — fixed, one clause says the earlier sentence is the closing
  review and the unit's review is a section of its own step.
- `correct:` rows 1 and 4 each naming the request template the other
  carries — fixed: row 1 names this repository's template, row 4 the
  generator of an adopter's.
- `correct:` the deleted section's claim that rows 1 to 4 carry every
  decision while ADR-016 d5 is in none — fixed by the deletion; d5
  changes nothing and belongs in no row.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main, 15 changed files on the branch since the trunk (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : the owner reads the page before the merge (ADR-001, decision 3); not required for the unit
remote                : pushed immediately after this record; the commit id is named in the resume section by S06
```

#### Checkpoint

```text
status      : running
current step: S05 complete on push
retention   : none (no-rewrite host)
changed     : docs/architecture/01-cairn-1-1.md, docs/architecture/index.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S06 — the candidate: merge origin/main in, run the gates bare on C, cairn-audit for the request's description with the definition of done answered item by item, open the pull request from path/cp-cairn-003 to main; the owner reads the pages before the merge
blockers    : none
```
