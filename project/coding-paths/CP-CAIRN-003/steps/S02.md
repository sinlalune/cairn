---
type: Cairn Coding Path Step
title: 'CP-CAIRN-003 S02 — ADR-017, the review movement, and ADR-018, two sentences of the cycle'
timestamp: 2026-09-07T00:00:00Z
cairn:
  path: CP-CAIRN-003
  step: S02
---

# CP-CAIRN-003 S02

### S02 — ADR-017, the review movement, and ADR-018, two sentences of the cycle — **COMPLETE**

#### Plan

Write the two records of section B of the coding-guidelines decisions page
at blob `48112c31`, from research note 2 at blob `65e7a2a9`. ADR-017, Q6
and Q7: a unit has five movements — plan, change, self-review, review,
verify — the review being the writer's own agent in a fresh context that
sees only the diff and the two criteria of ADR-016 decision 4, its findings
and their dispositions written into a section of the step record that a
new blocking rule requires; the owner's two sentences under Q6 quoted; the
rule, the fixture and the catalogue named; the manifesto read and its test
weighed, because Q7 is the one answer tagged *adds a rule*. ADR-018, Q8
and Q9: the failing test first when behaviour changes and no new test when
only structure does; the request's description answering the definition
of done item by item. List both in `docs/adr/index.md` after ADR-016.

Definition-of-done items advanced: the first (three of the five records
now exist in shape, indexed with no gap), the second for Q6 to Q9 (each
ticked answer named by one record, each refused option named with its
tag), and the fourth (the records name `skills/cairn-unit/SKILL.md`,
`spec/index.md` §5, `spec/concepts/work-unit.md`,
`spec/reference/path-template.md`, the rule `review` in
`tools/cairn-check.mjs` with `tools/cairn-rules.mjs`,
`tools/cairn-fixture.test.mjs` and `tools/soundness.md`, `cairn-close`
step 2, `requestTemplate` in `tools/cairn.mjs` and `tools/cairn-audit.mjs`).

Deliberately not in this unit: ADR-019 and ADR-020 (S03, S04), the 1.1
architecture page and the roadmap register (S05), and any change under
`spec/`, `skills/`, `tools/`, `site/`, `.github/` or `cairn.config.json`.
The notes are read, not written.

#### Work

```cairn-unit
step: S02
unit: 02
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Wrote `docs/adr/ADR-017-the-review-movement.md`**, two decisions. The
  reader is a second context of the writer's own agent given the diff and
  the two criteria and nothing else — not the plan, not the conversation —
  so that it needs no paid plan and no personal connector, which is what
  the owner asked for; the harness's review command is that reader where
  one exists. The movement sits between self-review and verify, and the
  record says why that place: after the writer's last cut, before the
  gates judge the diff a finding may change.
- **Named the rule `review`**, blocking, reading presence and non-emptiness
  of a `#### Review` section in the current unit's step file — the same
  file and the same kind of reading the rule `work-unit` does for the
  `cairn-unit` block — with its fixture holding a merged trunk commit as
  ADR-004 decision 4 requires. Naming a new rule follows ADR-003, which
  named `writes-overlap`. The `closure` type carries no step file and so
  no review.
- **Weighed the manifesto's test in the record**: the manifesto's own
  statement of the code lists *planning, coding, reviewing, tests* as
  isolated steps, so the movement restores a step the manifesto names; the
  reader is native; the rule reads one section in a file already opened.
  The cost, one context per unit, is stated and accepted on the Crumbz
  count of nine correctness findings.
- **Wrote `docs/adr/ADR-018-two-sentences-of-the-cycle.md`**, two
  decisions, each one sentence in a skill. Decision 1 says explicitly that
  it does not reopen the size rule ADR-016 refused with Q3. Decision 2
  keeps the checkboxes and the digest of ADR-002 untouched: the item-by-
  item answer lives in the request's description, printed by the audit
  tool from the record.
- **Corrected one phrase of the decisions page in the record**: the page
  says the audit's *first question* becomes the item list, but the four
  coherence questions ask about the diff, not the definition of done; the
  record says the description's coherence section *opens with* the lines.
  The page is not edited.
- **Added both lines to `docs/adr/index.md`.**
- **For S05:** `tools/cairn-audit.mjs` is in no row's `writes:` of the
  1.1 register today; the register must place it when row 1, 2 and 4 are
  widened — most likely row 3, which owns the tools, or row 1 beside the
  request template it prints.

#### Self-review

- `delete:` a paragraph of ADR-017 that listed Claude Code's `/code-review`
  and Codex's `/review` by name as the reader; the record names the
  harness's review command generically, because a named command is a
  personal configuration by another route.
- `delete:` a sentence in ADR-018 decision 2 proposing that the audit tool
  parse the ticked state of each box; ADR-002 says the boxes are never
  edited after acceptance, so there is nothing to parse.
- `yagni:` a numbering of the unit skill's steps in ADR-017 — the record
  names the movement by its place and lets path 1 renumber.
- `yagni:` a fixture for ADR-018 — nothing enters the checker; the record
  says *no fixture*.
- `shrink:` ADR-017's context, from the note's full side-by-side table to
  the one row that matters, the reader who is not the writer, with the
  Crumbz count.
- Found and added: ADR-017's consequences did not say what the rule does
  to steps written before it exists; one clause now says it reads the
  current unit's step only.
- No widening of `writes:`: every file of this unit is under `docs/adr/`
  or this path's folder. Net: two paragraphs cut from the drafts; lean.

#### Review

The movement ADR-017 decides is not yet in the unit skill; this unit ran
it anyway. A fresh context of the writer's agent was given the diff of the
two records and the index, and the two criteria of ADR-016 decision 4,
nothing else. Sixteen findings, eight under the ladder and eight on
correctness; each with what was done:

- `delete:` the Q7 *nothing changes* bullet repeats the Q6 *nobody new*
  one — refused: they are two questions' options, and the definition of
  done names every refused option.
- `delete:` the consequence that the unit skill's steps renumber — fixed,
  removed; it also contradicted ADR-018's *step 2*, which is today's name.
- `delete:` the consequence on two sections with two authors repeating
  decision 2 — fixed, shrunk to one line on what a reader can tell apart.
- `delete:` the preamble saying twice that the criterion is ADR-016's —
  fixed, once.
- `delete:` ADR-018's manifesto section repeating the tags — fixed,
  shrunk; the section itself stays, the definition of done requires it.
- `yagni:` ADR-018 decision 1 defending against reopening Q3 — fixed,
  shrunk to one sentence; it stays because ADR-016 says that half returns
  here.
- `shrink:` ADR-017's manifesto section restating context — refused: Q7 is
  the one answer that adds a rule, and the weighing is written out there.
- `shrink:` ADR-018 decision 2 listing its surfaces twice — fixed, the
  body keeps the sentence and *what this changes* keeps the files.
- `correct:` "the three agent products" after a list naming two — fixed,
  Copilot named among the cycles.
- `correct:` Beck credited with a second reader in one record and with
  test-first rules in the other — fixed: Beck is that reader himself.
- `correct:` "the criterion" singular against "the two criteria" — fixed.
- `correct:` "first rule that reads a section of a step file" against
  `work-unit` reading the block — fixed: the checker reads a second thing
  in the same file.
- `correct:` the renumber consequence against ADR-018's *step 2* — fixed
  by the deletion above.
- `correct:` one surface named `skills/cairn-unit/SKILL.md` in one table
  and `cairn-unit` in the other — fixed, `cairn-unit` in both.
- `correct:` "the third option, refused below" where two are listed —
  fixed: the page's third option.
- `correct:` the ADR-018 index line counting two refusals where the
  record has four — fixed.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main, 7 changed files on the branch since the trunk (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S03
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-017-the-review-movement.md, docs/adr/ADR-018-two-sentences-of-the-cycle.md, docs/adr/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S03 — ADR-019, areas and the architecture page (Q10 to Q12), from research note 3 at blob 945a201e; names cairn-open step 1, the architecture template in tools/cairn.mjs, spec/reference/configuration.md; builds on ADR-010 decision 2
blockers    : none
```
