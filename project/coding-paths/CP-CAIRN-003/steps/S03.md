---
type: Cairn Coding Path Step
title: 'CP-CAIRN-003 S03 — ADR-019, an area is a folder of the tree'
timestamp: 2026-09-07T00:00:00Z
cairn:
  path: CP-CAIRN-003
  step: S03
---

# CP-CAIRN-003 S03

### S03 — ADR-019, an area is a folder of the tree — **COMPLETE**

#### Plan

Write ADR-019 from section C of the coding-guidelines decisions page at
blob `48112c31` and research note 3 at blob `945a201e`: an area is a
folder of the tree (Q10); the architecture page says in one sentence which
way dependencies point (Q11); a path names its areas, `writes:` is their
patterns, and a path that needs a whole source root says why (Q12). Build
on ADR-010 decision 2 and supersede nothing. Answer in the record the
question the owner raised in the chat of 2026-09-07 after S01 — Crumbz's
one module note for many components — by saying whose decision the split
is and which refused option would have flagged it. List the record in
`docs/adr/index.md` after ADR-018.

Definition-of-done items advanced: the first (four of the five records
now exist in shape, indexed with no gap), the second for Q10 to Q12 (each
ticked answer named by one record, each refused option named with its
tag), and the fourth (the record names `cairn-open` step 1,
`spec/reference/configuration.md`, `spec/concepts/architecture.md`,
`docs/architecture/index.md` and `tools/cairn.mjs`).

Deliberately not in this unit: ADR-020 (S04), the 1.1 architecture page
and the roadmap register (S05), and any change under `spec/`, `skills/`,
`tools/`, `site/`, `.github/` or `cairn.config.json`. The notes are read,
not written.

#### Work

```cairn-unit
step: S03
unit: 03
type: decision
verified: cairn-check, cairn-active --check, test
```

- **Wrote `docs/adr/ADR-019-an-area-is-a-folder-of-the-tree.md`**, three
  decisions, each one sentence in a skill, a reference or a page.
- **Found that the kit has no architecture template.** The plan and the
  decisions page name *the architecture template in `tools/cairn.mjs`*
  as Q11's surface; `planInstall` writes a module-note template and the
  folder indexes, and nothing for an architecture page. The record says
  so and names the three places that state a page's shape today — the
  concept, the architecture index, the kit's index generator — with a
  template, if path 4 adds one, as the carrier. The plan's line is not
  edited; this step records the correction.
- **Put the owner's concern into the context**, quoted from the chat of
  2026-09-07, and answered it where it belongs: the split is ADR-010
  decision 2's, implemented by no path yet; the refused Q12 advisory was
  the one check that would have flagged Crumbz's whole-root paths, and
  the alternatives say it was offered twice and not taken.
- **Named the Crumbz facts by count, not by file**: eight of sixteen paths
  on `src/**`, a note grown to four hundred lines, two imports across a
  named line, forty-nine dead exports; every number is the note's.
- **Added the ADR-019 line to `docs/adr/index.md`.**

#### Self-review

- `delete:` a paragraph of decision 1 proposing Crumbz's `areas` entries
  by name — the record is not Crumbz's configuration; one clause now says
  *one note per main folder* and names four folders as an example.
- `yagni:` a sentence saying how `writes:` should be written when a path
  spans two folders; ADR-010 decision 2 already puts a two-area path on
  the `full` route and the record cites it.
- `shrink:` the context, from the note's import table to the one-way
  direction and the duplication count; the record links the section.
- Found and fixed: the plan's *architecture template* named a surface
  that does not exist; the record names what does.
- No widening of `writes:`: every file of this unit is under `docs/adr/`
  or this path's folder. Net: one paragraph cut from the draft; lean.

#### Review

A fresh context of the writer's agent was given the diff of the record
and the index line, and the two criteria of ADR-016 decision 4, nothing
else. Twelve findings, six under the ladder and six on correctness; each
with what was done:

- `shrink:` decision 3 is one line, its only new content being the
  whole-root clause — fixed, shrunk to what ADR-010 does not say.
- `delete:` the manifesto section's first sentence recounting the tags —
  fixed, removed; the section keeps its two substantive sentences.
- `shrink:` decision 2's second paragraph enumerating the three surfaces
  the table names — fixed, one clause on why they and not a template.
- `delete:` the first consequence restating the Q10 refusal's reason —
  fixed, the bullet keeps only the `areas` entries and the routing rule.
- `delete:` the consequence repeating that a reader can check the
  sentence by hand — fixed, removed.
- `delete:` *nothing enters the checker* said three times — fixed, once,
  in the manifesto section where every record of the folder says it.
- `correct:` the tag count inverted, one *simplest* and two *one
  sentence* — fixed by removing the counting sentence.
- `correct:` the owner's words say two notes and the context says one —
  fixed: two files in the folder, counting its index.
- `correct:` the option tagged *in a template* while the record says no
  template exists — fixed, the decision now says so in its own words
  before naming where the sentence goes.
- `correct:` decision 3 claiming the `full` route with no surface
  carrying it — fixed, the claim removed; ADR-010 decision 2 already
  routes a path spanning two areas.
- `correct:` the preamble reading as if ADR-010 said what a component is
  — fixed.
- `correct:` the title saying *area* and decision 1's heading saying
  *component* — fixed, the heading says *area*.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, base origin/main, 9 changed files on the branch since the trunk (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
user review           : not required for the unit; the owner reads the record with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-019-an-area-is-a-folder-of-the-tree.md, docs/adr/index.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S04 — ADR-020, the one edge and the one writer confirmed (Q13 to Q15), from research note 4 at blob a52ac0ec; a record of what was weighed and kept, as ADR-008 was; one sentence in cairn-unit on the helper agent
blockers    : none
```
