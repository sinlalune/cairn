---
type: Cairn Coding Path Step
title: 'CP-CAIRN-012 S01 — the records: ADR-030, ADR-031, ADR-032'
timestamp: 2026-09-21T00:00:00Z
cairn:
  path: CP-CAIRN-012
  step: S01
---

# CP-CAIRN-012 S01

### S01 — the records: ADR-030, ADR-031, ADR-032 — **COMPLETE**

#### Plan

Write themes 1 and 2 of the asks note as three records, as the plan's
first item says: ADR-030, what the open skill reads before the go-ahead
(K01, K02); ADR-031, one place for a fact — the measured figure, the
register's state cells, the word *pending* (K03, K04, K05); ADR-032,
registering on a trunk that takes no direct push — the precondition, the
`pull-request` sequence, the `registration` rule reading the change under
review, the installer refusing the pairing (K06, K07, K08, K20). ADR-032
supersedes two clauses of ADR-001 decision 1 for the `pull-request`
registration transport and says why the installer may read the host that
ADR-029 forbids the checker; ADR-029 is not touched, as the owner's answer
to Q2 says. Nine of the thirty-six lines are then named by one record
each.

Definition-of-done items advanced: the first (three records in the
layout's shape, promoted from both notes at their pinned blobs), the
second for K01 to K08 and K20, the third for ADR-001 decision 1 — the
record names the clauses, ADR-001 gains the line, the 1.1 page's sentence
is marked *superseded by* — and the fifth, every decision naming its
surface by today's name.

Type `decision`: the records are the change, and every document they
amend moves with them — ADR-001, the records' index, the 1.1 page and the
architecture index. No test: nothing under `tools/` changes, and no rule
is added or removed here.

Where this unit departs from the plan: the plan leaves the 1.1 page's and
the superseded records' marks to S05; they are written here, because a
superseded clause with no mark leaves the tree saying two things between
units, and the unit type asks for the amended documents in the same
commit. The register's 1.2 row is not touched: it names every record at
S05, and the word *pending* ADR-031 decision 3 first applies to ADR-015
needs the row's clause to exist, so both halves are S05's, and the record
says so.

Deliberately not in this unit: the twelve records of themes 3 to 7
(S02 to S04), the 1.2 page, the indexes' 1.2 lines beyond the three
records, the register (S05), and every skill, tool, reference and kit
file the records name — the coding paths' work.

Found while writing, and settled in the record rather than left: ADR-024
superseded the kit's-default clause of ADR-001 decision 1 on 2026-09-11
and ADR-001 was never marked for it; the line this unit adds under
decision 1 names both supersessions. The layout reference has no register
row, so ADR-031 names the register template `tools/cairn.mjs` installs
instead. The pilot registers on the trunk directly on both transports, so
ADR-032 decision 2 names it among the surfaces the sequence changes. The
test that checks the kit's count compares the lock to the plan and pins
no literal, so ADR-031 names three restatements and not the five the
source note counted.

#### Work

```cairn-unit
step: S01
unit: 01
type: decision
verified: cairn-check, cairn-test
```

- **`docs/adr/ADR-030-what-the-open-skill-reads-before-the-go-ahead.md`**,
  new and `accepted`, dated 2026-09-21: two decisions, two sentences in
  `cairn-open` step 2; three alternatives refused, a rule and a template
  field among them.
- **`docs/adr/ADR-031-one-place-for-a-fact.md`**, new and `accepted`:
  three decisions — the figure on the conformance page and three
  restatements pointed at it, one sentence in the close skill; the
  register's state cells generated by `cairn-active`, one write and one
  comparison; the word *pending* with the register row naming the clause
  back, first used on ADR-015 decision 2 by S05 — six alternatives
  refused, a rule among them.
- **`docs/adr/ADR-032-registering-on-a-trunk-that-takes-no-direct-push.md`**,
  new and `accepted`: four decisions — the precondition sentence; the
  `pull-request` sequence, the same commit through a request; the
  `registration` rule reading a registration in the change under review,
  two fixtures; `init` and `adopt` refusing the two pairings, the
  installer's one GitHub reading and why it may make it — seven
  alternatives refused, a checker rule and a question to the owner among
  them. Supersedes two clauses of ADR-001 decision 1 for `pull-request`
  registration.
- **`docs/adr/ADR-001-sole-owner-opens-and-closes-a-path.md`**: the
  status line names the two clauses and ADR-032; one paragraph under
  decision 1 says which clauses, and names ADR-024's earlier supersession
  of the kit's-default clause. The first token after `Status:` stays
  `accepted`.
- **`docs/adr/index.md`**: the three records listed after ADR-029 with no
  gap; ADR-001's line names the two clauses.
- **`docs/architecture/01-cairn-1-1.md`**: one paragraph at the head,
  *Marked from 2026-09-21*, saying the page is not amended and what a
  mark means; the sentence *there is no `register/` branch and no
  registration request* in *How a path opens* keeps its words and gains
  *superseded by ADR-032*.
- **`docs/architecture/index.md`**: the page's line records the mark.

#### Self-review

- `correctness:` ADR-032 decision 2's first draft said the pilot
  *registers by a direct push on both transports*; the pilot commits on a
  local trunk. Corrected to *lands the registration on the trunk
  directly*.
- `correctness:` ADR-032 decision 2 said the wait ADR-001 removed was
  *for a reviewer who is the writer*; ADR-001's wait was the sole owner
  merging their own plan request. Corrected.
- `correctness:` ADR-031 decision 2 named a register row of the layout
  reference that does not exist; cut, the installed template named.
- `yagni:` ADR-031 decision 1 named ADR-038 for what says *treated*, a
  number the plan fixes and no record yet carries; it names K27.
- `shrink:` ADR-031 decision 3 named a coding path for one sentence of
  the records' index; the three surfaces are records and the register,
  this path's own, and the decision says S05 writes them.
- `delete:` `package-lock.json`, written by `npm install` in this fresh
  worktree and untracked; removed, not committed, as CP-CAIRN-010 S01 did.
- No widening of `writes:`: every file of this unit is under `docs/adr/**`,
  `docs/architecture/**` or this path's folder.
- `shrink:` after the two reads, the three records lost a consequence
  restating a decision each, a manifesto sentence restating a decision
  each, one alternative said already in the context, and the K27 aside;
  ADR-032's description is a third the length.
- Net: three records, 494 lines; 29 lines added and 4 removed across the
  four amended documents; and this step.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's
diff in one file and the two criteria and nothing else; it returned in
335 seconds with twenty-three findings, on the first attempt. Each with
its disposition:

- `correctness:` ADR-031 placed `INSTALLER_ROW` in `tools/cairn.mjs`; it
  is `cairn-active`'s name for the row `cairn.mjs` writes as a literal —
  **fixed**, the decision and the table say whose name it is.
- `correctness:` the installer's header comment carries no file count —
  **fixed**: the context says which count stands where — the file count
  in the budget table, the kit row and the README; the skill count in the
  README, the module note and the comment.
- `correctness:` the conformance page carries the count twice, the budget
  table and the kit row, so *one place* named one row and left a copy on
  the same page — **fixed**, the budget row is the place and the kit row
  is the first of the four restatements.
- `correctness:` ADR-008 decision 4 is the journal's key, not a date —
  **fixed**, the sentence cut with the next finding.
- `yagni:` ADR-031 decision 2's sentence on several paths and a date
  decided nothing — **fixed**, cut.
- `correctness:` *one generated column* against cells in two tables —
  **fixed**, *generated cells*.
- `correctness:` the index said `init` and `adopt` refuse the profile
  pairing while the record says `init` never reaches it — **fixed**, the
  index entry names `adopt` for that pairing.
- `correctness:` ADR-001's status line and index line omitted ADR-024's
  supersession that the new paragraph names — **fixed**, both name it.
- `delete:` ADR-030's consequence restating decision 2's change — **fixed**.
- `delete:` ADR-030's manifesto section stating *two sentences, no rule*
  a third time — **fixed in part**: the tag cut, the section kept at one
  sentence because every record carries it.
- `shrink:` ADR-030's context retelling the two observations — **refused**:
  a record states its context, and a reader should not need the note
  open to know what the record stands on.
- `delete:` ADR-031's alternative repeating *four units and one more
  copy* — **fixed**.
- `delete:` ADR-031's two consequences repeating decisions 2 and 3 —
  **fixed**, both cut.
- `delete:` ADR-031's K27 aside — **fixed**, cut; the promoted-from
  paragraph carries A3's warning.
- `shrink:` ADR-031 decision 1's close-skill sentence restating the
  decision — **fixed**, only where the sentence goes.
- `shrink:` ADR-031's manifesto section repeating the consequences —
  **fixed**, two sentences.
- `shrink:` ADR-032's installer-may-ask point in five places — **fixed in
  part**: cut from the consequences; kept in the head paragraph as a
  pointer, in decision 4 where it is decided, in the alternative that
  names ADR-029, and in the manifesto section, where the tag *adds a host
  reading* is what is weighed.
- `delete:` ADR-032's migration-exception alternative, said in the
  context — **fixed**, cut; six alternatives, and the index follows.
- `shrink:` ADR-032 saying twice that `init` refuses the profile —
  **fixed**, decision 4 says *at `adopt`, the one command that meets it*.
- `shrink:` ADR-032's consequences 1 to 3 restating decisions 1 to 3 —
  **fixed in part**: 2 and 3 cut; the first kept, because Atomik's path
  registering is the outcome and not a decision.
- `delete:` ADR-032's manifesto sentence *a case, not a predicate* —
  **fixed**, cut.
- `shrink:` ADR-032 decision 2's sentence on the wait ADR-001 removed —
  **refused**: the clause superseded was decided for that wait, and the
  one sentence saying why the wait does not return is what keeps the
  supersession coherent with the record it supersedes.
- `shrink:` ADR-032's hundred-word description, restated by the index —
  **fixed in part**: the description is a third the length; the index
  entry keeps the index's shape, which restates every record.

Second read: a fresh context, a subagent of this harness, given only the
diff of those fixes and the same two criteria; it returned with three
findings, on the first attempt. Each with its disposition:

- `correctness:` the module note restates the file count too, so it
  stood in four places, not three — **fixed**, the context counts four.
- `correctness:` *one row* for two counts, when the budget table has a
  row for the file count and none for the skill count — **fixed**:
  decision 1 says one row per figure, and the consequence says each
  count is written once.
- `correctness:` *one more read of the records* for a decision that adds
  a write — **fixed**, *one more write, from the records it already
  reads*.

The fixes of the second read are not read a third time; the self-review
above carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this record complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare in this fresh worktree after the records were written; nothing under tools/ changed in this unit
cairn-active --check  : pass — the view is already current; no path state changed
record shape          : the `schema` rule read ADR-030, ADR-031 and ADR-032 green — id matches the file name, status accepted in both halves, ISO date; ADR-001 keeps `accepted` as the first token after `Status:`
measurements          : the two governing blobs by `git rev-parse HEAD:<path>`; the installer's comment, the conformance page's two count rows, the module note, the count test, the pilot, the layout reference and `INSTALLER_ROW` read by command, not from memory
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's three fixes not read again
user review           : not required for the unit; the owner reads the records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-030, ADR-031, ADR-032 (new), ADR-001 (status line and one paragraph), docs/adr/index.md, docs/architecture/01-cairn-1-1.md (head paragraph and one mark), docs/architecture/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S02 — theme 3, ADR-033 to ADR-036, ADR-016 decision 1 superseded and marked
blockers    : none
```
