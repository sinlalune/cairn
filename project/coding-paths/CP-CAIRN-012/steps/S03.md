---
type: Cairn Coding Path Step
title: 'CP-CAIRN-012 S03 — the records: ADR-037, ADR-038, ADR-039'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-012
  step: S03
---

# CP-CAIRN-012 S03

### S03 — the records: ADR-037, ADR-038, ADR-039 — **COMPLETE**

#### Plan

Write themes 4, 5 and 6 of the asks note as three records, as the plan's
third item says: ADR-037, a repository declares its link exemptions
(K21); ADR-038, the channel, second edition (K22, K26, K27, K28),
superseding the reading half of ADR-028 decision 2; ADR-039, the release
notes name what a release absorbed (K31). The six other lines of those
themes — K23, K24, K25, K29, K30, K36 — are ADR-034's since S02.
Thirty-two of the thirty-six lines are then named by one record each,
K27 by the record covering the channel as the convention it keeps.

Definition-of-done items advanced: the first (three records in the
layout's shape, promoted from both notes at their pinned blobs), the
second for the six lines above, K27 included, the third for ADR-028
decision 2 — the record names the half, ADR-028 gains the line, the 1.1
page's feedback-file row is marked *superseded by* — and the fifth,
every decision naming its surface by today's name.

Type `decision`; the amended documents move with the records — ADR-028,
the records' index, the 1.1 page and the architecture index. No test:
nothing under `tools/` changes, and no rule is added or removed. As S01
decided, this unit marks what it supersedes.

Deliberately not in this unit: the four records of theme 7 (S04), the
1.2 page, the register (S05), and every tool, skill, reference,
configuration field and kit file the records name — the coding paths'
work.

Found while writing, and settled in the record rather than left: no
rule of the checker reads a note's frontmatter `type` — `schema` reads
path and decision records by their folders — so ADR-038 decision 5
defines `Cairn Feedback` as the folder's type and adds no rule, rather
than the reading the asks note forecast; the conformance page carries no
literal *Exemptions: none* — the `links` row's last cell reads *none* —
so ADR-037 names that cell; Cairn 1.1.0 has no release notes anywhere,
in the repository, on the site or on the host, so ADR-039 decides the
surface, a `CHANGELOG.md` at the root shipped by the package's `files`
list; the asks note left whether `cairn.status` follows a treated note's
move to this record, and ADR-038 decision 3 keeps it as it is.

#### Work

```cairn-unit
step: S03
unit: 03
type: decision
verified: cairn-check, cairn-test
```

- **`docs/adr/ADR-037-a-repository-declares-its-link-exemptions.md`**,
  new and `accepted`, dated 2026-09-21: two decisions — the declared
  list with a reason each, read by the `links` rule; the conformance
  page's sentence and `adopt` naming the shape — four alternatives
  refused, the exemptions back in the checker among them.
- **`docs/adr/ADR-038-the-channel-second-edition.md`**, new and
  `accepted`: five decisions — the corpus, the installed folder and the
  route, the treated convention with `cairn.status` unchanged, the
  correction line, the type — seven alternatives refused, a status
  field, a generated view and a rule among them. Supersedes the reading
  half of ADR-028 decision 2.
- **`docs/adr/ADR-039-the-release-notes-name-what-a-release-absorbed.md`**,
  new and `accepted`: one decision, the changelog with its first section
  the 1.2 release's and ADR-015 decision 2's line given its place; four
  alternatives refused, a release on the host among them.
- **`docs/adr/ADR-028-a-feedback-file-when-nothing-broke.md`**: the
  status line names the half and ADR-038; one paragraph under decision 2
  says which half. The first token after `Status:` stays `accepted`.
- **`docs/adr/index.md`**: the three records listed after ADR-036 with
  no gap; ADR-028's line names the half it lost.
- **`docs/architecture/01-cairn-1-1.md`**: the feedback-file row of the
  tools table keeps its words and gains *superseded by ADR-038* where
  it says *no tool*.
- **`docs/architecture/index.md`**: the page's line adds ADR-038 to the
  marks.

#### Self-review

- `correctness:` ADR-039 said `package.json`, *if its shipped files are
  listed*; they are, in a `files` list read by command; the hedge cut.
- `delete:` `package-lock.json`, written by `npm install` in this
  worktree, whose `node_modules` had gone again; removed before the
  gate.
- No widening of `writes:`: every file of this unit is under `docs/adr/**`,
  `docs/architecture/**` or this path's folder.
- `shrink:` after the two reads, the three records lost five
  restatements of their own decisions, one sentence that decided nothing
  and a point made three times; ADR-038 was rewritten whole for it.
- Net: three records, 411 lines; 11 lines added and 4 removed across the
  four amended documents; and this step.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's
diff in one file and the two criteria and nothing else; it returned in
306 seconds with twenty findings, on the first attempt. Each with its
disposition:

- `correctness:` ADR-037 said `adopt` lists the fixtures folder and the
  flat journal among the 0.2 shapes; `staleShapes` names neither —
  **fixed**, the context says `adopt` names neither folder.
- `correctness:` *the unit skill's last section* — section 7 is followed
  by *What you must not do* — **fixed**, section 7 named.
- `correctness:` *a step two directories up* — one — **fixed**.
- `correctness:` *nine such links rewritten one level deeper* — the move
  rewrote four inside a moved note, nine in `project/` files already in
  the corpus — **fixed**, the sentence cut with the next finding.
- `correctness:` *like the two planes and the skills* against four roots
  — **fixed**, *like the other four*.
- `correctness:` the layout reference has no `feedbacks/` row —
  **fixed**, *one row, new* in decisions 3 and 5.
- `correctness:` the sources place decisions 3's and 4's sentences in
  ADR-028 and the record did not say it departs — **fixed**, the head
  paragraph says a record is not rewritten in place and this second
  edition carries them.
- `correctness:` decision 5 contradicts K22's reading that `schema`
  reads the type without saying so — **fixed**, the decision says the
  reading was wrong and what `schema` reads.
- `delete:` every *What this changes* paragraph, repeated by the table —
  **refused**, as in S02: the paragraph is the decision's prose, the
  table is the scope a coding path is opened from.
- `delete:` ADR-037's two consequences restating decisions 1 and 2 —
  **fixed**, cut.
- `delete:` ADR-038's supersession said four times — **fixed in part**:
  cut from decision 1; kept in the description, the head paragraph and
  the consequence that names the marks.
- `shrink:` the owner's ruling told in the context and in decision 3 —
  **fixed**, the context's paragraph cut to one sentence.
- `delete:` the moved note's links said three times — **fixed**, kept in
  decision 3 alone.
- `delete:` a consequence restating decision 2 — **fixed**, cut.
- `shrink:` *by being looked at* twice — **fixed**, cut from the
  alternative.
- `shrink:` *and does not start* — **fixed**, cut.
- `yagni:` ADR-039's sentence on a 1.1.0 section written after the fact
  — **fixed**, cut.
- `delete:` ADR-039's two consequences restating decision 1 — **fixed**,
  cut.
- `shrink:` ADR-028's mark repeating the paragraph below it — **fixed**,
  one line naming the two rules.
- `shrink:` the 1.1 page's mark carrying the installed folder, which the
  superseded clause does not cover — **fixed**, cut.

Second read: a fresh context, a subagent of this harness, given only the
diff of those fixes and the same two criteria; it returned with six
findings, on the first attempt. Each with its disposition:

- `correctness:` `schema` never reads the corpus — it reads path and
  decision records by their folders — so *`links` and `schema` cover
  the channel* was false in ADR-038 decision 1, its description, the
  ADR-028 mark and the 1.1 page's mark, and contradicted decision 5 —
  **fixed** in all four: `links` covers the channel; decision 1 says
  Q12's option named `schema` and why the rule has nothing to read
  there; the index line follows.
- `correctness:` *two of the sentences* placed in ADR-028 by the
  decisions page — one — **fixed**.
- `correctness:` *the one edit a frozen note takes* against decisions 1
  and 4 — **fixed**, the clause cut.
- `correctness:` *neither folder* where the journal is a file — **fixed**,
  *neither the folder nor the file*.
- `shrink:` decision 5's two sentences correcting the asks note —
  **fixed**, cut; the correction sits in decision 1 where `schema` is
  named.

The fixes of the second read are not read a third time; the self-review
above carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this record complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare in this worktree after the records were written; nothing under tools/ changed in this unit
cairn-active --check  : pass — the view is already current; no path state changed
record shape          : the `schema` rule read ADR-037 to ADR-039 green — id matches the file name, status accepted in both halves, ISO date; ADR-028 keeps `accepted` as the first token after `Status:`
measurements          : the two governing blobs unchanged since S01; the corpus roots, the schema rule's reads, the configuration schema, `staleShapes`, the notes' frontmatter types, the package's file list and the absence of any 1.1.0 release notes read by command, not from memory
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's five fixes not read again
user review           : not required for the unit; the owner reads the records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-037, ADR-038, ADR-039 (new), ADR-028 (status line and one paragraph), docs/adr/index.md, docs/architecture/01-cairn-1-1.md (one mark), docs/architecture/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S04 — theme 7, ADR-040 to ADR-043, ADR-002 decision 2 superseded and ADR-017 amended, both marked
blockers    : none
```
