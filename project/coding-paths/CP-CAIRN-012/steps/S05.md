---
type: Cairn Coding Path Step
title: 'CP-CAIRN-012 S05 — the 1.2 page, the indexes, the register and the first pending'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-012
  step: S05
---

# CP-CAIRN-012 S05

### S05 — the 1.2 page, the indexes, the register and the first *pending* — **COMPLETE**

#### Plan

Write the last unit the plan names: the 1.2 architecture page under
`docs/architecture/`, in the 1.1 page's sections, naming every record
ADR-030 to ADR-043 where the page relies on it and stating what 1.2
removes from 1.1; the 1.1 page's head paragraph linking it; the
architecture index's line for it; the records' index shape sentence for
*pending* and the first *pending* in ADR-015's table, with the clause
the register's row names back (ADR-031 decision 3); and the register's
1.2 row naming this path as the promotion and the five coding paths that
build 1.2, each *no path yet*, scoped from the records' implementation
tables. Then the candidate and the closing, under this path's own
ADR-040 and ADR-043.

Definition-of-done items advanced: the fourth (the 1.2 page), the sixth
(the register's row), the fifth's last clause (the coding paths scoped
from the records alone), and the seventh, kept: the two governing notes
and the ten notes are untouched, and nothing under `spec/`, `skills/`,
`tools/`, `site/` or `.github/` changed on this branch.

Type `decision`: the page and the register are what the records amend
last, and they move with the index lines and the one word in ADR-015.
No test: nothing under `tools/` changes.

Deliberately not in this unit: every skill, tool, reference, template
and kit file the records name — the coding paths' work, now scoped in
the register; the state cells of the register generated (ADR-031
decision 2) — the row is hand-written in today's shape, as the decision
says it is until the coding path lands.

Found while writing, and settled here: two of the five coding paths
share files with an earlier one — the layout reference between paths 1
and 3, the tools' module note between 2, 3 and 5, the installer's file
between 3 and 4 for the generated workflow and the register template —
so the table says which path depends on which, as the 1.1 table did;
the register's 1.2 row and ADR-015's cell name each other, as ADR-031
decision 3 requires of the first *pending*; the 1.2 page's diagram adds
one node at the right, the channel, that nothing depends on, so the
dependency sentence stays true.

#### Work

```cairn-unit
step: S05
unit: 05
type: decision
verified: cairn-check, cairn-test
```

- **`docs/architecture/02-cairn-1-2.md`**, new: the 1.1 page's eleven
  sections, two of them renamed to what the definition of done asks —
  *What the installer reads and reports*, *What a record and the
  register may say* — and one added, *The channel between an adopter
  and Cairn*; one Mermaid diagram with the channel as its last node and
  the dependency sentence; three tables — the checker's four new
  readings, the installer's nine, the tools — and the list of eight
  things 1.2 removes from 1.1 with the two it keeps.
- **`docs/architecture/01-cairn-1-1.md`**: the head paragraph of
  2026-09-21 links the 1.2 page.
- **`docs/architecture/index.md`**: the 1.2 page's line.
- **`docs/adr/index.md`**: the shape section says a row may say
  *pending*, and what the row and the register's row each name.
- **`docs/adr/ADR-015-a-release-reaches-an-edited-file.md`**: row 2 of
  the implementation table carries *pending*, naming row 5 of the
  register's 1.2 coding paths, the release path, and the date.
- **`project/coding-paths/index.md`**: the 1.2 row names the fourteen
  records, the seven themes, the five supersessions, this path as the
  promotion and the five coding paths below, and its state *running*;
  the table *The coding paths of 1.2* gives each path its outcome, its
  records by decision, its writes and *no path yet*, row 5 naming
  ADR-015 decision 2 back.

#### Self-review

- `delete:` `package-lock.json`, written by `npm install`; removed before
  the gate.
- No widening of `writes:`: every file of this unit is under
  `docs/adr/**`, `docs/architecture/**`, the register or this path's
  folder.
- `shrink:` after the two reads, the 1.2 page lost fourteen lines of
  restatement — two records each said four times, a copied sentence, a
  five-line description — and the register's row its fourteen titles;
  the page was rewritten whole for it.
- Net: the 1.2 page, 278 lines; 29 lines added and 5 removed across the
  four amended documents and the register; and this step.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's
diff in one file and the two criteria and nothing else; it returned in
342 seconds with twenty-five findings, on the first attempt. Each with
its disposition:

- `correctness:` *three restatements of a measured count* against the
  four ADR-031 names — **fixed**, four.
- `correctness:` *five 1.1 decisions superseded* over a list of four —
  **fixed**, *four, one of them in two clauses*.
- `correctness:` *in seven themes* over a list of fourteen titles —
  **fixed**, the row names the record range and no titles.
- `correctness:` *ten notes* where the folder holds nine beside the two
  governing notes, and path 6's journal entry omitted — **fixed**, nine
  notes, the journal entry and the two brainstorm notes, in both places.
- `correctness:` *no shape of a record* changes, against ADR-042's plain
  list and ADR-031's *pending* — **fixed**, the sentence says what
  changes in a record.
- `correctness:` *like the two planes and the skills* against four roots
  — **fixed**, *like the other four roots*.
- `correctness:` ADR-032 decision 2's pilot cited by no row — **fixed**,
  row 3 cites it and row 1 says the pilot is row 3's.
- `correctness:` ADR-035's skills line of this repository's `AGENTS.md`
  cited by no row — **fixed**, row 4 cites it.
- `correctness:` ADR-037 decision 2's conformance sentence cited by a row
  that writes no conformance page — **fixed**, row 2 cites the sentence
  and row 3 the `adopt` shape.
- `correctness:` shared files between rows with no `depends_on` named —
  **fixed**, the table's introduction names every pair and the file it
  shares, and the two row-level notes go.
- `correctness:` ADR-015's cell and the register's row dating the first
  *pending* differently — **fixed**, neither carries a date.
- `correctness:` *ECOS running its first three paths* unsupported —
  **refused**: the decisions page's section G says *ECOS ran its first
  three paths on 1.1.0 between 17/09 and 18/09*.
- `shrink:` the page's five-line description — **fixed**, one sentence.
- `shrink:` the dependency sentence copied from the 1.1 page — **fixed**,
  *as on the 1.1 page*, plus the one node added.
- `delete:` the pedagogy section, whose content is *nothing changes* —
  **refused**: the definition of done binds this page to the 1.1 page's
  sections, and that nothing changes there is what the section states;
  two sentences.
- `delete:` *What this page does not say*, a copy of the 1.1 page's —
  **refused** for the same reason; **fixed in part**: two sentences, the
  second the register's scoping, which the head paragraph no longer
  repeats.
- `shrink:` ADR-031 decision 3 stated four times on the page — **fixed**,
  cut from the closing section and the documentation-plane row; the
  register section carries it, and the records' index the sentence the
  record mandates.
- `shrink:` ADR-038 decision 2 stated four times — **fixed in part**: the
  route cut from *How a path runs*, which now points at the channel
  section; the installer paragraph keeps the folder as what the kit
  installs, the table row keeps where it is.
- `shrink:` the `cairn` row of the tools table restating the installer
  table — **fixed**, it points at the table.
- `shrink:` the removals list repeating each superseding clause —
  **fixed**, titles and record numbers.
- `shrink:` the architecture index's 02 entry — **fixed**, one line.
- `shrink:` the register's 1.2 row naming fourteen titles — **fixed**,
  the range and the page.
- `shrink:` ADR-015's cell — **fixed**, the word and the owing row.
- `shrink:` the table introduction's *pending* sentence — **fixed**, cut.
- `yagni:` the header and the last section both saying the register
  scopes the coding paths — **fixed**, the last section alone.

Second read: a fresh context, a subagent of this harness, given only the
diff of those fixes and the same two criteria; it returned with six
findings, on the first attempt. Each with its disposition:

- `correctness:` row 3 said to depend on row 2 for the configuration
  schema, a file row 3 does not write — **fixed**, with the next
  finding.
- `correctness:` the architecture index dating the page 2026-09-21 while
  the page and this unit are 2026-09-22 — **fixed**, 2026-09-22.
- `shrink:` the introduction's enumeration of shared files, which the
  Writes column already shows and would drift from — **fixed**, the rule
  and how to read the pairs off the column.
- `shrink:` the boxes and *pending* said in *What 1.2 is for* and again
  in their sections — **fixed**, cut from the opening.
- `delete:` *as the unit skill now says* — **fixed**, cut.
- `delete:` the cross-reference to the channel section — **fixed**, cut.

The fixes of the second read are not read a third time; the self-review
above carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this record complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare in this worktree after the page was written; nothing under tools/ changed on this branch
cairn-active --check  : pass — the view is already current; no path state changed
record shape          : the `schema` rule read the fourteen records green; `decision-drift` silent — the page changed with records beside it; `links` green over the 1.2 page's forty links
measurements          : the notes counted in the folder; the fourteen records' tables read for the five rows' records and writes; the 1.1 page's sections listed by command
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's six fixes not read again
user review           : the owner reads the fourteen records and the 1.2 page with the candidate, before the merge
remote                : pushed immediately after this record; its commit is the candidate's last unit, named in the resume section by the administrative commit
```

#### Checkpoint

```text
status      : running
current step: S05 complete on push
retention   : none (no-rewrite host)
changed     : docs/architecture/02-cairn-1-2.md (new), docs/architecture/01-cairn-1-1.md (one link), docs/architecture/index.md, docs/adr/index.md (the shape sentence), docs/adr/ADR-015 (one cell), project/coding-paths/index.md (the 1.2 row and the 1.2 coding-paths table), this path's index and this step
session     : safe boundary after a successful push
next action : close with cairn-close — the candidate, the fresh coherence read (ADR-043), A before the owner's reading (ADR-040), the request
blockers    : none
```
