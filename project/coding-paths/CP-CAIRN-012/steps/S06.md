---
type: Cairn Coding Path Step
title: 'CP-CAIRN-012 S06 — ADR-038 supersedes the whole no-tool clause of ADR-028'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-012
  step: S06
---

# CP-CAIRN-012 S06

### S06 — ADR-038 supersedes the whole no-tool clause of ADR-028 — **COMPLETE**

#### Plan

The closing coherence read of candidate `50f74b6` — a fresh context
given the diff, the two governing notes, the live view and the earlier
records, under ADR-043 as this path's own record has it — answered the
first question *Yes*: ADR-028 decision 2 says *no tool reads or writes
the folder*, ADR-038 superseded its reading half and said *no tool
writes the folder* stands, and ADR-038 decision 2 has the kit write
`feedbacks/` and its index in every adopter. A tool writing the folder,
unmarked. The other three questions were answered *No*. The reader also
named a softer tension, ADR-043 against ADR-016 decision 4's *and for
nothing else*; ADR-043 decision 1 already answers it — two reads, two
sets of questions, no criterion added to either — and it is left to the
owner's arbitration at the reading.

This unit corrects the first: ADR-038 supersedes the clause whole — the
`links` rule reads the folder, the kit writes the folder and its index,
no tool writes a note — in its description, its head paragraph and its
consequence; ADR-028's status line and mark, both indexes' lines, the
1.1 page's mark and the 1.2 page's removal bullet say the same. The
candidate is void by the close skill's own rule — implementation changed
after `C` — and the next commit is the new candidate.

Definition-of-done items advanced: the third, whose supersession now
names what was superseded exactly, and the ninth, the review against the
coherence questions, which found this. Type `decision`: a record's
supersession clause and every document that restates it. No test.

Deliberately not in this unit: anything else the coherence read did not
name; the closure surface, reverted uncommitted before this unit and
re-made after it.

#### Work

```cairn-unit
step: S06
unit: 06
type: decision
verified: cairn-check, cairn-test
```

- **`docs/adr/ADR-038-the-channel-second-edition.md`**: the description,
  the head paragraph and the consequence supersede the clause *no tool
  reads or writes the folder* whole, and keep *no tool writes a note*.
- **`docs/adr/ADR-028-a-feedback-file-when-nothing-broke.md`**: the
  status line and the mark under decision 2 name the clause.
- **`docs/adr/index.md`**: ADR-028's and ADR-038's lines follow.
- **`docs/architecture/01-cairn-1-1.md`**: the feedback-file row's mark
  says the kit installs the folder with its index, and no tool writes a
  note.
- **`docs/architecture/02-cairn-1-2.md`**: the removal bullet names the
  clause whole; **`docs/architecture/index.md`** and the register's 1.2
  row follow.

#### Self-review

- `correctness:` the reviewer's phrase *no tool writes the folder* was
  ADR-038's own, four times; every copy now says what is superseded and
  what stands, in the same words.
- Considered and kept: ADR-043 against ADR-016 decision 4, as the read's
  softer tension; the record argues it in decision 1, and the owner
  arbitrates it at the reading rather than this unit rewriting a record
  the read did not call contradicted.
- No widening of `writes:`.
- Net: 20 lines added and 17 removed across seven documents, and this
  step.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's
diff in one file and the two criteria and nothing else; it returned in
63 seconds with five findings, on the first attempt. Each with its
disposition:

- `shrink:` ADR-038's description explaining the supersession after a
  colon, which its first two clauses already say — **fixed**, cut at the
  colon.
- `shrink:` the 1.2 page's removal bullet, the only one carrying an
  explanation — **fixed**, bare like its siblings.
- `shrink:` the 1.1 page's mark explaining what the record says —
  **fixed**, the mark points at the record.
- `shrink:` the records' index's ADR-028 line repeating ADR-038's —
  **fixed**, the record and the date.
- `correctness:` the register's *no-tool clause* reads as decision 2's
  heading, which stands — **fixed**, the clause quoted in the words
  every other file uses.

Beside the unit's read, the fix was read once more for the four
coherence questions by a second fresh context given the fix's diff, the
records and the live view, as the candidate's read was (ADR-043): all
four **No**; ADR-038 and ADR-028 now agree on what is superseded and
what stands.

Second read: a fresh context, a subagent of this harness, given only the
diff of those fixes and the same two criteria; it returned with one
finding, on the first attempt:

- `shrink:` the register's parenthesis quoting the clause where its
  siblings are terse — **refused**: the first read found the terse form
  readable as decision 2's heading, which stands; the quoted clause is
  what is superseded.

The fixes of the second read are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this record complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare on the fixed tree; nothing under tools/ changed on this branch
cairn-active --check  : pass — the view is already current; no path state changed
record shape          : the `schema` rule read ADR-028 and ADR-038 green; the first token after `Status:` stays `accepted` in both
coherence             : the fix read once more by a fresh context for the four questions, all No; the first candidate's read, one Yes and three No, is carried into the request's description with this unit's answer
review                : two reads by fresh contexts for the two criteria, both obtained on the first attempt
user review           : the owner reads the fourteen records and the 1.2 page with the candidate, before the merge, and arbitrates the read's softer finding on ADR-043 against ADR-016 decision 4
remote                : pushed immediately after this record; this commit is the second candidate
```

#### Checkpoint

```text
status      : running
current step: S06 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-038 and ADR-028 (the supersession clause), docs/adr/index.md, docs/architecture/01-cairn-1-1.md and 02-cairn-1-2.md (the marks), docs/architecture/index.md, project/coding-paths/index.md (one phrase), this path's index and this step
session     : safe boundary after a successful push
next action : close — the candidate is this commit; audit, A before the reading (ADR-040), the request
blockers    : none
```
