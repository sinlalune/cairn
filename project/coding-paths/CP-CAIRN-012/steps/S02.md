---
type: Cairn Coding Path Step
title: 'CP-CAIRN-012 S02 — the records: ADR-033, ADR-034, ADR-035, ADR-036'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-012
  step: S02
---

# CP-CAIRN-012 S02

### S02 — the records: ADR-033, ADR-034, ADR-035, ADR-036 — **COMPLETE**

#### Plan

Write theme 3 of the asks note as four records, as the plan's second
item says: ADR-033, the installer reads what the repository declared and
says what it leaves (K12, K13, K14, K16); ADR-034, housekeeping with no
choice in it, second edition — the eleven corrections of Q16 (K09, K10,
K11, K15, K19, K23, K24, K25, K29, K30, K36); ADR-035, the seventh skill
`cairn-update` (K17); ADR-036, the kit ships Ponytail's skills where the
harness loads them (K18), superseding the first half of ADR-016
decision 1. Twenty-six of the thirty-six lines are then named by one
record each.

Definition-of-done items advanced: the first (four records in the
layout's shape, promoted from both notes at their pinned blobs), the
second for the seventeen lines above, the third for ADR-016 decision 1 —
the record names the half, ADR-016 gains the line, the 1.1 page's two
sentences on the pinned tag are marked *superseded by* — and the fifth,
every decision naming its surface by today's name.

Type `decision`; the amended documents move with the records — ADR-016,
the records' index, the 1.1 page and the architecture index. No test:
nothing under `tools/` changes, and no rule is added or removed. As S01
decided, this unit marks what it supersedes rather than leaving the
marks to S05.

Deliberately not in this unit: the seven records of themes 4 to 7 (S03,
S04), the 1.2 page, the register (S05), and every skill, tool, reference
and kit file the records name — the coding paths' work.

Found while writing, and settled in the record: the release's stamp,
`tools/release.json`, already carries `stampedAt`, so ADR-034 decision 2
names it as the date a generated page carries; the plugin on this
machine at 4.10.0 holds six skills, and ADR-036 names the two the stance
is built on, 1,418 words together, and leaves the other four to the
plugin; no test asserts on the `comparison` messages, so ADR-034
decision 6 names the checker alone; ADR-035's decisions before the
go-ahead lean on ADR-033 decision 2 for the declined file, which is why
the two are one unit.

#### Work

```cairn-unit
step: S02
unit: 02
type: decision
verified: cairn-check, cairn-test
```

- **`docs/adr/ADR-033-the-installer-reads-what-the-repository-declared.md`**,
  new and `accepted`, dated 2026-09-21: four decisions — every declared
  root read, a declined file the lock remembers, the template in the
  shape it describes, `adopt` naming the gate it leaves red — five
  alternatives refused, a rule and a stub README among them.
- **`docs/adr/ADR-034-housekeeping-with-no-choice-in-it-second-edition.md`**,
  new and `accepted`: eleven decisions in ADR-008's shape — the fault,
  the fix, the surface — three alternatives refused.
- **`docs/adr/ADR-035-the-seventh-skill-cairn-update.md`**, new and
  `accepted`: one decision, the seven movements of an adopter's update as
  the brainstorm note wrote them and Crumbz ran them; three alternatives
  refused.
- **`docs/adr/ADR-036-the-kit-ships-ponytails-skills-where-the-harness-loads-them.md`**,
  new and `accepted`: two decisions — the two skills extracted from the
  plugin's latest version, the lock naming the version, the pin gone; the
  kit's skills written where the harness loads them, `skills/` kept —
  six alternatives refused, the printed line the writer recommended on
  2026-09-16 among them. Supersedes the first half of ADR-016 decision 1.
- **`docs/adr/ADR-016-the-coding-stance-absorbs-ponytail.md`**: the
  status line names the half and ADR-036; one paragraph under decision 1
  says which half. The first token after `Status:` stays `accepted`.
- **`docs/adr/index.md`**: the four records listed after ADR-032 with no
  gap; ADR-016's line names the half it lost.
- **`docs/architecture/01-cairn-1-1.md`**: the stance sentence in *How a
  path runs* and the `cairn` row of the tools table keep their words and
  gain *superseded by ADR-036*.
- **`docs/architecture/index.md`**: the page's line adds ADR-036 to the
  marks.

#### Self-review

- `yagni:` ADR-036 decision 2 ended with *the concept `enforcement-profile`
  says nothing of this and does not change* — a sentence about a file
  the record does not touch; cut.
- `correctness:` ADR-034 decision 6 named *the fixture that asserts on
  them, if one does*; none does, read by command; the hedge cut.
- `delete:` `package-lock.json`, written by `npm install` in this
  worktree, whose `node_modules` had gone; removed before the gate.
- No widening of `writes:`: every file of this unit is under `docs/adr/**`,
  `docs/architecture/**` or this path's folder.
- `shrink:` after the two reads, the four records lost two repeats of
  their own context, a copied paragraph, a restated supersession line
  and a stub-README sentence said twice.
- Net: four records, 625 lines; 18 lines added and 5 removed across the
  four amended documents; and this step.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's
diff in one file and the two criteria and nothing else; it returned in
396 seconds with fifteen findings, on the first attempt. Each with its
disposition:

- `correctness:` ADR-036 quoted a README sentence, *Agent Skills your
  coding agent loads on demand*, that left the README in the release
  path; today's row reads *the six skills, as Agent Skills* — **fixed**,
  the quotation cut from the context, and decision 2 says the README's
  row then says what the harness lists.
- `correctness:` ADR-016 names no install line — **fixed**, *installed
  in the adopter's harness as that repository says*.
- `correctness:` *the kit's first network call* against ADR-032's GitHub
  reading at `init` and `adopt` — **fixed**: the consequence says it is
  a download of a release, not a reading of the host's settings, which
  ADR-032's stays the one of.
- `correctness:` ADR-034 decision 4 put both `plan.files.set` lines in
  `main`; one is in `applyAdopt` and the `status` branch has none —
  **fixed**, the sentence names `applyAdopt` and the `update` branch.
- `correctness:` a consequence said the bootloader gains a word where
  decision 5 says a line — **fixed**.
- `correctness:` ADR-035 sends the skill to release notes no record in
  this unit decides — **fixed**, *once they name the adopter repairs …
  (K31, theme 6's record)*.
- `correctness:` ADR-035 named the README a count surface against
  ADR-031 decision 1 — **fixed**, the count's row and the README's link
  to it.
- `correctness:` Q9 has no *as today* option — **fixed**, Q9's second
  option named as *no — the stale list is enough*.
- `shrink:` ADR-033 decision 1 repeating the context's ADR-011 sentence
  — **fixed**, cut from the decision.
- `shrink:` ADR-033 decision 3 and its alternative saying the stub
  README twice — **fixed**, cut from the decision.
- `shrink:` ADR-035's context restating the brainstorm note — **refused**:
  a record states its context, and a reader should not need the note
  open to know what the record stands on.
- `shrink:` ADR-036's supersession stated five times — **fixed in
  part**: cut from decision 1's line; kept in the description, the head
  paragraph, the consequence that names the marks, and the manifesto
  section, where the tag *reverses ADR-016 decision 1* is what is
  weighed.
- `shrink:` ADR-036's plugin paragraph copied from the brainstorm note —
  **fixed**, one sentence.
- `shrink:` *the brainstorm note's recommendation and the writer's* —
  **fixed**, one clause.
- `shrink:` every decision's *What this changes* restated by the
  implementation table — **refused**: the paragraph is the decision's
  own prose and the table is the scope a coding path is opened from, as
  every record since ADR-001 has both and the definition of done's
  fifth item asks for the table.

Second read: a fresh context, a subagent of this harness, given only the
diff of those fixes and the same two criteria; it returned with four
findings, on the first attempt. Each with its disposition:

- `shrink:` ADR-034 decision 4's fix restated the decision and lost the
  locator — **fixed**, the two `plan.files.set` sites named again, in
  `applyAdopt` and the `update` branch.
- `shrink:` ADR-036's consequence clause *which ADR-032's stays the one
  of* unparseable — **fixed**, *the one host reading stays ADR-032's*.
- `correctness:` *five lifecycle hooks* and *manifests for five
  harnesses* against the plugin on disk, hook files for four harnesses
  and manifests for at least nine — **fixed**, the counts dropped; six
  skills and 1,418 words stand, verified.
- `correctness:` ADR-035 named a skill-count row the budget table does
  not have yet, and omitted the files row the new skill moves — **fixed**,
  both rows named, the second as ADR-031 decision 1's.

The fixes of the second read are not read a third time; the self-review
above carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this record complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare in this worktree after the records were written; nothing under tools/ changed in this unit
cairn-active --check  : pass — the view is already current; no path state changed
record shape          : the `schema` rule read ADR-033 to ADR-036 green — id matches the file name, status accepted in both halves, ISO date; ADR-016 keeps `accepted` as the first token after `Status:`
measurements          : the two governing blobs unchanged since S01; the installer's functions, the post-mortem's readings, the checker's two messages, the close skill's two lines, the release stamp, the plugin's skills folder and the layout reference's lock row read by command, not from memory
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's four fixes not read again
user review           : not required for the unit; the owner reads the records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S03
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-033, ADR-034, ADR-035, ADR-036 (new), ADR-016 (status line and one paragraph), docs/adr/index.md, docs/architecture/01-cairn-1-1.md (two marks), docs/architecture/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S03 — themes 4, 5 and 6, ADR-037 to ADR-039, ADR-028 decision 2's reading half superseded and marked
blockers    : none
```
