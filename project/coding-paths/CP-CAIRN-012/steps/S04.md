---
type: Cairn Coding Path Step
title: 'CP-CAIRN-012 S04 — the records: ADR-040, ADR-041, ADR-042, ADR-043'
timestamp: 2026-09-22T00:00:00Z
cairn:
  path: CP-CAIRN-012
  step: S04
---

# CP-CAIRN-012 S04

### S04 — the records: ADR-040, ADR-041, ADR-042, ADR-043 — **COMPLETE**

#### Plan

Write theme 7 of the asks note as four records, as the plan's fourth
item says: ADR-040, the administrative commit lands before the reading
(K32); ADR-041, a home for deferred work, `project/backlog/` (K33);
ADR-042, the definition of done is a plain list (K34), superseding
ADR-002 decision 2; ADR-043, the coherence questions are a fresh-context
read (K35), extending ADR-017 decision 1. Every ticked line of the asks
note, K01 to K36, is then named by exactly one record.

Definition-of-done items advanced: the first (four records in the
layout's shape, promoted from both notes at their pinned blobs), the
second, now met in full, the third for ADR-002 decision 2 — the record
names it, ADR-002 gains the line, the 1.1 page's three sentences on the
checkboxes are marked *superseded by* — and the fifth, every decision
naming its surface by today's name.

Type `decision`; the amended documents move with the records — ADR-002,
the records' index, the 1.1 page and the architecture index. No test:
nothing under `tools/` changes, and no rule is added or removed. ADR-043
extends ADR-017 and supersedes nothing of it, so ADR-017 is not edited,
as ADR-028 did not edit ADR-014 when it extended it.

Deliberately not in this unit: the 1.2 page, the indexes' 1.2 lines
beyond the four records, the register with its *pending* clause, the
word in ADR-015's table and the sentence in the records' index shape
(S05), and every skill, tool, reference, template and kit file the
records name — the coding paths' work.

Found while writing, and settled in the record rather than left: no
record placed the administrative commit after the owner's reading — the
order is the close skill's and the execution protocol's — so ADR-040
supersedes nothing and reorders two steps; the four coherence questions
carry different criteria from the two of ADR-016 decision 4, so ADR-043
says the two reads stay two and neither gains the other's; the kit has
no generator of a path record, but the pilot writes one with two boxed
items and the kit's `requestTemplate` carries the four coherence boxes,
so ADR-042 names the pilot and ADR-043 names the generator.

#### Work

```cairn-unit
step: S04
unit: 04
type: decision
verified: cairn-check, cairn-test
```

- **`docs/adr/ADR-040-the-administrative-commit-lands-before-the-reading.md`**,
  new and `accepted`, dated 2026-09-21: one decision, steps 3 and 4 of
  the close skill swapped on `pull-request` with one owner, `manual-git`
  unchanged; two alternatives refused, a widened rule among them.
- **`docs/adr/ADR-041-a-home-for-deferred-work.md`**, new and
  `accepted`: one decision — the fourth folder of the execution plane,
  what a file says, who reads it, how an item is taken and removed —
  four alternatives refused, the register and an issue on the host among
  them.
- **`docs/adr/ADR-042-the-definition-of-done-is-a-plain-list.md`**, new
  and `accepted`: one decision, the plain list with accepted records
  keeping their text and the rule unchanged; three alternatives refused.
  Supersedes ADR-002 decision 2.
- **`docs/adr/ADR-043-the-coherence-questions-are-a-fresh-context-read.md`**,
  new and `accepted`: one decision — the fresh reader, what it is given,
  where its answers go, the owner arbitrating, the three facts the audit
  tool scaffolds — four alternatives refused, a checker rule among them.
  Extends ADR-017 decision 1.
- **`docs/adr/ADR-002-the-checkboxes-stay-and-the-seal-is-checked-everywhere.md`**:
  the status line names decision 2 and ADR-042; one paragraph under
  decision 2 says so. The first token after `Status:` stays `accepted`.
- **`docs/adr/index.md`**: the four records listed after ADR-039 with no
  gap; ADR-002's line names the decision it lost.
- **`docs/architecture/01-cairn-1-1.md`**: the three sentences on the
  checkboxes — the record's shape in *How a path opens*, the amendment
  paragraph's *ticks included*, and the list of what 1.1 kept — keep
  their words and gain *superseded by ADR-042*.
- **`docs/architecture/index.md`**: the page's line adds ADR-042 to the
  marks.

#### Self-review

- `correctness:` ADR-042 said the 1.1 page's list of what 1.1 kept
  loses an item *three months later than the notes asked*; the owner
  kept the boxes on 2026-09-06, fifteen days before; corrected.
- `correctness:` ADR-042 named *the generator of a new path record,
  where the kit has one*; the kit has none, read by command, and the
  pilot writes one; the pilot named.
- `correctness:` ADR-043 omitted the kit's `requestTemplate`, which
  generates an adopter's request template with the four boxes; added.
- `shrink:` ADR-041 named *the `registerIndex`-style template beside*
  the folder's index, a phrase that designed; cut to the folder and its
  index.
- `delete:` `package-lock.json`, written by `npm install` in this
  worktree; removed before the gate.
- No widening of `writes:`: every file of this unit is under `docs/adr/**`,
  `docs/architecture/**` or this path's folder.
- `shrink:` after the two reads, the four records lost seven
  restatements of their own decisions or alternatives, one speculative
  consequence and one false one; ADR-043's separate-criteria point
  stands in one place.
- Net: four records, 419 lines; 24 lines added and 8 removed across the
  four amended documents; and this step.

#### Review

First read: a fresh context, a subagent of this harness, given the unit's
diff in one file and the two criteria and nothing else; it returned in
250 seconds with nineteen findings, on the first attempt. Each with its
disposition:

- `correctness:` *the execution plane* is no page's term; the
  specification says *the project plane* and the binding
  *execution-state plane* — **fixed**, ADR-041 and the index say the
  project plane, once naming the binding's word.
- `correctness:` chapter 6 is the learning loop; the plane's folders are
  chapters 1 and 4 — **fixed**.
- `correctness:` ADR-042 omitted `definitionItems` in `tools/cairn-audit.mjs`,
  which reads an item only from a boxed line, and its test's fixtures —
  **fixed**, both named in the decision and the table.
- `correctness:` ADR-043 said the audit tool *already computes two of
  the four facts*; the checker's rules compute them, the tool none —
  **fixed**, *prints three facts the checker's rules already compute*.
- `correctness:` ADR-043 claimed to leave ADR-016 decision 4 untouched
  while giving a fresh context a diff and four other questions —
  **fixed by rewording, not by a supersession**: the coherence audit was
  the reviewer's act beside the two-criteria read before ADR-016 was
  written, and the record names its reader and adds no criterion to
  either read; the claim now stands in one place.
- `correctness:` *CP-R0-001 closed that way on 2026-09-17* — the note
  names no path and no date of closing — **fixed**, *ECOS met it, as its
  note of 2026-09-17 says*.
- `correctness:` *ticked them anyway on 2026-09-18* — the note's date,
  not the tick's — **fixed**, *as its note of 2026-09-18 says*.
- `correctness:` ADR-042 said the 1.1 page's list of what 1.1 kept
  *loses one of its two items*; the item is marked, not removed —
  **fixed**, the bullet cut.
- `delete:` ADR-040's consequence restating what this changes —
  **fixed**.
- `shrink:` ADR-040's manifesto sentence restating the decision and the
  alternative — **fixed**, one sentence.
- `delete:` ADR-041's two consequences restating decision 1 — **fixed**,
  cut.
- `yagni:` ADR-041's consequence on a predicate no rule makes —
  **fixed**, cut; one consequence stands.
- `shrink:` ADR-041's issue-tracker sentence said three times —
  **fixed**, cut from the decision.
- `shrink:` ADR-041's manifesto section restating the first alternative
  — **fixed**.
- `delete:` ADR-042's day-count consequence — **fixed**, cut with the
  correctness finding above.
- `shrink:` ADR-042's manifesto sentence restating the first alternative
  — **fixed**.
- `shrink:` ADR-043 saying three times that the two reads keep separate
  criteria — **fixed**, in decision 1 alone; the head and the fourth
  alternative shortened.
- `delete:` ADR-043's consequence restating the scaffold — **fixed**.
- `shrink:` the 1.1 page's mark nesting a third pair of dashes inside a
  dashed list — **fixed**, the mark in parentheses.

Second read: a fresh context, a subagent of this harness, given only the
diff of those fixes and the same two criteria; it returned with five
findings, on the first attempt. Each with its disposition:

- `correctness:` *the fourth folder of the project plane* — `project/`
  here also holds `sessions/`, a 0.2 folder `adopt` names as history —
  **fixed**, *a folder of the project plane*, in the record and the
  index.
- `correctness:` *three facts the checker's rules already compute* —
  two are a rule's, the third no rule's — **fixed**, *two of which*.
- `correctness:` *beside that read before ADR-016 was written* — the
  unit's read is ADR-017's — **fixed**, *the coherence audit predates
  ADR-016*.
- `shrink:` ADR-041's consequence repeating the first alternative —
  **fixed**, the clause cut.
- `shrink:` ADR-043's fourth alternative left without a reason —
  **fixed**, the reason given: the unit's reader is not given the
  records, the view or the candidate.

The fixes of the second read are not read a third time; the self-review
above carries the line the cuts add.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this record complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0, run bare in this worktree after the records were written; nothing under tools/ changed in this unit
cairn-active --check  : pass — the view is already current; no path state changed
record shape          : the `schema` rule read ADR-040 to ADR-043 green — id matches the file name, status accepted in both halves, ISO date; ADR-002 keeps `accepted` as the first token after `Status:`
measurements          : the two governing blobs unchanged since S01; the close skill's steps, the audit tool's questions and item reading, the request-template generator, the pilot's record, the template's list, the project plane's folders and the spec's chapters read by command, not from memory
review                : two reads by fresh contexts, both obtained on the first attempt; the second read's five fixes not read again
user review           : not required for the unit; the owner reads the records with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S05
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-040, ADR-041, ADR-042, ADR-043 (new), ADR-002 (status line and one paragraph), docs/adr/index.md, docs/architecture/01-cairn-1-1.md (three marks), docs/architecture/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S05 — the 1.2 page, the indexes, the register's 1.2 row with the coding paths of 1.2 and the first pending, then the candidate and the closing
blockers    : none
```
