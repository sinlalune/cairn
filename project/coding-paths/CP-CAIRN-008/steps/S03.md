---
type: Cairn Coding Path Step
title: 'CP-CAIRN-008 S03 — the two rules'
timestamp: 2026-09-14T00:00:00Z
cairn:
  path: CP-CAIRN-008
  step: S03
---

# CP-CAIRN-008 S03

### S03 — the two rules — **COMPLETE**

#### Plan

Make the `review` rule read the unit it claims to judge, and the
`acceptance` rule ask which transport it is judging.

[ADR-026](../../../../docs/adr/ADR-026-the-readings-that-must-not-lie.md)
decisions 2, 3 and 4. The `find(…) ??` goes, so the subject is the
ledger's newest completed unit; the merge-object refusal is gated by the
declared integration transport; and the comment and the catalogue say
one commit for one path, which is what the rule has always read.

Definition-of-done items advanced: the third, the fourth, and the
catalogue-and-matrix half of the fifth.

Type `implementation`: the rules, their fixtures, the catalogue and the
page it generates, the hand-written matrix row the generator cannot
reach, and the soundness note.

Deliberately not in this unit: the conformance page's sentence on the
supersession's writer half, the close skill and the post-mortem (S04);
the module note (S05).

#### Work

```cairn-unit
step: S03
unit: 03
type: implementation
verified: cairn-check, cairn-test
```

- **`tools/cairn-check.mjs`**: the `review` rule's subject is
  `workUnits.at(-1)`, the `find(…) ??` gone, with the comment rewritten
  to say why the field could only ever make the rule wrong. The
  `acceptance` rule's merge-object refusal is inside `if (pullRequest)`,
  the flag the function already derives from the declared transport; the
  per-commit refusal keeps its place and its comment names ADR-026
  decision 3 as the reading that binds.
- **`tools/cairn-fixture.test.mjs`**: two units, both complete, S01 with
  an honest review and S02 with the heading and nothing under it, and
  `current_step` left on S01 — green before the change, refused after.
  `integratedRepository` takes the transport, and the `--no-ff` merge
  carrying the edit is asserted NOT to be refused as a merge object on
  `manual-git`.
- **`tools/cairn-rules.mjs`** and the generated rows of
  **`spec/reference/conformance.md`**: both rules' `condition` and
  `enforcing` lines rewritten and regenerated with `cairn-rules --write`.
- **`spec/reference/conformance.md`**, the hand-written matrix row above
  the generated markers, which regenerating does not reach: the `review`
  row says the rule reads the ledger's newest completed unit, and what
  `current_step` was until today.
- **`tools/soundness.md`**: one paragraph per changed reading — a proxy
  that selects the wrong subject passes soundly over the right one, and
  a rule that reads a transport-shaped fact asks which transport.

#### Self-review

- `correctness:` the first version of the `review` fixture proved
  nothing. The baseline `pathRepository` writes no step file at all, so
  the path had zero work units and the rule never ran; adding one step
  with an empty review made it fire through the `?? at(-1)` fallback,
  not through the selection under test. It passed against the UNCHANGED
  checker, which is the signature of a fixture that tests nothing. Both
  units are now written, and the fixture is red before the change — the
  `cairn-code` line about the test that never fires, found in my own
  work.
- `shrink:` the fixture's record declares `route: full`, because two
  units on a `lightweight` path is a route trigger and the extra finding
  was noise in a fixture about which unit the review rule reads.
- `correctness:` *the gate reported OK eight times* in the soundness
  note — a number I arrived at by arithmetic, which is the finding S01's
  reader raised against the record. Replaced by what is derivable: the
  field said `S01` from registration through S08, so every unit after
  the first was judged on S01's section.
- `delete:` nothing else; the change is one deleted selection, one `if`,
  and the text that follows them.
- No widening of `writes:`.
- Net: 141 lines added, 29 removed — the two fixtures, one unit case and
  the soundness note are most of it.

#### Review

First read, a fresh context given this unit's diff — the two rules, the
fixtures, the catalogue, the conformance page and the soundness note —
and the two criteria, the ladder and correctness, and nothing else.
Eleven findings; each with its disposition:

- `correctness:` **the deletion opened a hole.** With the `find(…)` gone,
  a `cairn-unit` block in a folder record's own `index.md` with the
  highest ordinal becomes the newest unit, `isUnitLedger` is false, and
  the rule *skips* instead of falling back — so a block in the one file
  the rule refuses to read could make it read nothing. The reader proved
  it by construction. **Fixed**: the subject is the newest unit kept in a
  ledger, with a unit case that fails when the filter is reverted.
- `correctness:` the subject is still chosen by a writer field — `unit:`,
  which the checker validates as digits and not for uniqueness or
  monotonicity — so a unit numbered below an older one moves it back.
  **Named, not fixed**: validating the ordinal is a decision ADR-026 does
  not make. The soundness note states the residual instead of claiming it
  away, which is what that note is for.
- `correctness:` the soundness note and the catalogue cells overstated
  the fix — *nothing selects it* — when the ledger's sort does.
  **Fixed**, in all three places.
- `correctness:` the claim that a block in a folder record's `index.md`
  *is not read at all*, when `pathWorkUnits` reads it and it could decide
  the subject — **fixed** by the filter above, and the wording with it.
- `correctness:` **the transport gate does not free a `manual-git`
  closing.** `transition` still refuses the same shape, reading the
  merge's first parent for the `ready` behind it and finding the trunk
  rather than the branch. My comment claimed the refusal had been
  blocking every prescribed closing; gated, it still is, by another rule.
  **Fixed**: the comment, the fixture and the soundness note say so, and
  the fixture asserts the `transition` refusal so the debt cannot go
  quiet. The rule itself is ADR-001 decision 7's and is not this
  record's to change — **put to the owner** at this boundary.
- `correctness:` on `manual-git` the merge-object refusal is now absent
  for every arrival, not only the prescribed shape, and whether
  `transition` covers every dishonest one is unverified — **accepted as
  decided**: ADR-026 decision 4 binds the refusal to `pull-request`
  alone, and narrowing it further is not what the record says.
- `correctness:` `docs/modules/application.md` still described both
  readings in their old form — **fixed**; the branch passed `work-unit`
  only because S02 had already touched that file, which is the
  dangling-sentence defect path 3's journal named, caught this time.
- `correctness:` nothing in `tools/cairn-check.test.mjs` pinned the new
  selection — **fixed**, then narrowed by the second read.
- `shrink:` coding path 3's history written out four times — **fixed**,
  cut from the code comment and the conformance cell.
- `shrink:` the conformance page carrying a dated change-log sentence —
  **fixed**, cut; the page says what is enforced now and cites the record.
- `shrink:` the fixture's comment stating its point twice — **fixed**.

Second read, a fresh context given only the diff of those fixes and the
same two criteria, as ADR-017 decision 3 bounds it. Eight findings:

- `correctness:` a second escape the soundness paragraph did not name: a
  newest unit typed `closure` skips the rule for the whole record, and
  nothing forbids that block in a step record — **fixed**, named beside
  the ordinal.
- `correctness:` an *equal* ordinal moves the subject too, the sort being
  stable — **fixed**, *at or below*.
- `correctness:` my own shrink made the comment inexact — *the order the
  ledger itself carries*, when it is `pathWorkUnits`' numeric sort on
  `unit:`. The phrase I had cut was the exact one. **Fixed**, restored.
- `correctness:` the module note saying *the ledger's newest completed
  unit* where the code reads the newest unit **kept in a ledger** —
  **fixed**.
- `correctness:` `skills/cairn-close/reference.md` still walks an adopter
  through the `manual-git` integration and ends with a gate that fails on
  `transition` — **deferred to S04**, which owns that file, and which now
  has to say that the refusal is still there.
- `delete:` the stale-`current_step` unit case passes with the fix
  reverted; the fixture already pins it end to end — **fixed**, cut. A
  revert experiment confirmed the remaining case is the one that
  discriminates: three unit tests fail without the filter, and the
  fixture suite does not.
- `shrink:` two adjacent comment blocks explaining one selection —
  **fixed**, merged.
- `shrink:` *newest completed unit kept in a ledger* a tautology, and the
  `transition` debt stated three times — **fixed**; the note is its one
  home and the comment points at it.

The fixes above are not read a third time. One line the second read's
cuts add to the self-review: `delete:` a unit case I wrote in answer to
the first read proved nothing, which is the same finding I had raised
against my own first fixture in this unit.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci (exit 0)
cairn-test            : pass — 325 tests, 0 failed (exit 0)
cairn-rules --write   : regenerates spec/reference/conformance.md byte-identically after the catalogue edit
fixture, first and red: the two-unit ledger with a stale current_step — "review did not fire — blocking findings were: none" before the change, green after
fixture, manual-git   : the --no-ff merge carrying the edit is not refused as a merge object, and IS still refused by transition, which the fixture asserts so the debt cannot go quiet
revert experiment     : reverting the ledger filter fails three unit cases and no fixture; reverting the transport gate fails the manual-git test on the acceptance finding
user review           : not required for the unit; the owner reads the two rules at the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn-check.mjs, tools/cairn-check.test.mjs, tools/cairn-fixture.test.mjs, tools/cairn-rules.mjs, tools/soundness.md, spec/reference/conformance.md, docs/modules/application.md, this path's index and this step
session     : safe boundary after a successful push
next action : S04 — path 3's four leftovers: the conformance page's sentence on the supersession's writer half, the close skill and its reference saying what the checker refuses and allows by transport (including the transition refusal this unit found still standing on manual-git), and the post-mortem importing the checker's exported readings
blockers    : none. One decision is with the owner: the `transition` rule reads the merge's first parent for the `ready` behind it, so a manual-git closing is still refused; ADR-026 does not decide it.
```
