---
type: Cairn Coding Path Step
title: 'CP-CAIRN-008 S01 — ADR-026, the readings that must not lie'
timestamp: 2026-09-14T00:00:00Z
cairn:
  path: CP-CAIRN-008
  step: S01
---

# CP-CAIRN-008 S01

### S01 — ADR-026, the readings that must not lie — **COMPLETE**

#### Plan

Write, as one decision record, the four rulings this path implements,
before any line of the checker moves.

ADR-026 promotes the sentence
[path 3's S03](../../CP-CAIRN-007/steps/S03.md) wrote — *"not read" is an
honest line; a wrong reading is not* — and settles, beside it, three
readings the checker or a record has wrong: the `review` rule's
selection, which can only point at a unit older than the one being
judged; the sentence of ADR-008 decision 2 the checker outgrew; and the
merge-object refusal applied where that decision does not reach. Its
four decisions are the four items 2, 3, 4 and 6 of this path's
definition of done implement.

Definition-of-done item advanced: the first.

Type `decision`: the record is the change, and every document it amends
moves with it — the 1.1 page in place marked *since 2026-09-14*, the
architecture index, the ADR index, the register's milestone row and row
5. No test: nothing under `tools/` changes here and no rule is added.

Deliberately not in this unit: every line of `tools/cairn-check.mjs`,
its tests and fixtures, and the workflow's token (S02 and S03); the
conformance page, the close skill and the post-mortem tool (S04); the
tools' module note (S05). Observed and left: rows 1, 2 and 3 of the
register still read `running` for three paths that are done — no
decision of this record reaches them, and `cairn-active` is row 4's
tool.

#### Work

```cairn-unit
step: S01
unit: 01
type: decision
verified: cairn-check, cairn-test
```

- **`docs/adr/ADR-026-the-readings-that-must-not-lie.md`**, new and
  `accepted`, promoted from path 3's S03 at blob `fe9805f0` and that
  path's journal entry at blob `ad1ee640`, both pinned in this path's
  `governs:`. Four decisions: a withheld forge reading reported as
  unread, with the profile line printing read, withheld and unenforced
  and `GITHUB_TOKEN` returning to the checker's step (extends ADR-001
  d6); the `review` rule judging the ledger's newest completed unit,
  `current_step` selecting nothing (extends ADR-017 d2); one commit for
  one path as the reading that binds, superseding *never two paths in
  one request* of ADR-008 d2; the merge-object refusal bound to
  `pull-request` integration transport (extends the rest of ADR-008 d2).
  The forge's own documentation is cited for both halves of the lie —
  the elision of `bypass_actors` below ruleset write access, and the
  absence of `administration` from what a workflow token may be granted.
  Nine alternatives refused, a bigger token and a second rule among them.
- **`docs/adr/index.md`** lists it after ADR-025, with no gap.
- **`docs/architecture/01-cairn-1-1.md`** amended in place: the
  amendment paragraph naming the superseded sentence and the two blobs
  it is promoted from; *How a path runs*, the unit the review rule
  reads; *How a path closes*, the two integration sentences; three rows
  of *What the checker reads at each transition*; the `cairn-check` row
  of *Which tools exist*. Every addition marked *since 2026-09-14*; the
  frontmatter's description and timestamp follow.
- **`docs/architecture/index.md`** records the 2026-09-14 amendment on
  the page's line.
- **`project/coding-paths/index.md`**: the 1.1 milestone row counts
  twenty-six records; row 5 names ADR-026 in place of *a new record*.

#### Self-review

- `shrink:` the record's context listed all sixteen permissions a
  `permissions:` block may grant, to make the one claim that
  `administration` is not among them — cut to the claim and the
  citation.
- `correctness:` *the gate passed on S01's, eight times* — a count I
  arrived at by arithmetic and did not measure. Replaced by the number
  path 3's own journal entry reports, six units, attributed to it.
- `shrink:` two rejected alternatives — a finding rather than a line,
  and refusing the run — argue the same refusal from the same ground;
  merged into one, and the ADR index's count follows.
- `delete:` the consequence *ADR-008 decision 2 loses one sentence and
  keeps the rest*, which restates the record's own header; only the
  clause that becomes load-bearing survives.
- `shrink:` the 1.1 page's *two honest integrations in one request being
  none of the checker's business* — a flourish where the page states
  facts; *are not refused*.
- Considered and kept: the profile line's new half is stated twice on the
  1.1 page, in the transition table and in the tools table. Both already
  said *what the forge does not enforce* before this unit; leaving one of
  them at the old claim is the worse repetition.
- No widening of `writes:`: `docs/adr/**`,
  `docs/architecture/01-cairn-1-1.md`, `docs/architecture/index.md`,
  `project/coding-paths/index.md` and
  `project/coding-paths/CP-CAIRN-008/**` are all declared.
- Net: 37 lines added and 15 removed across the four amended documents,
  plus the record, 251 lines, and this step.

#### Review

First read, a fresh context given this unit's diff — the record and the
four documents it amends — and the two criteria, the ladder and
correctness, and nothing else. Eleven findings; each with its
disposition:

- `correctness:` the record crediting path 3's S03 with the sentence
  *"Not read" is an honest line; a wrong reading is not* — **fixed**.
  The reader proved it: the sentence is the checker's own comment, and it
  is path 3's **S04** that quotes it, *already in the checker, three
  times, in its own voice*, in the register row S04 opened. S03 took the
  token out and named the reading that lied; it wrote no such sentence.
  The record now says whose sentence it is, and this path's own record
  carried the same misattribution in its goal, corrected here with it.
- `correctness:` *six units' review sections were never read*, attributed
  to path 3's journal entry, which says seven — **fixed**. The entry is
  inconsistent with itself, saying *seven units* and *the same six* in
  one sentence, so the record now states what is derivable and exact:
  `current_step` said `S01` from registration through S08, so every unit
  from S02 on was judged on S01's section.
- `correctness:` decision 2 naming neither `tools/cairn-rules.mjs`, whose
  `enforcing` line still states the selection the decision deletes, nor
  the conformance page it writes — **fixed**, both named, and the
  implementation table with them. Both files are in this path's writes
  and neither was in the record.
- `correctness:` decision 4 naming `skills/cairn-close/SKILL.md` as a
  surface it changes, where step 5 already carries the transport split
  path 3 wrote — **fixed**: the decision and the table name the
  reference alone, which gives the `--no-ff` commands without saying
  what the checker allows there.
- `shrink:` the bigger token refused three times — in the context, in the
  first alternative, and in the manifesto section — **fixed**, the
  context's sentence cut.
- `shrink:` *The field cannot make the rule more right and can make it
  wrong* restating the two sentences before it — **fixed**, cut.
- `shrink:` *One of the two is a rule the checker outgrew…* restating the
  two sentences before it — **fixed**, cut.
- `delete:` the first consequence, decision 1's two paragraphs again —
  **fixed**, cut.
- `delete:` the last consequence, decision 4 again — **fixed**, cut.
- `shrink:` the second consequence restating decision 1's own
  generalising paragraph — **fixed**, cut.
- `shrink:` *What the manifesto's test weighed* adding nothing the
  alternatives had not said, in the second-longest record of the corpus —
  **fixed**, cut to the one thing that section is for: four decisions and
  no rule.

Second read, a fresh context given only the diff of those fixes and the
same two criteria, as ADR-017 decision 3 bounds it. Six findings:

- `correctness:` *the two rows it generates* into the conformance page —
  **fixed**. Only the catalogue row carries the `enforcing` text; the
  other generated row is the rule-to-requirement linkage and decision 2
  does not touch it.
- `correctness:` the surface the first fix missed — the hand-written
  matrix row of `spec/reference/conformance.md`, which states the same
  selection and sits **above** the generated markers, so
  `npm run cairn-rules` will not reach it — **fixed**, named in the
  decision and in the table. Without the reader this would have been
  found by S03 or not at all.
- `correctness:` *written in its forge section*, where the checker has no
  such section — **fixed** to the block that prints the profile line, and
  the comment on `githubSlug` named as the anchor.
- `shrink:` *That reading is path 3's S04, in the register row it
  opened* — provenance of provenance — **fixed**, cut.
- `shrink:` decision 4 repeating decision 3's clause about step 5, when
  dropping the file from the table already says it — **fixed**, cut.
- `shrink:` the manifesto section's closing sentence restating two
  alternatives — **fixed**, cut.

The fixes above are not read a third time. Two lines the second read's
cuts add to the self-review: `shrink:` the record lost 106 words across
the two reads, and `correctness:` two of its four decisions named an
incomplete surface list until a reader checked them against the files.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 7 changed files (exit 0)
cairn-test            : pass — 320 tests, 0 failed (exit 0)
cairn-active --check  : pass — the view is already current; no path state changed
dependencies          : none to install — the package declares none, and the gates ran bare in this fresh worktree
forge documentation   : read for the record, not quoted from memory — the bypass_actors elision at docs.github.com/en/rest/repos/rules, and the permissions a `permissions:` block may grant, which do not include `administration`
record shape          : `adrFrontmatterErrors` satisfied — id matches the file name, status accepted in both halves, ISO date
user review           : not required for the unit; the owner reads the record with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : docs/adr/ADR-026-the-readings-that-must-not-lie.md, docs/adr/index.md, docs/architecture/01-cairn-1-1.md, docs/architecture/index.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S02 — the forge reading and the token: `forgeGaps` telling an unread bypass list from an empty one, `readForge` naming the half it could not read, the profile line printing read, withheld and unenforced in one line, the withheld case written first and failing, then `GITHUB_TOKEN` back in the checker's step
blockers    : none
```
