---
type: Cairn Coding Path Step
title: 'CP-CAIRN-008 S06 — the register''s widened file'
timestamp: 2026-09-14T00:00:00Z
cairn:
  path: CP-CAIRN-008
  step: S06
---

# CP-CAIRN-008 S06

### S06 — the register's widened file — **COMPLETE**

#### Plan

Row 5 of the [roadmap register](../../index.md) declares this path's
write scope; S02 widened that scope by `tools/cairn-workflow.test.mjs`
and the register did not follow. This unit makes the two declarations
say the same thing. It does not touch the row's other columns, and it
decides nothing about what a register row ought to list.

Definition-of-done item advanced: the ninth — *the register, a write
surface, gains this path's id in row 5 and the widened files in its
writes column*.

Type `documentation`: the register, the forward plan whose numbering this
unit shifts, the path record, and the step. No test: the register carries
no claim a gate reads, and `scope-drift` reads `writes:` in the path
record, which has been right since S02.

Deliberately not in this unit: the closing, which adds only the
administrative surface; the three rows that still read `running` for
paths that are done, which S01 observed and left because no decision of
ADR-026 reaches them and `cairn-active` is row 4's tool.

#### Work

```cairn-unit
step: S06
unit: 06
type: documentation
verified: cairn-check, cairn-test
```

- **`project/coding-paths/index.md`**, row 5's writes column: the clause
  `widened in the path by tools/cairn-workflow.test.mjs` appended after
  the registration widening, in the words row 3 already uses for its own.
- **`project/coding-paths/CP-CAIRN-008/plan.md`**: a numbering note, as
  path 3's plan carries one and in its shape — the list itself is not
  renumbered — mapping the closing, item 6 there and `S06` in three
  pushed records, onto S07. The frontmatter enumerates the units and
  gains this one.
- **`project/coding-paths/CP-CAIRN-008/index.md`**: `current_step`, the
  steps list, and the resume section, whose next action and reading order
  named the closing as S06 and sent it to `cairn-close`.

#### Self-review

- `shrink:` the row's clause is six words and reuses row 3's wording
  rather than inventing a second way to say the same thing.
- `correctness:` the register's writes column is a row's **declared**
  scope, not the diff it realised: it already names two test files this
  path never changed, and omits the path's own folder and the register
  itself, as row 3 does. The widened file belonged there because S02
  widened the declaration, not because the file changed.
- Considered and refused: renumbering the plan's list, which paths 1 and
  3 both declined — path 1 left `6. S06 — the candidate` standing over a
  candidate that ran as S07 with no note at all, path 3 added a one-line
  mapping note and left its items alone. The note is the one mechanism
  here; it also maps the `S06` that S02, S04 and S05 name, which are
  append-only and stay as they are. The path record, which may be
  edited, is corrected outright.
- No widening of `writes:` — the register, the plan, this folder and this
  step are all in it.
- Net: 18 lines added, 13 removed across the register, the plan and the
  path record; the register's own share is one line for one.

#### Review

First read, a fresh context given this unit's diff and the two criteria,
the ladder and correctness, and nothing else. Ten findings; each with its
disposition:

- `correctness:` the diff never touched `index.md`, so `current_step`
  still said `S05`, the steps list had no S06 and the checkpoint was
  unchanged — **fixed**. Item 10 asks every completed step for a
  refreshed resume section, and every prior unit commit on this branch
  carries the record.
- `correctness:` the renumbering left three sentences of `index.md` false
  — the next action sending S06 to `cairn-close`, the reading order's
  *item 6 — what the closing owes*, and the checkpoint's *S06 names its
  commit here* — **fixed**, all three.
- `correctness:` the self-review weighed only the records the renumbering
  did **not** contradict, and missed the three pushed step records that
  call the candidate `S06` — **fixed**: the numbering note names them and
  the self-review says they are append-only and left as they are.
- `correctness:` the net count was the plan's alone and omitted the
  register's own line, the unit's actual product — **fixed**.
- `correctness:` the plan note's *recorded until now in the path record
  alone* was false; S02's step record records the widening twice —
  **fixed**, the note says the register is what it did not reach.
- `correctness:` a code span in the work section closed at the backtick
  before `tools`, rendering the filename as body text — **fixed**, the
  clause is written without nesting.
- `shrink:` the plan ran two paragraphs to explain a six-word edit to one
  table cell, against the three-line cap — **fixed**, cut to what it does,
  why it is the least and what it does not do.
- `shrink:` the numbering note ran four lines where path 3's runs one,
  its second sentence restating the step record's own refusal — **fixed**,
  and cut further by the second read.
- `correctness:` the unit's premise — *the register is the roadmap's own
  account of what each row writes* — is not the convention it appeals to:
  row 5 names two test files this path never changed and omits the path's
  own folder and the register, and row 3 omits the same class —
  **fixed**, and it is the finding that makes the unit coherent. The
  column is a row's declared scope; S02 widened the declaration, so the
  column owed the file. The self-review carries the correction.
- `correctness:` row 5's *and its test* over-claims `cairn-rules.test.mjs`
  and `cairn-postmortem.test.mjs`, which this path never wrote —
  **refused**, with the finding above: the column declares scope, and a
  declared file that stayed untouched is a scope not spent, not an error.
  Narrowing a declaration at the close would also be rewriting what the
  opening accepted.
- `correctness:` `verified: cairn-check, cairn-test` asserted a gate that
  did not pass on the tree as the reader had it — **accepted**: the block
  is completed at the fifth movement, after this section lands, and the
  verification below records the run that was actually read. The reader
  put the failure on the `review` rule and the second read found the real
  cause, which is in that read's findings.

Second read, a fresh context given only the diff of those fixes and the
same two criteria, as ADR-017 decision 3 bounds it. Ten findings:

- `correctness:` the fixes both renumbered the plan's list **and** added a
  mapping note — two mechanisms for one problem, and between them a
  contradiction: the note said *unit 6 below runs as S07* while item 6
  below had become this unit — **fixed**. The list is restored and the
  note is the one mechanism, which is what path 3 does.
- `correctness:` the record's next action inherited the same fault,
  calling S07 *unit 6 there* against a list where it had become item 7 —
  **fixed** with the above.
- `correctness:` the gate's actual refusal on the reader's tree was
  `record-integrity`, inconclusive because this step file was
  intent-to-add and `appendOnlyStepRecordMutations` cannot tell an
  addition from a mutation until it is staged or committed — not the
  `review` rule, which passed — **fixed** in the disposition above. The
  verification below records the run on the staged tree.
- `correctness:` the plan section gave S01's reason for leaving the three
  `running` rows as *a register's state column is the integrating unit's
  to write*, which S01 does not say and which those rows disprove —
  **fixed** to what S01 says: no decision of the record reaches them and
  `cairn-active` is row 4's tool.
- `correctness:` the net count was the plan's alone again, 15 and 5
  against an actual 22 and 12 — **fixed**, and recounted after these
  fixes rather than carried.
- `correctness:` the plan's frontmatter enumerates the units and did not
  gain this one — **fixed**.
- `ladder:` the renumbering was self-inflicted overhead with no precedent
  — path 1 left its plan's item 6 standing over a candidate that ran as
  S07 with no note at all — and the self-review weighed only an
  alternative nobody would take — **fixed**: the refusal now weighs the
  move paths 1 and 3 actually made, and taking it removed a list item,
  two sentences of the resume and one of the note.
- `shrink:` the note's second sentence, naming the three pushed records
  separately — **fixed**, folded into the mapping clause.
- `process:` the diff handed to this read carried the new step file whole
  and omitted the register clause, the unit's actual product — neither
  the unit's diff nor only the lines the fixes changed, as ADR-017
  decision 3 bounds it — **accepted**, and named here rather than
  re-read: the reader verified the register edit in the tree and found it
  correct, and a third read is what that decision forbids. The bound is
  on the reader's context, and the mistake was mine in cutting the diff.
- `correctness:` the reading order still sent a fresh session to the
  plan's *item 7* — **fixed**, it names the numbering note and item 6.

Those fixes are not read a third time. Two lines they add to the
self-review: `shrink:` the first fix I reached for was the bigger one,
and the precedent for the smaller one was in two plans in this folder;
`delete:` reverting the renumbering removed more than it restored.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci (exit 0), run on the staged tree
cairn-test            : pass — 325 tests, 0 failed (exit 0)
record-integrity      : the rule the second read found inconclusive on an intent-to-add step file; it resolves once the file is a real staged addition, which is the tree the run above judged
no new test           : a documentation unit; nothing under `tools/` changes and the register carries no claim a gate reads
register vs record    : row 5's writes column and the record's `writes:` now name the same widening; the column's two declared-and-unspent test files and its omission of the path's own folder are row 3's convention and are left
user review           : not required for the unit; the owner reads the register's row with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by the closing
```

#### Checkpoint

```text
status      : running
current step: S06 complete on push
retention   : none (no-rewrite host)
changed     : the register's row 5, this path's plan and index, and this step
session     : safe boundary after a successful push
next action : the closing, unit 6 of the plan, running as S07 — merge the trunk in, gates bare on C, the request's description from `cairn-audit`, the administrative commit with its check green, and the owner reads the profile line the request's run prints before the merge
blockers    : none. The `transition` decision is still with the owner; it is named for the candidate's review and blocks no unit of this path.
```
