---
type: Cairn Coding Path Step
title: 'CP-CAIRN-008 S04 — path 3''s four leftovers'
timestamp: 2026-09-14T00:00:00Z
cairn:
  path: CP-CAIRN-008
  step: S04
---

# CP-CAIRN-008 S04

### S04 — path 3's four leftovers — **COMPLETE**

#### Plan

Close the four debts [path 3's journal
entry](../../../log/2026-09-14-cp-cairn-007.md) named and did not own,
which this path's registration widened its writes to carry.

The first is one sentence on the conformance page; the second and third
are what the close skill and its reference say about the checker, by
transport; the fourth is the post-mortem repeating the checker's private
Git plumbing, because path 3 held the checker byte-identical and this
path does not.

Definition-of-done items advanced: the rest of the fifth, the sixth and
the seventh.

Type `implementation`: the two tools, the page and the two skill
documents. No new test: the post-mortem's behaviour does not change, and
its tests stay green on the same fixtures — which is the whole claim
this unit makes about it.

Deliberately not in this unit: the tools' module note (S05); the
`transition` reading itself, which is ADR-001 decision 7's and is with
the owner.

#### Work

```cairn-unit
step: S04
unit: 04
type: implementation
verified: cairn-check, cairn-test, the tool run by hand
```

- **`tools/cairn-check.mjs`** exports the four readings the post-mortem
  had copied — `gitOrNull`, `recordShapes`, `recordHistory`,
  `stepRecordOrigin` — and one it had written a private version of:
  `recordFrontAt`, factored out of `recordHistory`'s own `statusAt`,
  which had the same five lines inline. The checker is shorter by that
  factoring, not longer by the exports.
- **`tools/cairn-postmortem.mjs`** imports those five and deletes its
  copies: its `git`, its `recordShapes`, its `recordHistory`, its
  `stepRecordOrigin` — the last carrying a nine-line comment explaining
  the `--follow` guard it had copied from the checker verbatim. `frontAt`
  stays as four lines of memo over the imported reader: a range is walked
  as `c` and `c^`, two names for one commit, and without it every record
  in the range is read twice. Net −87 lines there.
- **`spec/reference/conformance.md`**: the sentence saying the writer's
  half of a step supersession *is not yet written* now names where it is
  written, `cairn-unit` movement 1 and the path template it sends the
  writer to. Beside it, two rows S03 left behind: the `acceptance` row,
  which still said the merge-object refusal binds on every transport, and
  the `transition` row's gap column, which now states the `manual-git`
  reading this path found and did not decide.
- **`skills/cairn-close/SKILL.md`** step 5 and **its reference** say what
  the checker refuses and allows, by transport, in ADR-008 decision 2's
  sentences: one commit for one path on both, the merge object refused on
  `pull-request` alone. The reference says, where an adopter would
  otherwise run into it, that `transition` still refuses that closing and
  that no record decides the reading.

#### Self-review

- `delete:` the whole shape of this unit is a deletion — 87 lines out of
  the post-mortem against 60 in, and most of what went in is an import
  list. The nine-line comment on the copied `--follow` guard was the
  clearest signal: a comment that says *with the guard
  `tools/cairn-check.mjs` puts on `--follow` and for its reason* is a
  citation where an import belongs.
- `delete:` `execFileSync` is no longer imported by the post-mortem; the
  only thing that used it was the `git` wrapper this unit deleted.
- `shrink:` `recordFrontAt` was not written for the tool — it was already
  in the checker, inline inside `recordHistory`'s `statusAt`. Factoring
  it out made the export and left `statusAt` one line, so the checker is
  shorter for having exported it.
- `correctness:` checked each replacement against the copy it replaces
  rather than assuming: the checker's `recordShapes` takes `file = null`
  where the copy required it, and every call site passes one; its
  `recordHistory` takes a `firstParent` flag defaulting to `false`, which
  is what the copy did; `stepRecordOrigin` and `gitOrNull` are
  byte-identical to the copies.
- Considered and kept: the `frontAt` memo. The checker has no cache and
  should not grow one for a tool's range walk, and the reading inside it
  is now the checker's.
- Observed and fixed here rather than left: two rows of the conformance
  page S03 changed the rules under — the `acceptance` row saying the
  merge-object refusal binds on every transport, and the `transition`
  row's gap column, silent about the reading that refuses an honest
  `manual-git` closing. The first is the same miss S01's reader caught on
  the generated-versus-hand-written split, one row over.
- No widening of `writes:`.
- Net: 84 lines added, 103 removed.

#### Review

First read, a fresh context given this unit's diff — the two tools, the
conformance page and the two skill documents — and the two criteria, the
ladder and correctness, and nothing else. It compared the tool's output
before and after across every path in the repository and found it
byte-identical. Six findings; each with its disposition:

- `correctness:` the post-mortem's own header still said the Git plumbing
  *is repeated, because the checker keeps it private and no path of 1.1
  writes the checker* — the paragraph this unit makes false, in the file
  it makes it false in — **fixed**.
- `delete:` `refExists`, which the diff rewrote, is byte-identical to two
  private copies in the checker: three copies of one expression left
  standing — **fixed**, one exported constant. The second read then found
  a fourth site the factoring had missed.
- `correctness:` `recordHistory().statusAt` calling the unmemoised
  reading, so the walk it used to cache was no longer cached — measured
  by the reader as 15 `git show` spawns against 13 — **fixed** by moving
  the memo onto `recordFrontAt`, which also deletes the wrapper.
- `shrink:` that wrapper, six lines around one imported call — **fixed**,
  gone with the memo's move.
- `shrink:` the close skill's new paragraph restating the three facts two
  lines above it — **fixed**, cut to what is new.
- `correctness:` that paragraph headed *what the checker holds you to, by
  transport* while omitting the one refusal a `manual-git` closing
  actually meets — **fixed**; it names the `transition` refusal and
  points at the two files that carry it.

Second read, a fresh context given only the diff of those fixes and the
same two criteria, as ADR-017 decision 3 bounds it. It verified the memo
sound — every key a full object id but `HEAD` and `<sha>^`, both stable
for a run; `null` cached as a fact rather than a miss; `statusAt`
semantics unchanged — and the blanket rename behaviour-identical. Ten
findings:

- `correctness:` the reference put the `transition` refusal after the
  bare gate in the post-commit sequence, where it does not fire: on the
  trunk the comparison is the tree against itself, nothing changed, and
  the rule is gated on the record being in the changed set. The refusal
  surfaces on a run whose comparison predates the merge — **fixed**, and
  the page now says which run it is.
- `correctness:` the rewritten header claiming the tool calls Git itself
  for *a ref's existence*, which it now imports, and omitting five calls
  it does make — **fixed**.
- `correctness:` the new `refExists` doc saying *the checker's two callers
  of `resolveBranchRef`*, when there is one of those and one of
  `resolveBase` — **fixed**.
- `delete:` a fourth inline copy of the same expression, at the trunk
  guard the factoring did not reach — **fixed**; the count in the comment
  was wrong because the copy was.
- `correctness:` `refExists` inserted between `gitOrNull`'s doc comment
  and `gitOrNull`, leaving it under three stacked JSDoc blocks, two of
  them about other functions — **fixed**, moved below them.
- `shrink:` the close skill's paragraph still restating its neighbour —
  **fixed**, cut again.
- `shrink:` the `transition` fact written in four places — **accepted**
  for three: the soundness note argues it, the conformance row records it
  as a gap, the reference warns where an adopter meets it. The skill's
  copy is now one clause pointing at the other two.
- `shrink:` *which is two too many for a question with one answer*,
  narrating the refactor rather than the code — **fixed**, cut.
- `correctness:` pre-existing — `resolveBranchRef` and `resolveBase` keep
  `refExists = () => false` defaults, so an omitted argument reports every
  ref missing rather than throwing, which a working module-scope
  `refExists` beside them makes easier to fall into. **Deferred**: it
  belongs to no decision this path carries, and changing a default's
  meaning needs its own test surface. Named here so it is not lost.
- `shrink:` the import inserted out of alphabetical order — **fixed**.

The fixes above are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci (exit 0)
cairn-test            : pass — 325 tests, 0 failed (exit 0)
cairn-rules --write   : regenerates spec/reference/conformance.md byte-identically; the three rows changed here are the hand-written ones above the markers
post-mortem, by hand  : `node tools/cairn-postmortem.mjs --branch path/cp-cairn-008` prints the five readings and the closing line, with the forge half unread for want of a token
output, before/after  : the first reader compared the tool's output across every path record in the repository — byte-identical, which is the whole claim this unit makes about it
no new test           : no behaviour changes; the post-mortem's twelve tests pass on the same fixtures, which is the definition of done's own wording
user review           : not required for the unit; the owner reads the close skill at the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S05
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn-check.mjs, tools/cairn-postmortem.mjs, spec/reference/conformance.md, skills/cairn-close/SKILL.md, skills/cairn-close/reference.md, this path's index and this step
session     : safe boundary after a successful push
next action : S05 — the module note: docs/modules/application.md describing the checker and the post-mortem as they are at the candidate, with no history
blockers    : none. The `transition` decision is still with the owner and does not block S05 or S06; the reference, the conformance row and the soundness note carry it meanwhile.
```
