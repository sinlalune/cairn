---
type: Cairn Coding Path Step
title: 'CP-CAIRN-008 S07 — the refusal that never fires'
timestamp: 2026-09-14T00:00:00Z
cairn:
  path: CP-CAIRN-008
  step: S07
---

# CP-CAIRN-008 S07

### S07 — the refusal that never fires — **COMPLETE**

#### Plan

S04 wrote that a `manual-git` closing is refused by `transition`, and that the
refusal "surfaces on the forge's run rather than here". It surfaces nowhere:
no run compares the trunk across an integrating commit, on either transport.
This unit says what is true wherever that sentence was written, and records the
scope amendment that makes the reading itself S08's to fix.

It is the least because the rules and their fixtures are correct — only the
deployed comparison is empty — so nothing here changes a rule, a test or the
workflow. It does not decide the `transition` reading, which stays ADR-001
decision 7's.

Definition-of-done item advanced: the sixth. A sentence naming a refusal that
cannot occur does not say what the checker refuses and allows. The amendment
this unit records opens the thirteenth.

Type `documentation`: the two skill documents, the conformance page, the
soundness note, the path record with its amendment, and this step. No new test:
nothing under `tools/` changes behaviour.

Deliberately not in this unit: the base resolution, the workflow, the workflow
test that pins the defective expression, the fixture and the kit's generator —
all S08's under item 13; and `docs/architecture/01-cairn-1-1.md`, whose
transition table states what the checker is *for* and is not falsified by a
deployment that fails to feed it.

**Candidate `a2489a9` is void.** `status` returns to `running` and
`subject_commit` to `null`, as `cairn-close` requires when implementation
changes after a candidate.

**Amendment of `writes:`**: `tools/cairn.mjs`, in the superseding acceptance of
2026-09-14 recorded in the path record. It generates the same base line for
every adopter, so item 13 cannot be met without it.

#### Work

```cairn-unit
step: S07
unit: 07
type: documentation
verified: cairn-check, cairn-test
```

- **`skills/cairn-close/reference.md`**: the false sentence replaced by the two
  reasons the comparison is empty — no base resolved off a path branch, and a
  push run based on `origin/<trunk>`, which after the push names the pushed
  commit — and by the rules that therefore receive nothing.
- **`skills/cairn-close/SKILL.md`**: the pointer sentence, which said the
  reference names where the refusal surfaces.
- **`spec/reference/conformance.md`**: the row that said `transition` "refuses
  an honest closing" on `manual-git`. Requirement 4 of the soundness note makes
  this page the authority on whether a requirement is enforced, so a false
  entry here is the worst copy of the three.
- **`tools/soundness.md`**: the "is still refused" claim made conditional, and
  a paragraph under *What none of this proves* — a sound rule proves nothing
  about a run that never reaches it — with the measured evidence and the
  remedy, including the part of the remedy that is not one line.
- **`tools/cairn-check.mjs`**: one comment, beside the merge-object refusal,
  which carried the same false sentence. Prose only; no line of the rule moves,
  which is why this stays a `documentation` unit.
- **The path record**: the amendment, item 13, item 10 amended with it,
  `writes:` widened, `running`, `subject_commit: null`, the Blockers section,
  the steps list, the resume and the routing.
- **`project/coding-paths/index.md`**: row 5's writes column gains
  `tools/cairn.mjs`, as S06 established it must when `writes:` moves.

#### Self-review

- `correctness:` the finding was verified twice before it was written — by
  reading `resolveBase`, `changedFiles` and `comparisonRef`, and against a real
  run: CP-CAIRN-007's integration, `34835675219`, printed `cairn-check —
  profile ci, branch main, base origin/main (flag), 0 changed file(s)`. The
  second settles it, because it is the deployed path and not a reading of it.
- `correctness:` the bot scoped the finding to `manual-git`. It is both
  transports: a `pull-request` integrating unit is a plain push to the trunk
  too. The corrections say so rather than repeating the narrower claim.
- `shrink:` the reference gains a paragraph where it had a clause — kept,
  because the sentence it replaces is a safety claim in a document the kit
  copies to every adopter, and the reader who trusts it has no other source.
- `delete:` nothing. Four sentences are corrected in place; each still has
  something true to say.
- **Amendment of `writes:`**: `tools/cairn.mjs`, named in the plan above and in
  the superseding acceptance.
- Net: 163 lines added, 42 removed, excluding this step record; the path
  record's amendment, its thirteenth item and the rerouting are most of it, and
  the five corrected sentences are a line or two each in five files.

#### Review

First read, a fresh context given this unit's diff and the two criteria, the
ladder and correctness, and nothing else. It reproduced the central claim
independently — `resolveBase`, the checkout's refspec, and the run log — and
returned twelve findings. Each with its disposition:

- `ladder:` the plan said fixing the base "is implementation this path did not
  open", when `tools/cairn-check.mjs`, its two test files and the workflow are
  all already in `writes:` and the path's subject is readings that lie — the
  smaller, higher rung was to fix it, not to describe it — **fixed**, and it is
  the finding that changed the unit. The owner granted the scope amendment;
  item 13 and S08 exist because of this line.
- `ladder:` the deferral named `CP-CAIRN-009`, which exists nowhere, and row 4
  is the kit, owning neither the checker nor the workflow — **fixed** with the
  above: the work has an item and a unit in this path instead of a target that
  was not there.
- `correctness:` `scope-drift` does not belong in the list of rules an arrival
  misses — it is inside `if (onPath)` and would not judge a trunk commit even
  with a base that spans the arrival — **fixed**, removed from all three lists.
- `correctness:` `journal-entry` was missing and is the most consequential: it
  is gated on `stateChanged.includes(path.file)` and exists for nothing but
  binding an arrival, so this hole disables it outright — **fixed**, named in
  all three, and in item 13.
- `correctness:` `scope-digest` missing as a sixth, and the conformance page
  already says so in its own words — **fixed**.
- `correctness:` the remedy `github.event.before` was stated without its edge
  cases, one fatal: on a branch's first push the forge sends all zeros, and
  `node tools/cairn-check.mjs --base 000…0` exits through an uncaught Git error
  rather than reporting inconclusive — a red gate with no finding, and the kit
  ships that workflow to adopters whose first push to the trunk is exactly this
  — **fixed**, and it is now the first clause of item 13.
- `correctness:` the remedy must keep `origin/<base_ref>` on `pull_request`,
  one variable serving both events — **fixed** in item 13.
- `correctness:` "the fixtures are not wrong" was inaccurate for one:
  `tools/cairn-workflow.test.mjs` asserts the defective expression verbatim, so
  the remedy changes an existing assertion rather than only adding one —
  **fixed**; the claim is gone and item 13 names the rewrite.
- `correctness:` the same false sentence survived in `spec/reference/
  conformance.md`, the page requirement 4 designates as the authority on
  enforcement status, in `writes:` and edited by S04 — **fixed**; it was the
  worst copy and the unit had missed it.
- `correctness:` the record's Blockers section still said the closing is "still
  refused", contradicting this step — **fixed**, and the two questions are
  separated there: item 13 gives the rule its input, the `transition` reading
  stays with the owner.
- `shrink:` soundness.md's "the next section says how" was off by one, and
  "Every rule above is judged on a changed set" overstated — **fixed**, both.
- `shrink:` "A bare run off a path branch resolves no base" inverts on a
  natural reading, since a path branch is the only case that does resolve one —
  **fixed**.
- `correctness:` the net count was wrong — **fixed**, recounted above.
- `correctness:` `tools/cairn.mjs` generates the same defective line for every
  adopter and was not in `writes:` — **fixed** by the amendment, which widens
  by exactly that file.

Second read, a fresh context given only the diff of those fixes and the same
two criteria, as ADR-017 decision 3 bounds it. It re-derived the rule gating
from the source, re-ran the crash, and confirmed the digest and the amendment's
shape against the template and path 3's precedent. Eight findings:

- `correctness:` item 13 contradicted item 10, which lists `tools/cairn.mjs`
  among the files nothing may change, while item 13 requires it to change — and
  the amendment claimed the twelve items were untouched — **fixed**. Item 10 is
  amended by exactly the one line item 13 needs, the amendment says so, and the
  digest was recomputed and rebound. This is the finding that makes the
  amendment honest rather than merely recorded.
- `correctness:` widening by `tools/cairn.mjs` creates the overlap the opening
  acceptance denies — *row 4 shares no file with it* — and the Goal's *does not
  touch the kit's installer* — **fixed**: the Goal is corrected, and the
  amendment names the narrowing, leaving the accepted prose standing as what
  was true when written. Row 4 has no path, so `writes-overlap`, which reports
  live paths only, stays silent; its registration will read this amendment.
- `correctness:` the steps list still said the base fix was CP-CAIRN-009's,
  three sections below an amendment saying it is this path's — **fixed**.
- `correctness:` the next action routed straight to the closing over an
  unimplemented item 13, and `S08` was assigned both to the closing by the plan
  and to the implementation by this step — **fixed**: S08 is item 13, S09 the
  closing, in the record, the plan and here.
- `correctness:` the register was not widened by `tools/cairn.mjs` — the exact
  divergence S06 existed to close, reappearing in the same unit that describes
  it — **fixed**.
- `correctness:` a fifth copy of the false sentence survived, in
  `tools/cairn-check.mjs`'s own comment beside the merge-object refusal, and
  was the last one in the repository — **fixed**.
- `correctness:` item 13 required an inconclusive reading for an unresolvable
  base and named an adversarial case only for the arrival — the stance's own
  *test that never fires* — **fixed**, a case for the crash is named in the
  item, and the kit's generated base gains its assertion.
- `ladder:` item 13 is five changes stated as one item against the three-line
  cap, with no unit boundary — **accepted, and answered in the plan**: it is one
  outcome, a run that judges the integration it reports on, and the five parts
  are one sequence with no useful intermediate state. The plan's numbering note
  names S08 as its unit; if S08's own read finds a boundary, it splits there.

Those fixes are not read a third time. One line they add to the self-review:
`delete:` I corrected the same sentence in four files and missed the fifth,
which was in the code the sentence is about — the copy a reader of the rule
meets first.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci (exit 0), on the staged tree
cairn-test            : pass — 325 tests, 0 failed (exit 0)
scope digest, rebound : `--scope-digest` prints sha256:99b10b19aa4fab0a430b1b7e7ae72af121a4252d31f14b41fd6c1b55904cefa5, the value written into the amendment; the bare gate raises no `scope-digest` finding, so the second block is read as the acceptance in force
the claim, measured   : run 34835675219 on 2816983 — CP-CAIRN-007's integration — logs `base origin/main (flag), 0 changed file(s)`; run 34840124576 on 99554f2 the same
the crash, measured   : `node tools/cairn-check.mjs --base 000…0` and `--base origin/nonexistent-ref-xyz` both exit through an uncaught Git error in `git()`, not a finding — the first clause of item 13
rule gating, read     : `transition`, `journal-entry`, `acceptance`'s arrival branch and `scope-digest` are each keyed on `stateChanged`; `scope-drift` is inside `if (onPath)` and is correctly excluded
last copy             : no occurrence of the false sentence remains — soundness, conformance, the close skill and its reference, the module note, ADR-026 and the checker's comments all read the same way now
no new test           : a documentation unit; the one code change is a comment, and no rule moves
user review           : the owner granted the scope amendment in the chat; the pages are read with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S08
```

#### Checkpoint

```text
status      : running
current step: S07 complete on push
retention   : none (no-rewrite host)
changed     : the two close-skill documents, the conformance page, the soundness note, one comment in the checker, the register, this path's index and plan, and this step
session     : safe boundary after a successful push
next action : S08 — item 13: the unresolvable base reported rather than fatal, the workflow's base per event, the workflow test that pins the defective expression, a fixture for an arrival, the kit's generated base, and the conformance and soundness lines that follow
blockers    : none. The `transition` reading stays with the owner, and item 13 will give it its input — closing one without the other leaves `manual-git` refused for a reason nobody ruled on.
```
