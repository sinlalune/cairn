---
type: Cairn Coding Path Step
title: 'CP-CAIRN-008 S08 — ADR-027, the ready behind an integrating commit'
timestamp: 2026-09-14T00:00:00Z
cairn:
  path: CP-CAIRN-008
  step: S08
---

# CP-CAIRN-008 S08

### S08 — ADR-027, the ready behind an integrating commit — **COMPLETE**

#### Plan

Write, as one decision record, the ruling item 14 rests on, before the rule
moves — as every rule this path changed was recorded first.

`integrationState` reads the `ready` behind an arrival from `commit^`, the
first parent. On `manual-git` the `--no-ff` merge is the integrating unit and
the declaration sits on the second, so that transport cannot close honestly.
It has never mattered, because until item 13 no run reached the rule; the
moment item 13 lands it does, which is why the two must land together.

It is the least because it changes one expression and adds no rule, and it does
not decide anything about transports: reading every parent is the same answer
on both. It does not implement the rule — that is S10 — and it does not touch
item 13.

Definition-of-done item advanced: the fourteenth, which the amendment of
2026-09-14 added; the record half of it.

Type `decision`: the record, every document it amends — the ADR index, the
architecture index, the 1.1 page in place and marked, the register's two rows —
and this step. No test: nothing under `tools/` changes here.

Deliberately not in this unit: `integrationState` itself, its fixture, the
catalogue, the conformance row and the soundness line (S10); the base and the
workflow (S09); and the `--no-ff` merge's own shape, which ADR-026 decision 4
already settled for `acceptance`.

#### Work

```cairn-unit
step: S08
unit: 08
type: decision
verified: cairn-check, cairn-test
```

- **`docs/adr/ADR-027-the-ready-behind-an-integrating-commit.md`**: one
  decision — the `ready` is read from every parent of the integrating commit —
  extending ADR-001 decision 7, with the reason the edge that record closes
  stays closed: a parent is an immediate predecessor, not a point in a range,
  so widening from one to both admits the commit the merge was made from and
  nothing older. Five alternatives refused, the transport gate among them.
- **`docs/adr/index.md`**: the entry, after ADR-026.
- **`docs/architecture/index.md`**: the 1.1 page's line gains it.
- **`docs/architecture/01-cairn-1-1.md`**: the 2026-09-14 amendment note gains
  ADR-001 decision 7; *How a path closes* gains the sentence, marked *since
  2026-09-14*; the transition table's trunk row names the parent and ADR-027.
- **`project/coding-paths/index.md`**: the milestone row counts twenty-seven
  records; row 5 gains ADR-027 with its own provenance, beside ADR-026's, which
  is a different one.
- **The five documents that carried the debt as undecided** — `skills/
  cairn-close/SKILL.md` and its reference, `spec/reference/conformance.md`,
  `tools/soundness.md` and `docs/modules/application.md` — now name ADR-027 as
  deciding it. Each still describes the rule as it is at this commit, reading
  the first parent, because S10 is what changes that: what became false here is
  *no record decides this*, not the account of the behaviour.
- **This path's record and plan**: the second amendment of 2026-09-14 with item
  14 and the digest it rebinds, the Blockers section, and the S08-to-S11
  routing.

#### Self-review

- `shrink:` the record states one decision. The first draft carried a second on
  which parent counts as "the branch"; it is a convention about how a merge was
  invoked, not a fact about the commit, and reading every parent makes the
  question disappear rather than answer it. Cut, and kept only as a refused
  alternative.
- `correctness:` `pull-request` is **not** unchanged, and the record says so.
  An arrival carried by a merge object is refused today by `acceptance` and by
  `transition` both; under this decision only `acceptance` refuses it. The
  first draft claimed no change and offered the existing fixture's unmodified
  passing as evidence — a fixture that asserts `acceptance` only, so it passes
  either way and proves nothing. The consequence is named in the record and
  item 14 requires the fixture to gain the assertion, so the lost refusal
  cannot come back unnoticed.
- `delete:` the decision's pending-`MERGE_HEAD` half, which the second read
  showed no prescribed sequence reaches — the close reference commits the merge
  before running the gate — and which was the only thing making *one expression
  widened* untrue. Refused in the record with its reason, so a later reader does
  not add it back.
- `delete:` no sentence retells the 2026-09-13 forge incident or item 13's
  measurements; the record links this path's S03 and ADR-001 decision 7 and
  states only what neither carries.
- Considered and refused: amending ADR-026 instead of writing a new record.
  ADR-026 says in its own text that it declines this reading; amending it to
  decide the thing it declined would rewrite what was accepted.
- No widening of `writes:` — the amendment of 2026-09-14 took none, and every
  file here was already in scope.
- Net: 459 lines added, 42 removed. The record is 134 of the additions and this
  step most of the rest; the nine documents it amends are a line or two each.

#### Review

First read, a fresh context given this unit's diff and the two criteria, the
ladder and correctness, and nothing else. It attacked the record's central
argument as asked, and returned nine findings. Each with its disposition:

- `correctness:` the record's provenance was invented. It said the debt was
  raised by **CP-CAIRN-007's** S03 and that ADR-026 "named and declined" the
  reading; `grep -c 'transition\|parent'` on ADR-026 returns **0**, and the
  debt is this path's own S03, which put it to the owner at its boundary. The
  false attribution was in five places — the record, the ADR index, item 14,
  the register row and this self-review — **fixed** in all five. Writing a
  provenance from memory into a decision record is the worst place I could have
  done it, and it is what the second criterion exists to catch.
- `correctness:` *`pull-request` sees no change in behaviour* was false, and the
  evidence offered for it was circular: the merge-object shape is refused today
  by `acceptance` **and** `transition`, and under any-parent only `acceptance`
  refuses; the fixture cited as evidence asserts `acceptance` alone, so it
  passes either way — **fixed**. The record now names the given-up refusal in
  *Consequences*, argues why it is right rather than merely tolerable, and item
  14 requires the fixture to gain the assertion.
- `correctness:` five documents the record makes false were in no amend list —
  the close skill, its reference, the conformance row, the soundness note and
  the module note — **fixed**: all five are amended in this unit and named in
  the record's table. They keep describing the rule as it is, because what
  became false is *no record decides this*, not the behaviour.
- `correctness:` the existing `manual-git` fixture already asserts `transition`
  refuses, with a comment stating the first-parent reading as correct; item 14
  spoke of *adding* a fixture when the truth is an assertion and a comment to
  **invert** — **fixed** in item 14.
- `correctness:` "either parent" and "a merge has two" are narrower than the
  decision, which an octopus merge breaks — **fixed**: any parent, in the title,
  the heading, the description and every citation.
- `correctness:` the path record's Blockers section still said the decision was
  with the owner, in the present tense, directly above the paragraph saying it
  was answered — **fixed**.
- `correctness:` the 1.1 page's frontmatter still ended at ADR-026, and the
  sentence for ADR-027 sat inside ADR-026's amendment paragraph, attributing it
  to that record — **fixed**: its own paragraph, the frontmatter, and the source
  pinned at a blob as that page's convention requires.
- `correctness:` the *abandoned `ready`* framing is the checker comment's
  refinement, not ADR-001 decision 7's text, and the fixture cited for it is the
  no-`ready`-at-all one — **fixed**: the record says what decision 7 forbids and
  attributes the refinement where it belongs.
- `ladder:` the uncommitted-arrival case was left undecided by a record quoting
  the very expression that handles it — **fixed**, then **deleted** by the
  second read below.

Second read, a fresh context given only the diff of those fixes and the same two
criteria, as ADR-017 decision 3 bounds it. It re-ran the `manual-git` fixture,
recomputed the digest and confirmed the provenance. Six findings:

- `ladder:` the pending-`MERGE_HEAD` reading the first fix added is plumbing no
  prescribed sequence reaches — `cairn-close`'s `manual-git` block commits the
  merge before running the gate — and `MERGE_HEAD` holds one sha per pending
  parent, a second shape to get right for no reader. It was also the only thing
  making *one expression widened* false — **fixed by deleting it**. The
  uncommitted case reads the head, as today, and the record refuses the
  alternative with its reason so it is not added back.
- `correctness:` *one expression widened* was untrue while that half stood —
  **fixed** with it; the manifesto section now says what the change costs.
- `correctness:` three documents shipping in this same commit still said *no
  record decides that reading* against an accepted ADR-027 — **fixed**, with the
  two others the first read named.
- `correctness:` three surviving "either parent" citations, one of them the ADR
  index's link title, naming the record by a title it does not have — **fixed**.
- `correctness:` the Blockers paragraph still opened in the present tense above
  its own answer — **fixed**.
- `correctness:` the 1.1 page's new amendment paragraph broke that page's own
  convention — no pinned blob, no *stays exactly as it was* — and the
  architecture index gained a comma splice folding ADR-027 into ADR-026's
  promotion sentence — **fixed**, both.

Those fixes are not read a third time. Two lines they add to the self-review:
`delete:` the pending case, above; and `correctness:` I wrote a provenance and a
behaviour claim from memory into a decision record, and both were wrong — the
record is the one artefact where that cannot be corrected later without a
superseding record.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci (exit 0), on the staged tree
cairn-test            : pass — 325 tests, 0 failed (exit 0)
provenance, checked   : `grep -c 'transition\|parent'` on ADR-026 is 0 — it does not reach this reading; the debt is this path's S03, which put it to the owner at its boundary
the failure, run      : the `manual-git` merge-carries-the-edit fixture is green today asserting `transition` blocks; merge^1 is the trunk at `running`, merge^2 the branch at `ready`
the given-up refusal  : the `pull-request` merge-object fixture asserts `acceptance` and its message only, so it passes before and after — which is why it is not evidence, and why item 14 makes it assert the rule that still refuses
scope digest, rebound : `--scope-digest` prints sha256:d4bfa74b199ddff35c2046cecbf30b0f9729ed748065fa98ac4d900129801a1c, the value in the second amendment; the gate raises no `scope-digest` finding
record shape          : `adrFrontmatterErrors` satisfied; sections and filename match ADR-020 to ADR-026
no rule moved         : nothing under `tools/` changes except `tools/soundness.md`'s prose; `integrationState` is untouched, which is S10's work
user review           : the owner ruled the reading in the chat; the record is read with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S09
```

#### Checkpoint

```text
status      : running
current step: S08 complete on push
retention   : none (no-rewrite host)
changed     : ADR-027 and the two indexes, the 1.1 page, the close skill and its reference, the conformance page, the soundness note, the module note, the register, this path's index and plan, and this step
session     : safe boundary after a successful push
next action : S09 — item 13: the unresolvable base reported rather than fatal, the workflow's base per event, the workflow test that pins the defective expression, a fixture for an arrival, the kit's generated base
blockers    : none. S10 must land with S09: item 13 alone would take a manual-git adopter from silently unchecked to refused, which is what ADR-027 and item 14 exist to prevent.
```
