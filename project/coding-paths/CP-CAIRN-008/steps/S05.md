---
type: Cairn Coding Path Step
title: 'CP-CAIRN-008 S05 — the module note'
timestamp: 2026-09-14T00:00:00Z
cairn:
  path: CP-CAIRN-008
  step: S05
---

# CP-CAIRN-008 S05

### S05 — the module note — **COMPLETE**

#### Plan

Make `docs/modules/application.md` describe the checker and the
post-mortem as they are at the candidate, with no history (ADR-010
decision 1).

Four units changed those two tools, and each refreshed the sentence it
made false — S02 the forge reading, S03 the two rules, S04 the
post-mortem's dependency. What none of them could do is the whole: *The
rules* had grown into one four-hundred-word chain of semicolons, and the
post-mortem had a table row and no paragraph, so the tool this path spent
a unit rebuilding was the one the note said least about.

Definition-of-done item advanced: the eighth.

Type `documentation`: the note, and the step. No test: nothing under
`tools/` changes, and the note carries no claim a test could hold.

Deliberately not in this unit: any fact the code does not already have,
and any the corpus already carries. `The kit` is row 4's and is not
touched.

#### Work

```cairn-unit
step: S05
unit: 05
type: documentation
verified: cairn-check, cairn-test
```

- **`docs/modules/application.md`**, *The rules* split at the four
  subjects it had run together: the invocation forms and the rule count;
  **the profile line**, with the forge's three answers; **what it reads
  of a path**; **what it reads of an integration**, where the two
  transport-shaped refusals now sit together.
- A section, **the post-mortem**, which the note did not have: what the
  tool prints, that it makes no judgement, and that every reading and
  every piece of Git plumbing under it is imported from the checker —
  with the local half it still asks Git itself named, so the sentence is
  checkable rather than a claim of purity.
- The tool table's `cairn-postmortem.mjs` row cut to one line now that
  the section carries the detail.
- The frontmatter timestamp follows.

#### Self-review

- `delete:` the table row listed the five readings and the section lists
  them again; the row is now one line and the section is the place.
- `shrink:` the four subjects were one paragraph because they were
  appended one at a time. Splitting them removed no sentence and made
  the two transport-shaped refusals — the merge object, the two paths —
  visible as the pair they are.
- `correctness:` the rule count was measured before it was left alone,
  not assumed: twenty-six distinct rules in the generated catalogue,
  twenty of them reaching a blocking row, six advisory only
  (`concept-growth`, `decision-drift`, `record-date`, `redaction`,
  `remote-checkpoint`, `writes-overlap`). The note's *twenty-six —
  twenty blocking, six advisory* holds.
- Considered and kept: `The kit`'s *installs 27 files*, which is row 4's
  to measure when it changes the kit. This unit does not touch the kit
  and does not re-count it.
- No widening of `writes:`.
- Net: 58 lines added, 38 removed.

#### Review

First read, a fresh context given this unit's diff and the two criteria,
the ladder and correctness, and nothing else. It checked every claim
against the code and measured the rule count independently. Eleven
findings; each with its disposition:

- `correctness:` *every reading of it imported from the checker* —
  **fixed**. The six readings are the tool's own; what comes from the
  checker are its predicates and the Git plumbing. I had written the
  sentence S04 earned as though it said more than it does.
- `correctness:` the body overstating the same way, and omitting that the
  tool imports from `cairn-config.mjs` too — **fixed** with the above.
- `correctness:` *what it asks Git itself* naming three things, when the
  list was longer and the `steps/` listing is not a Git call at all —
  **fixed**, then refused as a framing by the second read and cut.
- `correctness:` *the shape of the `ready` commit the branch declares*,
  dropping half the reading: the tool watches for `ready` **or** `done`,
  and a branch declaring `done` is the incident it exists for — **fixed**.
  This sentence I had carried over verbatim from the table row without
  checking it.
- `correctness:` *the registration commit against `base_commit`*, when
  `registrationReading` compares that commit's **parent** — **fixed**,
  and carried over unchecked in the same way.
- `delete:` the one sentence I added, that two paths reaching `done` in
  one commit is refused on both transports — the conformance page and
  ADR-026 decision 3 both carry it — **fixed**, cut, and the plan above
  corrected, since the unit no longer makes that addition.
- `shrink:` the table row restating the section three lines below it —
  **fixed**, the row is one line.
- `shrink:` *so the two tools cannot drift into two answers about one
  fact* — a record's rationale, not a fact about `tools/` — **fixed**.
- `shrink:` *what the adopter's notes read by hand* — history, in the one
  note that may carry none — **fixed**.
- `shrink:` my restatement of the transport clause longer than the
  sentence it replaced — **fixed**, the shorter one restored.
- `shrink:` the profile paragraph reproducing ADR-026 decision 1's
  context — **fixed**, cut to the fact.

Second read, a fresh context given only the diff of those fixes and the
same two criteria, as ADR-017 decision 3 bounds it. Nine findings:

- `correctness:` *the Git it calls on its own is what the checker has no
  reading for* — false for half the list: the checker reads a commit's
  parent and where the checkout stands itself — **fixed** by cutting the
  framing. An enumeration of Git calls is a fact that rots on the next
  edit, and it had already rotted twice inside one unit.
- `correctness:` the same list omitting `remote get-url` and the
  `base_commit` resolution — **fixed** with it.
- `correctness:` *what it adds is the rendering* — the tool also defines
  its own two forge readings, which the checker has no counterpart for —
  **fixed**, both named.
- `correctness:` *the step files are read from the filesystem*
  understating: every path record and each step's current content come
  off the working tree, and the digest is computed from that text —
  **fixed**.
- `correctness:` *which fields they move*, when `closureReading` reports
  only fields moved outside the set a closure may move — **fixed** by
  cutting the clause; the record's own enumeration is linked instead.
- `correctness:` the step record still describing the addition the fixes
  had removed — **fixed**. A step is append-only once pushed; this one
  was not, and the plan is corrected rather than contradicted.
- `shrink:` the CI sentence's *so* resting on the clause the first fix
  deleted — **fixed**: the reason is back inside that sentence, where it
  costs one clause.
- `shrink:` the withheld sentence restating ADR-026 decision 1 near
  verbatim with no link — **fixed**, cut to the fact.
- `shrink:` the reading list restating ADR-014 decision 1 and the
  architecture row — **fixed**: the note links the record and states only
  what the corpus does not carry, the `ready`-or-`done` widening and where
  the predicates come from.

The fixes above are not read a third time. One line the second read adds
to the self-review: `delete:` two of my sentences were inherited from the
table row and repeated without being checked, which is how both of their
errors survived four units of edits to this file.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci (exit 0)
cairn-test            : pass — 325 tests, 0 failed (exit 0)
rule count, measured  : 26 distinct rules in the generated catalogue, 20 reaching a blocking row, 6 advisory only — the note's figures hold and are left alone
post-mortem, by hand  : `node tools/cairn-postmortem.mjs --branch path/cp-cairn-008` prints six readings and the closing line, which is what the section claims
no new test           : a documentation unit; nothing under `tools/` changes and the note carries no claim a test could hold
user review           : not required for the unit; the owner reads the note at the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S06
```

#### Checkpoint

```text
status      : running
current step: S05 complete on push
retention   : none (no-rewrite host)
changed     : docs/modules/application.md, this path's index and this step
session     : safe boundary after a successful push
next action : S06 — the candidate: merge the trunk in, run the gates bare on C, open the request's description with what `cairn-audit` prints, the administrative commit on the branch with its check green, and the owner reads the profile line the request's run prints before the merge
blockers    : none. The `transition` decision is still with the owner; it is named for the candidate's review and blocks no unit of this path.
```
