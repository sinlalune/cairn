---
type: Cairn Coding Path Step
title: 'CP-CAIRN-001 S08 — every budget target bound, the pilot green from install to done, the version 1.0.0'
timestamp: 2026-09-02T00:00:00Z
cairn:
  path: CP-CAIRN-001
  step: S08
---

# CP-CAIRN-001 S08

### S08 — every budget target bound, the pilot green from install to done, the version 1.0.0 — **COMPLETE**

#### Plan

Measure the weight budget onto the conformance page with the tools, not by
estimate. Rerun the greenfield pilot from this tree — the release candidate —
as a command that drives a repository the kit installs from `init` to `done`
on each transport, and keep it in the suite. Set the package version to
`1.0.0` and rewrite the lock. Then close the path: the tag and the
publication belong to the trunk, after integration, and are stated as the
integrating unit's.

Deliberately not in this unit: the tag, which can only be made on the trunk
once this path has integrated; publishing `cairn-protocol` to npm, which is
the owner's credential; a rerun of the cold-resume trial against the resume
section, which is a 1.1 measurement.

#### Work

```cairn-unit
step: S08
unit: 08
type: implementation
verified: cairn-check, cairn-active --check, cairn-rules --write, test, cairn-pilot on both transports, site build
```

- **The budget, measured.** `spec/index.md` is 5,630 words against 8,000;
  the entry chain — bootloader, path convention, binding, execution protocol
  — 2,895 against 3,000; the kit 26 files and the lock on the `ci` profile
  against 30; one lightweight unit writes 2 protocol files against 6. Every
  target bound, on the conformance page, with the sentence that a release
  moving past one has to say so there.
- **The pilot, as a command.** `tools/cairn-pilot.mjs` installs a throwaway
  repository on a real bare remote, registers a lightweight path with its
  inline acceptance before branching, pushes one unit, merges the trunk in
  and checks the candidate, prints the request's description or scaffolds
  the closing record with the real `cairn-audit`, makes the one
  administrative commit, merges — the forge's merge simulated by the local
  `--no-ff` merge the integrating checkout would fetch back — and lands the
  integrating commit with `done` and the journal entry, running the real
  checker at every gate and counting the protocol files each stage writes.
  Green at all six gates on both transports; a whole lifecycle touches four
  protocol files on `pull-request` and five on `manual-git`. The 0.2 pilot
  of 2026-09-01, run by hand, wrote 24 for one unit and could not close on
  the first run. The pilot is a test in the suite, so it runs on every push.
- **Found by the pilot, in itself.** The pilot's first record linked its step
  file at registration, before the step existed, and the gate refused the
  link — which is what the template says happens. The pilot was fixed, not
  the rule.
- **Version `1.0.0`.** The package says so, the lock was rewritten by
  `update` to say so, and the site's footer names it. The lock's source
  commit is the S07 commit; the tag is the trunk's, after integration.
- Rejected: measuring the budget by hand and pasting numbers. A number the
  tools produce is one the next release can reproduce; the pilot prints the
  unit's cost and the table quotes it.
- Rejected: tagging on the path branch. A release is the trunk's; the tag
  follows the merge, in the integrating unit.

#### Measured

| Surface | 0.2 at genesis | 1.0.0 | Target |
| :-- | --: | --: | --: |
| specification index | 17,455 words | **5,630** | under 8,000 |
| entry chain | 3,600 words | **2,895** | under 3,000 |
| files the kit installs | 119 (86 of them the specification) | **26 and the lock** | under 30 |
| protocol files per lightweight unit | 24 for one unit, at the 0.2 pilot | **2** | under 6 |
| rule names | 38 | 24 (19 blocking, all with a fixture) | about 20 |
| own concepts | 50 | 31 | about 30 |
| specification tree | 86 files, 49,872 words | 64 files, 35,659 words | — |
| checker | 3,578 lines | 3,037 lines | — |
| tools, without tests | 6,200 lines | 5,167 lines, of which the command, the rules generator and the pilot are this repository's | — |
| tests | 277 subtests in Atomik | 226 subtests, 26 adversarial fixtures, the pilot on two transports | — |

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, 2 advisories (concept-growth, explained in S01; scope-drift on the 0.2 folders deleted in S03, S04 and S06, with the declaration moved)
cairn-active --check  : pass — view current
cairn-rules --write   : pass — catalogue and linkage rewritten without a second diff
npm test              : pass — 226 subtests, 0 failing
cairn-pilot           : pass — install to done, green at six gates, on pull-request and on manual-git
site build            : pass — 74 documents at 1.0.0
user review           : not required
remote                : pushed immediately after this record; the closure names this commit as the candidate
```

#### Checkpoint

```text
status      : running
current step: S08 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn-pilot.mjs and its test (new), package.json (1.0.0), cairn.lock.json (rewritten at 1.0.0), spec/reference/conformance.md (the budget measured, the pilot row, the claim), docs/modules/application.md, this record
session     : safe boundary after successful push and a green CI run
next action : close the path with cairn-close on pull-request transport — the trunk merged in, this commit the candidate C, the checks on C, the request opened from this branch with the review as its description, the administrative commit setting ready, the owner's approval and merge; then the integrating unit on the trunk: done, the journal entry, the tag 1.0.0, and the owner's npm publish
blockers    : none
```

#### Addendum — read after the push

CI was red on the S08 push: the pilot's merge commit had no Git identity on
the runner, where none is configured, while its ordinary commits carried one
per call. The pilot now runs every Git command as its own author, and the
suite is what noticed — on the first run of the file it added. The unit is
complete on the commit that carries this addendum, once its run is green.
