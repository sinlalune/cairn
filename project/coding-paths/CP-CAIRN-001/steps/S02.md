---
type: Cairn Coding Path Step
title: 'CP-CAIRN-001 S02 — the rules cut to twenty-four, every blocking one proved'
timestamp: 2026-09-02T00:00:00Z
cairn:
  path: CP-CAIRN-001
  step: S02
---

# CP-CAIRN-001 S02

### S02 — the rules cut to twenty-four, every blocking one proved — **COMPLETE**

#### Plan

Retire and demote per the convergence record's keep and retire lists; fold
`closure-surface`, `advisory-disposition` and `coherence-audit` into
`acceptance`; bring the fixture suite over from Atomik at `46bdd11` and keep
every fixture of every kept rule; regenerate the catalogue and the linkage;
rewrite the conformance matrix against the kept rules; settle or state the
CP-OPS-002 debts. Delete before adding: the checker loses code before the
suite gains a test.

Deliberately not in this unit: the path-record shape and the routes in the
checker (S03 — `foundation` is still accepted, opening acceptance is still read
from a session record, the brief still exists as a lifecycle record); the
pull-request transport (S04); the kit rebuild and the lock (S06). Each is
named on the conformance page where it leaves a row partial.

#### Work

```cairn-unit
step: S02
unit: 02
type: implementation
verified: cairn-check, cairn-active --check, cairn-rules --write, test
```

- **Cut the checker from thirty-nine rule names to twenty-four**: nineteen
  blocking, five advisory-only. Retired `checkpoint-retention` (dead under the
  default policy; a `retained` host supplies its own check, and the concept
  page and the configuration field say so), `migration-debt`, `ledger-size`,
  `brief-schema`, `base-parity`, `single-truth` and `path-staleness`. Folded
  `closure-surface`, `advisory-disposition`, `coherence-audit` and
  `role-collapse` into `acceptance`; `same-work-unit` and `area-note` into
  `work-unit`; `branch-identity` into `branch-path`; `opening-ceremony` into
  `schema`. Demoted `redaction` and `record-date` to advisory. Kept `rebase`
  under its historical id because the keep list names it; its remedy is the
  merge. The checker is 2,998 lines, from 3,602.
- **Beyond the lists, two more names went.** `single-truth` and
  `path-staleness` stood behind no requirement the 1.0 specification states:
  `derived-view` already blocks the one generated file, the roadmap register of
  chapter 4 is hand-written by design, and a quiet path is not a wrong
  repository. `role-collapse`, `branch-identity` and `opening-ceremony` were
  unlisted too and are folds, not cuts: the requirement stays, under the rule
  it is a case of.
- **One invocation form remains.** `--working-tree` and `--previous` are gone
  with `base-parity`. Record integrity is now judged against the merge-base
  with the trunk like every other changed-file rule, which settles the
  CP-OPS-002 debt that the working-tree form read status rather than content
  against the trunk: a committed and pushed edit to an immutable record is
  found by the local run, not only by the pull request (fixture added).
- **Configuration schema 2** is schema 1 without `sharedFiles` and
  `staleAfterDays`. A schema-1 file is refused by name with the migration
  named; `npx cairn adopt` writes it (S06). `cairn.config.json`, the JSON
  schema, the loader, the initializer and the configuration reference moved
  together.
- **The fixture suite came over and was completed.** Seven test files, 220
  subtests, ported from Atomik's 277 and rewritten against this repository's
  binding rather than another host's paths; the fixtures of retired rules left
  with them, and the fixtures of folded rules assert the name they fire under
  now. The adversarial file had eight fixtures and a declared list of sixteen
  uncovered blocking rules; it now has a fixture for **every one of the
  nineteen** — `registration` (a path branched before its declaration reached
  the trunk), `registration-base`, `branch-path`, `rebase`, `transition`,
  `scope-drift`, `path-history` (a published commit amended), `journal-entry`,
  `record-integrity`, `acceptance-drift` (another participant lands inside the
  surface on the remote trunk), `provisional` — and the uncovered list is
  empty, kept honest in both directions by the coverage test. `cairn-spec.test`
  was not ported: it tests the 0.2 HTML build and a renderer the kit does not
  ship; the site is S07.
- **`cairn-init` repaired enough to build fixtures**: it read the path
  convention from Atomik's project root, wrote a folder log into the
  specification, and generated a concept template the specification now
  carries as a file. It reads the convention from the source repository's own
  project plane, copies `tools/soundness.md` beside the tools it documents,
  and writes the area note its configuration names (pilot finding 16,
  settled). The specification links the manifesto by URL: the kit stops
  copying the specification in S06, and an adopter's repository carries its
  own vision. The generated workflow runs on `path/**` pushes like this
  repository's, and both run `npm test` before the gate in the one job that is
  the required check.
- **The conformance page rewritten** against the 1.0 specification: twenty-nine
  rows, every rule behind a stated row (the "no conformance row" map is empty
  and kept), three rows partial on the shapes S03 and S04 replace, the cut
  recorded name by name for readers arriving from 0.2 records. `cairn-rules`
  regenerated the catalogue and the linkage into it.
- Rejected: keying `work-unit`'s required parts to the declared type, which
  the specification's table states. The merge-deciding comparison sees every
  unit on the branch at once, so the current unit's type cannot select which
  parts of a cumulative diff must move together; the matrix says so.
- Rejected: renaming `rebase` to a name that does not tell a no-rewrite host
  to rebase. The keep list names it, and the remedy in the finding is the
  merge.
- Rejected: carrying the manifesto into every installed repository so the
  specification's relative link resolves. It dragged the convergence record
  behind it, and a kit that copies the protocol's design history into an
  adopter's docs is the heavy body the cut exists to remove.
- **CP-OPS-002 debts, each settled or stated.** Sixteen blocking rules without
  a fixture: settled, nineteen of nineteen. Pilot finding 16: settled in
  `cairn-init`. The working-tree form of `record-integrity`: settled. Forward-
  plan item 12: settled by this repository's existence. `resolveBase` on a
  non-path branch: stated — a trunk run is the corpus sweep, and under
  pull-request transport every trunk change was judged branch-versus-trunk
  before it landed (S04). The `ci-unread` advisory: stated — a checker cannot
  know CI was read; the pull request's required check makes it native at
  integration, and reading it after every unit is the skill's procedure (S05).
  The correction-record remedy with no predicate: stated on the repair row.
  Pilot finding 7 (`brief-stale`): overtaken — the brief merges into the
  record's resume section in S03, and staleness of that section is the
  cold-resume judgement. The lightweight reliefs, `cairn-new`, `cairn-close`
  and the update path: S03 and S06, stated on their rows.

#### Measured

| Surface | After S01 | After S02 |
| :-- | --: | --: |
| rule names in the catalogue | 39 (30 blocking) | **24 (19 blocking)** |
| blocking rules with an adversarial fixture | 8 of 30 | **19 of 19** |
| checker | 3,602 lines | 2,998 lines |
| tools, without tests | 5,299 lines | 4,613 lines |
| tests | none installed | 7 files, 2,881 lines, 220 subtests |
| conformance page | 5,080 words | 4,397 words |
| reference | 10 pages, 16,489 words | 10 pages, 15,842 words |
| specification tree | 65 files, 36,951 words | 65 files, 36,360 words |
| specification index | 5,588 words | 5,588 words (unchanged) |
| entry chain | 2,764 words | 2,778 words |
| files the kit installs | 101 (the install failed) | 100, and the install passes its own gate |

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, 1 advisory (concept-growth, the trunk's wiki listing at spec/concepts is still empty; explained in S01), base origin/main, 118 changed files
cairn-active --check  : pass — view current
cairn-rules --write   : pass — catalogue and linkage rewritten in spec/reference/conformance.md
npm test              : pass — 220 subtests, 0 failing, including 23 adversarial fixtures against real installed repositories and 4 parity tests
user review           : not required
remote                : pushed immediately after this record; the commit id is named in the path index and the brief of the next unit
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : tools/** (checker cut, rules maps, config schema 2, init, seven test files), cairn.config.json, package.json, .github/workflows/cairn.yml, AGENTS.md, spec/index.md (manifesto by URL), spec/reference/{conformance,configuration}.md, spec/concepts/{roles,checkpoint-retention}.md, docs/modules/**, this path's record and brief
session     : safe boundary after successful push and a green CI run
next action : S03 — one folder per path: opening acceptance inline in index.md, the resume section replacing the brief, the lightweight route as specified, depends_on: and the unblocked view, foundation folded into full, emergency dropped; migrate this path's own record in the same unit; schema then reads the opening acceptance from the record
blockers    : none
```
