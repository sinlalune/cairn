---
type: Cairn Coding Path Step
title: 'CP-CAIRN-014 S04 — the corpus, the messages, the fixture and the advisory'
timestamp: 2026-09-24T00:00:00Z
cairn:
  path: CP-CAIRN-014
  step: S04
---

# CP-CAIRN-014 S04

### S04 — the corpus, the messages, the fixture and the advisory — **COMPLETE**

#### Plan

Four small readings, as the plan's fourth item says: `feedbacks/` joins
the corpus `links` reads (ADR-038 decision 1); the two `comparison`
messages say GitHub (ADR-034 decision 6); the fixture that went red once
on `record-integrity` is read for the order it depends on (ADR-034
decision 7); and the advisory `current-step` (ADR-044 decision 4). One
fixture each for the corpus and the advisory, the rows on the
conformance page, in the catalogue, in the soundness note and in the
module note. And, on the owner's word at S03's boundary — *track it in
backlog* — the item S03 found, as a file under `project/backlog/`.

Definition-of-done item advanced: the fourth, in full; the fifth's
catalogue and module-note halves; the sixth, the messages.

Type `implementation`. The two fixtures were written after the code in
this unit, and each was run against the checker at `HEAD` before the
commit: both fail there and pass here.

Deliberately not in this unit: the candidate (S05).

#### Work

```cairn-unit
step: S04
unit: 04
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn-check.mjs`**:
  - `FEEDBACKS_DIR`, a fixed name beside `SKILLS_DIR`, is a root
    `markdownCorpus` walks; `links` reads it, and `schema` has nothing
    to read there.
  - The two `comparison` messages, and the comment above them, say
    GitHub where they said *the forge*; no printed message says it now.
  - `stepRecordOrigin` passes `--topo-order` to `git log --follow`.
  - The advisory `current-step`, over every `running` or `ready` record
    whose last step file — by number, from `lastStepFile` — is not its
    `current_step`; silent for a flat record and before the first unit.
- **`tools/cairn-fixture.test.mjs`**: a feedback note linking a page
  that moved, refused by `links` with the finding under `feedbacks/`;
  and the advisory — silent before the first unit, reported once with
  both names when S02 stands behind a field reading S01, never
  blocking, silent once the field names S02. Both on a published
  history carrying another path's merged unit.
- **`tools/cairn-rules.mjs`**, the catalogue and linkage it writes:
  `current-step` behind the work-unit row; the `links` condition names
  the roots.
- **`spec/reference/conformance.md`**: the work-unit row names the
  advisory; the links row names `skills/` and `feedbacks/`.
- **`tools/soundness.md`**: one sentence, beside the paragraph on the
  field that stopped selecting.
- **`docs/modules/application.md`**: the corpus and what it reports of
  a path.
- **`project/backlog/2026-09-24-concept-links-outside-a-concepts-folder.md`**:
  what S03 found in `conceptLinkTargets`, which path found it, the
  owner, the shape of the work. `writes:` widened by `project/backlog/**`
  for it; the folder had no file before this one, and its index is what
  the kit installs at row 3.

**The fixture red once.** CP-CAIRN-011 S01's first suite run went red on
one assertion — a fixture expecting no blocking finding met
`record-integrity` — with the test's name lost; two reruns passed. Read
once for the order it depends on, as decision 7 asks: `record-integrity`
compares a step record with the blob that added it, which
`stepRecordOrigin` takes as the oldest *added* entry of `git log
--follow`, and that listing was ordered by commit date. A record added
more than once — a delete and a re-add — across a merge whose commits
share a second, or under a skewed clock, could be read against the
newer blob. That is an order the rule depends on, and it is pinned:
`--topo-order`, which `recordHistory` beside it already passes for the
same reason. Whether it was the red run's cause is not proved — no
fixture here adds a step record twice — and the suite was run on a
frozen copy of `HEAD` eight times to look for the red: eight runs of
the fixture file, 69 tests each, none failed. Not reproduced.

#### Self-review

- `delete:` a first module-note edit rewrapped two whole paragraphs for
  two clauses, a 52-line diff; undone, the clauses placed with local
  breaks.
- `shrink:` `lastStepFile` first re-listed `steps/` beside
  `pathWorkUnits`, which already did — one `stepFiles` for both, on the
  review's finding.
- `yagni:` no fixture that adds a step record twice across a same-second
  merge to prove the order: the pin is one flag, the rule beside it
  already passes it, and the review reproduced the inversion by hand.
- Widening of `writes:`: `project/backlog/**`, named above.
- Net: 151 lines added and 27 removed across the eight files, 37 of them
  the backlog item and 40 the two fixtures, beside this step and the
  resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria, told to verify by running things; it
returned in about five minutes, on the first attempt, with eight
findings, having run both suites and a scratch history with a rename, a
merge and same-second commits:

- `correctness:` `tools/cairn-rules.test.mjs` declares the advisory
  rules and failed two tests on the new one — **fixed**, `current-step`
  added to its list; `writes:` names that file.
- `correctness:` the advisory told a writer mid-unit — the field
  advanced, the step file not yet written — to move the field back —
  **fixed** in the message, which now says to make the two agree by the
  step the newest completed unit wrote; the rule still reads *not the
  last*, as ADR-044 decision 4 says, and a field ahead is refused by
  `work-unit` in any commit, so the advisory there repeats a refusal.
- `correctness:` the module note said *a live record*, and `blocked` is
  live — **fixed**, *a `running` or `ready` record*.
- `correctness:` the module note's corpus sentence read as if `skills/`
  and `feedbacks/` were where a host binds its wiki — **fixed**, moved
  into the list.
- `correctness:` `FEEDBACKS_DIR`'s comment said the kit installs the
  folder, which is row 3's — **fixed**, *which ADR-038 decision 2 has
  the kit install*.
- `correctness:` the backlog item understated the defect: under such a
  root no correct link clears any note, the top-level ones included —
  **fixed**, title and text, with the four shapes it ran.
- `shrink:` `lastStepFile` re-listed `steps/` — **fixed**, `stepFiles`
  shared with `pathWorkUnits`.
- `correctness:` the `--topo-order` comment said a skewed clock alone
  inverts the order, which needs a merge too; and decision 7 names the
  fixture file as where a pin goes — **fixed** in the comment; the pin
  is in the tool because the order is the tool's, and this step says
  why there is no fixture for it (self-review).

It found sound: the messages, this repository's `feedbacks/` under
`links`, the advisory reporting only this path among this repository's
records — the two stale integrated records are `done` and unread — and
the rows against the code.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it returned
in about two minutes, on the first attempt, having compared
`pathWorkUnits` before and after the extraction on all 126 records here
— no difference — with four findings:

- `correctness:` the advisory's advice ended *in that unit's own
  commit*, which on a pushed unit points at an amend this host forbids —
  **fixed**, the clause dropped.
- `correctness:` the backlog item said the four shapes the first read
  ran returned nothing, beside a sentence about the bare forms, which do
  return a name — **fixed**, the correct links and the bare forms stated
  apart, the regex's unanchored `./` and `../` named.
- `shrink:` a stray space in the `--topo-order` comment, and an indent
  of four in the rules test — **fixed**, both.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0); `current-step` silent, the field and the last step file both S04
cairn-test            : pass — 341 tests, 0 failed, exit 0; the two fixtures and the rules test among them
cairn-active --check  : pass — the view is already current; no path state changed
the red first         : both fixtures run against HEAD's checker — both fail; with this unit's, both pass
remote                : pushed immediately after this record; the commit id is named in the resume section by S05
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn-check.mjs, tools/cairn-fixture.test.mjs, tools/cairn-rules.mjs, the conformance page, the soundness note, the module note, the backlog item, this path's index and this step
session     : safe boundary after a successful push
next action : S05 — the candidate, closed with cairn-close
blockers    : none
```
