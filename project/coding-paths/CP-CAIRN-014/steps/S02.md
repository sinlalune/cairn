---
type: Cairn Coding Path Step
title: 'CP-CAIRN-014 S02 — the registration rule reads the change under review'
timestamp: 2026-09-24T00:00:00Z
cairn:
  path: CP-CAIRN-014
  step: S02
---

# CP-CAIRN-014 S02

### S02 — the registration rule reads the change under review — **COMPLETE**

#### Plan

Make `registration` read a registration carried by the change under
review, on `pull-request` registration (ADR-032 decision 3): two
fixtures, the rule's rows on the conformance page, in the catalogue and
in the soundness note, and the module note.

Definition-of-done item advanced: the second, in full; the fifth's
module-note half for this rule.

Type `implementation`: the failing fixture first. Read before writing:
on `pull-request` registration the open skill carries the commit on
`register/<id>`, which is not a path branch, and `registration` returned
before reading anything there — so a registration request was green
today, and one carrying a product file too, refused only by `work-unit`
for the module note it lacked. The green fixture passes before and after
this unit and guards the new reading against refusing the honest shape;
the refused one failed first, with `work-unit` the only rule named.

Deliberately not in this unit: the installer's refusals and the pilot's
second sequence (row 3); the link exemptions (S03); the corpus, the
messages and the advisory (S04).

#### Work

```cairn-unit
step: S02
unit: 02
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn-check.mjs`**: `requestRegistrations` reads, on
  `pull-request` registration and off the trunk, each record the
  comparison makes `running` that the trunk does not declare — one
  already `running` at the merge-base is an edit and skipped. It is a
  registration when the commit that declared it `running` is not a merge,
  touches the record's folder and `ACTIVE.md` alone, is parented on
  `base_commit`, and the comparison carries nothing else; otherwise it
  gets its reason. Off a path branch a reason is a new `registration`
  refusal naming the remedy; on a path branch the existing refusal
  carries it. `declaredOnTrunk` is the trunk lookup the state function
  held inline, now shared; `REGISTRATION_TRANSPORT` is read from the
  configuration beside `INTEGRATION_TRANSPORT`, not exported.
- **`tools/cairn-fixture.test.mjs`**: `registrationRequest()` — a
  published `pull-request` repository whose trunk carries another path's
  merged unit (ADR-004 decision 4), a `register/` branch cut from it with
  the registration alone; the request read green, and the same request
  with a product file refused by `registration`, its remedy held.
- **`tools/cairn-rules.mjs`**, and the catalogue and linkage it writes on
  **`spec/reference/conformance.md`**: the rule's condition and
  enforcing expression; the page's hand-written row says the second
  reading.
- **`tools/soundness.md`**: the rule as the transport lesson's other
  side, and why the widening is narrow.
- **`docs/modules/application.md`**: what the checker reads of a path
  names the registration in the change under review.

A path branch is not read as registered by its own range: a path branch
carrying only the registration is refused by `work-unit` in any case — its
record changed with no unit block — and the open skill creates the branch
from the trunk after the request has merged. The definition of done's
second item is met on the branch the skill names, `register/<id>`, and the
closing reader is told so.

On `manual-git` nothing new is read, so this repository's own gate reads
exactly as before; ADR-032 decision 3 names no transport, and the reading
is limited to the one whose sequence it serves — a `manual-git`
registration arrives by the trunk push the old reading answers, and the
existing fixture *a path branched before its declaration reached the
trunk* keeps refusing that shape there.

#### Self-review

- `delete:` a first draft read the working tree as the pending
  registration, for the run before the commit; the default run on a
  `register/` branch has no base, and `--base` at the branch point is
  already inconclusive under `comparison` — dead code, cut.
- `stdlib:` none.
- `shrink:` the trunk lookup loop with its try/catch became
  `declaredOnTrunk` over `gitOrNull`, which already pipes stderr.
- `delete:` a `request` state that `pathRegistrationState` answered for
  a path branch carrying its own registration, with a branch in
  `registration-base` to accept it — the one verdict it turned green was
  still refused by `work-unit`; cut on the review's finding.
- Widening of `writes:`: none — every file is declared.
- Net: 149 lines added and 31 removed across the six files, 47 of the
  added the two fixtures, beside this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria, told to verify by running things; it
built nine scenarios in a scratch copy and ran both fixtures against the
old checker, and returned in about six minutes, on the first attempt,
with six findings. It found no honest flow refused — a request merging
the moved trunk in, a draft activated by request, a flat record, a trunk
run, a `manual-git` repository, a missing base:

- `correctness:` the soundness note and the fixture comment said the
  honest request *was refused*; on `register/<id>` the old rule never
  read it, and the green fixture passes on the old checker — **fixed**,
  both say what the old rule did, and the `request` state that turned
  the one changed verdict green is deleted (self-review).
- `correctness:` `statusCommit` over the range finds the first commit
  reading `running`, so an edit to a record already `running` was named
  a registration — **fixed**, a record `running` at the merge-base is
  skipped.
- `correctness:` the prose said *the record* where the code admits the
  record's folder — **fixed** in the prose, not the code: a registration
  commit carries the folder, this path's own `45c7fb6` its `plan.md`.
- `shrink:` the merge-base was recomputed where the caller holds it —
  **fixed**, passed in.
- `shrink:` an empty `if` branch in the rule — **fixed**, gone with the
  state.
- `shrink:` `REGISTRATION_TRANSPORT` exported, and named like the
  installer's default — **fixed**, not exported.

Found by the writer beside the six: the refusal's remedy used *move*,
which the remedy test's imperatives do not hold — reworded to *land*.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it returned
in about two minutes, on the first attempt, with the code confirmed —
the refs, the skip, no caller of the deleted state — and five findings
in the prose:

- `correctness:` the catalogue, the conformance row and the soundness
  note still claimed a path branch is read as registered by its own
  range — **fixed**, all three say the reading is off a path branch and
  a path branch still needs the declaration on the trunk.
- `correctness:` the conditions listed left out *not a merge*, and *the
  four* miscounted — **fixed**, listed, no count.
- `correctness:` this step's work section described the deleted state
  and an export — **fixed**, rewritten.
- `shrink:` two lines past the wrap width, in the function's comment and
  the module note — **fixed**, rewrapped.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0)
cairn-test            : pass — 336 tests, 0 failed, exit 0; the two new fixtures among them
cairn-active --check  : pass — the view is already current; no path state changed
the red first         : the product-file fixture failed before the rule changed, with work-unit the only blocking rule
remote                : pushed immediately after this record; the commit id is named in the resume section by S03
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn-check.mjs, tools/cairn-fixture.test.mjs, tools/cairn-rules.mjs, spec/reference/conformance.md, tools/soundness.md, docs/modules/application.md, this path's index and this step
session     : safe boundary after a successful push
next action : S03 — the links rule reads the declared exemptions
blockers    : none
```
