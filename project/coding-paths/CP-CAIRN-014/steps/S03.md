---
type: Cairn Coding Path Step
title: 'CP-CAIRN-014 S03 — the links rule reads the declared exemptions'
timestamp: 2026-09-24T00:00:00Z
cairn:
  path: CP-CAIRN-014
  step: S03
---

# CP-CAIRN-014 S03

### S03 — the links rule reads the declared exemptions — **COMPLETE**

#### Plan

Let `cairn.config.json` declare the files and folders whose relative
links `links` does not resolve, each with its reason (ADR-037 decision
1): the field in the schema and the loader, a declaration with no reason
refused; the rule skipping the declared files; one fixture; the field's
row of the configuration reference; the `links` rows of the conformance
page and the catalogue, the page's row saying the two 0.2 exemptions
left deliberately and are the adopter's to declare (decision 2's
conformance half).

Definition-of-done item advanced: the third, in full.

Type `implementation`: the failing tests first — the loader's refusals,
and the fixture, which failed first with the checker unable to load a
configuration carrying the unknown field.

Deliberately not in this unit: `adopt` naming the shape that wants a
declaration (decision 2's second half, `staleShapes` in
`tools/cairn.mjs`, row 3's); the corpus and the rest of S04.

#### Work

```cairn-unit
step: S03
unit: 03
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn-config.schema.json`** and **`tools/cairn-config.mjs`**:
  `linkExemptions`, optional, an array of `{ path, reason }` — a
  repository-relative path and a reason that is not blank, no other
  field, no path twice (the loader alone checks the last; JSON Schema
  has no uniqueness by key).
- **`tools/cairn-check.mjs`**: the `links` loop reads the corpus less
  each declared file, or each file under a declared folder. The
  declared files still count as linking a concept for `concept-orphan`:
  the exemption says their links resolve elsewhere, not that they link
  nothing.
- **`tools/cairn-config.test.mjs`**: one test of the six shapes — a good
  declaration, a missing reason, a blank one, an escaping path, a
  repeated path, a field that is not an array.
- **`tools/cairn-check.mjs`**, `markdownCorpus`: a concept root at the
  top level made the corpus walk `.`, whose paths printed as `./docs/…`,
  which no declaration matched — fixed where every caller reads, on the
  review's finding, with its own fixture below.
- **`tools/cairn-fixture.test.mjs`**: `publishedTrunk()`, the published
  trunk carrying another path's merged unit (ADR-004 decision 4), now
  shared with S02's `registrationRequest()`; the fixture writes a page
  with a link that resolves nowhere, declares its folder — green — then
  withdraws the declaration — refused by `links`; and a top-level concept
  root, a note linked only from inside the wiki, refused by
  `concept-orphan`.
- **`tools/cairn-rules.mjs`**, the catalogue and linkage it writes, and
  **`spec/reference/conformance.md`**'s hand-written row, with the line
  decision 2 asks for.
- **`spec/reference/configuration.md`**: the field's row.
- **`docs/modules/application.md`**: the corpus paragraph names the skip.
- **`project/coding-paths/CP-CAIRN-014/index.md`**: `writes:` widened by
  `tools/cairn-config.test.mjs`, where the loader's refusals are tested —
  the file the loader's own suite lives in, missed when `writes:` named
  the loader.

The installer keeps the field when it rewrites the configuration:
`migrateConfig` spreads the host's fields, read before writing; nothing
in `tools/cairn.mjs` changes.

#### Self-review

- `shrink:` the fixture's published trunk was the five lines S02 wrote
  inline — one helper for both.
- `yagni:` no glob in a declared path: ADR-037 names a file or a folder.
- Widening of `writes:`: `tools/cairn-config.test.mjs`, named above.
- Net: 130 lines added and 13 removed across the nine files, 74 of the
  added in the two test files, beside this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria, told to verify by running things; it
returned in about two minutes, on the first attempt, having run both
suites, the installer's migration with the field, and the prefix cases —
`docs/port` does not exempt `docs/portrait/x.md` — with six findings:

- `correctness:` a concept root bound at the top level makes
  `markdownCorpus` walk `.`, whose paths print as `./docs/...`, which no
  declaration matches — nor the corpus's own dedupe — **fixed** at the
  root, in `markdownCorpus`, for every caller.
- `correctness:` the schema accepts a repeated path the loader refuses —
  **fixed** in part: the field's `description` says the loader refuses
  it, JSON Schema having no uniqueness by key. The shared `$defs/path`
  accepting shapes `isRelativePath` refuses predates this field and
  covers every root; **refused** here, a change to every root's schema
  for a gap no reader has met.
- `shrink:` `slash()` handles a trailing slash the loader refuses —
  **refused**: it is the codebase's helper for the join, and reusing it
  is the ladder's second rung.
- `correctness:` two entries with no path raised a spurious *must not
  repeat* — **fixed**, only string paths are compared, and the test
  holds it.
- `correctness:` the conformance row said the exemptions left *in S02*
  as though S02's record said so; S02 does not mention them, S06 and
  ADR-037's context do — **fixed**, both cited.
- `yagni:` a declared path that no longer exists is silently a no-op —
  **refused**: ADR-037 asks for no report of it, and the declaration is
  read in the one file every reviewer reads.

It also noted decision 2's `adopt` half is absent; it is row 3's, as the
plan says, and no page claims it.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria — first handed
a diff that held the whole of five files, corrected by message to the
fix-only diff before it answered; it returned in about two minutes, on
the first attempt. It found no defect: it ran a scratch copy with a
top-level concept root, 112 `links` findings in `./`-and-plain pairs
before the fix and none doubled after, the declared file skipped; and it
found the same fix repairs `concept-orphan`, whose skip of the wiki's own
files never matched a `./` path. One finding:

- `correctness:` no test fails if the normalisation is reverted —
  **fixed**: a fixture with the concept root at `wiki`, a note at its top
  linked only from inside the wiki, refused by `concept-orphan`; run with
  the normalisation removed it fails, with it it passes. Its first draft
  put the note one folder down and passed both ways — `conceptLinkTargets`
  clears a note by a bare name or through `concepts/`, so a folder note
  linked by `./` was never cleared by either reading.

Found while writing that fixture, and left: `conceptLinkTargets` keys on
the word `concepts/`, so under a concept root named otherwise a note one
folder down is cleared by no link at all, from outside the wiki or from
in it. That is the rule's resolution, not this unit's reading; the
boundary report names it for the owner.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0)
cairn-test            : pass — 339 tests, 0 failed, exit 0; the loader test and the two fixtures among them
cairn-active --check  : pass — the view is already current; no path state changed
the red first         : the loader test failed on the unknown field; the fixture failed with the checker refusing to load the configuration
remote                : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : the configuration schema, loader and test, tools/cairn-check.mjs, tools/cairn-fixture.test.mjs, tools/cairn-rules.mjs, the conformance and configuration references, the module note, this path's index and this step
session     : safe boundary after a successful push
next action : S04 — the corpus, the messages, the fixture and the advisory
blockers    : none
```
