---
type: Cairn Coding Path Step
title: "CP-CAIRN-014 S07 — what the request's reviewer found"
timestamp: 2026-09-25T00:00:00Z
cairn:
  path: CP-CAIRN-014
  step: S07
---

# CP-CAIRN-014 S07

### S07 — what the request's reviewer found — **COMPLETE**

#### Plan

A reviewer on request 31, a bot of the host, left two findings on the
candidate `095ff6edf0c32ce8bf7767f54695ad208e1445c7`, both read against
the code before acting, both true. They change the candidate, so it is
void: the record returns from `ready` to `running`, `subject_commit`
empties, and the close starts again on this unit's commit.

- The registration reading compared file names, not commits: a
  registration commit followed by a second commit touching only the
  record's folder or the live view names no file outside the
  registration's, and was read as a registration. ADR-032 decision 2's
  request carries *that commit and nothing else*, and decision 3 refuses
  *a registration and anything else*.
- `docs/modules/application.md` counted twenty-six rules, twenty
  blocking and six advisory — stale before this path, which found
  twenty-seven at its base, and two behind once `current-step` was
  added. The same comment named `README.md:52`, which says
  twenty-seven.

Definition-of-done items advanced: the second (a registration is one
commit) and the fifth (the module note describes the checker as it is).

Type `implementation`: the failing fixture first — a registration
request with a second commit writing a step record inside the record's
folder, read green before this unit.

Not in this unit: `README.md`. The seventh item keeps it byte-identical,
and register row 5 owes the kit's counts written once on the conformance
page and linked from the README (ADR-031 decision 1); a count restated
there now is the restatement that row deletes. The reply on the request
says so.

#### Work

```cairn-unit
step: S07
unit: 07
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`tools/cairn-check.mjs`**, `requestRegistrations`: a registration
  whose declaring commit is not `HEAD` is refused, naming the commit —
  one commit, not one set of names.
- **`tools/cairn-fixture.test.mjs`**: the request with a second commit
  inside the record, refused by `registration`.
- **`docs/modules/application.md`**: twenty-eight rules, twenty-one
  blocking and seven advisory, counted from `extractRules` over the
  checker's source.
- **`project/coding-paths/CP-CAIRN-014/index.md`** and the live view:
  `running` again, `subject_commit` empty, `current_step` S07, as path 1
  returned from `ready` in its S08.

A request updated from the trunk — GitHub's *Update branch*, a merge of
the trunk into `register/<id>` — is refused by the same condition, and
the refusal says so: ADR-032 decision 2's branch *carries that commit and
nothing else*, and `registration-base` already accepts a registration
whose trunk moved while it waited, so the merge buys nothing. The open
skill's sentence — *a branch that holds that commit and nothing else* —
already asks it; `skills/` is outside this path.

The catalogue, the conformance row and the soundness note already said
*the comparison carries nothing else*; with this unit it is true commit
by commit, and they are unchanged.

#### Self-review

- `shrink:` one condition and one fixture; nothing in the codebase read
  a range's commits for this.
- Net: 21 lines added and 1 removed across the three files, 17 of them
  the fixture, beside this step, the resume and the view.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff, the new step and the two criteria, told to verify by
running things; it returned in about two and a half minutes, on the
first attempt, having run the new fixture with the line removed — red —
and probes for every honest flow — the trunk moved while the request
waited, a path branch after its registration merged, a trunk push, a
`manual-git` repository, CI checking out the request's own head — all
unchanged. Three findings:

- `correctness:` the new reason ran before the file reason, so a later
  commit adding a product file was refused without naming the file —
  **fixed**, the file reason first.
- `correctness:` a request updated from the trunk is now refused, which
  nobody had written down, and GitHub offers the button — **fixed** in
  the refusal, which says a merge of the trunk counts, and in this step;
  the open skill already asks for the one commit.
- `correctness:` `README.md:52` says twenty-seven — **refused**, as the
  plan says: the seventh item keeps it, and row 5 owes the counts.

Second read, of the fixes' lines alone: a fresh context, a subagent,
given those lines, the paragraph above and the two criteria; it returned
in about two minutes, on the first attempt, having run the four
scenarios. One finding: the refusal first said *a merge of the trunk
into the request among them* whatever the commits were — **fixed**, *and
a merge of the trunk into the request counts as one*, true either way.
Not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0)
cairn-test            : pass — 342 tests, 0 failed, exit 0; the new fixture among them
cairn-active --check  : pass — the view current after it was regenerated for `running`
the red first         : the fixture failed before the rule changed — registration did not fire
remote                : pushed immediately after this record
```

#### Checkpoint

```text
status      : running
current step: S07 complete on push
retention   : none (no-rewrite host)
changed     : tools/cairn-check.mjs, tools/cairn-fixture.test.mjs, docs/modules/application.md, the live view, this path's index and this step
session     : safe boundary after a successful push
next action : close again with cairn-close on this commit with the trunk merged in
blockers    : none
```
