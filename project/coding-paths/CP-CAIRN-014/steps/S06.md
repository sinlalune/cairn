---
type: Cairn Coding Path Step
title: 'CP-CAIRN-014 S06 — what the closing reader found'
timestamp: 2026-09-24T00:00:00Z
cairn:
  path: CP-CAIRN-014
  step: S06
---

# CP-CAIRN-014 S06

### S06 — what the closing reader found — **COMPLETE**

#### Plan

The first candidate, `c06dc3922295740f39a7e71a0a6fdd03cdaa039b` on base
`45c7fb64b6d5a22944fbf0205801b7397f95467a`, was handed to a fresh
context with its five inputs — the diff, the nine governing documents at
their blobs, the live view, no running sibling — and the four coherence
questions. It found no contradiction, no duplicate, no architecture
without a record, and five things; two are fixed here, which voids that
candidate:

- ADR-044 decision 4 stands next to what ADR-026 decision 2 refused —
  keeping `current_step` as a selector and guarding it with a second
  rule — and did not say so.
- The `concept-orphan` catalogue row said *normative or learning text*
  where the corpus it reads now holds `skills/`, `feedbacks/` and the
  declared exemptions too.

Definition-of-done item advanced: the first (the record says what it
stands beside) and the fifth (the catalogue says what the checker is).

Type `implementation`: a record's paragraph, one catalogue string with
the rows it generates, and one refusal's wording; no predicate moves, so
no new test — the refusal's remedy is unchanged, and the remedy test
holds it.

Not in this unit, and why — each is the owner's to arbitrate in the
request's *Coherence* section:

- The registration reading admits the record's whole folder where
  ADR-032 decision 3 says *the record and the view*: kept, because a
  registration commit carries the folder — this path's own `45c7fb6` its
  `plan.md` — and the code's comment, the conformance row, the soundness
  note and the module note say *the record's folder*; the request's line
  for the second item says it.
- The hand-written conformance row, the module note and the soundness
  note restate the predicates the catalogue generates: kept, and put to
  the owner. Every row of that page states in prose what the tools
  implement and what they do not prove, restating the predicate as it
  goes — the registration row nearly word for word — and this path
  follows the page's form rather than changing it for one rule.
- `project/backlog/` has no index here: the kit installs it, register
  row 3's; one line in the journal entry.

#### Work

```cairn-unit
step: S06
unit: 06
type: implementation
verified: cairn-check, cairn-test, cairn-active --check
```

- **`docs/adr/ADR-044-what-path-1-of-1-2-left-to-a-record.md`**, decision
  4: one paragraph naming ADR-026 decision 2's refused alternative and
  saying why the advisory is not it — `review` selects nothing by the
  field, `work-unit` asks only whether the named step carries a
  `cairn-unit` block, and the advisory guards no rule.
- **`tools/cairn-rules.mjs`**, and the catalogue it writes on
  **`spec/reference/conformance.md`**: `concept-orphan`'s condition says
  it reads any document `links` reads, its exemptions included.
- **`tools/cairn-check.mjs`**: `concept-orphan`'s message says *no
  document outside the wiki* where it said *no normative or learning
  text*, so the checker says one thing.

#### Self-review

- `shrink:` the review cut the ADR paragraph's last clause, which
  repeated the paragraph above it, and the catalogue string's list of
  roots, which repeated the `links` row.
- Net: 11 lines added and 3 removed across the four files, beside this
  step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff, the new step and the two criteria, told to verify by
command; it returned in about a minute and a half, on the first attempt,
with six findings:

- `correctness:` the catalogue now named the whole corpus while the
  rule's own refusal still said *no normative or learning text* —
  **fixed**, the message says *no document outside the wiki*; the type
  of this unit became `implementation` with it.
- `shrink:` the catalogue string listed the roots the `links` row
  already lists — **fixed**, *any document the links rule reads*.
- `correctness:` ADR-044's new paragraph said `work-unit` asks whether
  the named step is a completed unit; it asks whether a `cairn-unit`
  block names it — **fixed**.
- `shrink:` the paragraph's last clause repeated the one above it —
  **fixed**, cut to *the advisory guards no rule*.
- `correctness:` this step said every conformance row states only the
  requirement; the rows restate the predicate in prose — **fixed**, the
  bullet says so and puts the duplication to the owner.
- `correctness:` this step quoted ADR-032 decision 3 as *the record*
  where it says *the record and the view*, and named a module note the
  reader could not find — **fixed** for the quotation; the module note
  does say *the record's folder*, at line 69, and stays named.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria; it returned
in about a minute, on the first attempt, and found nothing — no test
matched the old wording, the catalogue condition holds against the
code, the ADR's sentence against `work-unit`, and the step's claims
against the notes and rows it names.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0)
cairn-test            : pass — 341 tests, 0 failed, exit 0; the concept-orphan fixtures among them
cairn-active --check  : pass — the view is already current; no path state changed
remote                : pushed immediately after this record
```

#### Checkpoint

```text
status      : running
current step: S06 complete on push
retention   : none (no-rewrite host)
changed     : ADR-044, tools/cairn-rules.mjs, the conformance catalogue, this path's index and this step
session     : safe boundary after a successful push
next action : close with cairn-close on the new candidate — this commit with the trunk merged in
blockers    : none
```
