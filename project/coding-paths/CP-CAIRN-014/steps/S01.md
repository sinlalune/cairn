---
type: Cairn Coding Path Step
title: 'CP-CAIRN-014 S01 — the record path 1 owes'
timestamp: 2026-09-24T00:00:00Z
cairn:
  path: CP-CAIRN-014
  step: S01
---

# CP-CAIRN-014 S01

### S01 — the record path 1 owes — **COMPLETE**

#### Plan

Write ADR-044, promoted from path 1's journal entry at blob
`6d4f869ab6242b99bd7e77e18306305c71d7ea0e`: what `ready` states
(amends ADR-040 decision 1), the fresh reader's fifth input (amends
ADR-043 decision 1), the squash-only limit of a registration by request
(amends ADR-032 decision 2), and the owner's decision of 2026-09-24, the
advisory `current-step` for a stale `current_step`. The three records
gain a line naming it, the 1.2 page is amended in place and marked
*since 2026-09-24*, and the records' index lists it after ADR-043.

Definition-of-done item advanced: the first, in full.

Type `decision`: the record and every document it amends. No rule, tool
or fixture moves, so this unit carries no new test; the advisory itself
is S04's.

Deliberately not in this unit: the advisory's code, fixture and rows
(S04); the skills and the specification, which already say decisions 1
to 3 since path 1 and are outside `writes:`.

#### Work

```cairn-unit
step: S01
unit: 01
type: decision
verified: cairn-check, cairn-test, cairn-active --check
```

- **`docs/adr/ADR-044-what-path-1-of-1-2-left-to-a-record.md`**: the
  record — four decisions, five alternatives refused, the implementation
  table naming chapter 5 and the lifecycle concept for decision 1, the close skill's
  reference for decision 2, the open skill for decision 3, and the
  checker's files for decision 4.
- **`docs/adr/ADR-040…`, `ADR-043…`, `ADR-032…`**: a clause on the status
  line and an **Amended** paragraph under the decision each amends; the
  ADR-043 and ADR-032 marks name the clause whose words give way — *and
  nothing else*, *the merge the trunk's own rules allow*.
- **`docs/adr/index.md`**: ADR-044's entry after ADR-043, and the three
  amended entries dated.
- **`docs/architecture/02-cairn-1-2.md`**: the description names ADR-044;
  an *Amended on 2026-09-24* paragraph in the head; four sentences and
  one table row marked *since 2026-09-24* — the squash limit under *How
  a path opens*, the fifth input and `ready` under *How a path closes*,
  the advisory in the checker's table — and the tools table's
  `cairn-check` row.
- **`docs/architecture/index.md`**: the 1.2 page's entry names the
  amendment, as the 1.1 entry names each of its own.
- **`project/coding-paths/CP-CAIRN-014/index.md`**: `writes:` widened by
  `docs/architecture/index.md`, the index entry above, which the review
  found missing; the steps list, the resume section, and *tried and
  rejected* corrected to two integrated records carrying the stale field.

The opening acceptance's paragraph says *three integrated records*; it
is the accepted text and stays as it was. The number is two —
CP-CAIRN-008 at S07 of S10 and CP-CAIRN-012 at S01 of S06; CP-CAIRN-013
was integrated at S10 of S10, its own staleness repaired before its
closing — and this step is where that is corrected.

#### Self-review

- `shrink:` the record's *what this changes* paragraph under decision 4
  repeated its implementation table's row word for word — cut, on the
  review's finding.
- `shrink:` *the skills already carry the sentences* was said three
  times in the record; kept once, in the context.
- `yagni:` no sentence names the advisory's message text or its
  function; S04 writes those.
- Widening of `writes:`: `docs/architecture/index.md`, named above.
- Net: 210 lines added and 14 removed across the seven files, the new
  record 159 of them, beside this step and the resume.

#### Review

First read: a fresh context, a subagent of this harness, given the
unit's diff and the two criteria and told to check claims by command; it
returned in about three minutes, on the first attempt, with eleven
findings:

- `correctness:` *three integrated records* carry a stale `current_step`
  — it is two, CP-CAIRN-008 and CP-CAIRN-012 — **fixed** in the record,
  its alternatives and the path's *tried and rejected*; the acceptance
  paragraph is left as accepted, and this step says so.
- `correctness:` a refusal scoped to `running` and `ready` would not
  meet a `done` record either, so *would have to grandfather* did not
  follow — **fixed**, the reason kept is that the field breaks no
  reading.
- `correctness:` the staleness was found by the fresh reader of path 1's
  S08, not by its closing reader — **fixed**.
- `correctness:` the close skill never says what `ready` states; four
  pages of the specification do — **fixed**, the table and the context
  name them.
- `correctness:` the live view also shows a sibling's title — **fixed**.
- `correctness:` *amends* beside the index's *supersedes* vocabulary and
  the journal's *superseding record* — **fixed**, one sentence says why
  the record amends and names the two clauses whose words give way.
- `correctness:` ADR-043's *and nothing else* contradicted by its mark's
  *the rest stands* — **fixed**, the mark names the clause.
- `correctness:` the architecture index's 1.2 entry did not name the
  amendment — **fixed**, `writes:` widened for it.
- `correctness:` the new table row did not parse — **fixed**.
- `shrink:` decision 4's *what this changes* paragraph duplicated the
  table — **fixed**, cut.
- `shrink:` the same sentence three times — **fixed**, kept once.

Found by the writer while fixing, beside the eleven: ADR-044 quoted
ADR-032 decision 2 as *the way the trunk's own rules allow*; its words
are *the merge the trunk's own rules allow* — fixed.

Second read, of the fixes' lines alone: a fresh context, a subagent of
this harness, given only those lines and the two criteria, told to check
claims by command; it returned in about a minute, on the first attempt,
with five findings and two on length:

- `correctness:` *the skills and the specification already say all
  three* — no page of the specification says the second or the third —
  **fixed**, *the pages path 1 wrote already say all three*.
- `correctness:` *four pages of the specification* say what `ready`
  states — two do, chapter 5 and the lifecycle concept; the path
  template gives the `manual-git` order and the candidate concept names
  neither — **fixed**, in the decision and the table. The first read's
  count came from path 1's S02, which edited four pages for it; the
  second read checked what each now says.
- `correctness:` the architecture index's entry named the journal alone
  as the source, where decision 4 is the owner's — **fixed**.
- `correctness:` *`done` and never read* — other rules read `done`
  records — **fixed**, *which the advisory never reads*.
- `correctness:` the live view's list omitted the blocked flag —
  **fixed** by naming what it lacks instead, *never its goal or
  `writes:`*.
- `shrink:` ADR-043's mark read awkwardly — **fixed**, *the inputs* and
  nothing else *bounds now include them*.
- `shrink:` one rewrapped line ran past the width — **fixed**, the
  paragraphs rewrapped at 72.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, run bare with this step complete (exit 0)
cairn-test            : pass — 334 tests, 0 failed, exit 0; nothing under tools/ moved in this unit
cairn-active --check  : pass — the view is already current; no path state changed
the blob              : git rev-parse HEAD:project/log/2026-09-24-cp-cairn-013.md = 6d4f869ab6242b99bd7e77e18306305c71d7ea0e
the two records       : current_step read against ls steps/ — CP-CAIRN-008 S07 of S10, CP-CAIRN-012 S01 of S06, CP-CAIRN-013 S10 of S10
the step walk         : pathWorkUnits in tools/cairn-check.mjs reads every file under steps/
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : ADR-044, ADR-032, ADR-040, ADR-043, the records' index, the 1.2 page, the architecture index, this path's index and this step
session     : safe boundary after a successful push
next action : S02 — the registration rule reads the change under review
blockers    : none
```
