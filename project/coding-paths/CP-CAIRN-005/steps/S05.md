---
type: Cairn Coding Path Step
title: 'CP-CAIRN-005 S05 — the path template and the configuration reference'
timestamp: 2026-09-10T00:00:00Z
cairn:
  path: CP-CAIRN-005
  step: S05
---

# CP-CAIRN-005 S05

### S05 — the path template and the configuration reference — **COMPLETE**

#### Plan

Make the two references show what the skills now say — eight decisions of
1.1, in seven records, each at its *What implements this record* table.

`spec/reference/path-template.md`: the goal opens with three plain lines
(ADR-021 decision 2); the definition of done is never edited after
acceptance, ticks included (ADR-002 decision 2); the step's plan names the
definition-of-done item it advances (ADR-009 decision 2); its self-review
is the five tags ending with the net line count or *Lean already*
(ADR-016 decision 2); and a `#### Review` section sits between the
self-review and the verification, the second read's lines under the
first's (ADR-017 decisions 2 and 3).

`spec/reference/configuration.md`: the `areas` row says an area is a
folder of the tree whose note describes that folder (ADR-019 decision 1),
and when a second entry is added (ADR-010 decision 2).

`skills/cairn-open/SKILL.md` gains the two clauses those same records put
there and S01 did not carry: what an area is, beside the sentence asking
which areas a path writes in (ADR-019 decision 1), and *ticks included* on
the definition of done (ADR-002 decision 2).

Definition-of-done item advanced: the sixth, and the part of the seventh
that the step shape shows. The open skill's two new clauses are warranted
by their records' tables rather than by an item: ADR-019 decision 1 puts
*what an area is* beside the sentence item 1 already asked for, and
ADR-002 decision 2 names the open skill's step 1 for *ticks included*,
which item 6 assigns only to the two templates and the configuration
reference.

The register needs nothing: item 7 allows this path one edit there — its
id in row 1 — and the registration commit `8ee7bcb` already made it. The
forward plan listed it under this unit; it was done before the branch
existed.

Item 6 asks the configuration reference for both halves — an area is a
folder of the tree, and `writes:` is the named areas' patterns — while
ADR-019 decision 3's table names `cairn-open` step 1 alone for the
second. The row carries both: decision 1's sentence, and the clause that
an area's `match` patterns are what a path's `writes:` names, which is
true of the field and is what the accepted item asks this file to say.
S01 wrote the open skill's half.

Type `implementation`. No new test: nothing in `tools/` changes behaviour
(ADR-018 decision 1). The configuration reference documents fields the
checker already validates; no field is added, so `tools/cairn-config.mjs`
and its schema are read, not written.

Deliberately not in this unit: the conformance page, the layout reference
and chapters 3 and 6 (path 4); `tools/**` and the `review` rule ADR-017
decision 2 adds (path 2); the README and the site (path 5). S06 is the
candidate.

#### Work

```cairn-unit
step: S05
unit: 05
type: implementation
verified: cairn-check, cairn-active --check, test
```

- **The template's step shape now shows all five movements**, the
  `#### Review` section between the self-review and the verification,
  each read given a fresh context and the second given only the fixes.
- **Both places that describe the definition of done say ticks are never
  added.** They say no more than that: the amendment mechanism was
  already written in both files, and the sentence ADR-002 decision 2 owes
  them is the half about ticking.
- **The `areas` row carries both halves item 6 asks of it** and one
  sentence below the table says when a second entry is added; the `writes:`
  half is stated as the direction that is true — a path writing in an area
  declares that area's patterns.
- **`cairn-open` step 1 gained what an area is and *ticks included***.

#### Self-review

- `delete:` a second sentence under the template's goal explaining why the
  owner reads the three lines — the clause after the dash says it.
- `shrink:` the `#### Review` placeholder written as prose with a worked
  example — the shape is three lines and two headings; a step of this path
  is the worked example, and the template points nowhere.
- `yagni:` a placeholder for *the reader found nothing* as its own block —
  one sentence in the section covers it.
- `yagni:` a row in the configuration table for `writes:` — it is a path
  record's field, not a configuration one, and ADR-019 decision 3 names
  the open skill for it, where S01 wrote it.
- `shrink:` the self-review placeholder listing all five tags where
  `cairn-unit` movement 3 defines them — kept: the template's job is to
  show the shape of the section a writer fills, and the tags are that
  shape.
- No widening of `writes:`: every file is declared. Net: twenty-six lines
  across three surfaces.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria — the ladder and correctness — and nothing else. Thirteen
findings; each with its disposition:

- `shrink:` the template's ticks sentence, of which only the ticking half
  was new — **fixed twice**, and the second read finished it: the sentence
  now says only that ticks are never added.
- `delete:` the configuration reference's *when a second entry is added*,
  restating the open skill — **kept**: ADR-010 decision 2's table names
  both surfaces, one sentence each, and an adopter reading the field's
  reference is not reading the open skill.
- `shrink:` the `areas` row describing the note's contents, which
  `spec/concepts/module-note.md` owns — **fixed**, cut to what the row is
  for.
- `shrink:` the template's two trailing sentences on the third read and on
  finding nothing, both already in `cairn-unit` movement 4 — **fixed**,
  cut; ADR-017 decision 3 asks the template only to show the second read's
  lines under the first's, which the two blocks do.
- `yagni:` a `<tag>:` prefix on the review placeholders, when no record
  gives review findings a tag vocabulary and a correctness finding has
  none — **fixed**, the prefix cut. The steps of this path tag their
  findings by their writer's habit; the template does not require it.
- `shrink:` the step's `#### Work` bullets restating its plan — **fixed**,
  compressed to what the plan does not say.
- `correctness:` "never edited once accepted" contradicting the amendment
  the sentence beside it describes, and the open skill's step 2 —
  **fixed**, see the second read.
- `correctness:` the template's *Second read, the same context* against
  `cairn-unit` movement 4, chapter 5 and ADR-017 decision 3, all of which
  give the second read a fresh context — **fixed**, a fresh context.
- `correctness:` "seven decisions in six records" — **fixed**, eight in
  seven.
- `correctness:` the self-review's `native:` line claiming the template
  does not list the five tags, when the block added does — **fixed**, the
  line now says what it kept and why.
- `correctness:` item 6 asking the configuration reference for the
  `writes:` half while ADR-019 decision 3's table names the open skill
  alone — **fixed**: the row carries both, so the item is answered as
  sealed rather than less a clause.
- `correctness:` the open skill's *ticks included* clause being named by
  no item — **fixed**, the plan now warrants it from ADR-002's table
  instead of from item 1.
- `correctness:` the step headed **COMPLETE** while the record still read
  *not started* — **fixed**, the steps index and the resume move here.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it. Four findings:

- `delete:` the `areas` row's "which are what a path's `writes:` names",
  restating the open skill — **kept, but rewritten**, for the reason
  above: item 6 asks this file for it.
- `correctness:` that same clause being false — it claimed every area
  pattern is what a `writes:` names, while this path's own `writes:`
  contains no area pattern at all — **fixed**: the row now states the
  direction that holds, that a path writing in an area declares that
  area's patterns.
- `correctness:` "never edited in place" being false against this
  repository — `72f62d9` amended item 6 of CP-CAIRN-004's definition of
  done by editing the item and recording a second acceptance block with
  `supersedes:` and a new digest, which is the mechanism working —
  **fixed**: both files now say only that ticks are never added, which is
  what ADR-002 decision 2's context is about, and the amendment sentence
  each file already carried is left to say the rest.
- `correctness:` (informational) the two sentences now read closer to the
  1.1 page than to ADR-002 decision 2's pinned wording *never edited after
  acceptance* — **accepted**: the record's own sentence is the one that
  reads as forbidding amendments, and the repository's practice is the
  amendment; a record found imprecise is a superseding record's business,
  and this unit implements what is true.

The fixes above are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 22 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
new test              : none — this unit changes no behaviour in `tools/` (ADR-018 decision 1)
claim checked         : git show 72f62d9 — CP-CAIRN-004 amended its definition of done by edit plus a superseding acceptance block
user review           : not required for the unit; the owner reads the references with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S06
```

#### Checkpoint

```text
status      : running
current step: S05 complete on push
retention   : none (no-rewrite host)
changed     : spec/reference/path-template.md, spec/reference/configuration.md, skills/cairn-open/SKILL.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S06 — the candidate: merge the trunk in, run the gates bare, open the request with the three plain lines and the surface link first, the definition of done item by item, then the ledger; the owner reads the skills before the merge
blockers    : none
```
