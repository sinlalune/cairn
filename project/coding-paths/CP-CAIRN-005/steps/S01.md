---
type: Cairn Coding Path Step
title: 'CP-CAIRN-005 S01 — the open skill, the configuration and the binding'
timestamp: 2026-09-09T00:00:00Z
cairn:
  path: CP-CAIRN-005
  step: S01
---

# CP-CAIRN-005 S01

### S01 — the open skill, the configuration and the binding — **COMPLETE**

#### Plan

Make the opening of a path say what six decisions of 1.1, in five
records, decided — each at its *What implements this record* table:
`cairn.config.json` declares `transport.registration: manual-git` and
`project/coding-paths/binding.md` names both transports (ADR-001,
decision 1); `cairn-open` step 1 asks which areas a path writes in and
has a path that needs a whole source root say why (ADR-010 decision 2,
ADR-019 decision 3), and says the goal's first three lines are the three
`cairn-code` asks of every change (ADR-021 decision 2); step 2 becomes
*the owner reviews the plan* — the question put in the chat and signalled
as a decision, a change asked before the go-ahead written into the record,
the owner's yes as the opening acceptance (ADR-001 decisions 1 and 2,
ADR-021 decision 3); step 3 and the reference land the registration commit
on the trunk directly, the `register/` sequence dropped (ADR-001 decision
1).

Definition-of-done item advanced: the first.

Type `implementation`: the declaration is the change, the gates are its
tests — the configuration must still load and the fixtures must still
pass. No new test: nothing in `tools/` changes behaviour here, and the
suite reads `transport.integration`, which stays `pull-request`
(ADR-018, decision 1).

Deliberately not in this unit: the unit skill, chapter 5 and the
work-unit concept (S02); the close skill and the request template (S03);
`cairn-code` and `cairn-learn` (S04); the path template and the
configuration reference (S05); the register's row 1 (S05). The checker
is read to name the rule a sentence refers to, never written — the
`registration` and `registration-base` rules do not change on either
transport, as ADR-001 decision 1 says.

#### Work

```cairn-unit
step: S01
unit: 01
type: implementation
verified: cairn-check, cairn-active --check, test
```

- **`cairn.config.json` declares `transport.registration: manual-git`**,
  `transport.integration` unchanged. The declaration now matches what
  this repository has done since CP-CAIRN-002: the gap ADR-001's context
  names is closed. The `registration` and `registration-base` rules read
  the same shape on both transports, so nothing in `tools/` moves.
- **`binding.md` names both transports**, the registration row above the
  integration row, the registration one saying the commit lands on the
  trunk directly.
- **`cairn-open` step 1 gains two paragraphs**: the areas the path writes
  in, `writes:` as their patterns, the whole-root reason the owner reads
  with the plan, and the split proposed when a note is touched by every
  path or a match covers every source file; then the goal whose first
  three lines are `cairn-code`'s three.
- **Step 2 becomes *The owner reviews the plan***: the question put in the
  chat and signalled as a decision — one opening line, what happened, two
  or three tagged ways to go on — nothing further until the answer; the
  change asked before the go-ahead written into the record; the go-ahead
  itself as the acceptance, `accepted_by` the participant who said yes and
  `accepted_at` the time of the go-ahead.
- **Step 3 lands the registration commit on the trunk directly**, and the
  reference drops the `register/` branch sequence and the transport fork;
  one push remains.

#### Self-review

- `delete:` the reference's repeat of "there is no registration branch and
  no registration request" and of "branch only once it is on the remote
  trunk" — the skill says the first, step 4 says the second; the reference
  keeps the push.
- `delete:` "a change asked for after it is a second acceptance block" —
  the paragraph below already says the amendment rule with `supersedes:`.
- `shrink:` "two areas put the path on the `full` route", which restates
  the route trigger of the paragraph above; the sentence now says the
  answer settles that trigger.
- `yagni:` naming the two signals of the split as a list — one sentence
  carries both, as ADR-010 decision 2 states them.
- `yagni:` a sentence in step 3 for the forge bypass ADR-001 decision 6
  prints — that is the checker's line, path 2's, not the skill's.
- No widening of `writes:`: every file of this unit is declared. Net:
  seven lines across the four surfaces.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria — the ladder and correctness — and nothing else. Twelve
findings; each with its disposition:

- `delete:` "The owner's reading of the plan is a step, between the record
  and the go-ahead", which restates the heading above it and the position
  the numbered sequence already gives — **fixed**, cut.
- `delete:` "The goal is what the owner reads at the plan review",
  restating the same heading and the whole-root sentence two paragraphs
  above — **fixed**, cut.
- `shrink:` the four-part shape of the question spelled out in a skill
  that is one of three to gain the sentence — **fixed by the second
  read**: the shape stays here, where the question first arises, and the
  pointer at the other two skills that the first read's remedy suggested
  was cut as both misplaced and false.
- `shrink:` step 3's "there is no `register/` branch and no registration
  request", the same fact stated negatively — **fixed**, cut; the
  reference carries the single push and the branch is named nowhere.
- `shrink:` step 3's "the trunk's own run judges the commit after it
  lands" as a justification rather than an instruction — **fixed**, it is
  now the instruction to read the run.
- `shrink:` the glosses on `accepted_by` and `accepted_at` — **half
  fixed**: the `accepted_by` gloss cut as a restatement; `(UTC, the time
  of the go-ahead)` kept, because it is the field's value and forecloses
  the time the block was written.
- `shrink:` the binding row's tail — **kept**: the row is what the record
  asks the binding to carry, and the neighbouring `path-history policy`
  row glosses its value the same way.
- `correctness:` the skill now tells every adopter to push the
  registration to the trunk, while the kit still installs
  `registration: pull-request` and its generated binding row prints only
  the integration transport — **kept, and named**: the record orders the
  request sequence dropped and the kit default kept, and the kit is path
  4's surface. Resolving the tension is a superseding record, not this
  unit.
- `correctness:` one actor under two names — the heading and step 1 say
  the owner, the surviving paragraph said "An authorised participant" —
  **fixed**: the paragraph says the owner, as the records of 1.1 do. The
  second read noted that this narrows what `spec/index.md` states
  generally; the chapters are path 4's, and `accepted_by` and
  `accepted_roles` stay the general fields they were.
- `correctness:` the self-review's line counts, eight short of the diff —
  **fixed**, the net alone is stated.
- `correctness:` the step headed **COMPLETE** while the record still read
  *not started* — **fixed**, the steps index and the resume section move
  in this unit.
- `correctness:` "five decisions" against five records carrying six —
  **fixed**, the plan says six decisions in five records.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it. Seven findings on
four points — three of the seven convict the same sentence:

- `delete:` the sentence claiming every question the three skills put to
  the owner has that shape — a rule about two other skills placed in a
  third — **fixed**, cut. The second read proved it false three ways: the
  areas question of step 1 does not have the shape, `cairn-close` puts its
  question in the request the owner opens, and `cairn-unit` puts none at
  all. S02 and S03 write their own sentence.
- `shrink:` "then read the trunk's own run: it judges the commit after it
  lands", two clauses for one fact — **fixed**, one clause: read the run
  it triggers there.
- `shrink:` "(UTC, the time of the go-ahead)" — **kept**, as above.
- `correctness:` "The owner reviews the outcome" narrowing the spec's
  "authorised participant" — **kept**, as above; the reader marked it the
  least confident of its findings, and 1.1 is the sole owner's shape.

The fixes above are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 6 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
dependencies          : none to install — the package has no dependencies; the gates ran bare in this fresh worktree
user review           : not required for the unit; the owner reads the skills with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : cairn.config.json, project/coding-paths/binding.md, skills/cairn-open/SKILL.md, skills/cairn-open/reference.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S02 — the unit skill, chapter 5 and the work-unit concept: five movements with the review section and the bounded second read, the self-review in five tags, the plan's item and `repair`, the failing test first, dependencies before the first gate, the framework's block, the helper agent as the writer, the question a unit cannot decide, the concept note and the offered session, the API refresh
blockers    : none
```
