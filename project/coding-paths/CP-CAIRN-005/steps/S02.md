---
type: Cairn Coding Path Step
title: 'CP-CAIRN-005 S02 — the unit skill, chapter 5 and the work-unit concept'
timestamp: 2026-09-09T00:00:00Z
cairn:
  path: CP-CAIRN-005
  step: S02
---

# CP-CAIRN-005 S02

### S02 — the unit skill, chapter 5 and the work-unit concept — **COMPLETE**

#### Plan

Make a work unit say what fifteen decisions of 1.1, in ten records,
decided — each at its *What implements this record* table.

`skills/cairn-unit/SKILL.md`: step 0 gains the dependencies a fresh
worktree installs before the first gate (ADR-008 decision 7), the
framework's block moved out of the bootloader (ADR-007) and the helper
agent inside the writer's session being the writer (ADR-020 decision 2);
step 1 names the definition-of-done item the unit advances and the type
table says what `repair` is for (ADR-009 decisions 1 and 2); step 2 gains
the failing test first, the API refreshed beside the module note, the
concept note with its offered session, and the decision a unit cannot make
(ADR-018 decision 1, ADR-023 decision 4, ADR-022 decision 3, ADR-021
decision 3); step 3 speaks in Ponytail's five tags (ADR-016 decision 2);
a fourth movement, **Review**, is added between the self-review and verify
(ADR-017, all three decisions), and the movements after it are renumbered.

`skills/cairn-unit/reference.md`: the first example reads each gate's exit
code (ADR-008 decision 7), and *publish incomplete work* says the
administrative closure is never pushed under a provisional trailer
(ADR-008 decision 3).

`spec/index.md`, *Advance one work unit at a time*: four movements become
five, and the type table carries `repair`'s sentence (ADR-017 decision 2,
ADR-009 decision 1). `spec/concepts/work-unit.md`: the review section is a
part every type requires (ADR-017 decision 2).

`skills/cairn-open/SKILL.md`, step 3: no object id is typed by hand
(ADR-009 decision 3).

Definition-of-done item advanced: the second. Two of its sentences land
in files the item does not name, because a record's table names the
surface and the item names the bundle: ADR-009 decision 3, *no object id
typed by hand*, is `cairn-open` step 3's sentence, and ADR-016 decision 2
carries the cap sentence of `skills/cairn-code/SKILL.md` that the five
tags would otherwise contradict.

One sentence moves between units: ADR-008 decision 3, which the forward
plan listed under S03 as *the administrative commit's content*, is the
unit reference's sentence by its own table, and the reference is open
here. S03 does not write it.

Type `implementation`: the skills and the specification are the artefact,
the gates are the tests. No new test — this unit changes no behaviour in
`tools/` (ADR-018 decision 1).

Deliberately not in this unit: the close skill and the request template
(S03); `cairn-code` cut to Cairn's own, and `cairn-learn` (S04); the path template's self-review and review sections
(S05); the register's row (S05). The `review` rule ADR-017 decision 2 adds
to the checker is path 2's — this unit says the section exists, and the
rule follows. Chapters 3 and 6 are read only so that chapter 5 contradicts
neither.

#### Work

```cairn-unit
step: S02
unit: 02
type: implementation
verified: cairn-check, cairn-active --check, test
```

- **The unit skill has five movements.** *Review* is movement 4, between
  the self-review and verify: the reader, what it is given and what it is
  not, the `#### Review` section and its three dispositions, the step
  without one not being a completed unit, and the fix read once more on
  its own lines and not a third time.
- **The self-review speaks in the five tags**, ending with the net line
  count or *Lean already*, a tag with nothing to name absent and a unit of
  documents read in the tags that fit prose. The free question it replaced,
  *what would you refuse?*, is what the tagged lines answer.
- **Step 0 gained three things a writer meets before the first gate**: the
  dependencies a fresh worktree has none of; one path, one worktree, one
  writer, with the agent run inside the session being the writer; and the
  framework's block moved out of the bootloader rather than committed in
  place or held by a widened `writes:`.
- **Step 1 names the definition-of-done item**, and says what advancing
  none means; the type table gained `repair`'s sentence.
- **Step 2 gained four instructions**: the failing test first when
  behaviour changes and none when only structure does, the API documented
  beside the area's module note, the concept note whose learning session is
  offered and parked, and the decision the unit cannot make put to the
  owner in the chat.
- **The reference reads exit codes, not output**, and says the
  administrative closure is never pushed under a provisional trailer.
- **Chapter 5 and the concept say the same in the specification's voice**:
  five movements with the review's ordering and its required section, and
  `repair`'s sentence under the type table.
- **`cairn-open` step 3 says no object id is typed by hand** — the
  record's own surface for ADR-009 decision 3.

#### Self-review

- `delete:` "A reader told to find gaps invents them" in movement 4 — the
  vendor's warning from the record's context, not an instruction; "and
  nothing else" carries it.
- `shrink:` the framework block written as three sentences where the
  record has one point — now two, the pointer and the two refusals joined.
- `shrink:` "beside the module note of the area it belongs to" — "beside
  the area's module note".
- `yagni:` a sentence on the `review` rule the checker gains — path 2's;
  the skill says the section exists and stops.
- `yagni:` restating in chapter 5 what movement 4 tells the writer to do —
  the chapter says what a unit is, the skill says how to run one.
- No widening of `writes:`: every file is declared. Net: sixty-four lines
  across six surfaces.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria — the ladder and correctness — and nothing else. Seventeen
findings, one of them outside the diff; each with its disposition:

- `shrink:` "One path, one worktree, one writer" in step 0, already in
  chapter 5, in `cairn-open` step 4 and in the README — **fixed**, the
  lead-in cut; ADR-020 decision 2 gives the skill only the helper agent's
  sentence, which stands alone.
- `shrink:` `cairn-open`'s "Paste `base_commit` from the command that
  printed it", restating the code block's own comment — **fixed**, cut;
  the general rule carries it.
- `delete:` chapter 5's sentence describing the reader, restating the
  movement block above it and the skill — **fixed**, cut. ADR-017
  decision 1 names the skill and no chapter.
- `delete:` chapter 5's "A fix is read once more…" — **fixed**, cut;
  ADR-017 decision 3's table names the skill and the path template only.
- `shrink:` chapter 5's paragraph arguing why the review sits where it
  does — **fixed**, cut; the movement block declares the order.
- `shrink:` the required section stated three times — **kept**: ADR-017
  decision 2 names the skill, chapter 5 and the concept, and each speaks
  to a different reader; chapter 5's is folded into the enumeration it
  already carries rather than standing as its own paragraph.
- `shrink:` "`repair` … which the step names", restating the table row
  above it in both files — **fixed**, cut in both.
- `shrink:` the reference's "It is one commit after the candidate, made
  when the gate is green", restating `cairn-close` step 4 — **fixed**,
  cut; ADR-008 decision 3 owes this file the trailer sentence only.
- `correctness:` the concept's `closure` exception contradicting the
  paragraph's own "every type requires the step record" — **fixed twice**:
  the first attempt made the condition empty, and the second read caught
  it; the paragraph now states the exception as ADR-017 decision 2 does
  and moves the step record behind it.
- `correctness:` the API clause reading as a location for the
  documentation — **fixed**: the documentation is refreshed in the same
  unit, as the module note is, which is ADR-023 decision 4's own framing.
- `correctness:` `cairn-code` still saying the plan and the self-review
  are the three lines twice, which the five tags contradict — **fixed
  here** rather than deferred to S04: this unit made the contradiction,
  and ADR-016 decision 2 names that sentence.
- `correctness:` "ten decisions … in eight records" — **fixed**, fifteen
  decisions in ten records.
- `correctness:` the net line count — **fixed**, recomputed after the
  review's own fixes.
- `correctness:` the definition-of-done item named wrongly — **fixed**,
  the second item, with the two sentences whose surfaces the records put
  in files the item does not name.
- `correctness:` "two sentences move between units", true of one —
  **fixed**, one moves between units and one moves between files.
- `correctness:` the step headed **COMPLETE** with no review or
  verification — **fixed**, both are written in this unit.
- `correctness:` `README.md` still saying a unit is "plan, change,
  self-review, verify" — **deferred to path 5**, which the register gives
  the README; this path's definition of done requires the README
  unchanged, and the resume section carries the debt forward.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it. Five findings:

- `correctness:` the concept's "Where a type carries a step file" being
  an empty condition, since the same paragraph requires the step record of
  every type — **fixed**: the paragraph now reads *every type but
  `closure`, which carries no step file*, as ADR-017 decision 2 states it.
- `correctness:` chapter 5's enumeration making the review universal,
  against the concept and its own type table — **fixed**: the refusal
  sentence names the exception.
- `correctness:` the deletion having taken chapter 5's only normative
  statement of the requirement — **fixed**: the refusal sentence carries
  it, beside the one it already had.
- `correctness:` `cairn-code` pointing at a "review skill" this
  repository has none of — **fixed**: it points at the tags `cairn-unit`
  names. S04 points the file at Ponytail at its tag, where the tags come
  from.
- `shrink:` "as the area's module note is" — **kept**: it names the
  obligation the new one rides on, which is what makes it a clause rather
  than a rule.

The fixes above are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 12 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
new test              : none — this unit changes no behaviour in `tools/` (ADR-018 decision 1)
user review           : not required for the unit; the owner reads the skills with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S03
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : skills/cairn-unit/SKILL.md and reference.md, skills/cairn-open/SKILL.md, skills/cairn-code/SKILL.md, spec/index.md, spec/concepts/work-unit.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S03 — the close skill and the request template: no closure step, the description's order, the reader's two criteria, the owner's try asked in the chat, the merge after the green run, the administrative commit's content, the README's line, what closing leaves behind, the post-mortem read on a red run
blockers    : none
```
