---
type: Cairn Coding Path Step
title: 'CP-CAIRN-005 S04 — the stance cut to Cairn''s own, and the sixth skill'
timestamp: 2026-09-10T00:00:00Z
cairn:
  path: CP-CAIRN-005
  step: S04
---

# CP-CAIRN-005 S04

### S04 — the stance cut to Cairn's own, and the sixth skill — **COMPLETE**

#### Plan

Cut `skills/cairn-code/SKILL.md` to what Ponytail does not say, and write
`skills/cairn-learn/SKILL.md`, the sixth skill (ADR-016 decisions 1 and
3, ADR-022 decision 2, at their tables).

`cairn-code` keeps four things and two lines: deletion turned on the
protocol itself, the test that never fires, the three-line step, absorbing
the ecosystem, and no secret in code or in a record, an error handled
where data would be lost or a trust boundary crossed and swallowed
nowhere. It points at `DietrichGebert/ponytail` at the tag `v4.9.0` for
the ladder and the review, and repeats none of it. The ladder's seven
rungs, *read the real flow before choosing*, the general statement of
deletion over addition, and the two refusals that are Ponytail's `yagni:`
all leave the file.

`cairn-learn` says what a learning session reads first — the concept
root's three folders, the page a newcomer reads first for the surface the
question is about, and `docs/inputs` — how it explores by prompting, what
it writes (a learning note as a concept note with an order, in the root's
`learning` folder, and a concept note for every word the session needed
that has none), and how it ends: the note linked from a document outside
the concept root, because `concept-orphan` counts no link from inside the
wiki.

`AGENTS.md`'s *start here* item 5 names the six skills.

Definition-of-done items advanced: the fourth and the fifth.

**Widening.** `AGENTS.md` is not in `writes:`, and the coverage excludes
it. This unit writes this repository's own `AGENTS.md`, one item, because
item 5 of the definition of done — what acceptance binds — says the
bootloader's *start here* item in this repository names the sixth skill
beside the five, and because a repository that ships a sixth skill while
its own bootloader lists five is wrong the moment this unit lands.
`writes:` gains `AGENTS.md` here.

The register disagrees with that item, and the disagreement is recorded
rather than resolved: row 4 names *the six-skill item in the bootloader
text and this repository's `AGENTS.md`* and carries `AGENTS.md` in its
writes, while row 1 does not; ADR-021 and ADR-022 both close by putting
the bootloader in path 4. Item 7 forbids this path any edit to the
register but its own id in row 1, so path 4 finds this one item already
made and owes only the kit's bootloader text in `tools/cairn.mjs`, which
this unit leaves untouched.

Type `implementation`. No new test: nothing in `tools/` changes behaviour
(ADR-018 decision 1).

Deliberately not in this unit: chapter 6's *Learning notes* paragraph and
its scopes table, which ADR-022 decision 1 changes to say a learning note
is a concept note in `docs/concepts/learning` — the register gives chapter
6 to path 4, and item 7 of the definition of done requires chapters 1 to 4
and 6 unchanged here. So `cairn-learn` names the root's `learning` folder
while chapter 6 still calls learning notes optional under a learning root;
path 4 closes that. Also not here: the concept template's sentence, the
pointer page, `planInstall`, the lock and the conformance page (path 4),
and the path template and the configuration reference (S05).

Two dependencies `cairn-learn` names are not yet built, both path 4's by
the register: the concept root's three folders of ADR-011 decision 2, and
`docs/inputs` of ADR-011 decision 1 — neither exists in this repository,
and `tools/cairn-check.mjs` still reads the concept root with one
non-recursive listing, so a note filed in a `learning` folder is invisible
to `concept-orphan` until that read becomes recursive. The skill says what
the records decide; the repository catches up on path 4.

Three claims elsewhere go stale the moment this unit lands, all outside
this path's writes: `README.md` says *five skills* twice, and
`tools/cairn.mjs` says *the five skills* in its header. The register gives
the README to path 5 and the kit to path 4.

#### Conditional coverage read

The Ponytail repository at the tag ADR-016 decision 1 names was read
before `cairn-code` was cut, as the coverage requires:
`raw.githubusercontent.com/DietrichGebert/ponytail/v4.9.0/skills/ponytail/SKILL.md`
and `.../skills/ponytail-review/SKILL.md`. It carries the seven-rung
ladder in the same order and near the same words as the file being cut,
read-the-real-flow-first, deletion over addition, the root-cause rule,
the floor under laziness, the check per non-trivial change and the
ceiling marker; the review skill carries the five tags with the closing
`net: -<N> lines possible.` or `Lean already. Ship.` It says nothing on
secrets, naming or formatting, and nothing on error handling beyond
preserving what prevents data loss — so Cairn's two lines are its own —
and `ponytail-review` puts correctness explicitly out of scope, which is
why ADR-016 decision 4 adds it.

#### Work

```cairn-unit
step: S04
unit: 04
type: implementation
verified: cairn-check, cairn-active --check, test
```

- **`cairn-code` lost the seven-rung ladder, *read the real flow before
  choosing*, the general statement of deletion over addition, and the two
  refusals that are Ponytail's `yagni:`** — a configuration option nobody
  asked for, an abstraction with one implementation — and the why-not-what
  comment bullet, which ADR-016 decision 1's enumeration of what the file
  keeps does not carry. It gained one paragraph naming Ponytail at
  `v4.9.0` and a section of two lines.
- **What stayed is the four things the record enumerates**: deletion
  turned on the protocol, the test that never fires, the three-line cap
  and absorbing the ecosystem — each rewritten only where it had leaned on
  a heading that left.
- **`skills/cairn-learn/SKILL.md` is new**: what the session reads before
  it asks, the exploration by prompting with the tone of ADR-021 decision
  1, the learning note as a concept note with an order in the root's
  `learning` folder, a concept note for every word the session needed, and
  the end condition — the note linked from outside the concept root,
  because `concept-orphan` counts no link from inside.
- **`AGENTS.md` names six skills** in its *start here* item.

#### Self-review

- `delete:` "A protocol grows the same way a codebase does, and it has no
  compiler to notice" — the reason, not the rule.
- `delete:` "This skill is that rule applied to itself" under *Absorb the
  ecosystem* — the pointer paragraph above already scopes what the file
  keeps.
- `delete:` "While I was there", the last refusal of the old file — not
  among the four ADR-016 decision 1 keeps, and `cairn-unit` movements 1
  and 2 already carry it: the plan names what the unit will not change,
  and writing outside `writes:` needs a widening.
- `shrink:` the secrets line's "rotation is what makes it harmless" — the
  record's own wording is *rotated before it is redacted*, which says it.
- `shrink:` `cairn-learn`'s two sentences on reading first — one.
- `shrink:` "where the temptation to be complete is strongest" after the
  abstractions of the trade — the list is ADR-021 decision 1's; the
  flourish is mine.
- `yagni:` a *What you must not do* section in `cairn-learn` — kept on
  the symmetry with the other five skills; the review reversed this, and
  the reversal is dispositioned below.
- Widening of `writes:`: `AGENTS.md`, for the reason in the plan. Net:
  `cairn-code` nineteen lines shorter, `cairn-learn` sixty-three lines
  new, the bootloader one line changed.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria — the ladder and correctness — and nothing else. Twenty
findings; each with its disposition:

- `delete:` `cairn-learn`'s whole *What you must not do* section, all four
  bullets restating sections above them — **fixed**, cut, reversing the
  self-review's *kept*: symmetry with the other five skills is a reason of
  shape, not of need, and the reader learns nothing from the fourth
  reading of a rule.
- `delete:` the tone rule copied into `cairn-learn` §2 — **fixed**, cut.
  ADR-021 decision 1 puts that line in the bootloader precisely because a
  skill is read only when its procedure runs; ADR-022 decision 2 asks the
  exploration to have that tone, not the skill to restate it.
- `shrink:` "An abstraction explained persists as a note; that rule is not
  this skill's" — **fixed**, the provenance clause cut.
- `delete:` `cairn-code`'s closing line about naming, comments and
  formatting — **fixed**, cut; a line whose content is that certain lines
  are absent is itself the line it warns against.
- `shrink:` "Nothing below repeats them" against the frontmatter
  description — **fixed**, cut.
- `correctness:` the plan claiming the register's row 4 owns only the
  kit's bootloader text — **fixed**: row 4 names this repository's
  `AGENTS.md` too and carries it in its writes. The widening stands on
  item 5 of the definition of done, which binds it; the divergence is
  recorded in the plan and in the record's *tried and rejected*, and item
  7 forbids this path the register edit that would settle it.
- `correctness:` `writes:` diverging from the register's row 1 — **kept
  and recorded**, same reason.
- `correctness:` "item 6" for the item that excludes chapters 1 to 4 and 6
  — **fixed**, item 7.
- `correctness:` `cairn-learn` attributing the concept root's three
  folders to the binding, which names one flat root — **fixed twice**: the
  first attempt hedged with *wherever the root keeps those apart*, which
  the second read showed contradicts §3 and lets a required reading be
  skipped; the list now states the three folders as the records define
  them.
- `correctness:` the `learning` folder, which does not exist here and
  which `tools/cairn-check.mjs` could not see if it did, its concept read
  being one non-recursive listing — **deferred to path 4**, which the
  register gives ADR-011 decision 2 and the recursive read; the skill says
  what the record decides and the dependency is named in the plan.
- `correctness:` `docs/inputs`, which does not exist here — **deferred to
  path 4** on the same ground; the first read's suggestion to hedge it was
  taken and then reversed by the second read, because the kit installs the
  folder unconditionally and a hedge invites skipping it.
- `correctness:` the plan saying ADR-016 decision 3 refuses the
  why-not-what comment bullet — **fixed**: decision 3 says the stance says
  nothing on comments *beyond* why-not-what, so it does not refuse that
  line. The bullet goes because decision 1's enumeration of what the file
  keeps does not carry it, and the plan now says so.
- `correctness:` "the decision ladder" left undefined in every Cairn file
  once `cairn-code` stops stating it, while `cairn-unit` movement 4 and
  `cairn-close` step 3 both name it and the fresh reader is given neither
  — **fixed**: both now say the ladder `cairn-code` points at.
- `correctness:` `README.md` twice and `tools/cairn.mjs` once saying *five
  skills* — **deferred**, to path 5 and path 4 by the register, and named
  in the plan.
- `correctness:` the step headed **COMPLETE** while the record still read
  *not started* — **fixed**, the steps index and the resume move here.
- `correctness:` "with no path open" as a condition ADR-022 decision 3
  does not set — **fixed**, cut.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it. Nine findings:

- `shrink:` the abstractions-of-the-trade tail left dangling when the
  sentence it modified was cut, and attached to the wrong rule — **fixed**,
  cut with its antecedent.
- `shrink:` "where the repository has one" on `docs/inputs` — **fixed**,
  cut; the kit installs it unconditionally and the hedge grants permission
  to skip a required reading.
- `shrink:` "wherever the root keeps those apart" — **fixed**, cut; it
  contradicted §3, which requires the folders.
- `shrink:` "in full", which turned a scoped read into the whole root —
  **fixed**, cut.
- `correctness:` §1 describing this repository's own root as holding three
  vocabularies when `spec/concepts/` is the `cairn` scope alone — **fixed**
  by the same cut; the skill states the adopter root the records define,
  and the protocol's own repository is the exception ADR-011 decision 2
  names.
- `correctness:` the deletions leaving ADR-021 decision 1's tone rule and
  ADR-011 decision 3's concept-note rule stated nowhere in this repository
  until path 4 writes the two bootloader lines — **accepted, and named**:
  both records put those lines in the bootloader and the register puts the
  bootloader in path 4, so the promise is in an accepted document, not in
  this step.
- `correctness:` the step's self-review disposing two of these cuts the
  other way — **fixed**: the two lines no longer assert what the review
  reversed, and both reversals are dispositioned above; the step is not yet
  committed, so nothing published was rewritten.
- `correctness:` `cairn-close` step 3 sending the owner of a
  documents-only path to a skill whose description scopes it to code —
  **kept**: ADR-016 decision 2 already reads a unit of documents for the
  same things in the tags that fit prose, and decision 4 names this
  sentence as it stands.
- `cairn-unit` movement 4's pointer verified accurate, no finding.

The fixes above are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 19 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
new test              : none — this unit changes no behaviour in `tools/` (ADR-018 decision 1)
coverage read         : Ponytail at v4.9.0 fetched and compared, as the plan records
user review           : not required for the unit; the owner reads the skills with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S05
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : skills/cairn-code/SKILL.md, skills/cairn-learn/SKILL.md (new), skills/cairn-unit/SKILL.md, skills/cairn-close/SKILL.md, AGENTS.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S05 — the path template's goal, tagged self-review, review section and amendment block, and the configuration reference on areas as folders and writes: as their patterns; the register's row 1 names this path
blockers    : none
```
