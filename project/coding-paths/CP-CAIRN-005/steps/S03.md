---
type: Cairn Coding Path Step
title: 'CP-CAIRN-005 S03 — the close skill and the request template'
timestamp: 2026-09-10T00:00:00Z
cairn:
  path: CP-CAIRN-005
  step: S03
---

# CP-CAIRN-005 S03

### S03 — the close skill and the request template — **COMPLETE**

#### Plan

Make closing a path say what twelve decisions of 1.1, in eight records,
decided — each at its *What implements this record* table.

`skills/cairn-close/SKILL.md`: step 2 says there is no closure step and
no step file for the review (ADR-008 decision 1), gives the description
its order — three plain lines and the surface link, the definition of
done item by item, then the ledger (ADR-021 decision 2, ADR-018 decision
2) — and asks whether the README lists a surface the path added (ADR-023
decision 1); step 3 says the two criteria whoever reads the diff reads by
(ADR-016 decision 4), that the owner tries the result before the merge as
a named step asked for in the chat (ADR-001 decision 3, ADR-021 decision
3), and that for a sole owner the merge click is the whole acceptance
(ADR-001 decision 4); step 5 merges only after the run on the exact
commit is read green, as a merge commit (ADR-001 decision 5); step 6
deletes every transport branch but `path/<id>`, which stays until the
path is archived, and is run by the writer, who reports a failure to
remove the worktree (ADR-006 decisions 1 and 2). The reference gains the
branch deletion's command.

`.github/pull_request_template.md` opens with the three plain lines and
the surface link, then the definition of done item by item, before
`## Candidate` (ADR-021 decision 2, ADR-018 decision 2).

`skills/cairn-unit/SKILL.md`, movement 5: `npm test` is the product's
suite, and the kit's own fixture suite is not an adopter's concern
(ADR-014 decision 2) — the register's *the skill's sentence* for that
record, which S02 did not carry.

Definition-of-done item advanced: the third, less its last clause.

That clause — *a red run's post-mortem is read before the next unit
(ADR-014 decision 2)* — is not written, because no record decides it.
ADR-014 decision 1 puts the post-mortem in a tool, a workflow step and a
package script, all of them path 3's surfaces; decision 2 is the sentence
about the two suites, written above in the unit skill; and ADR-021
decision 3 already carries the red run as one of the moments a question
reaches the owner in the chat. This unit implements the records as they
stand and names the gap rather than inventing a sentence for the close
skill. The candidate's answer to item 3 says the same.

Nothing is added to the request template for the owner's try: ADR-001
decision 3 says so in as many words, and the README question of ADR-023
decision 1 goes to the skill, whose table names step 2 and not the
template.

Type `implementation`. No new test: nothing in `tools/` changes behaviour
here (ADR-018 decision 1).

Deliberately not in this unit: `cairn-code` cut to Cairn's own and
`cairn-learn` (S04); the path template and the configuration reference
(S05); the register's row (S05). `tools/cairn-audit.mjs`, which prints
the description this unit reorders, is read to name its output and never
written — path 3's.

#### Work

```cairn-unit
step: S03
unit: 03
type: implementation
verified: cairn-check, cairn-active --check, test
```

- **Step 2 says the review is the description and nothing carries it** —
  no unit, no step file, no closure step — and then gives the description
  its order: three plain lines and the surface link, the definition of
  done item by item with nothing ticked, then the ledger the audit
  command prints. The README question is asked with the coherence
  questions, in the skill, because ADR-023 decision 1's table names step 2
  and not the template.
- **Step 3 carries the two criteria, the owner's try and the merge
  click**: whoever reads the diff reads it against the ladder and for
  correctness; the try is a step asked for in the chat with nothing added
  to the request, as ADR-001 decision 3 says in as many words; and where
  one owner runs the repository the merge click is the approval.
- **Step 5 merges only after the run on the exact commit is read green.**
- **Step 6 deletes the transport's branches, keeps `path/<id>` until the
  path is archived, and names the writer as the one who removes the
  worktree** before the report.
- **The reference gained the branch deletion**, written as *list, then
  delete every branch that is not `path/<id>`* rather than naming
  `register/`: S01 removed the registration branch from the open skill, so
  naming it here would contradict the skill this path just changed.
- **The request template opens with the three lines and the surface
  link, then the definition of done item by item**, both above
  `## Candidate`; the four coherence questions, the advisories and the
  roles are untouched.
- **The unit skill's verify movement says which suite is which** — the
  register's *the skill's sentence* for ADR-014 decision 2, which S02 did
  not carry.

#### Self-review

- `delete:` "this is not a work unit" beside "No unit and no step file
  carries it" — one clause said twice.
- `delete:` "Findings that are neither become new layers, defensive code
  and tests for cases that cannot happen" — ADR-016 decision 4's reason,
  not its instruction; "and for nothing else" is the whole of it.
- `delete:` the template's two long HTML comments explaining what the
  three lines and the item lines are — the placeholders show it.
- `correctness:` the reference's deletion example named
  `register/cp-example-001`, a branch S01 removed from the open skill —
  rewritten as a listing and a deletion of whatever is not `path/<id>`.
- `yagni:` a fifth coherence checkbox in the template for the README —
  ADR-023 decision 1 puts the question in the skill, and ADR-001 decision
  3 says nothing is added to the template.
- `yagni:` a sentence in the close skill for the red run's post-mortem —
  no record decides one; the gap is named in the plan above.
- No widening of `writes:`: every file is declared. Net: fifty-two lines
  across four surfaces.

#### Review

First read, a fresh context given the diff of this unit and the two
criteria — the ladder and correctness — and nothing else. Twelve
findings; each with its disposition:

- `shrink:` the reference repeating step 6's branch sentence — **fixed**,
  the prose cut; the reference is a command file and the two commands
  carry it.
- `shrink:` the reference repeating who removes the worktree and when —
  **fixed**, cut back to *From another checkout*.
- `delete:` "The path branch may stay", now said twice and weaker than
  the new section's rule — **fixed**, cut.
- `shrink:` step 5's merge sentence restating the sentence before it —
  **fixed**, *read green on the exact commit that will land* folded into
  the one sentence.
- `shrink:` the template's surviving explainer comment — **fixed**, cut;
  the placeholder line shows the shape.
- `correctness:` the template's header claiming `cairn-audit` prints the
  whole shape, false for the two sections this unit adds above it —
  **fixed**: the header says the command prints the ledger from
  `## Candidate` down and the two sections above are the writer's.
- `correctness:` the reference saying the same command prints the
  description — **fixed**, it prints the ledger half.
- `correctness:` the branch-deletion commands, which printed
  remote-prefixed names `git push origin --delete` rejects, and whose
  *every one that is not `path/…`* would have deleted the branch the
  section keeps — **fixed**: `--format='%(refname:strip=3)'`, verified in
  this worktree to print `path/cp-cairn-005`.
- `correctness:` the sole owner's merge click standing beside *a
  control-plane change needs an approval that is not the writer's own*
  with nothing saying which governs — **fixed twice**: the first attempt
  said there is no such reviewer, which the second read showed false
  against `spec/concepts/roles.md`; the sentence now says that where one
  owner holds every role, that approval is the merge click.
- `correctness:` the `closure` row of the unit skill's type table, which
  ADR-008 decision 1 names and this unit had not touched — **fixed**, the
  row says *and no step file*.
- `correctness:` "nine decisions in six records" — **fixed**, twelve
  decisions in eight records.
- `correctness:` item 3 also asking that the candidate be the last unit's
  commit, which no added sentence said, while the reference still carried
  *Finish any final implementation, then* — the very sentence ADR-008
  decision 1's context blames for the invented closing unit — **fixed**:
  both the skill and the reference now say closing adds no implementation
  commit of its own, and the reference's *final implementation candidate*
  commit is replaced by a clean-tree check.

Second read, a fresh context given only the diff of the fixes above and
the same two criteria, as ADR-017 decision 3 bounds it. Eleven findings:

- `shrink:` the reference repeating the skill's new candidate sentence —
  **fixed**, cut to what the commands need.
- `shrink:` the ledger split stated three times — **fixed**, the
  reference's copy cut; the skill states it and the template's header
  says which half the tool fills.
- `delete:` "and no step file" as entailed by *only* — **kept**: ADR-008
  decision 1 names this row, and the entailment is what the Crumbz
  closing unit did not read.
- `shrink:` "Where one owner holds every role there is no such reviewer"
  going beyond the one sentence ADR-001 decision 4 allows — **fixed**
  with the correctness finding below.
- `correctness:` that same clause being false against
  `spec/concepts/roles.md`, which says a solo developer holds every role,
  and against the roles line the request itself carries — **fixed**: the
  reviewer exists; what the merge click supplies is the approval.
- `correctness:` "`C` is that merge over the last unit's commit", false
  when the trunk has not moved and `git merge` reports *Already up to
  date* — **fixed**: `C` is the last unit's commit with the trunk merged
  in.
- `correctness:` *Resolve conflicts and commit the merge* sitting against
  *closing adds no implementation commit* — **fixed**: the skill says no
  implementation commit *of its own*, and the reference's restatement is
  gone.
- `correctness:` the `closure` row contradicting movements 1, 4 and 5,
  which require a step file unconditionally — **fixed**: movement 1 names
  the exception, as `spec/concepts/work-unit.md` does.
- `correctness:` `requestTemplate()` in `tools/cairn.mjs` still writing
  the old shape for an adopter — **deferred to path 4**, which the
  register already gives *the generator of an adopter's request template*
  under ADR-018 decision 2; nothing is owed that the register does not
  already carry.
- `deletion:` the template's *in its order* lost with its comment —
  **fixed**, the skill's sentence carries it.
- `deletion:` *as its one required check* dropped while shrinking step 5
  — **fixed**, restored.

The fixes above are not read a third time.

#### Verification

```text
cairn-check           : pass — OK, protocol satisfied, no advisory, profile ci, 16 changed files (exit 0)
cairn-active --check  : pass — view already current (exit 0)
tests                 : pass — 230 tests, 0 failed (npm test, exit 0)
new test              : none — this unit changes no behaviour in `tools/` (ADR-018 decision 1)
command checked       : git branch -r --list 'origin/*cp-cairn-005*' --format='%(refname:strip=3)' printed path/cp-cairn-005 (exit 0)
user review           : not required for the unit; the owner reads the skills with the candidate
remote                : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : skills/cairn-close/SKILL.md and reference.md, skills/cairn-unit/SKILL.md, .github/pull_request_template.md, this path's index and this step
session     : safe boundary after successful push and a green CI run
next action : S04 — cairn-code cut to Cairn's own with its two lines and Ponytail named at its tag, and cairn-learn written as the sixth skill, named in this repository's bootloader beside the five
blockers    : none
```
