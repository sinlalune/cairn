---
type: Cairn Coding Path Step
title: 'CP-CAIRN-011 S05 — the version, the page''s opening and the register'
timestamp: 2026-09-15T00:00:00Z
cairn:
  path: CP-CAIRN-011
  step: S05
---

# CP-CAIRN-011 S05

### S05 — the version, the page's opening and the register — **COMPLETE**

#### Plan

Make the release nameable: `package.json` at `1.1.0`, the pointer page
and the lock rewritten by a second `update` so they name it, the stamp
`prepack` writes proven from the tree. Replace the one sentence of the
1.1 page that 1.1 made false, marked *since 2026-09-15*, and nothing else
on it. In the register, close the 1.1 milestone row and open the *Cairn
1.2 — what 1.1 taught* row under it with *no path yet* and its agenda:
the five things the goal names, what the release found, the first
adopter's update, the sources linked.

Definition-of-done items advanced: the fifth and the sixth, and the
release's name in the fourth, which S04 left to this unit.

Type `documentation`: the page, the register, the package manifest's one
field, the two generated files and this step. No source changes; the
suite reads the version from `package.json` through `PROTOCOL_RELEASE`,
so it is the test that the name is consistent, and it is run below.

Deliberately not in this unit: the tag, which a release sets on the
trunk's integrating commit and which does not exist until the merge — the
1.1 row names the tag and the path whose integration carries it, not a
commit; fixing any item of the 1.2 row — each is a record's, a rule's, a
skill's or the installer's, and a release ships what was decided; the site
bundle, which reads the version at build and is rebuilt from the candidate
by S06 and by the trunk's workflow after the merge.

#### Work

```cairn-unit
step: S05
unit: 05
type: documentation
verified: cairn-check, cairn-test, update, stamp
```

- **`package.json`**: `"version": "1.1.0"`; nothing else.
- **`cairn/README.md`** and **`cairn.lock.json`**, regenerated by `update`:
  release 1.1.0, cut from the tree's HEAD at the run — S04's commit; the
  manifest thirty-three, the reconcile list seventeen, as at S04.
- **`docs/architecture/01-cairn-1-1.md`**: in the first paragraph, *Nothing
  here is implemented yet: the roadmap register names the coding paths
  that build it* becomes *Since 2026-09-15: its records are implemented
  by paths 1 to 6 of the roadmap register and released as 1.1.0 by the
  seventh*, the link kept; `git diff --stat` shows the page changed in
  that paragraph and nowhere else.
- **`project/coding-paths/index.md`**: the 1.1 milestone row's paths cell
  says *seven coding paths below* and its state *done 2026-09-15 by
  CP-CAIRN-011; the tag `1.1.0` goes on its integrating commit and the
  package is published from it, after the merge*; a new row, *Cairn 1.2 — what 1.1 taught*, *no path yet*,
  *planned*, whose outcome cell carries the agenda in three parts — the
  five things 1.1 leaves, what the release found, the first adopter's
  update — one clause each, and its three sources linked, where each is
  said in full: the first agent feedback file, path 6's journal entry,
  this record's S01, S03 and S04.
- **`project/coding-paths/CP-CAIRN-011/index.md`**: the steps line for
  S05; the resume section refreshed — checkpoint S04's commit, the next
  action S06 with the close's sequence and the seventh item's check, two
  lines added to *Tried and rejected*, the reading order for the close.

**The second `update`, at 1.1.0:**

```text
$ node tools/cairn.mjs update
cairn — installed release 1.0.0, this package is 1.1.0 (an update is available)
kit files: 13 pristine, 17 edited, 3 missing
cairn — updated to release 1.1.0: 4 written, 0 deleted
to reconcile by hand (17): AGENTS.md, package.json, skills/cairn-brainstorm/SKILL.md, skills/cairn-close/reference.md, skills/cairn-learn/SKILL.md, skills/cairn-open/SKILL.md, skills/cairn-open/reference.md, skills/cairn-unit/SKILL.md, project/coding-paths/binding.md, project/coding-paths/index.md, project/index.md, docs/index.md, docs/architecture/index.md, docs/modules/index.md, docs/modules/application.md, .github/workflows/cairn.yml, .github/pull_request_template.md
they are listed on cairn/README.md too, so the work is on disk and not only here
$ sed -n 12,13p cairn/README.md
This repository carries **release 1.1.0**, cut from commit
`0e9c22f734cd5f9778ca0c6784663796a60e57cc`. …
$ node -e "const l=require('./cairn.lock.json');console.log(l.release, l.sourceCommit, Object.keys(l.manifest).length)"
1.1.0 0e9c22f734cd5f9778ca0c6784663796a60e57cc 33
```

The first line is the reading the definition of done asked S04 for and
S04 could not give: *installed 1.0.0, this package 1.1.0, an update is
available*. The four written are the three concept indexes under
`spec/concepts/`, deleted again as S04 did and for S04's reason, and the
pointer page. The commit the page and the lock name is the commit the
tree was at, S04's; the candidate S06 cuts is what the tag names, and the
page is a generated file `update` rewrites at any later run.

**The stamp**, as `prepack` runs it, from the tree:

```text
$ node tools/cairn.mjs stamp
cairn — stamped tools/release.json at 1.1.0 from 0e9c22f
$ cat tools/release.json
{ "release": "1.1.0", "commit": "0e9c22f734cd5f9778ca0c6784663796a60e57cc", "stampedAt": "2026-09-15T21:02:03.262Z" }
```

The file is not tracked and was deleted after the reading; `npm publish`
runs `prepack` on the commit the owner publishes from, and the tarball
carries that commit.

**The page, checked for the one change:**

```text
$ git diff --stat -- docs/architecture/01-cairn-1-1.md
 docs/architecture/01-cairn-1-1.md | 7 ++++---
```

One hunk, the first paragraph's last sentence.

#### Self-review

- `shrink:` the 1.2 row's outcome cell — the longest in the table; each
  clause is an item a path will scope from, and the cell is the one place
  the register says what 1.2 is, so it stays; the sources are three links
  and not a paragraph.
- `yagni:` a commit id in the 1.1 row's state — the integrating commit
  does not exist; the row names the tag and the path.
- `yagni:` a decision record for the page's sentence — the sentence
  records a fact, that the paths landed, and decides nothing; the
  `decision-drift` advisory it raises is dispositioned below and at the
  candidate.
- `correctness:` *seven coding paths below* for *six* in the 1.1 row's
  paths cell — this path is the seventh row of that table.
- `shrink:` after the first read: the 1.2 row's elaborations cut to one
  clause per item, the sources saying the rest; the page's sentence
  counts once.
- net: 7 lines changed on the page, 2 rows on the register, 1 field in
  the manifest, 2 generated files; this step and the record's refresh
  beside it.

#### Review

First read — reader: a fresh context, a subagent of this harness on a
smaller model, given the unit's diff in one file and the two criteria,
returned in 345 seconds on the first attempt — given this unit's diff and
the two criteria. Four findings, each with its disposition:

- `correctness:` the 1.1 row stated the tag and the package as already
  done, and no tag exists — **fixed**: the row says *done 2026-09-15 by
  CP-CAIRN-011* and that the tag and the package follow the merge.
- `correctness:` the page said *the six coding paths* while the register
  lists seven — **fixed**: *paths 1 to 6 … and released as 1.1.0 by the
  seventh*, which is what the table says and what this path is.
- `shrink:` the page's sentence counted twice, *six* and *1 to 6* —
  **fixed** by the same sentence.
- `shrink:` the 1.2 row restated its sources' substance — **fixed in
  part**: each item is one clause and the sources are linked for the
  rest; the row keeps one clause per item because the register is where
  the next path reads what 1.2 is, and a row that only links is a row a
  reader must leave to scope from.

The reader regenerated the pointer page and the lock from a clean
checkout bumped to 1.1.0 and found them byte-identical but for the
timestamp, and checked every link and every item against its source.

Second read — reader: a fresh context, a subagent of this harness on the
same smaller model, given only the diff of those fixes and the same two
criteria, returned in 247 seconds on the first attempt — given only the
diff of those fixes. Three findings:

- `correctness:` *released as 1.1.0 by the seventh* on the page states as
  fact what is not yet true at this checkout — **refused**: the fifth item
  of the definition of done prescribes that sentence, and a release path
  writes the release's facts into the candidate it cuts, as 1.0.0's S08
  set the version before the tag; the sentence becomes true at the merge
  that lands it, which is the acceptance, and the owner reads it before.
- `correctness:` *done 2026-09-15* on the 1.1 row, while row 7 below says
  *running* — **refused** for the same reason: the sixth item prescribes
  the row, and the integrating unit that sets row 7 to *done* is the
  commit after the one that lands this row; the date is the day the
  candidate is cut.
- `shrink:` *Each is said in full at its source:* for *Sources:* —
  **fixed**.

The fixes of the second read are not read a third time; the self-review
above carries the line the cuts add.

#### Verification

```text
cairn-check            : pass — OK, protocol satisfied, one advisory, profile ci (exit 0), run bare with this record complete
decision-drift         : advisory — the architecture page changed with no record in the same change; dispositioned: the sentence replaced records a fact, that paths 1 to 6 landed, and decides nothing, as the fifth item of the definition of done has it; carried to the candidate's ledger
cairn-test             : pass — 334 tests, 0 failed (exit 0), bare, at 1.1.0 — PROTOCOL_RELEASE read from package.json by the suite
update                 : installed 1.0.0, this package 1.1.0, an update available; updated to 1.1.0, 4 written, 0 deleted, 17 to reconcile — quoted above
stamp                  : tools/release.json at 1.1.0 from 0e9c22f, then deleted — quoted above
the page               : one hunk, the first paragraph
review                 : two reads by fresh contexts, 345 and 247 seconds, both on the first attempt
user review            : not required for the unit; the owner reads the page's sentence and the register before the merge
remote                 : pushed immediately after this record; the commit id is named in the resume section by the close
```

#### Checkpoint

```text
status      : running
current step: S05 complete on push
retention   : none (no-rewrite host)
changed     : package.json, cairn.lock.json, cairn/README.md, docs/architecture/01-cairn-1-1.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S06 — the candidate, with cairn-close: trunk merged in, gates bare, the site rebuilt, the request opened with the audit's description, the administrative commit and its check green, the owner asked to open the site and the README and run one quick start
blockers    : none
```
