---
type: Cairn Coding Path Step
title: 'CP-CAIRN-011 S03 — the weight measured at 1.1.0'
timestamp: 2026-09-15T00:00:00Z
cairn:
  path: CP-CAIRN-011
  step: S03
---

# CP-CAIRN-011 S03

### S03 — the weight measured at 1.1.0 — **COMPLETE**

#### Plan

Take the four readings of the weight budget again at 1.1.0 with the tools
that took them at 1.0.0, and write them into the conformance page's table
as a column beside the 1.0.0 one, which stays: the specification's words,
the required entry chain's words, the files the kit installs on each
profile with no target, the protocol files one lightweight unit writes.
Report each target that has one as bound or not, and name the method so
the next release counts the same way.

Definition-of-done item advanced: the third.

Type `documentation`: the page and this step, the record's steps line and
resume section. No source changes, no test; every number is a command's
output kept below.

Deliberately not in this unit: chapter 6 of the specification, whose
*Conformance and weight* section still says *Cairn 1.0 is measured
against* — `spec/index.md` is outside this path's writes, the chapter
names the targets and no reading, and the sentence is one of the restated
facts the 1.2 row will carry; the *Installation, update and adoption* row
of the same page, which already says 33 and 32 since coding path 4 and is
what the table's kit row now agrees with.

#### Work

```cairn-unit
step: S03
unit: 03
type: documentation
verified: cairn-check, cairn-test, the readings below
```

- **`spec/reference/conformance.md`**: the description no longer says the
  budget is *Cairn 1.0*'s; the section opens on *each release*; the table
  has a fourth column, *Measured at 1.1.0*, each cell bold with its
  verdict — *bound* where a target exists, *no target* on the kit's row;
  the paragraph under it says the method — `wc -w` over each file as
  committed, frontmatter included, the lock's manifest, the pilot's print
  — proves it by the 1.0.0 tag giving the 1.0.0 figures, names where the
  entry chain's 69 words came from, and points the kit's seven new files
  at the installation row.
- **`project/coding-paths/CP-CAIRN-011/index.md`**: the steps line for
  S03; the resume section refreshed — checkpoint S02's commit, next action
  S04 with what to name, one line added to *Tried and rejected*, the
  reading order for the update.

**The readings, at `689647b`:**

```text
$ wc -w < spec/index.md
6112
$ for f in AGENTS.md spec/reference/paths.md project/coding-paths/binding.md spec/reference/execution-protocol.md; do printf "%-42s %s\n" "$f" "$(wc -w < $f)"; done
AGENTS.md                                  326
spec/reference/paths.md                    1362
project/coding-paths/binding.md            302
spec/reference/execution-protocol.md       974
$ cat AGENTS.md spec/reference/paths.md project/coding-paths/binding.md spec/reference/execution-protocol.md | wc -w
2964
```

The same method at the 1.0.0 tag, to prove it is the same method:

```text
$ git show 1.0.0:spec/index.md | wc -w
5630
$ (git show 1.0.0:AGENTS.md; git show 1.0.0:spec/reference/paths.md; git show 1.0.0:project/coding-paths/binding.md; git show 1.0.0:spec/reference/execution-protocol.md) | wc -w
2895
$ for f in …; do printf "%-42s %5s -> %5s\n" "$f" "$(git show 1.0.0:$f | wc -w)" "$(wc -w < $f)"; done
AGENTS.md                                    272 ->   326
spec/reference/paths.md                     1362 ->  1362
project/coding-paths/binding.md              287 ->   302
spec/reference/execution-protocol.md         974 ->   974
```

The 69 words: `git diff 1.0.0 -- AGENTS.md project/coding-paths/binding.md`
shows the sixth skill on the *start here* list, `npm test` named as the
alias of `cairn-test`, the two absolute rules on explaining — the concept
note of ADR-011, the reader who is learning of ADR-021 — and the binding's
registration-transport row.

The kit, from S01's quick start 1 on `ci` and the same `init` on `local`:

```text
$ node tools/cairn.mjs init --target . --profile ci --source src
cairn — installed 33 file(s) and the lock into …/qs1
$ node -e "const l=require('./cairn.lock.json');console.log(Object.keys(l.manifest).length)"
33
$ node tools/cairn.mjs init --target . --profile local --source src
cairn — installed 32 file(s) and the lock into …/qs-local
$ node -e "const l=require('./cairn.lock.json');console.log(Object.keys(l.manifest).length)"
32
$ diff <(ci files) <(local files)
< .github/workflows/cairn.yml
```

The one file `local` does not install is the workflow, as at 1.0.0.

The unit's files, both transports:

```text
$ node tools/cairn-pilot.mjs --transport pull-request
  one lightweight unit writes 2 protocol file(s); the whole lifecycle touches 4: project/coding-paths/ACTIVE.md, project/coding-paths/CP-PILOT-001/index.md, project/coding-paths/CP-PILOT-001/steps/S01.md, project/log/2026-09-15-cp-pilot-001.md
$ node tools/cairn-pilot.mjs --transport manual-git
  one lightweight unit writes 2 protocol file(s); the whole lifecycle touches 5: project/coding-paths/ACTIVE.md, project/coding-paths/CP-PILOT-001/closing-ee7c3297a09a847fe6cc9c2f5912a0b4dc03a4a5.md, project/coding-paths/CP-PILOT-001/index.md, project/coding-paths/CP-PILOT-001/steps/S01.md, project/log/2026-09-15-cp-pilot-001.md
```

Every target that has one bound. The closest is the entry chain, 36
words under 3,000; the page says so, because the next release is the one
that could move it past.

#### Self-review

- `correctness:` my first draft of the paragraph put the entry chain's
  growth in the execution protocol, which did not change a word; the
  per-file readings above caught it before the reader did, and the
  sentence names the two files that grew and the two that did not.
- `shrink:` the method stated once, in the paragraph under the table,
  rather than in each cell.
- `yagni:` a fifth column for the delta — the two columns side by side are
  the delta; the one that matters, 69 words and 36 to go, is a sentence.
- `delete:` after the first read: the 36 stated in the table cell and
  again in the sentence under it — the sentence keeps the row to watch and
  drops the number.
- net: 23 lines added, 16 removed, on the page; this step and the
  record's refresh beside it.

#### Review

First read — reader: a fresh context, a subagent of this harness on a
smaller model, given the unit's diff in one file and the two criteria,
returned in 354 seconds on the first attempt — given this unit's diff and
the two criteria. Two findings, each with its disposition:

- `shrink:` *36 under* stated in the table cell and again in the sentence
  below it — **fixed**: the sentence names the row to watch and no number.
- `correctness:` the three causes named for the entry chain's 69 words sum
  to 66; the fourth, `npm test` named as the alias of `cairn-test`, is
  three words — **fixed**: the fourth cause named; 1 + 50 + 3 + 15.

The reader recounted every figure itself: the two word counts at the tag
and at HEAD, the arithmetic, the kit on both profiles by a fresh `init`,
the pilot on both transports, the records named, the row pointed at. All
true. It noted the installation row states totals and not which seven
files 1.1 added — true, and the lock's manifest is where the list is; the
row is the right pointer for the count, which is what the sentence claims.

Second read — reader: a fresh context, a subagent of this harness on the
same smaller model, given only the diff of those fixes and the same two
criteria, returned in 132 seconds on the first attempt — given only the
diff of those fixes. It found nothing under either criterion: the four
causes account for the 69 exactly, the alias is what `package.json`
declares, and the deleted clause was the duplicate.

The fixes of the second read are not read a third time; the self-review
above carries the line the cut adds.

#### Verification

```text
cairn-check            : pass — OK, protocol satisfied, no advisory, profile ci (exit 0), run bare with this record complete
cairn-test             : pass — 334 tests, 0 failed (exit 0), bare
the readings           : every number in the column is a command's output kept above; the method reproduces 5,630 and 2,895 at the 1.0.0 tag
targets                : all bound at 1.1.0; the entry chain 36 under, the closest
review                 : two reads by fresh contexts, 354 and 132 seconds, both on the first attempt
user review            : not required for the unit; the page is one the owner may read before the merge
remote                 : pushed immediately after this record; the commit id is named in the resume section by S04
```

#### Checkpoint

```text
status      : running
current step: S03 complete on push
retention   : none (no-rewrite host)
changed     : spec/reference/conformance.md, this path's index and this step
session     : safe boundary after a successful push
next action : S04 — this repository updated with its own kit: status and update run here, reports quoted, the pointer page installed, the lock refreshed, unwanted additions named with the reason
blockers    : none
```
