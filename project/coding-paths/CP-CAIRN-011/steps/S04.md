---
type: Cairn Coding Path Step
title: 'CP-CAIRN-011 S04 — this repository updated with its own kit'
timestamp: 2026-09-15T00:00:00Z
cairn:
  path: CP-CAIRN-011
  step: S04
---

# CP-CAIRN-011 S04

### S04 — this repository updated with its own kit — **COMPLETE**

#### Plan

Run the kit coding path 4 rewrote against the repository that ships it,
for the first time: `status`, then `update`, from this tree. Quote both
reports. Let `update` rewrite what is pristine, leave what is edited and
print what the release changes in each, install the pointer page and the
lock at 1.1's manifest (ADR-013, ADR-015). Name every file it would add
that this repository's binding does not want, with the reason.

Definition-of-done item advanced: the fourth — all of it but the
release's name on the page and in the lock, which is `package.json`'s and
reads *1.0.0* until S05 sets it, so S05 runs `update` once more.

Type `documentation`: the pointer page, the lock, two folder indexes and
this step. No file under `tools/` or `site/` changes — the kit's tools are
this tree's own files, and rewriting a pristine one writes the bytes it
holds — so no test, and the tree's own diff is the proof.

Deliberately not in this unit: reconciling any of the seventeen edited
files by hand or by `--take` — the definition of done says they are left
and listed, and each one's difference is this repository being itself,
read below; the bootloader's line to the pointer page, which the
release's template has and this repository's `AGENTS.md` does not —
outside this path's writes, and on the pointer page's reconcile list,
where ADR-015 decision 2 puts it.

#### Work

```cairn-unit
step: S04
unit: 04
type: documentation
verified: cairn-check, cairn-test, the kit's own reports
```

- **`cairn/README.md`**, new, generated: the manifesto's first sentence,
  the release and the commit, the six chapters linked at that commit, the
  six skills linked in this repository, Ponytail at its tag, the
  thirty-three files the kit owns and the lock, and the seventeen files to
  reconcile by hand.
- **`cairn.lock.json`**: release `1.0.0` still, `sourceCommit` the tree's
  HEAD at the run, `installedAt` today; the manifest thirty-three entries
  for twenty-six — eight in, the post-mortem, the sixth skill, the pointer
  page, the architecture and inputs indexes, the three concept indexes;
  one out, `spec/concepts/index.md` — and the host list fifteen for
  eleven, the five indexes in and the wiki's index out.
- **`docs/inputs/index.md`**, new, generated: the folder of ADR-011
  decision 1; this repository had no inputs, and the index says what the
  folder is for.
- **`docs/index.md`**: one line for the inputs folder, saying it is the
  kit's and empty here; nothing else, because the release's template
  lists surface pages and concept folders this repository does not have.
- **`project/coding-paths/CP-CAIRN-011/index.md`**: the steps line for
  S04; the resume section refreshed — checkpoint S03's commit, the next
  action S05 with the second `update` and the 1.2 row's two new items,
  three lines added to *Tried and rejected*, the reading order for the
  register and the page.

**First reading — `status` on the tree before anything was written:**

```text
$ node tools/cairn.mjs status
cairn — installed release 1.0.0, this package is 1.0.0 (current)
kit files: 23 edited, 2 pristine, 3 unmanaged, 5 missing
  update would write   docs/inputs/index.md (missing)
  update would write   spec/concepts/cairn/index.md (missing)
  update would write   spec/concepts/product/index.md (missing)
  update would write   spec/concepts/learning/index.md (missing)
  update would write   cairn/README.md (missing)
  update would keep    cairn.config.json — edited here; review it against the kit's
  update would keep    AGENTS.md — edited here; review it against the kit's
  update would keep    package.json — edited here; review it against the kit's
  update would keep    tools/cairn-check.mjs — edited here; review it against the kit's
  … (twenty-three kept in all: the three tools, the eight skill files of 1.0, the binding, the register, the two project indexes and the view, the three docs files, the workflow, the request template)
  update would report  spec/concepts/index.md — no longer part of the kit, and edited here
```

*Edited* here means *differs from the 1.0.0 lock's digest*: every tool and
skill 1.1 changed reads as edited against a lock cut on 2026-09-03, and the
three files 1.1 added — the post-mortem, the sixth skill, the architecture
index — as *unmanaged*, present with no lock entry. The kit's two pristine
files were the configuration loader and its schema, untouched since 1.0.0.

**The update.** The first run was made with a stale `tools/release.json`
in the tree — the stamp `npm pack` left at S01, naming the registration
commit — and its pointer page said *cut from `891b07d`*. The stamp is not
tracked; it was deleted, the run repeated, and the second run is the one
that stands. Its report, the diffs elided here and read in full below:

```text
$ node tools/cairn.mjs update
cairn — installed release 1.0.0, this package is 1.0.0 (current)
kit files: 13 pristine, 17 edited, 3 missing
cairn — updated to release 1.0.0: 4 written, 0 deleted
to reconcile by hand (17): AGENTS.md, package.json, skills/cairn-brainstorm/SKILL.md, skills/cairn-close/reference.md, skills/cairn-learn/SKILL.md, skills/cairn-open/SKILL.md, skills/cairn-open/reference.md, skills/cairn-unit/SKILL.md, project/coding-paths/binding.md, project/coding-paths/index.md, project/index.md, docs/index.md, docs/architecture/index.md, docs/modules/index.md, docs/modules/application.md, .github/workflows/cairn.yml, .github/pull_request_template.md
they are listed on cairn/README.md too, so the work is on disk and not only here
$ node -e "const l=require('./cairn.lock.json');console.log(l.release, l.sourceCommit, Object.keys(l.manifest).length, l.host.length)"
1.0.0 0b40d5ef95b72690581b67e5816c56ab703c3d49 33 15
$ git status --short
 M cairn.lock.json
?? cairn/
?? docs/inputs/
```

Thirteen pristine after the first run's lock — the six tools and the
schema, `cairn-close`'s and `cairn-code`'s skill files, `cairn-unit`'s
reference, the view, the configuration and the pointer page — rewritten
with the bytes they hold, which is why `git status` shows none of them.
Four written: the three concept indexes the tree lacked again, and the
pointer page — pristine after the first run, rewritten because the commit
it names had changed. Seventeen edited and left. Nothing
deleted: `spec/concepts/index.md`, the file 1.0 installed as the wiki's
index and 1.1 no longer owns, is reported and kept, as ADR-015 has it.

**What the release changes in each edited file**, from the diffs `update`
printed — the release's template against the file here:

- `AGENTS.md` — the template links the path convention and the execution
  protocol on GitHub at the commit and adds a sixth *start here* line to
  the pointer page and a paragraph on the installed release; this
  repository's links the files beside it and names the test alias and
  the `cairn -- status` script. The pointer-page line is the one to
  reconcile.
- `package.json` — the template is the four-line manifest of an adopter,
  `cairn-repository`, private; this file is the package itself, with its
  name, binary, files and scripts.
- the six skill files — `cairn-brainstorm`, `cairn-learn`, `cairn-open`
  and `cairn-unit`'s `SKILL.md`, `cairn-close`'s and `cairn-open`'s
  `reference.md`: one link each, the template's on GitHub at the commit,
  this repository's relative into its own `spec/`.
- `project/coding-paths/binding.md` — the template's rows are bare; this
  repository's say the wiki is the protocol's own and the registration
  commit lands on the trunk directly, and its *concept wiki* section says
  what the index separates.
- `project/coding-paths/index.md` — the template is the installer's one
  row; this file is the roadmap register of 1.0 and 1.1.
- `project/index.md` and `docs/index.md` — the templates describe an
  adopter's planes; these describe this repository's — `brainstorm/`,
  `sessions/`, the protocol documents under `docs/cairn/`, the wiki in
  `spec/concepts/`.
- `docs/architecture/index.md` — the template says one page per feature,
  interface, contract or flow with its dependency sentence and diagram;
  this file says one page per system, numbered, and lists the 1.1 page.
- `docs/modules/index.md` and `docs/modules/application.md` — the
  templates are an adopter's one area; these are the two areas of this
  repository, the tools and the site, and the tools' note in full.
- `.github/workflows/cairn.yml` — the template runs no test step, as
  ADR-014 decision 2 has it for an adopter; this repository's runs
  `cairn-test` first and says why.
- `.github/pull_request_template.md` — two placeholders in code spans
  here, bare in the template.

None of the seventeen is a drift: each difference is this repository
being the protocol's own — the specification beside it, the package
itself, the register of its own paths — and none is taken.

**What `update` would add that this repository does not want.** The kit
plans three concept folders under the binding's concepts root —
`cairn/`, `product/`, `learning/`, one index each, the adopter's wiki of
ADR-011 decision 2 and ADR-022 — and the root here is `spec/concepts/`,
the protocol's own wiki. This path's definition of done excludes
`spec/concepts/` from change, the wiki's own index already separates
borrowed vocabulary from Cairn's, and a folder for *this product's own
words* inside the protocol's specification would be the protocol's terms
twice. The three were written by `update` — there is no way to decline a
host file — and deleted from the tree after each run, so every `status`
here will report them *missing* and every `update` will write them
again. That is the reading the kit gives a repository that binds its
wiki where the protocol keeps its own; it is named for the 1.2 row, and
nothing is done about it in a release.

`docs/inputs/index.md` is taken: the folder of ADR-011 decision 1, in
this path's writes, empty here and said so in the docs index.

**The pointer page**, read: *release 1.0.0, cut from commit
`0b40d5ef95b72690581b67e5816c56ab703c3d49`*; six chapters linked at that
commit; six skills linked at `../skills/`; Ponytail `v4.9.0`; thirty-four
paths under *What the kit owns* — the thirty-three and the lock; the
seventeen under *To reconcile by hand*. The release it names is
`package.json`'s and the commit is the tree's HEAD at the run, which is
what `sourceCommit` reads when the directory is the protocol's own
repository; S05 runs `update` again after the version, and the candidate
S06 cuts is the commit the tag names.

**`status` after:**

```text
$ node tools/cairn.mjs status
cairn — installed release 1.0.0, this package is 1.0.0 (current)
kit files: 13 pristine, 17 edited, 3 missing
  update would write   cairn.config.json (pristine)
  update would write   spec/concepts/cairn/index.md (missing)
  update would write   spec/concepts/product/index.md (missing)
  update would write   spec/concepts/learning/index.md (missing)
  update would write   cairn/README.md (pristine)
  update would keep    AGENTS.md — edited here; review it against the kit's
  …
```

The two pristine files it would write are the configuration and the
pointer page, whose templates carry the day's date; writing them is
writing what is there.

#### Self-review

- `delete:` the first run's pointer page and lock, made with a stale
  stamp — overwritten by the second run rather than kept and explained.
- `shrink:` the seventeen differences told as one line each, grouped
  where the difference is the same, instead of the four hundred diff
  lines the report printed; the report is reproducible from the tree.
- `yagni:` `--take` on any of the seventeen — refused by the definition
  of done, and each difference is this repository being itself.
- `yagni:` a link from `docs/index.md` to the pointer page — the page
  sits at the root by name, which is ADR-013's reason for the folder.
- net: 2 files added by the kit, 1 rewritten, 1 line added by hand,
  outside this path's folder and this step.

#### Review

First read — reader: a fresh context, a subagent of this harness on a
smaller model, given the unit's diff in one file and the two criteria,
returned in 238 seconds on the first attempt — given this unit's diff and
the two criteria. Two findings, each with its disposition:

- `correctness:` the lock's manifest lists three files that are not on
  disk, the concept indexes under `spec/concepts/` — **refused**: declined
  on purpose, for the reason the plan gives, and named above as what
  every `status` here will report; the lock records what the kit
  installs, which is what the definition of done asks of it, and the tree
  records what this repository takes.
- `correctness:` the pointer page's *What the kit owns* has thirty-four
  entries to the manifest's thirty-three, the lock itself appended by the
  generator — **refused**: the page says the kit owns the lock, which it
  does, and the count above already reads *the thirty-three and the
  lock*.

Everything else checked true: the release and commit on the page and in
the lock against `package.json` and HEAD, every relative link, the
reconcile list item for item against `status`, Ponytail's tag, the
inputs line. Nothing was fixed, so there is no second read.

#### Verification

```text
cairn-check            : pass — OK, protocol satisfied, no advisory, profile ci (exit 0), run bare with this record complete
cairn-test             : pass — 334 tests, 0 failed (exit 0), bare
status before          : 23 edited, 2 pristine, 3 unmanaged, 5 missing — quoted above
update                 : 13 pristine, 17 edited, 3 missing; 4 written, 0 deleted; 17 to reconcile — quoted above, the second run, from the tree's own HEAD
status after           : 13 pristine, 17 edited, 3 missing — the three declined concept indexes
the tree               : cairn.lock.json rewritten, cairn/README.md and docs/inputs/index.md added, docs/index.md one line; no file under tools/, site/, skills/ or spec/ changed
review                 : one read by a fresh context, 238 seconds, on the first attempt; nothing fixed, no second read
user review            : not required for the unit; the owner reads the pointer page before the merge if they wish
remote                 : pushed immediately after this record; the commit id is named in the resume section by S05
```

#### Checkpoint

```text
status      : running
current step: S04 complete on push
retention   : none (no-rewrite host)
changed     : cairn.lock.json, cairn/README.md, docs/inputs/index.md, docs/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S05 — package.json at 1.1.0, update run again for the page and the lock, the 1.1 page's opening sentence, the register's 1.1 row done and the 1.2 row opened with its agenda
blockers    : none
```
