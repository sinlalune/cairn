---
type: Cairn Coding Path Step
title: 'CP-CAIRN-011 S01 — the README: what Cairn is, its surfaces, every count 1.1's'
timestamp: 2026-09-15T00:00:00Z
cairn:
  path: CP-CAIRN-011
  step: S01
---

# CP-CAIRN-011 S01

### S01 — the README: what Cairn is, its surfaces, every count 1.1's — **COMPLETE**

#### Plan

Make `README.md` the page ADR-023 decision 1 and ADR-012 say it is: what
Cairn is in one paragraph, then its surfaces one line each in the order a
newcomer meets them — adopting the protocol, opening and running a path,
closing one, learning — each naming the skill or command behind it; and
make every count and sequence in it 1.1's, each read off a tool at this
commit rather than typed: six skills, the rules as `cairn-rules` counts
them, five movements, the files `init` installs as the lock counts them,
registration on the trunk directly under `manual-git`. Run the three
quick starts against the kit at this commit and keep their output here.
Point the *Weight* section at the conformance page with no number.

Definition-of-done item advanced: the first.

Type `documentation`: the README and this step, the record's steps line
and resume section. No source changes, so no test; the proof is the
output of the commands the README tells a reader to run.

Deliberately not in this unit: the site, which projects the README and
hard-codes a *Start* section (S02); the conformance page's budget table,
which still reads *26 and the lock* at 1.0.0 beside a row of the same
page already saying 33 (S03); `package.json`, still `1.0.0`, which is why
every report below says *release 1.0.0* — the version is S05's; the
pointer page of this repository, which does not exist until S04 runs
`update` here, so the README's surface lines link the skills and not
`cairn/README.md`.

#### Work

```cairn-unit
step: S01
unit: 01
type: documentation
verified: cairn-check, cairn-test, the three quick starts, cairn-pilot
```

- **`README.md`**: the opening paragraph says what Cairn is — files in the
  repository and one checker reading them — and keeps the six questions
  the protocol answers; then the four surface lines, each linking the
  skill's `SKILL.md`; the four pages that were a bullet list become one
  sentence naming the sixth skill, `cairn-brainstorm`, and the stance.
  *In one screen*: five movements with the review named as what it is —
  the writer's own agent in a fresh context, two criteria; the merge as
  the acceptance; twenty-seven rules, twenty-one blocking; the checker
  reads the repository and asks the host nothing (ADR-029); *recorded on
  the pull request* in place of *where the forge already keeps it*.
  Quick start 1: thirty-three files and the lock on the `ci` profile, the
  six skills, both planes' indexes, the three concept folders, the inputs
  folder, the pointer page, and the declaration `init` writes — `manual-git`
  registration, pull-request integration (ADR-024). Quick start 2: the
  goal's three plain lines (ADR-021 d2), the owner's yes in the chat as the
  acceptance, the registration commit landed on the trunk directly with
  the reason there is no request, the fresh-context review in the unit,
  the owner asked to try the result and the merge as the acceptance at
  the close. Quick start 3 split in two, because `adopt` refuses a
  repository that carries a lock (`applyAdopt`, first line): a repository
  installed from an earlier release runs `status` then `update`; one that
  copied the protocol by hand runs `adopt`. *Where to go next* gains the
  1.1 page and the decision records. *Weight* names the four measures and
  the page, and no number; the spec's word count leaves the opening list
  too.
- **`project/coding-paths/CP-CAIRN-011/index.md`**: the steps line for
  S01; the resume section refreshed — the checkpoint still the
  registration commit, the next action S02, two lines added to *Tried and
  rejected*, the reading order for the site.

**The counts, as the tools give them at `891b07d`:**

```text
$ npm run cairn-rules --silent | grep '^| \*' | awk -F'|' '{gsub(/ /,"",$3); print $3}' | sort -u | wc -l
27
$ npm run cairn-rules --silent | grep '^| \*\*Blocking' | awk -F'|' '{gsub(/ /,"",$3); print $3}' | sort -u | wc -l
21
advisory only: concept-growth, decision-drift, record-date, redaction, remote-checkpoint, writes-overlap
$ ls skills/*/SKILL.md | wc -l
6
$ grep -c '^## [1-6]\. ' spec/index.md
6
```

**The kit at this commit.** `npm pack` from the worktree gave
`cairn-protocol-1.0.0.tgz`, stamped by `prepack` from `891b07d`; `npx`
was given the tarball with `--package`, so each command below is the
README's own with the registry's 1.0.0 replaced by the kit at this
commit. The scratch repositories live outside the tree and are not
committed.

**Quick start 1 — a new repository:**

```text
$ git init -b main && npx --package=cairn-protocol-1.0.0.tgz cairn init --target . --profile ci --source src
cairn — installed 33 file(s) and the lock into …/qs1
release 1.0.0 from 891b07d; trunk main via origin; profile ci; registration manual-git, integration pull-request; path history forbidden
next — commit this installation, then read AGENTS.md and open your first path
$ git add -A && git commit -m "Install Cairn"
3f8df50 Install Cairn
$ git ls-files | grep -v '^cairn.lock.json$' | wc -l
33
$ node -e "const l=require('./cairn.lock.json');console.log(Object.keys(l.manifest).length, l.host.length, l.release, l.sourceCommit)"
33 15 1.0.0 891b07de4afb4239a26ecccfa1f250a046701c34
$ npm run cairn-check
cairn-check — profile ci, branch main, base working tree vs HEAD (trunk-work), 0 changed file(s)
binding — schema 2; trunk main via origin; metadata cairn; new-path route lightweight; docs docs; project project; source src; path history forbidden (no rewriting)
profile — ci; transports registration manual-git, integration pull-request

OK — protocol satisfied
```

The thirty-three: the pull-request template and the workflow; `AGENTS.md`;
`cairn.config.json`; `cairn/README.md`; `docs/index.md`, the architecture,
inputs and modules indexes with `docs/modules/application.md`, the three
concept indexes under `docs/concepts/`; `package.json`;
`project/index.md`, `ACTIVE.md`, `binding.md` and the register; the six
skills' `SKILL.md` and three reference files; the five tools and the
configuration schema. The lock's manifest counts the same thirty-three.

**Quick start 2 — one coding cycle**, in the same repository, given a
bare `origin` so the pushes are real:

```text
$ node tools/cairn-check.mjs --scope-digest project/coding-paths/CP-FIRST-001/index.md#definition-of-done
sha256:9ec487c0e0378f997aaeb2dadc528277b29880fcf06837174248a4aaaec9b225
$ npm run cairn-active
cairn-active — the roadmap register still carries the installer's row, `M1 — the first milestone`, while 1 path is registered: project/coding-paths/index.md
cairn-active — rewrote the running-paths view in project/coding-paths/ACTIVE.md
$ npm run cairn-check
OK — protocol satisfied
$ git commit -m "Register CP-FIRST-001" && git push origin main
registered 077fc8b
$ git worktree add ../qs1-cp-first-001 -b path/cp-first-001 main && cd ../qs1-cp-first-001 && git push -u origin path/cp-first-001
pushed origin/path/cp-first-001
$ npm run cairn-check
cairn-check — profile ci, branch path/cp-first-001, base origin/main (default-trunk), 0 changed file(s)
profile — ci; transports registration manual-git, integration pull-request
OK — protocol satisfied
```

The record was written from the path template as step 1 says, the
acceptance recorded with that digest, `running` with `origin/main` as its
base. The rest of the quick start is the unit and the close, which the
skills drive; that a path of this kit runs from `init` to `done` is the
pilot's proof:

```text
$ node tools/cairn-pilot.mjs --transport pull-request
cairn-pilot — install to done on pull-request: green at every gate
  installed    OK
  registered   OK — protocol files: project/coding-paths/ACTIVE.md, project/coding-paths/CP-PILOT-001/index.md
  unit pushed  OK — protocol files: project/coding-paths/CP-PILOT-001/index.md, project/coding-paths/CP-PILOT-001/steps/S01.md
  candidate    OK
  ready        OK — protocol files: project/coding-paths/ACTIVE.md, project/coding-paths/CP-PILOT-001/index.md
  done         OK — protocol files: project/coding-paths/ACTIVE.md, project/coding-paths/CP-PILOT-001/index.md, project/log/2026-09-15-cp-pilot-001.md
  one lightweight unit writes 2 protocol file(s); the whole lifecycle touches 4
```

**Quick start 3 — a repository installed from an earlier release.** The
published 1.0.0 was installed from the registry with
`npx cairn-protocol@1.0.0 init --target . --profile ci --source src` —
*installed 26 file(s) and the lock*, *release 1.0.0 from e26f19d* — and
committed; then, with the tarball:

```text
$ npx --package=cairn-protocol-1.0.0.tgz cairn status
cairn — installed release 1.0.0, this package is 1.0.0 (current)
kit files: 25 pristine, 8 missing
  update would write   AGENTS.md (pristine)
  update would write   package.json (pristine)
  update would write   tools/cairn-check.mjs (pristine)
  …
  update would write   tools/cairn-postmortem.mjs (missing)
  update would write   skills/cairn-learn/SKILL.md (missing)
  update would write   docs/inputs/index.md (missing)
  update would write   docs/architecture/index.md (missing)
  update would write   docs/concepts/cairn/index.md (missing)
  update would write   docs/concepts/product/index.md (missing)
  update would write   docs/concepts/learning/index.md (missing)
  update would write   cairn/README.md (missing)
  update would report  docs/concepts/index.md — no longer part of the kit, and yours to delete
$ npx --package=cairn-protocol-1.0.0.tgz cairn update
cairn — updated to release 1.0.0: 27 written, 0 deleted
$ git add -A && git commit -m "Update Cairn"
750ecae Update Cairn
$ node -e "const l=require('./cairn.lock.json');console.log(Object.keys(l.manifest).length, l.release, l.sourceCommit.slice(0,7))"
33 1.0.0 891b07d
$ npm run cairn-check
profile — ci; transports registration pull-request, integration pull-request
OK — protocol satisfied
```

Twenty-five pristine files rewritten, eight the release adds restored,
the file 1.0 installed and 1.1 no longer owns reported and kept; the 1.0
installation keeps the `pull-request` registration it declared (ADR-024).
*This package is 1.0.0 (current)* is `package.json` at this commit; the
line reads *1.1.0* once S05 lands.

**Quick start 3 — a repository that copied the protocol by hand.** A copy
of that 1.0.0 installation with its lock removed and committed, then:

```text
$ npx --package=cairn-protocol-1.0.0.tgz cairn adopt --target .
cairn — adopted …/qs3-adopt at release 1.0.0: 24 written, 9 host files kept
  kept     AGENTS.md — yours; review it against the kit's
  kept     package.json — yours; review it against the kit's
  kept     project/coding-paths/binding.md — yours; review it against the kit's
  kept     project/coding-paths/index.md — yours; review it against the kit's
  kept     project/index.md — yours; review it against the kit's
  kept     docs/index.md — yours; review it against the kit's
  kept     docs/modules/index.md — yours; review it against the kit's
  kept     docs/modules/application.md — yours; review it against the kit's
  kept     .github/workflows/cairn.yml — yours; review it against the kit's
next — run npm run cairn-check, read its findings, and commit the adoption as one unit
$ git add -A && git commit -m "Adopt Cairn" && npm run cairn-check
OK — protocol satisfied
```

#### Self-review

- `delete:` the opening's list of four pages — one sentence now, after the
  surface lines, which is where a reader who has met the surfaces wants
  the pages; the spec's word count gone from it, and every number gone
  from *Weight*, which pointed at the page and then repeated four of its
  numbers.
- `correctness:` quick start 3 told a repository installed from an earlier
  release to run `adopt`, which refuses a repository that carries a lock;
  split into the two commands the tool has.
- `shrink:` the *Adopting* surface line named all four subcommands with a
  clause each — cut to one clause per pair.
- `yagni:` a fifth surface line for `cairn-brainstorm` — the definition of
  done lists four surfaces; the skill is named where the six are.
- `yagni:` linking `cairn/README.md` from the surface lines — the file does
  not exist in this repository until S04, and the `links` rule reads the
  README.
- `delete:` after the first read: the *Around them* paragraph, three
  links the *Where to go next* table already carried; the skills get a
  row of that table instead, the one place they were not yet named.
- net: 98 lines added, 61 removed, in the README; this step and the
  record's refresh beside it.

#### Review

First read — reader: a fresh context, a subagent of this harness on a
smaller model, given the unit's diff in one file and the two criteria,
returned in 229 seconds on the second attempt; the first attempt, on this
harness's default model, was cut off by a session rate limit after 2,104
seconds with nothing returned — given this unit's diff and the two
criteria. Two findings, each with its disposition:

- `correctness:` the close paragraph of quick start 2 put the
  administrative commit before the owner tries the result, the reverse of
  `cairn-close` §3 and §4 — **fixed**: the owner tries the result, then
  the one administrative commit, then the merge.
- `delete:` the surface lines and the *Around them* paragraph restate what
  *In one screen* and the quick starts say, and the paragraph's three
  links duplicate the *Where to go next* table — **fixed in part**: the
  paragraph is cut and the skills become one row of the table; the
  surface lines stay, being what ADR-023 decision 1 and the first item of
  the definition of done ask the README to carry — one line per surface
  naming its page — and not a restatement of the mechanics below them.

Every count, command, sequence and link the reader checked came back
true: six skills, twenty-seven rules and twenty-one blocking, no network
call in the checker, thirty-three files and the lock from a scratch
`init`, the declaration `init` writes, the pointer page, `update`, `adopt`
and `--take` reproduced on a scratch target, the goal's three lines, and
every relative link.

Second read — reader: a fresh context, a subagent of this harness on the
same smaller model, given only the diff of those fixes and the same two
criteria, returned in 140 seconds on the first attempt — given only the
diff of those fixes. It found nothing under either criterion: the cut
paragraph's three targets each have a row of the table, the added row is
the least text that names the sixth skill, *the five above* counts the
five the surface lines name, both links resolve, and the closing order
now matches the skill's steps 2 to 5.

The fixes of the second read are not read a third time; the self-review
above carries the line the cut adds.

#### Verification

```text
cairn-check            : pass — OK, protocol satisfied, no advisory, profile ci (exit 0), run bare with this record complete
cairn-test             : pass — 334 tests, 0 failed (exit 0), bare, on the second and third runs; the FIRST run went red on one assertion — a fixture expecting no blocking finding met `record-integrity` — with the test's name lost to the tail of its output; the two reruns, on the same tree, passed every test; not reproduced, not diagnosed, and outside this path's writes — named here so the next milestone's row can carry it if it returns
quick start 1          : pass — 33 files and the lock on `ci`, the gate green on the installed repository, output above
quick start 2          : pass — the digest computed by the checker, the registration commit on the scratch trunk, the branch pushed, the gate green on the branch, output above; the unit and the close proved by cairn-pilot from init to done on pull-request, green at every gate, 2 protocol files per unit and 4 per lifecycle
quick start 3          : pass — the registry's 1.0.0 updated by the packed kit, 27 written and 0 deleted, the lock at 33; a lock-less copy adopted, 24 written and 9 host files kept; both gates green, output above
counts                 : 27 rules and 21 blocking from `cairn-rules`; 6 skills from `skills/`; 6 chapters from `spec/index.md`; 33 from the lock — none typed
links                  : the `links` rule read the README green; the reader resolved each relative link by hand
review                 : two reads by fresh contexts, 229 and 140 seconds; the first attempt lost to a rate limit after 2,104 seconds
user review            : not required for the unit; the owner opens the README before the merge, as the ninth item says
remote                 : pushed immediately after this record; the commit id is named in the resume section by S02
```

#### Checkpoint

```text
status      : running
current step: S01 complete on push
retention   : none (no-rewrite host)
changed     : README.md, this path's index and this step
session     : safe boundary after a successful push
next action : S02 — the site: built from the candidate, projecting the README, the manifesto, the specification and the six skills; its Start section and any hard-coded list saying what the README says; the build's output in the step
blockers    : none
```
