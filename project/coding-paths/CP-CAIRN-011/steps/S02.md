---
type: Cairn Coding Path Step
title: 'CP-CAIRN-011 S02 — the site: built from the candidate, the six skills projected, one hard-coded count made the README''s'
timestamp: 2026-09-15T00:00:00Z
cairn:
  path: CP-CAIRN-011
  step: S02
---

# CP-CAIRN-011 S02

### S02 — the site: built from the candidate, the six skills projected, one hard-coded count made the README's — **COMPLETE**

#### Plan

Prove the site builds from the candidate and projects the README, the
manifesto, the specification and the six skills; read what it hard-codes
against the README S01 left, and change what disagrees. Keep the build's
output here.

Definition-of-done item advanced: the second.

Type `implementation`: source under `site/`, its module note, the step.
The change is to one sentence of `site/index.html` that no code reads —
structure, not behaviour — so it carries no new test and says so; the
build is the site's test, as its module note says, and the built shell is
read for the sentence below.

**`writes:` widened** by `docs/modules/site.md`: a change under `site/`
is a source change of the site area, and the `work-unit` rule requires a
module note to move with it; the note is refreshed to the current state
in the same unit, as ADR-010 decision 1 has it. Named in the record's
`writes:` and in the register's row 7.

Deliberately not in this unit: the tagline and the footer, which print
`package.json`'s version and read *1.0.0* until S05; the navigation's
*Start* label, *Overview and quick starts*, which still names what the
README is — what Cairn is, then three quick starts — and changes for no
reader; bundling `docs/` so the README's two new links resolve inside the
site — a link the site does not bundle opens the file on GitHub at the
bundled commit, as every reference link already does, and the site
projects the four layers the owner asked for, not the documentation
plane.

#### Work

```cairn-unit
step: S02
unit: 02
type: implementation
verified: cairn-check, cairn-test, site build
```

- **`site/index.html`**: the meta description said *five skills teach it*
  and *the overview*; it says what the README's first paragraph says —
  files inside the repository and one checker that reads them, one
  command, six skills — and names the four things the site holds.
- **`docs/modules/site.md`**: the *Flow* section says the *Skills* group is
  read off the bundle, so a skill the kit adds appears without a change,
  that the *Start* and *Specification* groups are the five entries the app
  names, and that the shell's description is the one sentence written for
  the site alone, kept to the README's opening; timestamp moved to today.
- **`project/coding-paths/CP-CAIRN-011/index.md`**: `writes:` widened; the
  steps line for S02; the resume section refreshed — checkpoint S01's
  commit, next action S03, one line added to *Tried and rejected*, the
  reading order for the weight.
- **`project/coding-paths/index.md`**, row 7: the writes column names the
  module note and the unit that widened by it.

**What the site hard-codes, read against the README.** `site/src/App.jsx`
names five navigation entries — *Manifesto*, *Overview and quick starts*,
*The specification*, *Concept wiki*, *Reference* — and derives every other
group from the bundle: the six skills from `skills/**/SKILL.md`, the
concepts and the reference pages from their folders. The README's *Where
to go next* table names the manifesto, the specification, the skills, the
concept wiki and the reference, and the site's five entries are those.
`site/index.html` carried the one count, *five skills*; `site/src/styles.css`
and `site/src/route.mjs` carry no prose. The tagline and the footer print
the release from `package.json`.

**The build, from the candidate at `00c0e78` with the shell changed:**

```text
$ cd site && npm ci && npm run build
site — bundled 75 documents at 1.0.0 (00c0e78)
vite v6.4.3 building for production...
✓ 2363 modules transformed.
dist/index.html                                         0.70 kB │ gzip:   0.43 kB
…
dist/assets/index-BZ_tBI0_.js                         650.60 kB │ gzip: 199.84 kB
dist/assets/mermaid.core-DVUrCSGe.js                  696.22 kB │ gzip: 171.95 kB
✓ built in 5.65s
$ node -e "const b=require('./src/content.json');const f=Object.keys(b.files);console.log(f.length);console.log(f.filter(x=>x.endsWith('SKILL.md')).join('\n'))"
75
skills/cairn-brainstorm/SKILL.md
skills/cairn-close/SKILL.md
skills/cairn-code/SKILL.md
skills/cairn-learn/SKILL.md
skills/cairn-open/SKILL.md
skills/cairn-unit/SKILL.md
$ grep -o '<meta name="description"[^>]*>' dist/index.html
<meta name="description" content="Cairn: files inside your Git repository and one checker that reads them; one command installs it, six skills teach it. The manifesto, what it is with three quick starts, the whole specification and the skills." />
```

Seventy-five documents: the manifesto, the README, the specification's
page with its concepts and reference, and the six skills with their
reference files — one more than the seventy-four of 1.0.0, the sixth
skill. Vite's warning on chunk size is the Mermaid bundle, loaded only
when a diagram renders, as at 1.0.0. *At 1.0.0* is `package.json` at this
commit; the bundle reads *1.1.0* once S05 lands, and the site is rebuilt
by its workflow on the push to the trunk.

#### Self-review

- `shrink:` the module note's new sentences — cut from a paragraph to
  three clauses, each a fact a reader of `App.jsx` could check.
- `yagni:` a *Start* label rewritten to match the README's new opening —
  the label still names what the page is; not changed.
- `yagni:` a test asserting the description — a sentence no code reads
  has no predicate; the build and the grep above are the check.
- `writes:` widened by one file, named in the plan with the reason, in
  the record's `writes:` and in the register's row.
- `shrink:` after the first read: the note's exception stated where the
  absolute claim is, in the projection sentence, and the separate sentence
  in *Flow* cut.
- net: 7 lines added, 2 removed, outside this path's own folder and this
  step.

#### Review

First read — reader: a fresh context, a subagent of this harness on a
smaller model, given the unit's diff in one file and the two criteria,
returned in 200 seconds on the first attempt — given this unit's diff and
the two criteria. Three findings, each with its disposition:

- `shrink:` the note's new sentence on the description belongs where the
  note claims nothing is written for the site alone — **fixed**: the
  exception is a clause of that sentence, and the separate sentence is
  cut.
- `correctness:` *kept to what the README's first paragraph says* was true
  of the description's first sentence only; its second names the four
  layers — **fixed**: the clause says one sentence from the README's
  opening, one naming the four layers below.
- `correctness:` the note's *nothing is written for the site that is not
  already a document* contradicted the new exception — **fixed** by the
  same clause.

The rest checked true: six skills, three quick starts, the *Start* and
*Specification* groups as five hard-coded entries and the skills derived
from the bundle, the register's row.

Second read — reader: a fresh context, a subagent of this harness on the
same smaller model, given only the diff of those fixes and the same two
criteria, returned in 95 seconds on the first attempt — given only the
diff of those fixes. It found nothing under either criterion: the fact
now stands in one place, both halves of the clause are true of
`index.html` against the README's opening and the note's four layers, and
the note no longer contradicts itself.

The fixes of the second read are not read a third time; the self-review
above carries the line the cut adds.

#### Verification

```text
cairn-check            : pass — OK, protocol satisfied, no advisory, profile ci (exit 0), run bare with this record complete; before the review section was written it refused this step under `review`, as it should
cairn-test             : pass — 334 tests, 0 failed (exit 0), bare
site build             : pass — `npm ci && npm run build` in `site/`, 75 documents bundled at 1.0.0 from 00c0e78, the six `SKILL.md` among them, Vite green in 5.65 s, the built `dist/index.html` carrying the new description; output above
no new test            : the change is one sentence no code reads; the build and the grep of the built shell are the check
review                 : two reads by fresh contexts, 200 and 95 seconds, both on the first attempt
user review            : not required for the unit; the owner opens the site before the merge, as the ninth item says
remote                 : pushed immediately after this record; the commit id is named in the resume section by S03
```

#### Checkpoint

```text
status      : running
current step: S02 complete on push
retention   : none (no-rewrite host)
changed     : site/index.html, docs/modules/site.md, project/coding-paths/index.md, this path's index and this step
session     : safe boundary after a successful push
next action : S03 — the weight measured at 1.1.0 on the conformance page: the 1.1.0 column beside the 1.0.0 one, every number from a tool, each target reported bound or not, the kit's count with none
blockers    : none
```
