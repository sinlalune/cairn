---
type: Cairn Journal Entry
title: CP-CAIRN-013 — the skills and the specification of 1.2
timestamp: 2026-09-24T12:36:04Z
cairn:
  path: CP-CAIRN-013
  subject_commit: 7f74c2cf0eae03578776e56da51c231452567ca4
  integration_commit: cee3f03972199c4b16f06ae9ee9bb3575cf95196
  outcome: completed
---

# CP-CAIRN-013 — the skills and the specification of 1.2

Opened 2026-09-22 on the owner's go-ahead in the chat, registered directly
on the trunk as `86f02d4`, governed by the 1.2 page and eleven records at
their blob ids. Closed 2026-09-24 at candidate `7f74c2c`, the fifth: four
were voided before it, three by what the closing coherence read found and
one by a reviewer on the request. The trunk moved once while the path ran
— two feedback notes through request 29 — and was merged in, touching
nothing in `writes:` or `governs:`. Ten units, each pushed and green, each
read by a fresh context and its fixes read once more, every read obtained
on the first attempt. The opening acceptance was never amended: the scope
digest at the candidate equals the one accepted at registration. The
administrative commit `e1ffbbd` stood on the branch with its check green
before the owner was asked to read, as this path's own close skill now
orders it; the owner merged as `cee3f03` through request 30. The merge
click was the acceptance.

## What changed

- **The open skill** (S01): the two reads before the go-ahead, the direct
  push's precondition and the `pull-request` registration sequence beside
  it, keeping the registration commit through the merge; step 1 reading
  `project/backlog/`; the definition of done as a plain list, with the
  path template's own list.
- **The close skill** (S02, S03): the administrative commit before the
  reading on `pull-request`, after it on `manual-git`, with the execution
  protocol, chapter 5, the portable convention and the concepts following;
  the four coherence questions handed to a fresh context; a deferral
  naming its owner and a backlog file; the release path's changelog and
  moved notes; the measured figure; the `soundness.md` pointers gone.
- **The unit skill and the seventh skill** (S04): which note travels to
  the protocol's repository and which stays; `skills/cairn-update/SKILL.md`,
  an adopter's update as a path in seven movements; the bootloader naming
  seven.
- **The chapters and the layout** (S05): the backlog space in chapters 1
  and 4; the layout's rows for the backlog, `feedbacks/` with its release
  subfolders, and `CHANGELOG.md`.
- **What `ready` states**, on the owner's ruling of 2026-09-22: the
  candidate checked, administratively closed and proposed; the acceptance
  required at `ready` on `manual-git`, following it on `pull-request`.

## What the closing found, and what that says

The close skill this path wrote asks for a fresh reader of the four
coherence questions, and this path's own closing was the first to run
under it. It paid: five reads, and every one but the last found something
the candidate should not have carried — the backlog described as absent
where ADR-041 installs it; the portable convention still printing the old
closing order; a concept chain in the old order; an edit rule for notes
that the repository does not follow; *the reader* meaning two people. A
reviewer on the request found the worst of it: the command block that
hands the fresh reader its inputs globbed every path folder's record,
this path's own rationale among them — the one input the read exists to
be without. Three fresh reads of that unit had passed the line; none of
them had run it. From S09 on, every reader was told to check claims by
command.

The same unit's reader found that `current_step` had read `S01` since
registration, through eight units, with the gate green: `work-unit` asks
whether the named step is a completed unit, not whether it is the current
one. CP-CAIRN-008 and CP-CAIRN-012 were integrated with the same
staleness.

## What the owner ruled, and what is owed

The owner ruled during the path that pages stating the superseded order
or meaning are fixed in the candidate; `writes:` was widened by nine
files for it, each named in the unit that widened it. Item 7 of the
definition of done — the register gaining this path's id *and nothing
else* — was therefore not met as written, and the owner accepted it at
the merge.

Owed by the next path, one superseding record amending three decisions
this path found wanting: **what `ready` states** (ADR-040); **the fresh
reader's fifth input**, the records of the running sibling paths, which
the live view cannot stand in for (ADR-043); and **the squash limit** —
the open skill bans squash and rebase-merge for a registration request
because `registration-base` reads the parent of the commit that declared
the path `running`, so a trunk allowing only squash merges cannot register
by request (ADR-032 decision 2).

Still disagreeing with the specification in surfaces later rows own:
`README.md` (row 5); `tools/cairn.mjs`, `cairn.lock.json` and
`tools/cairn-pilot.mjs` (row 3); `tools/cairn-audit.mjs` (row 4); the
stale `current_step` no rule catches (row 2). The register's row 1 cell
still reads *running*; it is flipped from this entry, as the 1.1 rows
were.
