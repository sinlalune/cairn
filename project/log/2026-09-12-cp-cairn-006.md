---
type: Cairn Journal Entry
title: CP-CAIRN-006 — the checker reads what it did not, and two records settle path 1's debts
timestamp: 2026-09-12T15:00:00Z
cairn:
  path: CP-CAIRN-006
  subject_commit: adfe226f4b972272a7611d0607e7b15aaaed3d3e
  integration_commit: 68f2c3d9bde8783a1e57b66a9f4059f04ed277fb
  outcome: completed
---

# CP-CAIRN-006 — the checker reads what it did not

Opened 2026-09-11 on the owner's go-ahead in the chat, registered
directly on the trunk as `dcfe988`, governed by the 1.1 page, path 1's
journal entry and seven records at their blob ids. Closed 2026-09-12 at
candidate `adfe226`, the second: the first, `6828a7e`, was voided by the
closing request's reviewer, who found that two matchers disagreed about
`**`, answered by S09. Nine units, each pushed and green, each read by a
fresh context and its fixes read once more; the plan was amended twice,
at S04 and at S06, where a unit the plan named as one turned out to be
two subjects. The administrative commit stood on the branch with its
check green before the merge was asked for, as path 1's journal said it
should. Merged by the owner as `68f2c3d` through pull request #15; the
merge click was the acceptance.

## What changed

- Two records settled path 1's debts before any rule was written:
  ADR-024 supersedes the clause of ADR-001 decision 1 on the kit's
  installed default — a repository installed at 1.1 declares `manual-git`
  registration, the one sequence `cairn-open` ships — and ADR-025 decides
  that a red run's post-mortem is read before the next unit. The 1.1 page
  is amended in place, marked *since 2026-09-11*; the register's rows 3
  and 4 name what each record changes.
- The checker reads what the records of 1.1 asked: every run prints a
  profile line with the declared transports and what the forge does not
  enforce; a trunk commit taking a path from `running` to `done` with no
  `ready` commit is refused, proved on the shape path 1's own closing
  took; the scope digest is judged for every path record a run sees
  changed, whatever its status; two live paths whose `writes:` meet raise
  `writes-overlap`, silent under `depends_on`; the registration commit,
  the remote checkpoint and the branch's tip are read as ADR-004 decides,
  and a range means this path's own commits, a draft resolved only by a
  later valid unit and an edited step answered only by a later step
  binding the blobs; the integrating commit is one commit for one path,
  never a merge object carrying the edit; the journal is read under
  `cairn.path`; every blocking refusal names its remedy, and an audit of
  the checker's own source holds it there; ten dangling citations of an
  earlier repository's records are replaced by the facts they stood for;
  the blocking rule `review` reads the current unit's ledger for a
  non-empty review section, `closure` excepted; every fixture built on
  the path and closure harnesses carries another path's completed unit;
  `globToRegExp` and `patternsMeet` share one meaning of a glob.
- The conformance page's catalogue and matrix, `tools/soundness.md` and
  `docs/modules/application.md` say what the twenty-six rules are.
- Nothing under `skills/`, `.github/`, `tools/cairn.mjs`,
  `tools/cairn-audit.mjs`, `tools/cairn-active.mjs`, `spec/index.md`,
  `README.md` or `site/` changed; the eight governing documents other
  than the page are byte-identical to `base_commit`.

## Decided along the way

- ADR-004's six corrections and ADR-008's three decisions were each two
  subjects, not one; the plan was amended and the units split, the
  definition of done untouched.
- The fixture sweep of ADR-004 decision 4 is not total: the fixtures
  that build their own registration on a fresh installation, and the
  corpus ones, have no harness to carry a merged trunk commit, and the
  conformance page names them rather than claiming otherwise.
- The closing request's reviewer found the glob defect; the fix gave both
  rules the one matcher, with an agreement test whose alphabet had left
  `**` out, which was the hole.

## What remains

Paths 3, 4 and 5 of the roadmap register. Debts this path found and does
not own, each named in its record: the writer's half of a same-branch
step supersession, owed to the path template and the unit skill; the
close skill's sentence against an integrating merge object; the sentence
in `cairn-open` giving the two answers to an overlap, `depends_on` or the
accepted race; the `GITHUB_TOKEN` entry the checker's workflow step needs
so the profile line can read the trunk's rules on CI; the README's rule
count, twenty-four where it is twenty-six; and the 1.1 page's opening
sentence *nothing here is implemented yet*, false since path 1 landed.
