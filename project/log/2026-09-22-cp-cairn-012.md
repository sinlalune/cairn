---
type: Cairn Journal Entry
title: CP-CAIRN-012 — Cairn 1.2 promoted
timestamp: 2026-09-22T09:30:00Z
cairn:
  path: CP-CAIRN-012
  subject_commit: 702cc14f1ab069d2694f9f6cb4e1a3f102cde304
  integration_commit: 2c36963b7f36e8b634fe02a954cdb5c36c54d0ab
  outcome: completed
---

# CP-CAIRN-012 — Cairn 1.2 promoted

Opened 2026-09-21 on the owner's go-ahead in the chat, registered
directly on the trunk as `714a9d0`, governed by the 1.2 decisions page
and the asks note at their blob ids. Closed 2026-09-22 at candidate
`702cc14`, the second: the first, `50f74b6`, was voided by the closing
coherence read, below. The trunk never moved while the path ran, so the
merge that produced each candidate was a no-op and `C` is S06's own
commit. Six units, each pushed and green, each read by a fresh context
and its fixes read once more — twelve reads for the two criteria, all
obtained on the first attempt, 132 findings in all. The opening
acceptance was never amended: the scope digest at the candidate equals
the one accepted at registration. The administrative commit `51a051a`
stood on the branch with its check green before the reading was asked
for, as ADR-040 has it; the owner read the fourteen records and the 1.2
page, and merged as `2c36963` through pull request #28. The merge click
was the acceptance.

## What changed

- **Fourteen decision records, ADR-030 to ADR-043** (S01 to S04), one per
  choice, from the thirty-six asks the owner took on 2026-09-21: what the
  open skill reads before the go-ahead; one place for a fact; registering
  on a trunk that takes no direct push; the installer reading what the
  repository declared; housekeeping with no choice in it, second edition;
  the seventh skill `cairn-update`; the kit shipping Ponytail's skills
  where the harness loads them; a repository declaring its link
  exemptions; the channel, second edition; the release notes naming what
  a release absorbed; the administrative commit before the reading; a
  home for deferred work; the plain-list definition of done; the
  coherence questions read fresh. Every ticked line, K01 to K36, is named
  by exactly one record.
- **Four 1.1 decisions superseded and marked** (S01 to S04, S06): two
  clauses of ADR-001 decision 1 by ADR-032, the first half of ADR-016
  decision 1 by ADR-036, the clause *no tool reads or writes the folder*
  of ADR-028 decision 2 by ADR-038, ADR-002 decision 2 by ADR-042 — each
  named in the superseding record, marked in the superseded one's status
  line and under the decision, in the records' index, and on the 1.1
  page, whose sentences keep their words.
- **The 1.2 architecture page** (S05), in the 1.1 page's sections, naming
  every record where it relies on it and what 1.2 removes from 1.1; the
  first *pending* under ADR-031 decision 3, on ADR-015 decision 2, named
  back by the register; the register's 1.2 row naming this path as the
  promotion and the five coding paths that build 1.2 — the skills and
  the specification, the checker, the kit, the tools and this
  repository's files, the release — each *no path yet*, scoped from the
  records' implementation tables alone.

## What the closing found, and what that says

The closing coherence read ran for the first time as ADR-043 describes
it, on this path's own candidate: a fresh context given the diff, the
two governing notes, the live view and the earlier records answered the
four questions, and answered the first *Yes*. ADR-038 had superseded the
reading half of ADR-028's clause and said *no tool writes the folder*
stands, while its own decision 2 has the kit install `feedbacks/` and
its index in every adopter. A tool writing the folder, unmarked, and
missed by two fresh reads of the unit that wrote it and by the writer.
S06 superseded the clause whole; the fix was read once more for the four
questions and answered *No* four times; the first candidate was void
and the second is what landed. The read cost one unit and found what the
unit's two-criteria reads are not asked to find, which is the argument
ADR-043 makes.

The same read named a softer tension and left it to the owner: ADR-016
decision 4 gives whoever reads a diff *the ladder and correctness, and
nothing else*, and ADR-043 gives a fresh reader the candidate with four
other questions. The request's description carried ADR-043's answer —
the coherence audit predates ADR-016 and adds no criterion to either
read — and the owner merged on it.

## What it leaves

Nothing is implemented: the specification, the skills, the tools, the
kit and the site are exactly what 1.1.0 released. The five coding paths
of 1.2 are scoped in the register and none is opened; where two share a
file, the later declares `depends_on` at its registration. The register's
state cells are still typed, until row 4 lands ADR-031 decision 2. The
word *pending* on ADR-015 decision 2 waits for row 5, the release, to
write the line or say why not. The eleven notes 1.2 answers stay at the
folder's level until that release moves them under `feedbacks/1.2/`.
