---
type: Cairn Journal Entry
title: CP-CAIRN-015 — the kit of 1.2
timestamp: 2026-09-25T14:36:18Z
cairn:
  path: CP-CAIRN-015
  subject_commit: 59dcddaf6cb5b4cf4df3750ba7075d3b6793fe74
  integration_commit: 491060012341fd946f15c487f68bf7de8621d445
  outcome: completed
---

# CP-CAIRN-015 — the kit of 1.2

Opened 2026-09-25 on the owner's go-ahead in the chat, registered directly
on the trunk as `4d5462d`, governed by the 1.2 page and eleven records at
their blob ids, with Atomik's update note of 2026-09-22 placed observation
by observation. Closed at candidate `59dcdda`, the third: `1e7a32c` was
voided by what its fresh coherence reader found, and `67f1cb0` by two
review comments Codex's bot left on request 32, which the owner brought
into the chat. The trunk did not move while the path ran. Ten units, each
read by a fresh context and its fixes read once more, every read obtained
on the first attempt; one, S07, was committed and pushed on a red gate and
repaired by S08, which supersedes its step by its two blob ids. The
opening acceptance was never amended: the scope digest at the candidate
equals the one accepted at registration. The administrative commit
`4218d10` stood on the branch with its check green before the owner was
asked to merge; the owner merged as `4910600` through request 32. The
merge click was the acceptance.

## What changed

- **What the installer reads** (S01): every role root the configuration
  declares, derived only where none is, the plan refusing a path outside
  them; `update --decline`, the lock's `declined`, `status` reading
  *declined*, `--take` taking one back; the documentation index's two
  sentences in the shape they describe.
- **What `update` and `status` report** (S02): one plan, one reading, the
  reconcile list the pointer page and the report share, the view on
  neither; every generated page stamped with the release's day, two runs
  byte-equal; files an update starts managing; the lock's `hostBaseline`.
- **What `adopt` and `init` say and refuse** (S03, S09): the kept manifest
  and workflows that still call a stale file, and the gate red until they
  go; a page that wants a `linkExemptions` declaration; `protected` beside
  `manual-git` refused at `adopt`, and `manual-git` registration refused
  where GitHub's rulesets require the owner to open a request, read once
  with the owner's token and said in one line when not read; the pilot
  registering through a request on `pull-request`.
- **What the kit installs** (S04, S05, S10): `cairn-update` named,
  `feedbacks/` and the project root's `backlog/` with their indexes, the
  release notes linked; Ponytail's two skills fetched from its latest
  release, the lock naming the version, the pin gone, the repository's
  copies kept and a missing one named when the fetch fails; every skill
  copied to `.claude/skills/`, where Claude Code loads it.
- **The two templates** (S06): the pilot's plain list; the request
  template's reader line and every blank backticked, asserted.

## What the closing found, and what that says

The unit reviews caught two defects the tests could not have: this
repository is itself an installation, so after one `update` its own
`skills/` would hold the stance and every later plan would have thrown on
a duplicate; and an offline `update` took an adopter's edit of the stance
as the kit's. The coherence reader found a reading that said nothing when
it had not read, and a sentence that would go false where no path could
correct it. The bot on the request found the two gaps no fresh reader of
this path's own agent had run: a decline forgotten across releases, and a
missing copy reported nowhere. S07's red push is the path's own: a gate
and a commit in one command that did not wait on the exit code.

## What is owed

Three backlog items stand from this path: the empty `steps/` folder Git
does not carry, for the next path that writes the skills; the GitHub
reading carried twice, by the installer and the post-mortem, for row 4;
the checker's link pattern copied by the installer, for the next path
that writes the checker. `cairn-audit` still reads only a boxed
definition of done — row 4. `CHANGELOG.md`, which the package now ships
and `status` links, is written by row 5 before the tag; until then the
link names a file the commit does not carry. GitHub warns that the
workflow's actions target Node 20, which it is retiring — `.github/` is
row 4's. The register's row 3 cell still reads *running*; it is flipped
from this entry, as rows 1 and 2 were.
