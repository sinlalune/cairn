---
type: Cairn Journal Entry
title: CP-CAIRN-016 — the tools of 1.2
timestamp: 2026-09-26T10:16:06Z
cairn:
  path: CP-CAIRN-016
  subject_commit: b9aa9e70208da03c2f42755166188369fce83983
  integration_commit: 03f65ed38e64b356a90102b6940dac67e7f477a2
  outcome: completed
---

# CP-CAIRN-016 — the tools of 1.2

Opened 2026-09-25 on the owner's go-ahead in the chat, registered directly
on the trunk as `9538da4`, governed by the 1.2 page, row 3's journal entry
and five records at their blob ids. Closed at candidate `b9aa9e7`, the
fourth: `eab7ddd` was voided by what its fresh coherence reader found,
`47d3292` by an administrative commit the gate refused, and `4ea0101` by
the request's own run. The trunk did not move while the path ran. Eight
units, each read by a fresh context and its fixes read once more, every
read obtained on the first attempt. The owner ruled four times in the
chat: how a milestone's state is derived, and that a done path is dated
from its journal entry (2026-09-25); that the two GitHub helpers live in
`tools/cairn-config.mjs`, recorded as the scope amendment of 2026-09-25;
that `cairn-active --check` printing a stale register cell without
failing is an accepted interim, and that row 4's Writes cell is widened on
the trunk after the merge (2026-09-26). The administrative commit
`546359f` stood on the branch with its check green before the owner was
asked to merge; the owner merged as `03f65ed` through request 33. The
merge click was the acceptance.

## What changed

- **`cairn-active`** (S01, S07): fills every state cell of the roadmap
  register from the records — a path's from its `status:`, dated from its
  journal entry when done; a milestone's from the paths its row names and
  the table under the heading ending with its short name. `--check` says
  a stale cell and keeps its exit code for the view.
- **The post-mortem and the two workflows** (S02, S08): the run it prints
  in counted red, a request closed without a merge printed *closed*, the
  path that arrived read on the trunk — or the checker's finding and no
  path; the base set once on the job and passed to it; the actions at v5;
  the two GitHub helpers in `tools/cairn-config.mjs`, imported by the
  post-mortem and the installer, both earlier copies gone.
- **`cairn-audit`** (S03, S06): a plain-list definition of done read as a
  boxed one; the coherence section opening on the reader line, the three
  facts the tool can see under the first three questions, a deferral
  pointed at its backlog file; the reader line tied to the request
  template by a test.
- **This repository's files** (S04, S05): `AGENTS.md` names the pointer
  page; row 4 of 1.2 names this path.

## What the closing found, and what that says

The path's own tool broke closing. S01 made `cairn-active --check` exit 1
on a stale register cell; the checker's `derived-view` blocks on that exit
code, and neither closing commit may carry the register — so no path,
here or at an adopter, could have closed. The gate refused the
administrative commit before it was made, and S07 turned the refusal into
a report. The last coherence reader corrected the writer's reason: ADR-031
does not forbid the exit code, it only makes a checker rule moot; the
report is an interim the owner accepted, not what the record demands.
The request's first run cancelled a test file that passed here by timing
— a fake that held nothing open under `AbortSignal.timeout`, the change
S02's own reviewer had asked for. Every unit review found something; the
coherence reader found something at each of the four candidates.

## What is owed

Five backlog items stand from this path: the milestone rule, which no
record carries, for a promotion; the closing commits that cannot carry
the register's cells, for the next path that writes the checker and the
close skill — which also gives a stale cell its exit code back; the kit's
bootloader line that says the skills are linked at the release's commit,
for the next path that writes the installer; and the two it inherited,
the empty `steps/` folder and the copied link pattern. `cairn-audit`'s
architecture fact restates the checker's `decision-drift` inline, for the
next path that writes the checker. The register's row 4 cell reads
*running* until the trunk commit after this entry flips it, which also
widens its Writes cell by `tools/cairn-config.mjs`, as the owner ruled.
