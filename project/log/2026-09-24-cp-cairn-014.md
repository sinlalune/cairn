---
type: Cairn Journal Entry
title: CP-CAIRN-014 — the checker of 1.2
timestamp: 2026-09-24T23:17:12Z
cairn:
  path: CP-CAIRN-014
  subject_commit: e65880ae77b916f524391d29a128567a5da612ea
  integration_commit: 298f2087f9df5efbb2605ea8e338182344e5bf3c
  outcome: completed
---

# CP-CAIRN-014 — the checker of 1.2

Opened 2026-09-24 on the owner's go-ahead in the chat, registered directly
on the trunk as `45c7fb6`, governed by the 1.2 page, path 1's journal
entry and seven records at their blob ids. Closed at candidate `e65880a`,
the fourth: `c06dc39` was voided by its fresh coherence reader, `095ff6e`
by two review comments a bot left on request 31, and `32a8fdd` by its
coherence reader again. The trunk did not move while the path ran. Eight
units, each pushed and green, each read by a fresh context and its fixes
read once more, every read obtained on the first attempt. The opening
acceptance was never amended: the scope digest at the candidate equals
the one accepted at registration. The administrative commit `4a90c2d`
stood on the branch with its check green before the owner was asked to
read; the owner merged as `298f208` through request 31. The merge click
was the acceptance.

## What changed

- **ADR-044** (S01, S06, S08): what `ready` states (amends ADR-040), the
  fresh reader's fifth input (amends ADR-043), the squash limit of a
  registration by request (amends ADR-032 decision 2), and the owner's
  decision of 2026-09-24 — a stale `current_step` is the advisory
  `current-step`, never a refusal; the three records marked, the 1.2 page
  amended in place.
- **`registration`** (S02, S07): on `pull-request` registration, off a
  path branch, a change that makes a path `running` is a registration —
  one commit, the tip of the request, touching the record's folder and
  the view alone, parented on `base_commit` — or it is refused, naming
  what failed. Before, a `register/<id>` request was never read.
- **`links`** (S03, S04): `linkExemptions` in `cairn.config.json`, each
  path with its reason; `feedbacks/` in the corpus; a top-level concept
  root no longer walks as `./`.
- **The rest** (S04): the `comparison` messages say GitHub;
  `stepRecordOrigin` reads in ancestry order, the one order
  `record-integrity` depended on, the red run of 1.1's release not
  reproduced in eight; the advisory `current-step`.

## What the closing found, and what that says

Four candidates, three voided, and each voiding was a claim nobody had
run: a record not naming the alternative it stands beside, a rule
comparing file names where the record said commits, and a record saying
a skill asks for what no skill asks. The first two readers of each unit
were told to verify by command, as path 1 learned; the closing readers
were too, and the third found the false premise only by grepping the
skills. The bot on the request found the one-commit gap no fresh reader
of this path's own agent had run.

## What the owner ruled, and what is owed

The owner said *track it in backlog* for what S03 found; two backlog
items now stand, both the owner's: `conceptLinkTargets` clears no note
under a concept root not named `concepts`, and the unit skill never
names `current_step`.

Still disagreeing with the checker in surfaces later rows own:
`README.md` counts twenty-seven rules where the checker has twenty-eight
— the owner merged with it left for row 5, which writes the kit's
figures once and links them; `project/backlog/` has no index until row
3's kit installs it; `adopt` naming a file that wants a link exemption is
row 3's. The register's row 2 cell still reads *running*; it is flipped
from this entry, as row 1 was.
