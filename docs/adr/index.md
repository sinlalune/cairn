---
type: Cairn Folder Index
title: Decision records
description: One file per durable decision about the protocol or its repository, with a stable id, a status and a date; the folder's history is Git's.
tags: [index, cairn, adr]
timestamp: 2026-09-15T00:00:00Z
---

# Decision records

One file per durable decision, named `ADR-<NNN>-<decision>.md`. A record
states one choice, its context, the alternatives it rejected and its
consequences, and names the rule, skill, file or command it changes by the
name the conformance page, the skills and the kit use today. It does not
design the implementation: that is the coding path's work.

A record promoted from a note links back to the note at the blob id it was
read at, as *promoted from*, and leaves the note exactly as it was. A
record that changes an earlier decision is a new record naming the one it
supersedes; nothing is rewritten in place.

## Shape

The frontmatter carries an `adr:` block with `id`, `status` and `date`, and
the body carries a matching `Status:` line under its heading; the checker's
`schema` rule reads both and refuses a record whose two halves disagree.
Status is one of `proposed`, `accepted`, `superseded`, `rejected`. A record
written from a decision the owner has already taken lands `accepted`, dated
the day of the decision; the pull request that lands it is its review.
A row of a record's implementation table may say *pending* against a
decision no path has landed, naming the register row that owes the
mechanism; the row names the clause back, and the path that lands it
clears the word (ADR-031, decision 3).

## Numbering

This repository's own records start at `ADR-001` and count up, with no
gaps. The specification, the conformance page and the checker's comments
still cite decision numbers from the repository the protocol was built in
before this one (ADR-017, ADR-020, ADR-022 and a few others); those records
were never brought over, so the citations point at nothing here. No number
is reserved for them. Each citation is replaced by the reason itself, or by
a record of this folder, when a coding path of 1.1 edits the file that
carries it; the roadmap register names that work.

## Records

- [ADR-001 — a sole owner opens and closes a path](./ADR-001-sole-owner-opens-and-closes-a-path.md) — accepted 2026-09-06; the owner's go-ahead in the chat is the opening acceptance, the plan lands on the trunk directly, the owner reads the plan and tries the result, the merge click is the closing acceptance. Rulings R01, R02, R04, R05, R06, R08; R03 refused. Decision 6, the profile line's host reading, superseded by ADR-029 on 2026-09-14; the kit's-default clause of decision 1 superseded by ADR-024 on 2026-09-11, and two more clauses of it, the request sequence gone and the two registration rules unchanged, superseded by ADR-032 on 2026-09-21 for `pull-request` registration.
- [ADR-002 — the checkboxes stay and the seal is checked at every transition](./ADR-002-the-checkboxes-stay-and-the-seal-is-checked-everywhere.md) — accepted 2026-09-06; the scope digest is judged whenever the record changes, on every ref, the trunk's integrating unit included; the definition of done keeps its checkboxes and is never edited after acceptance. Ruling R10; R11 refused. Decision 2, the checkboxes, superseded by ADR-042 on 2026-09-21.
- [ADR-003 — two live paths on the same files](./ADR-003-two-live-paths-on-the-same-files.md) — accepted 2026-09-06; intersecting `writes:` between live paths is the advisory `writes-overlap`, raised at registration and every unit; the later path declares `depends_on` or the owner records the accepted race. Ruling R12.
- [ADR-004 — the checker reads what it did not](./ADR-004-the-checker-reads-what-it-did-not.md) — accepted 2026-09-06; six corrections and no new control: the registration commit is the activation, a running path names its remote checkpoint, range rules are path-scoped, every blocking fixture holds a trunk merge, the branch is resolved from where the checker stands, the three Crumbz repairs come upstream. Rulings R09, R13, R14, R15, R16, R17.
- [ADR-005 — one run per commit that can land](./ADR-005-one-run-per-commit-that-can-land.md) — accepted 2026-09-06; the request's run judges a candidate, the trunk's run judges a registration and an integration, and the push run on path branches goes. Ruling R07.
- [ADR-006 — what closing leaves behind](./ADR-006-what-closing-leaves-behind.md) — accepted 2026-09-06; the path branch stays until archived, every other transport branch is deleted on merge, the writer removes the worktree and reports a failure. Rulings R23, R24.
- [ADR-007 — a framework writes into the bootloader](./ADR-007-a-framework-writes-into-the-bootloader.md) — accepted 2026-09-06; a block a tool writes into `AGENTS.md` moves to a file the kit does not own, and the bootloader points at it. Ruling R21.
- [ADR-008 — housekeeping with no choice in it](./ADR-008-housekeeping-with-no-choice-in-it.md) — accepted 2026-09-06; no closure step, one commit for one path, no closure metadata in a provisional commit, one key for the journal's path, a reportable roadmap register, refusals that name the remedy, the worktree precondition. Rulings R18, R19, R20, R22, R28, R29, R38.
- [ADR-009 — what the unit skill tells the agent](./ADR-009-what-the-unit-skill-tells-the-agent.md) — accepted 2026-09-06; `repair` defined where the type is chosen, every unit plan names the definition-of-done item it advances, no object id typed by hand. Rulings R25, R26, R27.
- [ADR-010 — the module note describes now](./ADR-010-the-module-note-describes-now.md) — accepted 2026-09-06; a module note describes the current state only, history stays in the journal, and a note every path touches is split by main component. Rulings R39, R30.
- [ADR-011 — the adopter's documentation plane](./ADR-011-the-adopters-documentation-plane.md) — accepted 2026-09-06; `docs/inputs` read by the first session, the concept wiki in three folders under `docs/concepts`, and an abstraction explained in any session becomes a concept note, by one line in the bootloader. Rulings R31, R34, R35.
- [ADR-012 — a page a newcomer reads first](./ADR-012-a-page-a-newcomer-reads-first.md) — accepted 2026-09-06; promotion's third output, one readable page per product surface at the documentation root, with the concept notes as its glossary. Ruling R32.
- [ADR-013 — a local pointer to the protocol](./ADR-013-a-local-pointer-to-the-protocol.md) — accepted 2026-09-06; the kit installs `cairn/README.md` naming the release, linking the chapters and skills, listing the files the kit owns; the kit reaches its thirty-file budget exactly. Ruling R33.
- [ADR-014 — two tools of 1.1](./ADR-014-two-tools-of-1-1.md) — accepted 2026-09-06; `cairn-postmortem` prints the mechanical reading on a red run and on demand, and the kit's self-test is `cairn-test` so adopters keep `npm test`. Rulings R36, R37.
- [ADR-015 — a release reaches an edited file](./ADR-015-a-release-reaches-an-edited-file.md) — accepted 2026-09-07; `update` rewrites a pristine file whoever owns it, never rewrites an edited one but prints what the release changes in it and lists it on the pointer page, and takes the release's version of a named file on request. Raised by the review of this path's first candidate; promotes no ruling of its own.
- [ADR-016 — the coding stance absorbs Ponytail and keeps Cairn's own](./ADR-016-the-coding-stance-absorbs-ponytail.md) — accepted 2026-09-07; the kit points at Ponytail at tag `v4.9.0` for the ladder and the review and `cairn-code` keeps only Cairn's own, the self-review speaks in the five tags, two lines on secrets and errors, the ladder and correctness are the only criteria of any reader, a unit has no size. Decisions Q1 to Q5 of the coding-guidelines page; nine options refused, three of them rules. Decision 1's first half, the pinned tag the kit copies nothing of, superseded by ADR-036 on 2026-09-21.
- [ADR-017 — the review movement](./ADR-017-the-review-movement.md) — accepted 2026-09-07, amended 2026-09-08 and 2026-09-15; a unit has five movements, plan, change, self-review, review, verify; the review is the writer's own agent in a fresh context given only the diff and the two criteria of ADR-016, its findings and dispositions in a `#### Review` section the blocking rule `review` requires. Decisions Q6 and Q7, the owner's words quoted; the one answer of the page that adds a rule, the manifesto's test weighed. Decision 3, 2026-09-08, raised by the request's reviewer: a fix is read once more, on its own lines, and not a third time. Decision 4, 2026-09-15, the owner's ruling after coding path 4's count: when no fresh context can be obtained the writer reads the diff and says so, and the section's first line names the reader; five options refused, a rule and a fixed wait among them.
- [ADR-018 — two sentences of the cycle](./ADR-018-two-sentences-of-the-cycle.md) — accepted 2026-09-07; the failing test first when behaviour changes and no new test when only structure does; the request's description answers the definition of done item by item. Decisions Q8 and Q9; four options refused, a step and a rule among them.
- [ADR-019 — an area is a folder of the tree](./ADR-019-an-area-is-a-folder-of-the-tree.md) — accepted 2026-09-07; the main component ADR-010 splits by is a folder and its note describes that folder, the architecture page says in one sentence which way dependencies point, a path names its areas and a whole source root says why. Decisions Q10 to Q12; six options refused, two of them rules, one of those refused twice.
- [ADR-020 — the one edge and the one writer, confirmed](./ADR-020-the-one-edge-and-the-one-writer.md) — accepted 2026-09-07; the edge between paths lives in the record only, a helper agent inside the writer's session is the writer, the register carries no edge between milestones. Decisions Q13 to Q15, all kept as today, one sentence added; five options refused, a second copy, a tool and a shape among them.
- [ADR-021 — what a session writes for its reader](./ADR-021-what-a-session-writes-for-its-reader.md) — accepted 2026-09-09; one line in the bootloader says an explanation is written for the reader who is learning it, the request's description and the record's goal open with three plain lines before the ledger, and a question to the owner is put in the chat and well signalled as what it is, in the owner's words. Decisions P1, P5 and P6 of the pedagogy feedback; five options refused, a file among them; no rule.
- [ADR-022 — the learning note and the learning session](./ADR-022-the-learning-note-and-the-learning-session.md) — accepted 2026-09-09; a learning note is a concept note with an order in `docs/concepts/learning`, a sixth skill `cairn-learn` explores what the user knows by prompting and writes it, and a unit that explains an abstraction writes the concept note, offers the session in its step and parks it. Decisions P2, P3 and P4 of the pedagogy feedback; the one answer that adds a kit file, and the owner's ruling that no fixed count is a rule, superseding ADR-013's count clause; seven options refused; no rule.
- [ADR-023 — the pages a reader meets](./ADR-023-the-pages-a-reader-meets.md) — accepted 2026-09-09; the adopter's README says what the project is and lists the surface pages, a workflow page is an architecture page of kind flow with a diagram, the architecture page carries one diagram and the surface page opens with one worked example, Cairn owns no API page and the agent creates and maintains the API documentation, nothing more on where the senior searches. Decisions P7 to P11 of the pedagogy feedback, the owner's words under P10; eight options refused, a file among them; no rule, no kit file, no new folder.
- [ADR-024 — the kit installs what the skills perform](./ADR-024-the-kit-installs-what-the-skills-perform.md) — accepted 2026-09-11; a repository installed at 1.1 declares `transport.registration: manual-git`, so the one registration sequence `cairn-open` ships is the one the configuration written beside it names, the `--transport` option naming the integration transport alone; `pull-request` stays a value the field accepts and an installed repository keeps the declaration it made. Promoted from CP-CAIRN-005's journal entry; supersedes the clause of ADR-001 decision 1 that kept the kit's default; five options refused, a rule among them.
- [ADR-025 — a red run is read before the next unit](./ADR-025-a-red-run-is-read-before-the-next-unit.md) — accepted 2026-09-11; the post-mortem a red run produces, in the run's log and on the request, is read by the writer before the next unit starts, one sentence in the unit skill's resume movement. Promoted from CP-CAIRN-005's journal entry, which named the clause no record decided; extends ADR-014 decision 1 by naming its reader; five options refused, a rule among them; nothing enters the checker.
- [ADR-026 — the readings that must not lie](./ADR-026-the-readings-that-must-not-lie.md) — accepted 2026-09-14; a reading the forge withheld is reported as unread and never as an answer, and the profile line names what it could not read; the `review` rule judges the ledger's newest completed unit and `current_step` selects nothing; the integrating commit is one commit for one path, the reading the checker already has; and the refusal of a merge object carrying `done` binds on `pull-request` integration alone, the `--no-ff` merge being the integrating unit on `manual-git`. Promoted from CP-CAIRN-007's S03 and journal entry; extends ADR-001 decision 6, ADR-017 decision 2 and ADR-008 decision 2, and supersedes that decision's sentence *never two paths in one request*; nine alternatives refused, a bigger token and a second rule among them; no rule added. Decision 1, the withheld reading, superseded by ADR-029 on 2026-09-14.
- [ADR-027 — the ready behind an integrating commit is on any parent](./ADR-027-the-ready-behind-an-integrating-commit.md) — accepted 2026-09-14; the `ready` a path declared before its arrival is read from any parent of the integrating commit, an octopus merge included, so the `--no-ff` merge `cairn-close` prescribes on `manual-git` is judged where the declaration actually sits. Extends ADR-001 decision 7, which names no parent while its own reasoning covers that merge unit; ends the debt CP-CAIRN-008's S03 raised at its boundary, which ADR-026 does not reach; gives up two `transition` refusals, both named in its consequences — one incidental, which fired on a merge's parent layout rather than on the violation, and one real, a withdrawn closure re-integrated from a stale side parent, carried as a debt with a fixture that pins it as not refused; five alternatives refused, a transport gate among them; no rule added, one expression widened.
- [ADR-028 — a feedback file when nothing broke](./ADR-028-a-feedback-file-when-nothing-broke.md) — accepted 2026-09-15; a file under `feedbacks/` is the channel an agent writes when the gate stayed green and the protocol still cost more than it should, naming the movement, the cost and the change to Cairn that would remove it, written when there is something to say and never as a ritual; a defect of the harness earns no file; no tool, no skill, no rule, one sentence in the unit skill. Promoted from the owner's words of 2026-09-15 over coding path 4's request, as that path's journal entry and CP-CAIRN-010's record carry them; extends ADR-014 decision 1 by naming what the post-mortem does not carry; six options refused, a skill, a tool and a step among them. Decision 2's clause *no tool reads or writes the folder* superseded by ADR-038 on 2026-09-21.
- [ADR-029 — the checker asks the host nothing](./ADR-029-the-checker-asks-the-host-nothing.md) — accepted 2026-09-14; the GitHub reading behind the profile line is deleted — `forgeGaps`, `readForge`, the host half of `profileLine`, eleven tests and the token on the checker's workflow step, with `githubSlug` and `githubRequest` moved to the post-mortem that calls them — so `cairn-check` reads the repository alone, makes no network call and prints the enforcement profile and the declared transports with no claim about the host. Supersedes ADR-001 decision 6 and ADR-026 decision 1, from the owner's ruling of 2026-09-14 and the reading's measured cost, 133 lines and 48 assertions for one line of output on one host; six alternatives refused, an option and a portable reading among them; nothing added but the test that proves the absence.
- [ADR-030 — what the open skill reads before the go-ahead](./ADR-030-what-the-open-skill-reads-before-the-go-ahead.md) — accepted 2026-09-21; before the go-ahead the agent reads the plan for a placeholder in a surface name and for a definition-of-done item that contradicts the record it cites, and puts both in the record's questions; two sentences in `cairn-open` step 2, no rule. Asks K01 and K02 of 1.2, from Q1; three options refused, a rule among them.
- [ADR-031 — one place for a fact](./ADR-031-one-place-for-a-fact.md) — accepted 2026-09-21; a measured figure is written on the conformance page and linked from everywhere else, one sentence in the close skill; the register's state cells are generated by `cairn-active` from each path's `status:`; a record's implementation table may say *pending* naming the register row that owes the mechanism, the row naming the clause back, first used on ADR-015 decision 2. Asks K03, K04 and K05 of 1.2, from Q3, Q4 and Q5; six options refused, a rule among them.
- [ADR-032 — registering on a trunk that takes no direct push](./ADR-032-registering-on-a-trunk-that-takes-no-direct-push.md) — accepted 2026-09-21; the direct-push sequence names its precondition; `pull-request` registration lands the same metadata-only commit through a request, and the `registration` rule reads a registration that is in the change under review; `adopt` refuses `protected` with `manual-git` registration from the declarations alone, and `init` and `adopt` refuse `manual-git` registration on a trunk with no bypass where the owner's token lets the installer read GitHub's rules — the one host reading the kit may make, once, which ADR-029 still forbids the checker. Supersedes two clauses of ADR-001 decision 1 for `pull-request` registration. Asks K06, K07, K08 and K20 of 1.2, from Q2; six options refused, a checker rule and a question to the owner among them.
- [ADR-033 — the installer reads what the repository declared, and says what it leaves](./ADR-033-the-installer-reads-what-the-repository-declared.md) — accepted 2026-09-21; the kit plans under every declared root and derives one only where none is declared; `update` declines a host file and the lock remembers it as *declined*; the documentation index's template is written in the shape it describes; `adopt` says under the stale line that a kept manifest or workflow still calls what it made stale. Asks K12, K13, K14 and K16 of 1.2, from Q6, Q9 and Q10; five options refused, a rule and a stub README among them.
- [ADR-034 — housekeeping with no choice in it, second edition](./ADR-034-housekeeping-with-no-choice-in-it-second-edition.md) — accepted 2026-09-21; eleven corrections nobody disputes: the generated view off the reconcile list, byte-equal generations of one release, the files an update starts managing, `status` planning as `update` does, this repository's bootloader pointing at the pointer page, the `comparison` messages saying GitHub, a fixture red once read for its order, the close skill's dangling link dropped, the post-mortem counting the run it runs in, printing a closed request as closed and reading the one path that arrived on the trunk. Asks K09, K10, K11, K15, K19, K23, K24, K25, K29, K30 and K36 of 1.2, from Q16; three options refused.
- [ADR-035 — the seventh skill, `cairn-update`](./ADR-035-the-seventh-skill-cairn-update.md) — accepted 2026-09-21; an adopter's update is a path with a skill of its own — the reading first, the owner's two decisions on edited kit files and unwanted host files before the go-ahead, the run, the reconciliation of only what the report named, what it cost said back to Cairn. Ask K17 of 1.2, from Q7; three options refused, a section of the open skill among them.
- [ADR-036 — the kit ships Ponytail's skills where the harness loads them](./ADR-036-the-kit-ships-ponytails-skills-where-the-harness-loads-them.md) — accepted 2026-09-21; at `init` and `update` the kit extracts `ponytail` and `ponytail-review` from the plugin's latest version, the lock names the version, the pinned tag goes; every kit skill is also written where the adopter's harness loads skills, Claude Code's `.claude/skills/` first, `skills/` staying the bootloader's folder. Supersedes the first half of ADR-016 decision 1. Ask K18 of 1.2, from Q8; six options refused, the printed line the writer recommended among them.
- [ADR-037 — a repository declares its link exemptions](./ADR-037-a-repository-declares-its-link-exemptions.md) — accepted 2026-09-21; a list in `cairn.config.json` names the paths whose relative links the `links` rule does not resolve, a reason beside each; the conformance page says the two 0.2 exemptions left deliberately and are the adopter's to declare; `adopt` names a file whose links do not resolve as a shape that wants a declaration. Ask K21 of 1.2, from Q11; four options refused, the exemptions back in the checker among them.
- [ADR-038 — the channel, second edition](./ADR-038-the-channel-second-edition.md) — accepted 2026-09-21; `feedbacks/` joins the corpus `links` reads, `schema` having nothing to read there; the kit installs the folder in every adopter and the unit skill names how a note reaches this repository; a treated note moves into `feedbacks/<release>/`, whose index names what answered it, the owner's ruling of 2026-09-21 carried out that day; a withdrawn claim is said in one line at the note's head; a note's type is `Cairn Feedback`, read by no rule. Supersedes ADR-028 decision 2's clause that no tool reads or writes the folder. Asks K22, K26, K27 and K28 of 1.2, from Q12, Q13 and Q14; seven options refused, a status field, a generated view and a rule among them.
- [ADR-039 — the release notes name what a release absorbed](./ADR-039-the-release-notes-name-what-a-release-absorbed.md) — accepted 2026-09-21; a `CHANGELOG.md` at the root, one section per release written by the release path, naming first the adopter repairs absorbed and not, by the adopter's path ids, shipped with the package and linked from `status` and the pointer page; ADR-015 decision 2's per-template line has its place there. Ask K31 of 1.2, from Q15; four options refused, a release on the host among them.
- [ADR-040 — the administrative commit lands before the reading](./ADR-040-the-administrative-commit-lands-before-the-reading.md) — accepted 2026-09-21; on `pull-request` with one owner the writer lands `A` on the branch before asking for the reading, so an owner who reads and clicks merge integrates a branch whose `ready` is there; steps 3 and 4 of the close skill change places, the execution protocol follows, no rule changes. Ask K32 of 1.2, from Q17; two options refused, a widened rule among them.
- [ADR-041 — a home for deferred work](./ADR-041-a-home-for-deferred-work.md) — accepted 2026-09-21; `project/backlog/`, a folder of the project plane, one file per deferred item naming what, which path, the owner and the shape of the work; a `deferred` disposition names the file, `cairn-open` reads the folder, a taking path deletes it; the kit installs the folder. Ask K33 of 1.2, from Q18; four options refused, the register and an issue on the host among them.
- [ADR-042 — the definition of done is a plain list](./ADR-042-the-definition-of-done-is-a-plain-list.md) — accepted 2026-09-21; the template writes `-` items, not boxes, so nothing in the pinned text invites the tick the digest forbids; accepted records keep their text and `scope-digest` normalises nothing. Supersedes ADR-002 decision 2. Ask K34 of 1.2, from Q19; three options refused, a substitution in the digest among them.
- [ADR-043 — the coherence questions are a fresh-context read](./ADR-043-the-coherence-questions-are-a-fresh-context-read.md) — accepted 2026-09-21; a second context given the candidate's diff, the governing documents at their ids and the live view answers the four, the owner arbitrates, and `cairn-audit` scaffolds the records the diff touches, the siblings' `writes:` and the pages changed with no record; no checker rule. Extends ADR-017 decision 1 to the closing read. Ask K35 of 1.2, from Q20; four options refused, a checker rule among them.
